# Goal

Run a PR #230 physics-loop campaign for up to 12 hours, stopping early only if
full closure is genuinely achieved.

The current blocker is not the numerical Planck criticality solve.  That solve
already gives a promising `y_t(v)` readout.  The blocker is whether the missing
condition

```text
beta_lambda(M_Pl) = 0
```

can be derived from the current `Cl(3)/Z^3` substrate rather than assumed as a
new selector.

Claim firewall: no artifact in this campaign may claim retained closure while
this premise remains open.
