# Goal

Run a PR #230 physics-loop campaign for up to 12 hours on the current
top-Yukawa closure blocker:

```text
derive O_sp = O_H, or find the exact current-surface obstruction.
```

`O_sp` is the Legendre/LSZ-normalized source-pole operator derived on
2026-05-03.  `O_H` is the canonical Higgs radial operator whose VEV supplies
`v`.

Target status requested by the user: positive retained closure with no open
dependencies.

Claim firewall: no branch artifact may claim retained or proposed-retained
closure unless the `O_sp/O_H` identity, or an equivalent source-Higgs/WZ/rank
one certificate, actually passes.
