# Route Portfolio

## Route A: In-Packet Derivation

State the exact finite-box source object, derive the shell source as
`H_0 Pi_R^ext phi`, derive the bridge by Dirichlet uniqueness, derive
`rho` and `S` by triangular solve of the static conformal constraints, and
derive the boundary action through the Schur complement.

Status: executed.

## Route B: Runner-Only Repair

Rely only on canonical runners and stdout.

Status: rejected as insufficient, because the audit already objected that the
note text itself presented a package assertion rather than the derivation.

## Route C: Broader Finite-Rank Promotion

Promote the sampled finite-rank bridge-class runner to a broader theorem.

Status: rejected for this repair. The sampled rows remain support only and the
current target is the exact local `O_h` theorem.
