# Artifact Plan

1. Rewrite `docs/RESTRICTED_STRONG_FIELD_CLOSURE_NOTE.md` as a derivation
   packet rather than a status assertion.
2. Capture runner stdout under
   `outputs/physics_loop/restricted_strong_field_closure/`.
3. Run the canonical source/lift/action/minimizer/reduced-shell runners.
4. Run local review-loop checks without applying audit verdicts.
5. Leave independent audit as the required next step.
