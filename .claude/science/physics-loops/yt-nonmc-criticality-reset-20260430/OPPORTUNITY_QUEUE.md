# Opportunity Queue

1. **Derive `beta_lambda(M_Pl)=0` from boundary-action stationarity.**
   Highest leverage: it turns the conditional selector into a serious
   replacement candidate for the MC route.

2. **Prove a no-go: `lambda(M_Pl)=0` does not imply `beta_lambda(M_Pl)=0`.**
   Useful if stationarity fails; prevents accidental overclaim.

3. **Run the IR-attractor sweep.**
   Lower closure probability, but quick support/falsifier surface.

4. **Audit weak-scale gauge input authority for this selector.**
   Needed before any future promotion attempt.

5. **Top pole bridge audit.**
   Only needed after `y_t(v)` closure; not load-bearing for the selector.

