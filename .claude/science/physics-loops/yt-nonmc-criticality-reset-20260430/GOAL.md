# Goal

Find a non-MC `y_t` route after the PR #230 production-lattice path became
too expensive and the top-sector substrate-pin search closed negatively.

The immediate target is best-honest-status: identify a route that either
retires the top-Yukawa blocker, isolates a smaller hard premise, or records a
useful no-go without using the old `H_unit` / Ward-definition trap.

