# Artifact Plan

1. Add a short Python runner that checks the zero-authority metadata invariant.
2. Update the source note so its load-bearing audit item is the runner-backed
   invariant, not an unsupported scope sentence.
3. Run syntax and runner checks.
4. Run audit pipeline/lint if practical and record any generated-file
   implications without applying an audit verdict.

