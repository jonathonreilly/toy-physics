# Route Portfolio

## Route A: zero-authority invariant runner

Score: high. This route changes the load-bearing object from a bare scope
definition to a deterministic set of source-text predicates. It is narrow,
replayable, and does not require new physics.

## Route B: cite the underlying theorem stack

Score: reject. This would reintroduce the original auditor problem by making a
packaging note look like a theorem wrapper. It also risks downstream reuse of a
meta row as physics authority.

## Route C: leave as prose-only meta note

Score: reject. The previous audit already closed only the packaging boundary
and correctly found no runner or derivation artifact.

Selected route: Route A.

