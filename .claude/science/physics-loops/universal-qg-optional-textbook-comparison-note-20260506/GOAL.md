# Goal

Close the universal-QG optional textbook comparison note as a replayable meta
artifact.

The source row must remain packaging-only: no continuum theorem, no comparison
claim, no new authority surface, and no import of the underlying universal-QG
textbook closure. The closure route is a deterministic zero-authority metadata
invariant plus a short runner that fails if the note stops satisfying that
boundary.

