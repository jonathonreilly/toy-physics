# Assumptions And Imports

## Minimal Premise Set

- Geometry family: dense `Z2 x Z2` generated by
  `scripts/higher_symmetry_dag.py`.
- Propagation/readout functions: `compute_field_3d` and `propagate_3d` from
  `scripts/mirror_chokepoint_joint.py`.
- Audit surface: `N=80,100,120`, `n_seeds=16`, `z2z2_quarter=16`,
  `connect_radius=5.2`, `anchor_b=5.0`, `mass_count=4`.
- Mass fit window: candidate rows inside `M in {2,3,5,8}`.
- Fit input guard: only rows with `delta > 0` enter the log-log power-law fit.

## Explicit Non-Imports

- No Born-safety claim is imported or proved by this note.
- No clean inverse-distance gravity law is claimed from the distance sweep.
- No rowwise positivity over all `M in {2,3,5,8}` is claimed, because
  `N=80, M=2` and `N=100, M=3` are negative.
- No literature values or observational comparators are load-bearing.

## Retired Import / Gap

The stale default-runner cache is retired as a support artifact for this note.
The cache consumed by the audit lane must be the SHA-pinned dense-surface run
of `scripts/higher_symmetry_gravity_probe.py`.

