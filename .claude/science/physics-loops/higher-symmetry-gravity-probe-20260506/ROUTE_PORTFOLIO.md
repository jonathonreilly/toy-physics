# Route Portfolio

| Route | Expected movement | Risk | Decision |
|---|---|---:|---|
| Change runner defaults to the note's dense surface and refresh the cache | Directly closes the auditor's stated mismatch | Low | Executed |
| Add a separate certificate runner with constants from the dated log | Could prove consistency but would create another artifact for auditors to inspect | Medium | Not needed if the primary cache is fresh |
| Demote the note to historical-only | Honest but loses the bounded numerical result | Low | Not chosen |
| Try to prove rowwise positivity over the full window | Contradicted by the runner rows | Blocked | Rejected |

The executed route changes the audit packet itself: source defaults, stdout,
and note wording now describe one consistent bounded claim.

