# No-Go Ledger

No no-go route was encountered.

The natural route was unobstructed: the affine-base tangent difference has
denominator `(1 - mu*rho)*(1 - rho)` and numerator `eta*(mu - 1)`. On the
finite-tangent subdomain and with `eta > 0`, equality is equivalent to
`mu = 1`.
