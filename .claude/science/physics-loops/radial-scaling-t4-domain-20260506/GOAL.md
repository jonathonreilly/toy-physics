# Goal

Close the missing derivation for
`docs/RADIAL_SCALING_PROTECTED_ANGLE_NARROW_THEOREM_NOTE_2026-05-02.md`
by making the T4 finite-tangent domain explicit and runner-checked.

Target claim: pure Euclidean radial scaling of a first-quadrant point
preserves slope, origin angle, doubled-angle readouts, and radius scaling,
while the `(1, 0)`-based finite tangent readout is not preserved except on
the `mu = 1` branch.

Stop condition: source note, runner, runner cache, and audit-derived files
are ready for independent re-audit. The loop does not apply an audit verdict.
