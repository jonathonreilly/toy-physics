# Assumptions And Imports

## Minimal Premise Set

- `rho > 0`, `eta > 0`, and `mu > 0`.
- Radial scaling is defined by `(rho_bar, eta_bar) = (mu*rho, mu*eta)`.
- T4 finite tangent subdomain adds `rho != 1` and `mu*rho != 1`.

## Load-Bearing Imports

None. The proof is elementary algebra over positive real symbols.

## Non-Load-Bearing Context

- CKM/Wolfenstein language appears only in the relation-to-parent section and
  is explicitly not consumed by the narrow theorem.
- The framework instance `(rho, eta) = (1/6, sqrt(5)/6)` is a replay example,
  not an input to the universal algebraic proof.

## Forbidden Import Audit

- No observed PDG values.
- No fitted selectors.
- No literature comparator.
- No admitted unit convention.
- No same-surface family premise.
