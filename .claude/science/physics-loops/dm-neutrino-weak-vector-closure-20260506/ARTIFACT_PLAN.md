# Artifact Plan

- Patch the theorem note with explicit Pauli/tensor-product definitions.
- Add a line-by-line derivation of `[B_a,Y_b] = i eps_{abc} Y_c`.
- Correct the summed bivector normalization.
- Harden the runner so its source checks Clifford and chirality premises.
- Capture runner stdout under `outputs/`.
- Run an adversarial local review and final verification.

