# Assumptions And Imports

## Minimal Premise Set

- Pauli matrices `sigma_x`, `sigma_y`, `sigma_z` with the standard finite
  matrix products listed inside the theorem note.
- The `C^8` and `C^16` `Gamma_i` are explicitly defined tensor products of
  those Pauli matrices.
- `gamma_5 = Gamma_0 Gamma_1 Gamma_2 Gamma_3`.
- `P_L = (I + gamma_5)/2`, `P_R = (I - gamma_5)/2`.
- `B_a = -(i/4) sum_{m,n=1}^3 eps_{amn} Gamma_m Gamma_n`.

## Imports Retired

- No external literature theorem is load-bearing.
- No observed neutrino value, fitted selector, or physical coefficient is used.
- The previous missing import "runner source/stdout not present in restricted
  packet" is retired by the relative source link and captured stdout artifact.

## Boundary

The packet proves representation content only. It does not fix the absolute
Yukawa coefficient or the second-order suppression law.

