# Goal

Close — or honestly bound — the audit-ledger row
`gauge_vacuum_plaquette_local_environment_factorization_theorem_note`
that currently carries `claim_type=open_gate` and blocks ~247 transitive
descendants from clearing the audit chain.

## Target outcome

Convert `claim_type` from `open_gate` to `bounded_theorem` by:

1. Tightening the note title and stated scope to expose the bounded
   character of what is actually proven (mixed-kernel locality
   factorization on the marked-plaquette character sector at the
   accepted Wilson `3+1` source surface);
2. Sharpening the explicit "What this closes / What this does not close"
   sections so that the bounded scope is unambiguous;
3. Submitting an independent fresh-context re-audit as a non-Codex
   family auditor recording `claim_type=bounded_theorem` and rationale.

The mixed-kernel-locality theorem itself is already exact and the runner
already PASSes 4 theorem + 3 support checks at zero numerical residual.
The factorization closes; the remaining framework-point object
(residual source-sector environment data outside the normalized mixed
kernel) is named explicitly and lives in separate companion notes
(`residual_environment_identification`,
`spatial_environment_character_measure`, etc.).

## What this is not

- Not a closure of analytic `P(beta=6)` — that remains open downstream.
- Not a re-derivation of the underlying physics — the chain already
  closes in the existing note and runner.
- Not a self-audit — the prior auditors are Codex GPT-5.5 / GPT-5 /
  current-family. This loop is run by Claude Opus 4.7 (`anthropic`
  family), which is `cross_family` independent of those auditors.

## Honest scope

The auditor on 2026-05-02 explicitly chose `claim_type=open_gate` over
`bounded_theorem`. That conservative reading was based on a strict
title-as-scope interpretation. The independent reading here is that
the note's Theorems 1+2+3 close a bounded factorization claim, with
residual environment data carved out by named successor notes — the
textbook `bounded_theorem` shape under `docs/audit/README.md`.
