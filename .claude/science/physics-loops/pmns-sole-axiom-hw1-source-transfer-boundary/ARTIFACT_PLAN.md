# Artifact Plan

- Update the runner so the contested identity blocks are derived from an
  explicit finite `Cl(3)` / `Z^3` `hw=1` packet.
- Update the note with the projector and resolvent chain.
- Refresh the cached runner output.
- Verify direct dependent PMNS runners still pass.
