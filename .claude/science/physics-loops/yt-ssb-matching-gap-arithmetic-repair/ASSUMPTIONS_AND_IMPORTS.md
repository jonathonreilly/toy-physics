# Assumptions And Imports

## Claimed Target

The claimed target is finite-dimensional arithmetic:

- positive integers `N_iso, N_c`;
- orthonormal pair basis `|alpha,a>`;
- explicit operator definition `H_unit = I_D / sqrt(D)` with
  `D = N_iso * N_c`.

Under these hypotheses, each diagonal component overlap equals `1/sqrt(D)`.

## Imports Retired

The repaired note no longer imports or asserts:

- physical Standard Model Yukawa trilinear matching;
- Hubbard-Stratonovich/source normalization;
- SSB VEV division;
- chirality projection;
- LSZ/external-state normalization;
- absence of extra physical finite factors.

## Open Boundary

The physical readout map from the normalized `H_unit` component overlap to the
SM top Yukawa trilinear remains open. It is explicitly outside this repaired
claim.

## Measured/Fitted Inputs

None. No PDG value, literature comparator, fitted selector, or admitted unit
convention is used in the scoped theorem.
