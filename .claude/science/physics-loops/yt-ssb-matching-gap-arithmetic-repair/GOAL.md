# Goal

Repair `docs/YT_SSB_MATCHING_GAP_ANALYSIS_NOTE_2026-04-18.md` after the
`audited_renaming` verdict.

The intended close is not a new physical SSB/Yukawa matching theorem. The
repair isolates the only theorem-grade part of the old note:

```text
H_unit = I_(N_iso*N_c) / sqrt(N_iso*N_c)
```

has diagonal component overlap

```text
<alpha_0,a_0 | H_unit | alpha_0,a_0> = 1 / sqrt(N_iso*N_c).
```

For `(N_iso,N_c)=(2,3)`, this gives `1/sqrt(6)`. The physical trilinear
matching problem remains outside scope.
