# Goal

Close the missing derivation chain for `docs/MIRROR_CHOKEPOINT_BOUNDARY_FIT_NOTE.md`
inside the narrowed finite-window scope:

- fixed dense boundary mirror card only;
- Born-clean, `k=0`-clean, gravity-positive, decohering rows through `N = 100`;
- gravity wall at `N = 120`;
- weak post-retention descriptive exponent fit `alpha = -0.245`, `R^2 = 0.126`;
- no mirror-family theorem and no asymptotic law.

The intended outcome is audit readiness for a future independent re-audit, not
an in-branch audit verdict.
