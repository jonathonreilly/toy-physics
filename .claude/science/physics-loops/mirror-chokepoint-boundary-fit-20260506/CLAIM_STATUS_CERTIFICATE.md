# Claim Status Certificate

```yaml
actual_current_surface_status: bounded-support
conditional_surface_status: null
hypothetical_axiom_status: null
admitted_observation_status: null
proposal_allowed: false
proposal_allowed_reason: "The branch supplies an audit-ready finite-window certificate, but independent audit has not rerun."
audit_required_before_effective_retained: true
bare_retained_allowed: false
```

The edited source note supports only a bounded finite-window claim for the
named dense boundary card. It does not assert a retained mirror-family theorem,
a bounded asymptotic theorem, or a predictive extrapolation from the weak fit.

The certificate runner checks dependency classes by executing the companion
runner, enforcing pre-fit gates, and computing the fit after row selection.
The `alpha` and `R^2` values are not selector inputs.
