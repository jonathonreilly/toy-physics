# No-Go Ledger

| Route | Obstruction | Consequence |
|---|---|---|
| Treat `alpha=-0.245` as an asymptotic law | Four retained rows are non-monotone and `R^2=0.126` is weak | Excluded from source note and certificate |
| Treat the dense boundary card as a mirror-family theorem | The result is a selected finite parameter card | Source note states named-card scope only |
| Treat `N=120` as Born-certified | Replay prints `nan` in the Born column for the wall row | Note now uses only zero-gravity wall status |
