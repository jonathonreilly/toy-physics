# Goal

Repair or rule out the PR #230 Ward-identity route to the top Yukawa coupling.

The target is narrow: derive the physical Standard Model Yukawa readout from
the retained `Cl(3)/Z^3` action without defining the coupling by the old
`H_unit`-to-top matrix element.  If closure is not possible on the current
surface, leave an executable repair map and the next concrete route.

User request: work the physics-loop route for the PR #230 top-Yukawa blocker,
with Ward repair as the direct target after the direct production-MC route was
judged too large for immediate closure.

2026-05-06 update: the active subgoal is to find the cleanest genuine artifact
inside the remaining closure contracts.  The current genuine artifact is the
same-source Legendre/LSZ source-pole operator `O_sp`; it is exact source-side
support, not retained/proposed-retained closure.  The next clean closure
artifact is `O_sp`-Higgs pole-residue rows (`C_spH/C_HH`) or an equivalent
same-surface canonical `O_H` identity/normalization certificate.
