# PR Backlog

This block is being added directly to PR #230 because the user requested the
PR carry the result.  If split later, suggested title:

```text
[physics-loop] yt-beta-lambda-planck-stationarity no-go
```

Suggested body:

- Proves `lambda(M_Pl)=0` does not imply `beta_lambda(M_Pl)=0`.
- Shows `beta_lambda(M_Pl)=0` is a separate codimension-one selector.
- Keeps the Planck double-criticality route conditional, not retained.

