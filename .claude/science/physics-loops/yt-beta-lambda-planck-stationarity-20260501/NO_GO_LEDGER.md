# No-Go Ledger

## Current Block

`lambda(M_Pl)=0`, finite source response, and taste-scalar isotropy do not
imply `beta_lambda(M_Pl)=0`.

Exact obstruction:

```text
beta_lambda^(1)|_{lambda=0}
  = -6 y_t^4 + (3/8)[2 g_2^4 + (g_2^2 + g'^2)^2]
```

This is not the zero polynomial.

## Inherited

- top-sector substrate pin no-go;
- Ward-decomposition no-go under H_unit forbiddance;
- direct-correlator route remains calibrated readout absent an independent
  mass-parameter pin.

