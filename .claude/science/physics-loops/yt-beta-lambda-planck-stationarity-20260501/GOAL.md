# Goal

Derive or rule out `beta_lambda(M_Pl)=0` from the current `Cl(3)/Z^3`
substrate surface, specifically as the blocker exposed by the Planck
double-criticality `y_t` selector.

The user asked for full retained closure in PR #230.  The loop must only grant
that if the premise is actually derived without a new hidden selector.

