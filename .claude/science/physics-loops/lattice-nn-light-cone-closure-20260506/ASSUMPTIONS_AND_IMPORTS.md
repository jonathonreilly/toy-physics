# Assumptions And Imports

## Minimal Premise Set

- A finite vertex set `V`.
- A one-step dependency relation `R subset V x V`.
- An `R`-local update rule whose value at `v` after one tick uses only
  predecessor values `(u, v) in R`.
- For stochastic implementations, paired comparisons use the same external
  random choices.
- A finite update horizon.

## Explicit Non-Imports

- No continuum limit.
- No metric distance or physical spacetime geometry.
- No Lorentz invariance.
- No universal signal speed.
- No causal-field runner, fixed-mass log, mass-scaling log, distance-law
  result, or Newtonian falloff.
- No literature, observed target value, fitted selector, or admitted unit
  convention.

## Import Status

The closure is zero-input structural mathematics over the declared finite
dependency graph. There are no retained-grade upstream dependencies to close.
