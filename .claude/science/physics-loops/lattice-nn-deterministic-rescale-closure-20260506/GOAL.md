# Goal

Close the bounded computational audit gap in
`docs/LATTICE_NN_DETERMINISTIC_RESCALE_NOTE.md`: the previous failed audit did
not see runner source or stdout tying the deterministic `spacing / sqrt(3)`
schedule to the canonical rows through `h = 0.0625`.

Target status for this block: `proposed_retained` pending independent re-audit.
