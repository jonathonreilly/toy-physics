# Route Portfolio

## Attempted

- Split/correct the ambient eta assertion. The runner computes
  `|eta|/fixed_site = 2/9`, so the mismatch residual was false and has been
  removed from the retained claim.
- Preserve the rank-two selected-line obstruction. The runner still constructs
  two orthonormal zero-mode lines with the same spin-lift `Z3` character and
  verifies a family of equally retained rank-one mixtures.

## Blocked

- Promoting the finite Wilson model to a physical selected endpoint remains
  blocked by the missing rank-one selection and endpoint-lift theorems.
