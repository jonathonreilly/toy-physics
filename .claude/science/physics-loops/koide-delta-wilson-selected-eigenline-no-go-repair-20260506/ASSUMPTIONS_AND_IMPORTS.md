# Assumptions And Imports

- No mass data, fitted Koide value, or selected endpoint target is used.
- The finite L=3 Wilson-Dirac support model and body-diagonal `Z3` action are
  the runner's computational surface.
- The APS comparator is `2/9`; the repaired runner checks that the finite
  Wilson eta proxy matches it.
- The retained no-go does not use an ambient eta mismatch. The open imports
  remain a selected rank-one eigenline theorem and a based endpoint-lift
  theorem.
