# No-Go Ledger

- `ambient_eta_mismatch`: closed as false for this runner. The finite Wilson
  eta proxy matches the APS comparator `2/9`.
- `selected_rank_one_eigenline`: still a no-go on the finite Wilson surface.
  The relevant character sector has multiplicity two.
- `endpoint_lift_basepoint`: still open. Multiplying a lift by `exp(i s t)`
  leaves the Wilson eigenline/projector unchanged while shifting the open
  endpoint.
