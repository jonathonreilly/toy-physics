# Artifact Plan

- Update the runner so the eta-proxy check matches the computed value and all
  checks pass.
- Update the note so the retained no-go claim excludes the false ambient
  mismatch residual.
- Refresh SHA-pinned runner caches for the edited selected-eigenline runner
  and the affected hostile-review guard.
- Refresh the tracked hostile-review output snapshot.
