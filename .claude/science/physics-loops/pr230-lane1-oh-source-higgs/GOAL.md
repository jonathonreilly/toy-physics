# PR230 Lane 1 O_H / Source-Higgs Campaign Goal

Run the PR230 lane-1 physics-loop campaign against the action-first canonical
`O_H` plus strict source-Higgs pole-row route.

Target status: full positive top-Yukawa closure if the bridge can be derived
or measured honestly; otherwise the narrowest current-surface status with no
overclaiming.

Primary bridge:

```text
Cl(3)/Z3 source coordinate
-> canonical O_H / accepted action authority
-> strict C_ss/C_sH/C_HH pole rows
-> Gram purity / kappa_sH
-> y_t physical readout
```

Forbidden shortcuts: `H_unit` matrix-element readout, `yt_ward_identity`,
`y_t_bare`, observed target selection, `alpha_LM/u0/plaquette`, relabeling
`C_sx/C_xx` as `C_sH/C_HH`, or setting `kappa_s`, `c2`, `Z_match`, or `g2`
to one.
