# Opportunity Queue

Only this lane is in scope.

1. Oriented boundary incidence and variational first response.
   - Executed. Boundary incidence failed as a selector. The variational
     subroute produced the link-local first-variation positive candidate, with
     current effective status still conditional through upstream authority rows.

2. Intrinsic active-module induction.
   - Lower plausibility because `Cl_4(C)` acts irreducibly on a chosen
     four-dimensional module, while `H_cell` is sixteen-dimensional and
     already has many admissible rank-four projectors.

3. Retarded/reflection-positive time orientation.
   - Lower plausibility unless the accepted time theorem already contains a
     directed boundary response, not merely single-clock dimensionality.

4. Exterior derivative versus codifferential.
   - Likely Hodge-conjugate unless the substrate action already privileges
     `d` over `delta`.

Current lane checkpoint:

```text
best current route = A_min link-local source differential
status = intrinsic candidate, effective conditional on current audit graph
```

If audit rejects this as conditional/renaming, the next science move is not a
wording pass. It is either route 2 (intrinsic active-module induction) with a
new falsifier, or honest acceptance that the boundary-block selection is an
additional structural input.
