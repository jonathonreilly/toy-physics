# Source-Resolved Exact Green Self-Consistent Loop Goal

Close the missing-derivation prompt for
`docs/SOURCE_RESOLVED_EXACT_GREEN_SELF_CONSISTENT_NOTE.md` without widening
the physics claim.

The requested repair target was:

- add explicit PASS/FAIL assertions for zero-source exactness, `TOWARD` sign,
  exponent tolerances, and frozen table reproduction
- declare the calibrated gain as a frozen input, not independent amplitude
  evidence
- keep the result scoped to the specified `h=0.25`, `W=3`, `L=6` finite run
  with one source-cluster reweighting update

Current source and audit data show that this target has already been reached
on `origin/main`.
