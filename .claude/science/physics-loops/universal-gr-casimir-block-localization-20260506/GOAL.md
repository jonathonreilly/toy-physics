# Goal

Close the missing derivation in
`docs/UNIVERSAL_GR_CASIMIR_BLOCK_LOCALIZATION_NOTE.md` by replacing the
asserted Casimir spectrum with a restricted-packet derivation and an executable
runner.

The narrow target is exact block localization:

- `Pi_A1` fixes lapse plus spatial trace.
- The 8D complement is an `SO(3)` representation.
- The complement Casimir has eigenvalues `-2` with multiplicity `3` and `-6`
  with multiplicity `5`.
- The spectral projectors give lapse / shift / trace / shear blocks.

The target is not a full Einstein/Regge dynamics proof.
