# Assumptions And Imports

## Retained Inputs

- The frozen L=3 Wilson-Dirac support construction already used by the source
  runner.
- The body-diagonal `Z3` spin-lift action in the existing runner.
- The APS comparator value `2/9`, used as a displayed comparator and not as a
  selected-line derivation.

## New Assumptions

None.  This block removes a stale residual claim rather than adding a physics
axiom.

## Import Boundary

The finite ambient scalar equality to `2/9` is not used to identify the
physical Brannen selected line.  The surviving no-go depends on the computed
multiplicity-two zero-mode character sector and the independent endpoint-lift
offset.
