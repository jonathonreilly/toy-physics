## Block100 W/Z Explicit-v Authority Firewall

This checkpoint packages the post-chunk W/Z absolute-normalization firewall.

What landed:

- Added `scripts/frontier_yt_pr230_wz_v_authority_firewall.py`.
- Added `outputs/yt_pr230_wz_v_authority_firewall_2026-05-15.json`.
- Added `docs/YT_PR230_WZ_V_AUTHORITY_FIREWALL_NOTE_2026-05-15.md`.
- Wired the certificate into campaign status, assumption/import stress,
  retained-route, full assembly, and completion-audit runners.

Result:

- Physical higher-shell chunk production is complete; no active PR230 chunk
  worker is running.
- The strict PR230 explicit-`v` authority certificate is absent.
- Package hierarchy `v=246.282818290129 GeV` is rejected as a load-bearing W/Z
  absolute-normalization pin because its visible derivation traverses forbidden
  `alpha_LM` / plaquette / `u0` / `M_Pl * alpha_LM^16` surfaces.
- W/Z still requires accepted action, production W/Z rows, same-source top
  rows, matched covariance, `delta_perp`, final W-response rows, and one
  allowed absolute pin: strict non-observed `g2`, strict non-forbidden
  explicit-`v`, or canonical source-response normalization.

Validation:

```text
W/Z v authority firewall PASS=12 FAIL=0
retained route PASS=321 FAIL=0
campaign status PASS=423 FAIL=0
full positive closure assembly PASS=196 FAIL=0
positive closure completion audit PASS=75 FAIL=0
assumption stress PASS=107 FAIL=0
```

No closure statement: this is an exact negative boundary for the package-`v`
shortcut only. PR #230 remains draft/open. No retained or `proposed_retained`
top-Yukawa closure is authorized.
