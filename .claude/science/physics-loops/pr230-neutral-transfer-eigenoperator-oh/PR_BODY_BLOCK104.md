PR230 Block104 checkpoint: Z3 heat-kernel source-coupling/H4 no-go.

What landed:

- Added `scripts/frontier_yt_pr230_z3_heat_kernel_source_coupling_no_go.py`.
- Added `outputs/yt_pr230_z3_heat_kernel_source_coupling_no_go_2026-05-15.json`.
- Added `docs/YT_PR230_Z3_HEAT_KERNEL_SOURCE_COUPLING_NO_GO_NOTE_2026-05-15.md`.
- Wired the certificate into retained-route, campaign-status, full-assembly,
  completion-audit, and assumption/import stress gates.
- Updated the PR230 loop pack state, handoff, claim status, no-go ledger,
  assumptions, opportunity queue, artifact plan, and review history.

Physics result:

- The source-plus-triplet block extension `diag(1, K_tau)` keeps the triplet
  heat-kernel support but is reducible; the source remains isolated.
- A positive source-triplet `eta` can make a formal four-state transfer
  primitive, but `eta` is independent action/transfer data and is not derived
  by the current PR230 surface.
- No current artifact supplies canonical `O_H`, physical `C_sH/C_HH` pole
  rows, same-source W/Z response rows, or scalar-LSZ authority.

Validation:

```text
Z3 heat-kernel source-coupling no-go PASS=13 FAIL=0
retained route PASS=325 FAIL=0
campaign status PASS=427 FAIL=0
full positive assembly PASS=200 FAIL=0
positive completion audit PASS=79 FAIL=0
assumption/import stress PASS=111 FAIL=0
```

Claim boundary:

No retained or `proposed_retained` closure is claimed. The PR remains
draft/open. The neutral heat-kernel route now needs a same-surface physical
source-triplet/action row or off-diagonal generator fixing `eta`, plus H4
source/canonical-Higgs coupling; otherwise the clean closure targets remain
accepted `O_H`/action with strict `C_ss/C_sH/C_HH` pole rows, strict W/Z
response with an allowed absolute pin, or strict Schur/scalar-LSZ pole
authority.
