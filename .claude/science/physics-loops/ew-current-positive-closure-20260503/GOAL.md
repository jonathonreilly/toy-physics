# Goal

Obtain positive closure for the EW current matching rule, or identify the
precise route blocker if the physics loop cannot reach it.

Concrete success criteria:

- Derive `kappa_EW = 0` from the framework-native lattice EW current
  construction; or
- compute the disconnected/color-singlet coefficient exactly and show the
  physical readout gives `kappa_EW = 0`; or
- produce a runner-checked blocker proving the available current-surface routes
  cannot supply the coefficient.

This block achieved the third outcome for the strongest new route attempted.
