# Goal

Work prompt `wave_direct_dm_h025_seed0_crossfamily_note` to a clean block-level resolution.

The prompt was not a route to a family theorem.  The useful resolution is a bounded-support artifact: the note now states only the seed-`0`, `H = 0.25` Fam1/Fam2 two-row compression and attaches a fast runner that checks the frozen source logs.

Success criteria:

- narrow the note away from theorem-wide wording;
- register an executable assertion runner;
- preserve explicit exclusions for stable amplitude, Fam3, and portability claims;
- refresh audit metadata so the changed note returns to independent audit;
- ship one review PR for the block.
