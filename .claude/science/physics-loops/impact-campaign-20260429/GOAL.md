# Physics Loop Impact Campaign 2026-04-29

Run the repo-native `physics-loop` skill for 4 hours on the most impactful open
science lanes from current `origin/main`.

Primary instruction:

- Use campaign mode.
- Build and refresh an opportunity queue from the live repo surfaces.
- Prefer retained-grade positive closure opportunities, but use the narrowest
  honest status if a route only supports `exact-support`, `bounded-support`,
  `conditional-support`, `no-go`, `open`, or `demotion`.
- Do not use bare `retained` or `promoted` status in branch-local artifacts.
- Use `proposed_retained` / `proposed_promoted` only when the retained-proposal
  certificate permits it and explicitly records that independent audit remains
  required before effective repo status changes.
- Close coherent science blocks with a pushed branch and review PR, or a
  complete `PR_BACKLOG.md` if GitHub access fails.

Seed priority should be checked against current repo authority surfaces before
route selection. The initial high-impact lane map is:

1. Lane 6 charged-lepton mass retention / `y_tau` Ward identity / Koide ratio
   dependencies.
2. Lane 5 Hubble constant derivation, especially absolute-scale or cosmic-`L`
   gates that could move the two-gate firewall.
3. Lane 3 quark mass retention, especially narrow retained-positive
   denominator/readout or up-type amplitude routes.
4. Lane 2 atomic-scale predictions, if Lane 6/absolute-scale prerequisites can
   be moved or a firewall theorem can be sharpened.
5. Lane 1 hadron mass program, mostly after Lane 3 dependencies move.
6. Lane 4 neutrino quantitative closure, prioritizing tractable theorem gates
   over broad support prose.

