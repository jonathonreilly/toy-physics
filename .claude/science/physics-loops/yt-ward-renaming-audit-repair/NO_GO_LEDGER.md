# No-Go Ledger

## Theorem-grade promotion

Blocked. The row's prior audit identified the load-bearing step as a
definition/renaming: `y_t_bare` is introduced as the `H_unit` matrix element
and then read as the top Yukawa surface. The runner checks coefficient
consistency after that identification; it does not independently derive the
physical readout map.

## Ledger-only fix

Rejected. Manually editing auditor-owned verdict/type fields would bypass the
fresh-look audit protocol. This block uses source metadata and citation repair,
then lets the pipeline reset the changed row to `unaudited`.
