# Route Portfolio

## Route A: Exhaustive Enumeration

Rejected for this short closure. The default surface has 890633 possible triples, dominated by `dim=3 side=4`, and exhaustive telemetry is not needed if the logical proof is written explicitly.

## Route B: Encoded-Isometry Teleportation Derivation

Chosen. Conjugate the standard two-qubit teleportation identity by the three encoding isometries `V_A`, `V_R`, and `V_B`; then record how each negative control deletes one premise.

## Route C: Scope Demotion Only

Rejected. The auditor already accepted the open-gate artifact; merely restating the bounded sampling caveat would not retire the residual sampling risk.
