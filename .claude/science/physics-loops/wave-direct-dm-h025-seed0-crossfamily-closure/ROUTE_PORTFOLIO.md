# Route Portfolio

1. Registered finite-runner route: add a primary runner that computes both
   seed-0 ladders and checks the bounded sign/order/control claims.
2. Dependency-edge route: link the control notes directly.  Rejected for this
   block because the existing control notes include contextual backlinks that
   would create a non-load-bearing cycle.
3. Broader theorem route: prove a family law.  Out of scope for this closure.

