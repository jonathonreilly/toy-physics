# Handoff

The closure strategy is to make the cross-family note self-contained around a
single primary runner instead of adding direct links to neighboring control
notes that currently backlink into this note.

Expected re-audit packet:

- source note:
  `docs/WAVE_DIRECT_DM_H025_SEED0_CROSSFAMILY_NOTE.md`
- primary runner:
  `scripts/wave_direct_dm_h025_seed0_crossfamily_audit.py`
- runner cache:
  `logs/runner-cache/wave_direct_dm_h025_seed0_crossfamily_audit.txt`

The intended clean scope is only the finite observation: exact nulls, common
negative weak-field sign, bounded within-family weak-field spread, and Fam1/Fam2
ordering at `S = 0.004`.  The note explicitly excludes amplitude-law,
portability, and Fam3 claims.

