# Artifact Plan

- Add `scripts/wave_direct_dm_h025_seed0_crossfamily_audit.py`.
- Refresh `logs/runner-cache/wave_direct_dm_h025_seed0_crossfamily_audit.txt`.
- Update `docs/WAVE_DIRECT_DM_H025_SEED0_CROSSFAMILY_NOTE.md` so the audit
  graph registers the primary runner and the note states the exact bounded
  derivation chain.
- Run the audit pipeline and lint.

