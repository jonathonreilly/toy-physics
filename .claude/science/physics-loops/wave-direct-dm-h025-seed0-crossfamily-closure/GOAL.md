# Goal

Close the missing-derivation gap for
`docs/WAVE_DIRECT_DM_H025_SEED0_CROSSFAMILY_NOTE.md`.

The closure target is narrow: a bounded finite-runner observation at
`H = 0.25`, `seed = 0`, and families `Fam1`/`Fam2`.  The note must not
claim a stable amplitude law, a full family theorem, a Fam3 transfer, or a
portability result beyond the two listed rows.

