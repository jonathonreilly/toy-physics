# Goal

Try to build a primitive-bearing route from PR230 scalar-source/Stieltjes
moments to pole atom/residue authority using extremal moment, flat extension,
and localizing-matrix theory.

The target is not chunk work.  It must not use `H_unit`, Ward authority,
`y_t_bare`, `alpha_LM`, plaquette/`u0`, observed targets, `kappa_s=1`,
`c2=1`, or `Z_match=1`.

Deliver the strongest theorem/proof attempt, state whether it closes anything,
and, if it does not close, name the exact missing certificate fields with an
executable mathematical witness.
