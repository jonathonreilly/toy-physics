# Goal

Close the missing derivation in
`docs/UNIVERSAL_GR_SUPERMETRIC_NORMAL_FORM_NOTE.md`.

The audit blocker was the unsupported identification

`B_D(h,k) = -Tr(D^-1 h D^-1 k)`

between the route-defined universal Hessian and the inverse-metric
supermetric pairing on `D = diag(a,b,b,b)`.

## Target

Produce an auditable first-principles derivation from the route log-det
generator, pair it with an executable proof artifact, and leave the full
Einstein/Regge gluing gap explicitly out of scope.
