# Charged-Lepton Loop — PR Backlog

**Loop:** `charged-lepton-pickup-20260428`
**Status:** PR opened at loop end per new physics-loop skill PR
policy. Tracked separately from PR #113 (neutrino block).

## Pending PRs

(To be populated at loop end.)

## Opened PRs

(None yet — PR opens at loop end.)

## PR creation method

`gh pr create` with `gh` authenticated as `JONbridgerapps`.
Single invocation at loop close-out.

PR title format:
`[physics-loop] charged-lepton-pickup-20260428 — <status>`
where `<status>` is `retained` / `exact-support` / `bounded-support`
/ `no-go` / `open` / `demotion` per skill policy.
