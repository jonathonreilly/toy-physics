# Charged-Lepton Mass Retirement Pickup Loop

**Goal:** retire the 3-real PDG charged-lepton mass observational
pin per `docs/lanes/open_science/06_CHARGED_LEPTON_MASS_RETENTION_OPEN_LANE_2026-04-26.md`
by retaining absolute `m_e`, `m_μ`, `m_τ` from one axiom.

**User invocation context:** continuation of the user's 10h
autonomous loop. Pivoted from `neutrino-quantitative-20260428` after
Lane 4 Phase-1 work landed (4D Dirac global lift via Cycles 2-8, 4F
cosmology bridge Cycle 9, 4A structurally distant Cycle 10) and
Lane-4-internal claim-state movement was exhausted.

**Target status:** best-honest-status. Phase-1 priority per lane file:
**`y_τ` Ward identity construction → absolute lepton scale `V_0`
retention** (analog of retained `y_t(M_Pl)/g_s(M_Pl) = 1/√6`).

## Phase-1 closure targets (per lane file §5)

1. **`y_τ` Ward identity** (analog of YT-lane). With Koide ratios
   `Q = 2/3` and `δ = 2/9` (in flight separately on the Koide
   flagship lane), `y_τ` retention pins absolute lepton scale `V_0`,
   and chains via Koide to all three charged-lepton masses retained.
2. **Koide flagship integration:** verify the Koide flagship lane's
   recent retentions are consistent with this loop's Phase-1 targets;
   identify any cross-lane consistency check.

## Phase-2 (after Phase 1)

3. **`V_0` absolute scale fix** — once `y_τ` Ward identity lands,
   `V_0 = √2 m_τ / y_τ` defines the absolute lepton scale.
4. **Generation hierarchy via Koide ratios:** with `V_0` retained
   and Koide retentions, all three masses retained.

## Phase-3

5. **Cross-lane impact** — closing `y_τ` opens 4A Route A for the
   neutrino-Yukawa Ward identity attempt (per
   `frontier/neutrino-quantitative-20260428` Cycle 10 stretch-attempt
   note).

## Delivery

Science-only on `frontier/charged-lepton-pickup-20260428`. Push to
`origin`. Open one PR per science block at end (per new physics-loop
skill PR policy). PR #113 is the neutrino block (separate); this
loop's PR is its own block. Repo-wide weaving recorded in HANDOFF
only.
