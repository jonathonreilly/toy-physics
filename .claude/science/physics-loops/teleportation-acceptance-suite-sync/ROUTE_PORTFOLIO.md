# Route Portfolio

## R1: Strict-Lane Inventory Synchronization

Status: executed.

Synchronize `docs/TELEPORTATION_ACCEPTANCE_SUITE_NOTE.md` with the exact
`--strict-lane --list-probes` output. Add cached default and strict list-probe
outputs plus a required-only run artifact.

## R2: Scope To Default-Only Harness

Status: rejected.

This would avoid documenting the strict-lane surface but would discard the
auditor's requested repair target. R1 is the narrower direct fix.

## R3: Promote Child Scripts As Theorem Dependencies

Status: rejected.

The prompt is a harness documentation repair. Child-script physics claims
remain outside this block's re-audit packet.
