# NO-GO LEDGER — VEV V-Singlet Derivation Campaign

**Date:** 2026-05-02

Prior negative results that bound the search space; do not re-explore unless
a new premise is named.

## Already-closed no-go's relevant to this campaign

| # | No-go | Authority | Implication for H2 |
|---|---|---|---|
| N1 | `OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md` proves the W = log\|det(D+J)\| - log\|det D\| route only after admitting 4 bridge assumptions (additivity, CPT-even phase blindness, continuity, normalization) plus the hierarchy baseline | `OBSERVABLE_PRINCIPLE_AUDIT_NOTE_2026-05-02.md` (cycle 8 / PR #267) | H2 must use a DIFFERENT load-bearing observable (f_vac), not the same W route with different prose |
| N2 | `<P>(β=6)` analytic insertion is open: bridge-support stack pins window `0.5934 ≤ ⟨P⟩(β=6) ≤ 0.59353` but does not close to a single value | `PLAQUETTE_SELF_CONSISTENCY_NOTE.md` (status amended 2026-05-01) | H1 Route 1 cannot expect full closure; partial result acceptable |
| N3 | Lattice → physical matching cluster: 4 same-shape obstructions (cycles 5, 9, 11, 17) all need a Nature-grade non-perturbative matching theorem | `LATTICE_PHYSICAL_MATCHING_CLUSTER_OBSTRUCTION_NOTE_2026-05-02.md` (PR #274) | H2 should NOT attempt a matching theorem; it should reformulate INSIDE the framework's retained surface |
| N4 | Constant-lift law `P(β) = P_1plaq(β·(3/2)·(2/√3)^(1/4))` is ruled out as an exact form for the plaquette | `GAUGE_VACUUM_PLAQUETTE_CONSTANT_LIFT_OBSTRUCTION_NOTE.md` | H1 Route 1 saddle attempt cannot use the constant-lift ansatz |
| N5 | Local Wilson data on the source-sector matrix-element factorization does not fix the residual environment factor `ρ_(p,q)(6)` — three reference rho families give strictly different P(6) | `GAUGE_VACUUM_PLAQUETTE_TENSOR_TRANSFER_PERRON_SOLVE_NOTE.md` | H1 Route 1 saddle attempt cannot rely on local Wilson data alone |
| N6 | Source-note status prose cannot be presented as audit-ratified retained status (controlled vocabulary firewall) | `docs/repo/CONTROLLED_VOCABULARY.md` + skill | All H2 artifacts must use the controlled vocabulary; status lines must be `bounded` / `exact support` / `open` etc., not bare `retained`, `proposed_retained` is allowed only with passing certificate |

## Active no-go inheritance

| Inherited from | Constraint on this campaign |
|---|---|
| `feedback_hostile_review_semantics.md` (memory) | Trace-ratio derivations can be arithmetically perfect while comparing convention-defined sources; every action-level identification must be stress-tested |
| `feedback_consistency_vs_derivation_below_w2.md` (memory) | Consistency equality is not derivation; runners must verify, not hard-code, the closure value |
| `feedback_retained_tier_purity_and_package_wiring.md` (memory) | N-way retained equalities cannot include support-tier routes; new retained theorems must wire into canonical harness + publication control-plane indexes mirroring sister-theorem rows |
| `feedback_physics_loop_corollary_churn.md` (memory) | `--max-cycles` is a ceiling not a target; stop at corollary exhaustion; don't mine relabelings |

## Routes NOT to re-explore

- Same-surface family arguments for v derivation (per memory `consistency_vs_derivation_below_w2`)
- Trace-ratio identifications without action-level verification (per memory `hostile_review_semantics`)
- Hard-coding the (7/8)^(1/4) value into runners (forbidden import)
- Re-deriving the bilinear-curvature observable principle with different prose (closed by N1)
