# Generation Axiom Boundary Note

**Date:** 2026-04-16
**Type:** bounded_theorem
**Claim scope:** Local `H_hw=1` finite-dimensional algebra check only:
the supplied translation-character projectors and the C3 cycle generator
generate the full nine-dimensional `M_3(C)` observable algebra and have
scalar commutant on the three-dimensional `H_hw=1` sector. The
physical-species bridge from observable-separation to physically
distinct species, the substrate-fundamentality question, and any
historical reduced-stack witness about the older five-item implementation
memo are explicitly **out of scope** here and live in separate authority
notes (admitted-context to this note). No historical-memo claim is
asserted by this note or by its runner.
**Status authority:** independent audit lane only. This source note does not
set or predict any audit outcome; later status is generated by the audit
pipeline after independent review.
**Script:** `scripts/frontier_generation_axiom_boundary.py`
**Authority role:** canonical main-branch tool note for the local `M_3(C)`
observable-algebra reconstruction on `H_hw=1` only; the note carries no
historical reduced-stack authority and no physical-species or
substrate-fundamentality authority.

## Safe statement (in-scope)

On the framework's local `M_3(C)` observable-algebra reconstruction over
`H_hw=1`, the local exact observable algebra reconstructs from the
supplied translation-projector and C3 cycle generators. Concretely:

- the local exact `M_3(C)` observable algebra on `H_hw=1` reconstructs from
  the supplied translation-projector + C3 cycle generators, with no input
  from the substrate-fundamentality premise;
- the three exact rank-one observable projectors on `H_hw=1` correspond to
  the pairwise-distinct translation characters and generate the full
  9-dimensional `M_3(C)`;
- the commutant of these generators on `H_hw=1` is scalar (dimension 1),
  so no proper exact quotient kernel can be invariant under the retained
  observable algebra.

## Out of scope (admitted-context to this note)

The following stronger statements are explicitly **NOT** claimed here. They
belong to separate authority notes and enter this note only as
admitted-context:

- **Physical-species bridge.** The step from "exact observable separation +
  no-proper-quotient closure" to "physically distinct species sectors of the
  accepted theory" depends on accepted Hilbert/no-proper-quotient semantics
  outside this note's claim. Whether that bridge closes is decided by the
  audit lane on the corresponding semantic authority rows, not by this note.
- **Substrate fundamentality.** The global substrate-level question of
  whether the lattice is fundamental rather than a regulator-family surrogate
  is carried by the **separate** physical-lattice necessity lane. This
  boundary note does not assert that lane's conclusion and does not consume
  it as a load-bearing input.
- **Historical reduced-stack witness about the older five-item memo.** The
  older reduced five-item implementation memo's bookkeeping of the
  substrate premise is historical context, not part of this note's
  retained-grade content. Any historical-memo claim requires a direct
  authority dependency that is **not** wired here, so it is
  admitted-context rather than in-scope. The runner does not encode any
  historical-memo PASS check; its computational checks cover only the
  local `M_3(C)` algebraic facts above.

## Why this belongs in the toolbox

This is not just narrative framing. As a local algebraic reconstruction,
it is a reusable in-scope artifact for any future lane that wants the
algebraic content (the `M_3(C)` reconstruction on `H_hw=1`) without
re-deriving it. The physical-species and substrate-fundamentality
conclusions named above are **not** part of the in-scope reusable content
of this note; downstream lanes that need those must cite the separate
authority notes directly.

In-scope reusable content:

- the local `M_3(C)` observable-algebra reconstruction on `H_hw=1` from
  translation-projector + C3 cycle generators

## Relation to the downstream matter stack

This note supports but does not replace the generation-structure,
physical-lattice, one-generation matter-closure, and anomaly-forced-time
surfaces. Those notes carry their own authority surfaces.

This note locally reconstructs the exact `hw=1` observable algebra. That
keeps the algebraic content reusable without turning this boundary note
into a downstream dependency cycle.

The clean current division of labor is:

- this note:
  local construction of the exact `M_3(C)` observable algebra on `H_hw=1`
  from translation-projector + C3 cycle generators
- downstream generation notes:
  consume this local boundary witness when they state no-proper-quotient
  conclusions
- the separate physical-lattice necessity lane:
  carries the no-same-stack regulator boundary and accepted one-axiom substrate
  necessity

## Validation

- primary runner:
  [frontier_generation_axiom_boundary.py](./../scripts/frontier_generation_axiom_boundary.py)

Current branch runner state:

- `frontier_generation_axiom_boundary.py`: local `M_3(C)`
  observable-algebra construction on `H_hw=1`
