# Generation Axiom Boundary Note

**Date:** 2026-04-16
**Type:** bounded_theorem
**Claim scope:** local M_3(C) observable-algebra reconstruction on H_hw=1.
The physical-species bridge from observable-separation to physically distinct
species, and the question of substrate fundamentality vs regulator-family,
are explicitly **out of scope** here and live in separate authority notes
(admitted-context to this note).
**Status authority:** independent audit lane only. This source note does not
set or predict any audit outcome; later status is generated by the audit
pipeline after independent review.
**Script:** `scripts/frontier_generation_axiom_boundary.py`
**Authority role:** canonical main-branch tool note for the local
`M_3(C)` observable-algebra reconstruction on `H_hw=1`

## Audit scope

**In-scope (this note's audited claim):**

- the local exact `M_3(C)` observable-algebra reconstruction on `H_hw=1`
  from the supplied translation-projector + cycle generators.

**Conditional / out-of-scope here (do not consume as load-bearing input from
this note):**

- the reduced-stack historical witness about the older five-item
  implementation memo's substrate-premise role. This material is retained
  below under "Historical context" only as narrative; it is **not** part of
  the audited in-scope claim of this note. **Status: conditional, see
  auditor verdict.**
- the physical-species bridge from observable-separation to physically
  distinct species sectors. Carried by separate semantic authority rows.
- substrate fundamentality vs regulator-family. Carried by the separate
  physical-lattice necessity lane.

## Safe statement (in-scope)

On the framework's local `M_3(C)` observable-algebra reconstruction over
`H_hw=1`, the local exact `M_3(C)` observable algebra on `H_hw=1`
reconstructs from the supplied translation-projector + cycle generators,
with no input from any substrate-fundamentality premise.

## Out of scope (admitted-context to this note)

The following stronger statements are explicitly **NOT** claimed here. They
belong to separate authority notes and enter this note only as
admitted-context:

- **Physical-species bridge.** The step from "exact observable separation +
  no-proper-quotient closure" to "physically distinct species sectors of the
  accepted theory" depends on accepted Hilbert/no-proper-quotient semantics
  outside this note's claim. Whether that bridge closes is decided by the
  audit lane on the corresponding semantic authority rows, not by this note.
- **Substrate fundamentality.** The global substrate-level question of
  whether the lattice is fundamental rather than a regulator-family surrogate
  is carried by the **separate** physical-lattice necessity lane. This
  boundary note does not assert that lane's conclusion and does not consume
  it as a load-bearing input.

## Why this belongs in the toolbox

This is a local algebraic reconstruction. It is a reusable in-scope artifact
for any future lane that wants the algebraic content (the `M_3(C)`
reconstruction on `H_hw=1`) without re-deriving it. The physical-species
and substrate-fundamentality conclusions named above are **not** part of the
in-scope reusable content of this note; downstream lanes that need those
must cite the separate authority notes directly.

In-scope reusable content:

- the local `M_3(C)` observable-algebra reconstruction on `H_hw=1`

## Relation to the downstream matter stack

This note supports but does not replace the generation-structure,
physical-lattice, one-generation matter-closure, and anomaly-forced-time
surfaces. Those notes carry their own authority surfaces.

The clean current division of labor is:

- this note:
  local construction of the exact `M_3(C)` observable algebra on `H_hw=1`
- downstream generation notes:
  consume this local boundary reconstruction when they state
  no-proper-quotient conclusions
- the separate physical-lattice necessity lane:
  carries the no-same-stack regulator boundary and accepted one-axiom substrate
  necessity

## Historical context (NOT in audited scope)

**Status: conditional, see auditor verdict.**

The following narrative is retained for repository continuity only. It is
**not** part of the audited in-scope claim of this note and must not be
cited as a load-bearing input by downstream lanes. Any lane that needs the
older five-item memo content must cite that memo (or an equivalent
authority) directly.

- The older reduced five-item implementation memo still listed substrate
  fundamentality explicitly. Earlier revisions of this note served as a
  witness isolating that reduced-stack fact from the local algebraic
  reconstruction. That witness role is no longer part of the audited claim
  here; it is preserved only as historical context.

## Validation

- primary runner:
  [frontier_generation_axiom_boundary.py](./../scripts/frontier_generation_axiom_boundary.py)

Current branch runner state:

- `frontier_generation_axiom_boundary.py`: `PASS=35`, `FAIL=0`;
  local `M_3(C)` observable-algebra construction on `H_hw=1`
