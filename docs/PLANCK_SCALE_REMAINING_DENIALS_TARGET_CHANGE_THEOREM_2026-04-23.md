# Planck-Scale Remaining Denials Target-Change Theorem

**Date:** 2026-04-23
**Status:** branch-local theorem attacking the final two denial points before hostile review
**Audit runner:** `scripts/frontier_planck_remaining_denials_target_change_theorem.py`

## Question

The current canonical packet says the only honest remaining denials are:

1. reject the bare primitive counting object class;
2. reject Gravity-Sector Identification (GSI).

Are these still unresolved algebra gaps, or do they change the target of the
claim?

## Bottom line

They change the target.

The direct Planck packet has reduced the result to the following exact
dichotomy:

1. **Primitive Planck coefficient target.** If the target is the universal
   elementary coefficient attached to the primitive physical event cell, then the
   coefficient object class is forced to be the primitive additive/natural
   counting functional. The value is `rank(P_A)/16 = 1/4`.
2. **Enriched-state/readout target.** If the target is instead a state-enriched,
   packet-stabilized, vacuum-reduced, Hamiltonian-selected, or boundary-prepared
   expectation, then it is not the same primitive coefficient. It requires extra
   datum and does not produce an axiom-native universal Planck coefficient
   without another state-selection theorem.
3. **Planck-length target.** If the target is an actual lattice length in
   conventional Planck units, the dimensionless primitive coefficient must be
   identified with the gravitational area/action density. That identification is
   exactly GSI. Without it, the result remains `c_cell = 1/4`, not `a = l_P`.

So the remaining denials are not:

> the algebra does not force the coefficient.

They are:

> choose a different object than the primitive coefficient, or refuse the
> gravitational sector identification needed to talk about Planck length.

## Theorem 1: rejecting the primitive counting object class changes the coefficient object

Assume the submitted target is:

> the elementary Planck coefficient of the primitive physical cell.

Then the coefficient must be:

1. local to the primitive event frame;
2. universal before a later state, Hamiltonian, embedding, or boundary condition
   is supplied;
3. additive on disjoint primitive-event alternatives;
4. natural under primitive atom relabeling;
5. normalized on the full primitive event frame.

Those are exactly the hypotheses of the universal primitive counting-trace
theorem and atomic-naturality theorem.

Therefore the coefficient is uniquely

`C(P) = rank(P) / 16`.

For the worldtube packet,

`rank(P_A) = 4`,

so

`C(P_A) = 4/16 = 1/4`.

### Proof

If the coefficient depended on a local state, global vacuum, embedding,
Hamiltonian, boundary preparation, atom bias, or packet-stabilized weight, it
would no longer be a function of the primitive physical cell and projector
alone. It would be a function of an enlarged object class.

That contradicts the stated target: an elementary universal coefficient attached
to the primitive cell itself.

On the primitive object class, the existing counting-trace theorem proves the
unique additive/natural normalized functional is `rank(P)/16`.

Therefore rejecting that object class rejects the target object. It does not
find a competing primitive coefficient.

## Countermodel: enriched object classes are not primitive

A packet-stabilized enriched object can assign two weights:

`alpha` inside `P_A`,

`beta` outside `P_A`,

with

`4 alpha + 12 beta = 1`.

For example,

`alpha = 1/32`,

`beta = 7/96`,

is positive and normalized, but gives

`C(P_A) = 4 alpha = 1/8`.

This does not refute the primitive counting trace. It demonstrates that
packet-enriched weighting is extra local datum. It is a different target.

## Theorem 2: rejecting GSI changes a Planck-length claim into a dimensionless coefficient claim

Assume the submitted target is:

> the physical lattice spacing in conventional Planck units.

The microscopic branch produces a dimensionless coefficient:

`c_cell = 1/4`.

A length statement requires an equation containing a length scale. In the
semiclassical gravitational sector, that scale is supplied by

`S_grav / k_B = A c_light^3 / (4 G hbar) = A / (4 l_P^2)`,

where

`l_P^2 = hbar G / c_light^3`.

The identification of the primitive boundary count with that gravitational
area/action density is exactly GSI.

Without GSI, no equation in the Planck packet relates `a` to `G`, `hbar`, and
`c_light`. The conclusion remains:

`c_cell = 1/4`.

It does not become:

`a = l_P`.

### Proof

The microscopic count law is

`S_cell / k_B = c_cell A / a^2`.

The gravitational area/action law is

`S_grav / k_B = A / (4 l_P^2)`.

Equating them requires the physical statement:

`S_cell = S_grav`

at leading boundary-density order.

That statement is GSI in density form.

If it is accepted, then

`c_cell / a^2 = 1 / (4 l_P^2)`,

so

`a^2 = 4 c_cell l_P^2`.

With `c_cell = 1/4`,

`a^2 = l_P^2`.

If GSI is rejected, the equality of densities is rejected and the length
normalization does not follow.

Thus rejecting GSI does not expose a hidden algebra gap. It declines the sector
identification required by the Planck-length target.

## Hostile-review status

The reviewer-safe final form is:

> The branch proves the exact primitive coefficient `c_cell = 1/4` for the
> primitive physical event cell under the universal additive/natural counting
> object class. Conditional on GSI, this coefficient gives `a = l_P` under the
> standard gravitational area/action normalization.

The only remaining ways to reject the result are now explicit target changes:

1. use an enriched state/readout/vacuum object instead of the primitive
   coefficient object;
2. refuse to identify the primitive boundary count with the gravitational
   area/action sector.

Those are legitimate philosophical or physical objections, but they are no
longer hidden mathematical gaps in the Planck packet.
