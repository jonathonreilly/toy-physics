# DM / PF Compressed-Route Attack Plan

**Date:** 2026-04-18  
**Status:** current-`main` attack memo for the next non-redundant DM selector
push; not a theorem, not a closure note

## Purpose

Record the current best theorem attack order on `main` after reviewing two
remote science workstreams before opening a new branch attack:

- `origin/codex/dm-selector-science-main-2026-04-18` at `0206ba8d`
- `origin/codex/pf-path-ground-2026-04-17` at `5b749138`

The goal is not to restate the selector gap in prose. It is to identify the
smallest genuinely new object whose proof would move the DM flagship gate
forward without re-running already-exhausted internal selector search.

## Reviewed branch read

### 1. DM selector science branch

The direct top-down selector branch does **not** close constructively.
Its final exact reduction is:

1. `observable-grammar exhaustion / intrinsic-family descent`
2. `exact-carrier completeness / global dominance`

More sharply:

- the selector-class side is compressed to the canonical positive-probe
  extremal law on the recovered carrier;
- the carrier-side broad rival geometry is no longer diffuse;
- on the tested broad bundle, endpoint and split-1 are already dominated;
- split-2 alone remains live there;
- and even split-2 reduces to a narrow low-slack edge interval on the tested
  broad bundle, with the tested broad split-2 edge looking transport-
  incompatible with the preferred recovered lane.

So the direct branch result is not "search better." It is:

- stop searching more positive-probe families;
- stop doing more broad recovered-carrier archaeology;
- if a constructive route exists, it must import genuinely new upstream
  information.

### 2. PF path-ground branch

The PF branch is not current enough to treat as live authority on `main`
because it sits on an older base, but it is useful as a route audit.

Its key reduction is:

- the cleaner step-2 target is not `Wilson -> D_-` first;
- it is the compressed Hermitian descendant route
  `Wilson -> dW_e^H = Schur_Ee(D_-)`;
- and the sharpest candidate package there is one local Wilson certificate,
  phrased as a minimal local path-algebra or Hermitian-source embedding
  object.

So the PF branch contributes one useful structural lesson:

- if we want a new approach, it should be an **upstream Wilson descendant
  theorem into `dW_e^H`**, not another internal selector functional inside the
  already-reduced DM source sheet.

## Decision

The next attack on `main` should be:

- **do not** continue the direct positive-probe / family-scalarization /
  carrier-window program;
- **do not** try to upgrade the current imposed branch-choice rule by another
  internal `H(m, delta, q_+)` functional;
- **do** attack a new upstream descendant law that lands right-sensitive data
  on `dW_e^H`.

This is the only route reviewed here that changes the information content of
the problem instead of optimizing inside a selector framework that has already
collapsed to obstruction.

## Exact theorem target surface

The current retained reduction on `main` already says the open microscopic
object is

`dW_e^H = Schur_Ee(D_-)`,

equivalently the intrinsic right-sensitive `2`-real `Z_3` doublet-block law;
see
[DM_LEPTOGENESIS_PMNS_MICROSCOPIC_SELECTOR_REDUCTION_THEOREM_NOTE_2026-04-17.md](./DM_LEPTOGENESIS_PMNS_MICROSCOPIC_SELECTOR_REDUCTION_THEOREM_NOTE_2026-04-17.md).

The new attack therefore needs to land **before** the current source-sheet
selector, not after it.

### Primary target theorem

Prove a theorem-grade **Wilson-to-`dW_e^H` descendant law**:

> there exists a Wilson-side charged Hermitian source family whose first
> variations descend canonically to `dW_e^H`, and whose descended responses are
> right-sensitive on the live source sheet.

At the level of theorem shape, the clean form to target is:

1. one Wilson-side Hermitian source plane or embedding object,
2. one descendant / compression identity carrying that Wilson family into
   `dW_e^H`,
3. one right-sensitive score on the descended family that is not constant on
   the chamber and does not factor through the frozen current bank.

### Secondary target theorem

Once the descendant family exists, prove:

> the Wilson-descended right-sensitive score excludes the competing
> non-baseline-connected basins and singles out the physical branch without
> observational PMNS pinning.

This is the actual replacement target for the current imposed branch-choice
rule.

### Tertiary target theorem

If the descendant score lands cleanly, then and only then attack:

> a PF-style dominant-mode or positive-map selector theorem on the descended
> Wilson family.

This is downstream. It is not the first thing to prove.

## Hard constraints from current `main`

Any new attack must respect the following already-landed boundaries.

### 1. The object must be right-sensitive or right-frame-fixing

`H`-only data is insufficient because exact right-unitary motion keeps
`H = Y Y^dag` fixed while changing the right-sensitive kernel `K = Y^dag Y`
and the heavy-basis CP tensor; see
[DM_LEPTOGENESIS_PMNS_MICROSCOPIC_SELECTOR_REDUCTION_THEOREM_NOTE_2026-04-17.md](./DM_LEPTOGENESIS_PMNS_MICROSCOPIC_SELECTOR_REDUCTION_THEOREM_NOTE_2026-04-17.md).

### 2. The object must not factor through the frozen chamber bank

Any candidate that only reads

`B_frozen = (gamma, E1, E2, cp1, cp2, K00, a_*, b_*, T_slot)`

is dead on arrival; see
[DM_NEUTRINO_OBSERVABLE_BANK_EXHAUSTION_THEOREM_NOTE_2026-04-17.md](./DM_NEUTRINO_OBSERVABLE_BANK_EXHAUSTION_THEOREM_NOTE_2026-04-17.md)
and
[DM_NEUTRINO_SOURCE_BANK_Z3_DOUBLET_BLOCK_SELECTION_OBSTRUCTION_THEOREM_NOTE_2026-04-16.md](./DM_NEUTRINO_SOURCE_BANK_Z3_DOUBLET_BLOCK_SELECTION_OBSTRUCTION_THEOREM_NOTE_2026-04-16.md).

### 3. The object cannot be another local microscopic polynomial functional

Local microscopic polynomial, heat-kernel, spectral-gap, and Ward-identity
routes are already obstructed; see
[DM_NEUTRINO_SOURCE_SURFACE_MICROSCOPIC_POLYNOMIAL_IMPOSSIBILITY_THEOREM_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_MICROSCOPIC_POLYNOMIAL_IMPOSSIBILITY_THEOREM_NOTE_2026-04-17.md).

So the new route must use genuinely nonlocal / descendant / transport-side
structure, not a prettier local scalar on `H(m, delta, q_+)`.

### 4. The object must live on the actual live sheet

It may not rely on promoting the Schur commutant-class scalar baseline to the
live source sheet; that promotion is explicitly blocked; see
[DM_NEUTRINO_SOURCE_SURFACE_SCHUR_SCALAR_BASELINE_THEOREM_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_SCHUR_SCALAR_BASELINE_THEOREM_NOTE_2026-04-17.md)
and
[DM_FLAGSHIP_CLOSURE_REVIEW_NOTE_2026-04-17.md](./DM_FLAGSHIP_CLOSURE_REVIEW_NOTE_2026-04-17.md).

### 5. The object must not depend on the observational PMNS closure pin

The point of this route is to replace the current conditional/support closure,
not to repackage it. So the new theorem must stand upstream of the PMNS
observational level-set used in
[PMNS_FROM_DM_NEUTRINO_SOURCE_H_DIAGONALIZATION_CLOSURE_THEOREM_NOTE_2026-04-17.md](./PMNS_FROM_DM_NEUTRINO_SOURCE_H_DIAGONALization_CLOSURE_THEOREM_NOTE_2026-04-17.md).

### 6. The object must preserve the current exact package

The new route must be compatible with:

- the exact source package `gamma = 1/2`, `E1 = sqrt(8/3)`, `E2 = sqrt(8)/3`;
- the exact chamber `q_+ >= sqrt(8/3) - delta`;
- the gauge-retained status of `q_H`;
- the theorem-grade same-surface thermal bounding and transport results already
  on `main`.

It is not allowed to solve the gate by weakening those surfaces.

## Tactical constraints from the reviewed remotes

These are not current-`main` authority notes. They are branch-review guidance.

### From `dm-selector-science`

- Do not spend another cycle on family-choice ambiguity.
- Do not spend another cycle on broad recovered-carrier window search.
- If the direct selector branch is revisited at all, only revisit it after a
  genuinely new upstream datum has been introduced.

### From `pf-path-ground`

- Prefer the compressed descendant codomain `Wilson -> dW_e^H` before the
  stronger `Wilson -> D_-` route.
- Treat local path-algebra / Hermitian source embedding language as the best
  constructive packaging candidate.
- Treat PF compatibility or dominant-mode rhetoric as downstream of the
  descendant theorem, not as a substitute for it.

## Smallest genuinely new object worth proving

The smallest new object worth trying to prove on current `main` is:

> one theorem-grade Wilson-side **Hermitian source embedding / source plane**
> whose descended first variations reconstruct `dW_e^H`.

The cleanest strong form, suggested by the PF route audit, is:

`Phi_chain : A_chain -> End(H_W)`

for one local adjacent two-edge chain path algebra `A_chain`, together with
the induced Wilson Hermitian source plane on the charged codomain.

But that full local path-algebra statement may be stronger than necessary.
The weakest acceptable positive object is:

> a finite Wilson-side Hermitian source family, minimal at nine real channels,
> whose first-variation responses reconstruct `dW_e^H` and vary right-
> sensitively across the live source sheet.

That weaker statement is enough to start a real selector theorem. It is the
first thing to try.

## Immediate work order

1. **Boundary reduction on current `main`.**
   State explicitly that the new attack target is a Wilson-to-`dW_e^H`
   descendant law, not another internal selector functional.

2. **Weakest constructive attack first.**
   Try to realize a finite Wilson Hermitian source family whose first
   variations reconstruct `dW_e^H`.

3. **Only then strengthen to local algebra.**
   If the finite-family route works but looks noncanonical, strengthen it to a
   local path-algebra or source-embedding theorem.

4. **Only then attack the selector itself.**
   Once a descended family exists, attack right-sensitive dominance or
   PF-style selection on that family.

5. **Fail honestly if step 2 is impossible.**
   If the current bank does not realize even the finite Wilson Hermitian source
   family, the next honest result is a no-go or nonrealization theorem there,
   not another return to direct selector search.

## Stop-rule

This attack should be abandoned immediately if it collapses back into any of
the following:

- another local scalar functional on `H(m, delta, q_+)`;
- another source-bank or slot-bank repackaging;
- another observational PMNS pinning argument;
- another broad-window recovered-carrier search.

Those surfaces have already been reduced.

## Bottom line

The next non-redundant DM attack on `main` is:

- **not** a better internal selector search,
- **not** a better carrier sweep,
- **not** a stronger restatement of the current conditional PMNS closure,
- but one new upstream theorem:

> construct a Wilson-side charged Hermitian descendant into
> `dW_e^H = Schur_Ee(D_-)`, then use that descended right-sensitive family to
> replace the current imposed source-branch rule.

That is the sharpest current route that is both new and potentially decisive.
