# Discrete Dirichlet-to-Neumann Shell Kernel for Strong-Field Gravity

**Date:** 2026-04-13  
**Branch:** `codex/review-active`  
**Script:** `scripts/frontier_discrete_dtn_shell_kernel.py`  
**Status:** Exact operator-level shell-kernel derivation plus bounded closure consequence

## Purpose

The gravity line had already shown:

- the sewing shell exists exactly on the lattice
- its radial profile is universal across the current exact source families
- the shell-level coarse-grained exterior is fixed exactly by total charge on
  the current star-supported source classes

That still left one legitimate complaint:

> the universal shell kernel had been extracted from solved source families, but
> had not yet been written as an exact boundary operator of the lattice
> problem itself

This note removes that ambiguity.

## Exterior boundary problem

Fix the cutoff radius `R = 4` and define the exterior domain

`Omega_R = {x : r(x) > R}`

inside the finite Dirichlet lattice box.

Let `Gamma_R` be the first exterior layer, i.e. those lattice sites in
`Omega_R` adjacent to the excluded interior region `r <= R`.

For any prescribed inner trace `f` on `Gamma_R`, there is a unique discrete
harmonic extension `u_f` on `Omega_R` with:

- `u_f = f` on `Gamma_R`
- `u_f = 0` on the outer box boundary
- `H_0 u_f = 0` on the harmonic bulk of `Omega_R`

Extending `u_f` by zero into the excluded interior, define the shell source

`Lambda_R(f) = H_0 u_f`

This is the discrete Dirichlet-to-Neumann shell source associated with the
cutoff sphere.

## Exact point-Green identification

Take the centered point-Green field `G_0`.

Restrict it to the trace:

`f_0 = G_0 |_(Gamma_R)`

The script solves the exterior Dirichlet problem with this trace and finds:

> the resulting harmonic extension equals the exterior projector
> `Pi_R^ext G_0` to machine precision

So the point-source shell law is not an empirical fit. It is the exact image
of the boundary trace under the lattice DtN operator:

`Lambda_R(f_0) = H_0 (Pi_R^ext G_0)`

## Exact shell-kernel result

Radially average the DtN shell source and normalize by the total enclosed
charge:

`k_DTN(r) = sigma_rad(r) / Q`

The script proves:

1. the DtN shell source is supported exactly in the finite sewing band
   `3 < r <= 5`
2. all seven star-support point-Green columns induce the same normalized
   DtN shell kernel to machine precision
3. this DtN kernel matches, to machine precision, the previously extracted
   universal shell profile from:
   - the exact local `O_h` source family
   - the broader exact finite-rank source family

So the universal shell profile already present on the branch is not merely
“the profile seen in two solved families.” It is:

> the charge-normalized discrete DtN image of the star-support exterior trace
> mode

## Interpretation

This is the cleanest microscopic derivation of the shell kernel so far.

Before this note:

- the shell kernel was exact as a family-universal extracted profile

After this note:

- the shell kernel is exact as a lattice boundary operator object

That is a real upgrade in the gravity program, because the shell law is now
attached directly to the discrete harmonic boundary problem rather than to a
post-processed family comparison.

## What this closes

This closes another real ambiguity:

> the universal shell kernel is not an empirical fitting artifact; it is the
> exact discrete Dirichlet-to-Neumann shell source generated by the point-Green
> exterior trace on the physical lattice box

## What this still does not close

This note still does **not** close:

1. the continuum / effective-stress interpretation of the DtN shell kernel
2. the theorem-grade control of the anisotropic zero-monopole shell remainder
3. the final nonlinear 4D spacetime closure

## Updated gravity target

After this note, the cleanest remaining gravity target is sharper again:

- the universal shell kernel itself is now derived as an exact lattice DtN
  object
- the live blocker is to interpret that DtN shell kernel as the effective
  shell stress / sewing law of the nonlinear spacetime closure
- and then control the anisotropic correction well enough to promote the
  resulting exterior-plus-shell metric to theorem grade
