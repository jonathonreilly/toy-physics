# Perron-Frobenius Selection Axiom Boundary

**Date:** 2026-04-17  
**Status:** exact current-stack boundary on whether Perron-Frobenius selection is already derived from the sole axiom  
**Script:** `scripts/frontier_perron_frobenius_selection_axiom_boundary.py`  
**Framework convention:** "axiom" means only the single framework axiom `Cl(3)` on `Z^3`.

## Question

Does the current exact stack already promote the existing sector-level
Perron / dominant-mode theorems into one common sole-axiom physical-state
selection principle?

## Bottom line

Partially, but not globally.

What is exact now:

- on the finite Wilson plaquette source surface, every explicit positive
  transfer operator has one unique strictly positive Perron state
- on the retained strong-CP action surface, positive topological-sector
  weights force the free-energy minimum at `theta = 0`
- on the aligned `hw=1` PMNS transfer kernel, there is one unique dominant
  symmetric mode and one doubly-degenerate orthogonal mode

What is not exact now:

- the sole axiom still does not derive the nontrivial PMNS
  source/transfer pack; the strongest canonical sole-axiom `hw=1` pack
  remains trivial
- the new Wilson parent/compression theorem links the Wilson parent
  object to the plaquette and strong-CP descendants, but there is still no
  theorem that this parent structure also reaches the PMNS transfer sector
- even inside the sharpened plaquette spatial-transfer class, distinct
  admissible positive `S_6^env` / `eta_6` pairs can still induce different
  boundary data and different Perron moments for the same explicit source
  operator `J`
- more sharply still, the earliest missing constructive datum on that lane is
  the explicit orthogonal-slice kernel `K_6^env` together with the rim map to
  `eta_6`
- the new one-slab orthogonal-kernel integral-boundary theorem sharpens the
  kernel side further: `K_beta^env` is already fixed as one exact Wilson/Haar
  slab integral, but its explicit `beta = 6` evaluation is still open
- the new full-slice rim-lift integral-boundary theorem sharpens the local
  side further: `B_beta(W)` is already fixed as one exact local Wilson/Haar
  rim integral with `eta_beta(W) = P_cls B_beta(W)`, but explicit `beta = 6`
  evaluation is still open
- the new `beta = 6` evaluation-seam reduction theorem sharpens the live
  framework-point target further: after compression the remaining plaquette PF
  seam is exactly evaluation of the integral-defined class-sector matrix
  elements of `K_6^env` and `B_6(W)`, not a new post-compression formalism
- and the new kernel/rim compression theorem closes that once `K_6^env` and
  the rim map are explicit, the compressed transfer object `S_6^env`, the
  coefficients `rho_(p,q)(6)`, and the downstream plaquette PF data follow
  canonically
- and the new `beta = 6` scalar-value insufficiency theorem closes a tempting
  shortcut: even a fixed same-surface plaquette value at `beta = 6` is still
  only one scalar constraint and does **not** determine the full class-sector
  vector `v_6`, the coefficients `rho_(p,q)(6)`, or the explicit PF data
- and the new retained class-sampling inversion theorem closes the next finite
  constructive step: on every retained finite marked class sector, once enough
  generic marked-holonomy samples are explicit, the truncated coefficient vector
  is recovered exactly by linear inversion
- and the new conjugation-symmetric retained-sampling reduction theorem closes a
  physically sharper step: because the retained plaquette coefficients already
  satisfy conjugation symmetry, the retained sample burden drops from retained
  basis size to conjugation-orbit count
- and the new first symmetric three-sample reconstruction theorem closes the
  first concrete sample target set: the first symmetric retained coefficient
  triple is recoverable from three explicit regular rational-angle marked
  holonomies
- and the new exact radical-form three-sample reconstruction-map theorem closes
  one further step: the first named sample matrix and its inverse are already
  exact algebraic data, the `W_A` row kills the `chi_(1,1)` orbit for a
  structural antipodal-pair reason, and the remaining first retained plaquette
  seam is explicit evaluation of `Z_6^env(W_A)`, `Z_6^env(W_B)`, `Z_6^env(W_C)`,
  not numerical reconstruction design
- and the new current-stack constraint-boundary theorem closes the next honest
  sharpened statement: the three named samples are not collapsed by centrality,
  inverse-conjugacy reality, or the current source-observable stack, so the
  first retained `beta = 6` seam is irreducibly evaluative on the current bank
- and the new local Wilson partial-evaluation theorem closes the strongest
  current exact sample-side evaluation short of the full environment solve: the
  local source values `J(W_A), J(W_B), J(W_C)`, the marked half-slice factors
  `exp(3J(W_i))`, and the normalized local one-plaquette values are all already
  explicit on the named seam, even though the full `Z_6^env(W_i)` values are
  still open
- and the new local-Wilson retained positive-cone obstruction theorem sharpens
  that exact evaluator route further: the explicit local Wilson three-sample
  triple already lies outside the first symmetric retained positive cone, so
  the open `K_6^env / B_6(W)` completion is mathematically necessary rather
  than a minor refinement of an already-admissible local answer
- and the new `Tau`-controlled retained-coefficient wedge theorem gives the
  strongest current coefficient-side control compatible with that obstruction:
  `rho_(1,0)(6)` and `rho_(1,1)(6)` already lie in one exact outer wedge with
  explicit linear bounds in terms of `Tau_(>1)`, but `Tau_(>1)` itself is
  still open, so this is not yet a framework-point solve
- and the new `Tau` upper-bound nonderivation theorem closes the next exact
  negative statement: the currently landed seam equalities and inequalities
  alone still do not imply any finite theorem-grade upper bound on
  `Tau_(>1)`, because they admit an explicit one-parameter family with
  arbitrarily large tail mass
- and the new `Tau`-bound route theorem sharpens the strongest local-Wilson
  repair attempt: if the exact local Wilson three-sample triple is forced into
  the first-seam truncation model with a uniform tail box, then necessarily
  `tau >= 0.7015600400931378...`, so no `tau < 0.7` repair exists on that
  route
- and the new three-sample environment-evaluator route theorem closes the
  actual compressed-sector geometry: the three named environment values factor
  through one common beta-side vector and one fixed three-row Peter-Weyl
  sample operator, but the current exact stack still does not determine that
  beta-side vector, so even the normalized three-sample triple is not yet
  unique
- and the new identity-`Tau` insufficiency theorem closes the next tempting
  scalar shortcut: even fixing `Z_hat_6(e)` together with `Tau_(>1)` still
  leaves an affine family of retained first-seam completions, so identity
  closure plus tail-mass closure is still not enough to determine the retained
  coefficient pair or the first symmetric retained three-sample triple
- and the new identity-plus-three-sample higher-orbit underdetermination
  theorem closes the next deeper shortcut: even full closure of the first
  sample packet `{e, W_A, W_B, W_C}` still does not determine the full
  higher-orbit beta-side coefficient stack, so no finite first sample packet
  by itself closes the full beta-side vector `v_6`
- and the new finite-sample-packet nonclosure theorem closes the sample-only
  strategy completely: no finite marked-holonomy sample packet, no matter how
  large, can by itself determine the full beta-side vector `v_6`
- and the new parent / intertwiner boundary theorem closes the correct
  `30k`-foot theory reading of step 1: it is already exact on the Wilson gauge
  surface but not yet globally across the live retained sectors, so more
  step-1 work is still required before a positive global PF-selector theorem is
  honest
- and the new three-step global-program boundary theorem closes the full
  top-to-bottom work order: step 1 is only partially closed, step 2 is the
  actual bottleneck, and step 3 is downstream and not yet available
- and the new Wilson-to-PMNS descendant boundary theorem sharpens step 2
  itself: the actual bottleneck is cross-sector provenance, not lack of PMNS
  internal exactness
- and the new Wilson-to-Hermitian current-bank nonrealization theorem closes
  the hidden-bank loophole on step 2A: the current exact bank does **not**
  already contain the missing Wilson-to-`D_-` / Wilson-to-`dW_e^H` bridge
  under another name
- and the new step-2 minimal positive-completion theorem closes the sharp
  positive target class: any future success must supply one new
  Wilson-to-Hermitian descendant law and, if needed after that, at most one
  reduced PMNS mixed-bridge amplitude
- and the new step-2 bridge-candidate-shape theorem closes the admissible
  candidate class further: scalar-only and support-only constructions are no
  longer live positive bridge candidates
- and the new step-2 operator-form theorem closes the proof-form issue: any
  future positive step-2A theorem must be written as an operator-level
  descendant/intertwiner law into charged-sector data, not as scalar rhetoric
- and the new step-2 charged-embedding boundary theorem closes the next
  constructive prerequisite: before any positive operator candidate can be
  instantiated, the branch still needs an explicit Wilson-side charged
  embedding/compression object realizing the support label `E_e`
- and the new step-2 charged-support pullback boundary theorem closes the
  tempting support shortcut: the current taste-cube / BZ-corner support bank
  cannot by itself pull back the PMNS-side charged support `E_e` into a
  Wilson-side embedding/compression object, so any future `I_e` / `P_e` must
  carry genuinely new cross-sector microscopic content
- and the new step-2 microscopic-channel target theorem closes the next
  constructive reduction: after support labeling, projector readout, and
  downstream Hermitian packet algebra are stripped off, the live unresolved
  step-2A object is exactly a new Wilson-to-charged microscopic channel, with
  `Wilson -> D_-` the cleanest strong target
- and the new step-2 direct-`dW_e^H` route reduction theorem closes the
  compressed alternative just as sharply: `Wilson -> dW_e^H` is a fully typed
  upstream route, and if it lands the only remaining PMNS-side blocker is the
  right-sensitive selector on `dW_e^H`, equivalently the reduced single-
  amplitude bridge class already isolated on the PF lane
- and the new step-2 two-route current-bank closure theorem closes the route
  analysis itself: the honest upstream step-2A route space is exhausted by
  exactly `Wilson -> D_-` and `Wilson -> dW_e^H`, the current exact bank
  realizes neither, and the remaining work is now genuinely constructive
- and the new step-2 strong-route breaking-source target theorem sharpens the
  strong route itself: on the active charged-lepton branch `Wilson -> D_-`
  already reduces to the off-seed `5`-real breaking-source law beyond the
  aligned seed patch, so even the strong route is no longer an unconstrained
  full-operator search
- and the new step-2 active-five-real target theorem cleans that up one level
  further: the active off-seed `5`-real packet is the smallest live
  `D_-`-level target on the strong route, while the compressed `Wilson ->
  dW_e^H` route already factors through the smaller charged projected-source
  law
- and the new step-2 active-five-real current-bank nonrealization theorem
  closes the corresponding loophole: the current exact bank still does **not**
  determine that off-seed `5`-real packet, so the PMNS-side step-2A
  obstruction is now pinned to one exact missing packet
- and the new step-2 charged-source-family target theorem closes the compressed
  route’s construction shape: the remaining primitive there is not more
  `dW_e^H -> H_e -> packet -> triplet` algebra, but one Wilson-side charged
  source family / channel whose responses land on the projected Hermitian law
- and the new step-2 charged-source-family current-bank nonrealization theorem
  closes the matching hidden-bank loophole: the current exact bank still does
  **not** already realize that compressed-route primitive, so the compressed PF
  route is now a genuinely constructive one-primitive gap
- and the new step-2 nine-channel charged-source target theorem sharpens that
  compressed primitive one level further: because `dW_e^H` reconstructs `H_e`
  from nine real Hermitian-basis responses, the minimal constructive
  Wilson-side target may already be posed as a finite nine-channel charged
  Hermitian source family on `E_e`, rather than as an unconstrained operator
  hunt
- and the new step-2 rank-3 embedded nine-probe candidate-boundary theorem
  sharpens the strongest live positive compressed-route candidate one level
  further still: the best remaining positive class is now a rank-3 Wilson-side
  charged embedding/compression `I_e` or `P_e` together with the induced
  embedded nine-probe Hermitian response family, while scalar-only,
  support-only, and hidden-current-bank classes are already dead
- and the new step-2 rank-3 embedded nine-probe explicit-response boundary
  theorem sharpens that live class from candidate shape to formula-level
  operator/source form: for embedded probes `J_a(t) = t I_e B_a I_e^*`, the
  nine Wilson first variations reconstruct the Hermitian compression
  `H_e^(cand) = (I_e^* D^(-1) I_e + (I_e^* D^(-1) I_e)^*) / 2`, so the exact
  positive compressed-route target is now explicitly to prove
  `(d/dt) W[t I_e X I_e^*]|_(t=0) = Re Tr(X H_e)` on `Herm(3)`; and the same
  theorem closes the sharper current-bank no-go that even this formula-level
  class is still not instantiated on the present bank
- and the new step-2 Hermitian resolvent-compression target theorem sharpens
  that frontier one level further still: the full explicit-response family and
  its nine basis probes are only the coordinate form of one operator identity,
  namely
  `H_e^(cand) = (I_e^* D^(-1) I_e + (I_e^* D^(-1) I_e)^*) / 2 = H_e`, so the
  strongest honest next compressed-route theorem surface is now exactly that
  single Hermitian resolvent-compression identity together with the same
  sharper current-bank no-go that the bank still lacks theorem-grade
  `I_e / P_e` and theorem-grade identification of `H_e^(cand)` with `H_e`
- and the new step-2 Wilson matrix-source embedding target theorem sharpens
  the Wilson primitive one level further still: theorem-grade `I_e / P_e` is
  exactly equivalent to theorem-grade realization of a rank-3 Wilson
  matrix-source embedding `Phi_e : Mat_3(C) -> End(H_W)` with
  `Phi_e(X) = I_e X I_e^*` and `Phi_e(1_3) = P_e`, so the strongest honest
  next compressed-route Wilson object is now that invariant source-algebra
  realization class together with the same Hermitian resolvent-compression
  identity; and the current bank still does **not** realize even that sharper
  invariant form
- and the new step-2 nine-channel minimality theorem sharpens that same
  Wilson compressed route at the finite coordinate level: because the target
  space is `Herm(3)`, nine real response channels are not only sufficient but
  dimensionally minimal, so no honest generic compressed-route closure can use
  eight or fewer real channels without additional structure
- and the new step-2 Hermitian source-embedding target theorem separates the
  stronger invariant Wilson source algebra from the weaker minimal object
  actually used by the compressed theorem: full
  `Phi_e : Mat_3(C) -> End(H_W)` is still a clean sufficient source-algebra
  formulation, but the live compressed response theorem only probes the
  Hermitian restriction
  `Psi_e := Phi_e |_(Herm(3)) : Herm(3) -> Herm(H_W)`, whose image is the
  exact nine-dimensional Wilson Hermitian source plane carrying the minimal
  finite response family; and the current bank still does **not** realize even
  that weaker Hermitian source embedding, so the self-adjoint reformulation
  does not open a new loophole
- and the new step-2 Wilson compressed-resolvent block target theorem then
  collapses the Wilson compressed theorem one level further still: theorem-
  grade `Phi_e` together with `H_e^(cand) = H_e` is exactly equivalent to one
  invariant rank-`3` Wilson block law
  `P_e S_W P_e |_(Ran(P_e)) ~= H_e`, equivalently
  `P_e S_W P_e = I_e H_e I_e^*` for
  `S_W := (D^(-1) + (D^(-1))^*) / 2`; so the strongest honest next Wilson
  compressed-route theorem surface is now one projector-compression law rather
  than separate source-embedding and response identities, and the current bank
  still does **not** realize even that sharper block law
- and the new step-2 Wilson compressed-block spectral-reduction theorem
  sharpens that same Wilson frontier one level further still: once the
  rank-`3` Wilson support exists, the invariant block law reduces to the three
  scalar spectral identities `Tr(B_e^k) = Tr(H_e^k)` for `k = 1, 2, 3`, so
  the post-support verification target is no longer a full coordinate-level
  `3 x 3` block identity but only one finite spectral packet; while the
  current bank still does **not** reach even that post-support stage because
  theorem-grade support realization is still missing
- and the new step-2 Wilson Hermitian source-packet realization theorem
  sharpens the Wilson support problem one level further still: theorem-grade
  support realization is exactly equivalent to one finite `9`-element
  Hermitian Wilson source packet whose reconstructed operators satisfy the
  explicit matrix-unit table and whose diagonal sum is a rank-`3` projector;
  so the live Wilson construction problem is already finite before any
  post-support spectral verification, while the current bank still does
  **not** realize even that finite packet
- and the new step-2 Wilson finite-certificate target theorem packages that
  whole Wilson front in the sharpest reviewer-facing way yet: the compressed
  Wilson route is now exactly one finite `9 + 3` certificate, namely one
  finite `9`-element Hermitian support packet followed by only the `3` scalar
  spectral identities `Tr(B_e^k) = Tr(H_e^k)` for `k = 1, 2, 3`; and the
  present bank still fails already at the first support-side layer rather than
  at the later spectral layer
- and the new step-2 Wilson seven-packet chain-reduction theorem sharpens the
  support side one level further still: the long `(1,3)` corner is already
  downstream of nearest-neighbor chain data, so theorem-grade Wilson support
  realization is already equivalent to one smaller nearest-neighbor `7`-packet
  plus finite chain identities, rather than even the full `9`-packet as
  primitive input; and the present bank still does **not** realize even that
  sharper support layer
- and the new step-2 Wilson sharp finite-certificate target theorem packages
  the whole Wilson front at that sharper level too: the compressed Wilson
  route is now exactly one sharp `7 + 3` certificate, namely one
  nearest-neighbor `7`-packet support layer followed by only the `3` scalar
  spectral identities `Tr(B_e^k) = Tr(H_e^k)` for `k = 1, 2, 3`; and the
  present bank still fails already at the first support-side layer
- and the new step-2 Wilson four-packet off-diagonal reduction theorem
  sharpens the support side again: the diagonal support data are already
  downstream of the off-diagonal nearest-neighbor chain, so theorem-grade
  Wilson support realization is already equivalent to one Hermitian
  off-diagonal `4`-packet plus finite chain identities; and the present bank
  still does **not** realize even that sharper support layer
- and the new step-2 Wilson sharpest finite-certificate target theorem then
  packages the whole Wilson front at that sharpest level too: the compressed
  Wilson route is now exactly one sharpest `4 + 3` certificate, namely one
  Hermitian off-diagonal `4`-packet support layer followed by only the `3`
  scalar spectral identities `Tr(B_e^k) = Tr(H_e^k)` for `k = 1, 2, 3`; and
  the present bank still fails already at the first support-side layer
- and the new step-2 Wilson four-packet minimality theorem closes the obvious
  last finite shortcut question on that Hermitian support lane: the sharpest
  off-diagonal `4`-packet is now known to be dimensionally minimal, so no
  honest generic `3`-channel shortcut can determine arbitrary sharpest-route
  support data
- and the new step-2 Wilson two-edge chain-reduction theorem gives that same
  Wilson front its right local reading once the lattice is treated as
  physical: the sharpest support target is now one adjacent directed
  nearest-neighbor two-edge chain, with reverse edges, diagonal support data,
  and the long corner all downstream of that local chain; and the present bank
  still does **not** realize even that physical local chain
- and the new step-2 Wilson two-edge chain minimality theorem closes the last
  obvious local-lattice shortcut on that same physical lane: the adjacent
  directed two-edge chain is not just a convenient local presentation but the
  exact minimal one, so no honest generic one-edge shortcut can determine
  arbitrary sharpest-route physical chain data
- and the new step-2 Wilson physical sharpest-certificate target theorem then
  packages the whole Wilson front in the same physical language end-to-end:
  the compressed Wilson route is now exactly one local physical `2-edge + 3`
  certificate, namely one adjacent directed nearest-neighbor two-edge chain
  followed by only the `3` scalar spectral identities
  `Tr(B_e^k) = Tr(H_e^k)` for `k = 1, 2, 3`; and the present bank still fails
  already at the first local support-side layer
- and the new step-2 Wilson local two-edge source-target theorem sharpens the
  remaining constructive primitive one level further still: the open Wilson
  problem is no longer to realize an abstract charged source family, but to
  build one local adjacent two-edge Wilson source law on the physical
  nearest-neighbor lattice whose first responses land on the compressed
  Hermitian route; and the present bank still does **not** realize even that
  sharper local source layer
- and the new step-2 Wilson local Hermitian four-source reduction theorem
  sharpens that same Wilson primitive one level further still: the local
  adjacent two-edge source law is already exactly equivalent to one local
  Hermitian nearest-neighbor `4`-source packet, so the live source-side Wilson
  target is now finite again in local Hermitian packet language; and the
  present bank still does **not** realize even that sharper local Hermitian
  packet
- and the new step-2 Wilson local Hermitian sharpest-certificate theorem then
  packages the whole Wilson front at that same local constructive level too:
  the compressed Wilson route is now exactly one local Hermitian `4 + 3`
  certificate, namely one nearest-neighbor Hermitian `4`-source packet on the
  physical lattice followed by only the `3` scalar spectral identities
  `Tr(B_e^k) = Tr(H_e^k)` for `k = 1, 2, 3`; and the present bank still fails
  already at that first local Hermitian source layer
- and the new PMNS sole-axiom native-current route-exhaustion theorem closes
  the last obvious PMNS-native loophole: there is no overlooked exact
  sole-axiom PMNS-native route to nonzero `J_chi` on the current bank, so the
  next honest PMNS-native theorem target is now a graph-first current-image
  collapse law, in particular a fixed-slice nontrivial-current activation law,
  rather than another route scan
- and the new PMNS graph-first fixed-slice selector/holonomy noncollapse
  theorem sharpens that native target one level further still: even after
  fixing the trivial-character slice `w = w0`, the graph-first selector bundle
  `(tau, q)`, and one exact native twisted-flux holonomy value, the current
  bank still realizes distinct nonzero `chi`; so the next honest PMNS-native
  target is now a genuinely new fixed-slice current-image collapse law beyond
  the current selector/holonomy bank
- and the new PMNS graph-first fixed-slice two-holonomy collapse theorem then
  closes that fixed-slice readout frontier positively: once `w = w0` is fixed,
  any two independent native holonomies reconstruct `chi` exactly, so the
  remaining PMNS-native blocker is no longer collapse/readout but a sole-axiom
  law that actually produces nonzero `J_chi = chi`
- and the new PMNS graph-first fixed-slice two-holonomy production-boundary
  theorem sharpens that remaining native blocker one level further still:
  after fixed-slice readout closure, producing nonzero `J_chi = chi` is
  exactly equivalent to producing a nontrivial fixed-slice native holonomy
  pair, and the current bank still does **not** realize that source law
- and the new remaining-paths route-audit theorem closes the surviving search
  geometry more sharply: the strong and compressed PMNS-side routes now both
  reduce to one genuinely new Wilson-side charged embedding / source-family
  construction, with the compressed `Wilson -> dW_e^H` codomain the cleaner
  first target and the strong `Wilson -> D_-` packet best read as an optional
  lift only if that level is still needed afterward
- and the same route-audit theorem closes the surviving plaquette geometry more
  sharply too: the live `beta = 6` operator lane is now best read at the
  compressed class-sector level `S_6^env = P_cls K_6^env P_cls` and
  `eta_6(W) = P_cls B_6(W)`, with the rim side already further advanced than
  the bulk side and with no overlooked stronger repo-wide no-go replacing the
  need for explicit nonlocal class-sector evaluation
- and the new step-2 active five-real target theorem sharpens the PMNS-native
  strong route one level further still: the smallest live `D_-`-level step-2A
  target is now exactly the active off-seed `5`-real packet, while the
  compressed route already factors further through the smaller charged
  projected-source law
- and the matching active five-real current-bank nonrealization theorem closes
  the sharper negative statement too: the current exact bank still does **not**
  determine that off-seed `5`-real packet, so the PMNS-side current-producing
  obstruction is now pinned to one exact packet rather than a diffuse family
- and the new first propagated retained-triple target theorem sharpens that
  plaquette lane one level further still: the first honest next evaluative
  target is the propagated retained three-sample triple
  `(Z_6^env(W_A), Z_6^env(W_B), Z_6^env(W_C))`, equivalently the first
  retained propagated coefficient triple of `v_6`, rather than the full
  infinite class-sector matrix of `S_6^env` or the full family
  `W -> eta_6(W)`
- and the new plaquette `beta = 6` identity-rim reduction theorem sharpens
  the same operator lane structurally: explicit class-sector closure depends
  on the compressed bulk data together with the identity rim datum
  `eta_6(e) = P_cls B_6(e)`, while generic marked-holonomy dependence is
  already downstream through the universal evaluation functional `K(W)`
- and the new plaquette compressed rim-evaluation theorem sharpens the same
  rim side one level further still: after compression to the marked class
  sector, the boundary class function is already exactly
  `Z_beta^env(W) = <K(W), v_beta>`, so the compressed `W`-dependence is no
  longer open at all and the remaining compressed unknown is only the
  beta-dependent coefficient vector `v_beta`
- and the new plaquette `beta = 6` identity-rim cyclic-bulk reduction
  theorem sharpens that same bulk lane one level further still: the live
  upstream operator object is not the whole compressed bulk operator
  `S_6^env`, but only its `eta_6(e)`-cyclic compression, equivalently the
  cyclic spectral measure / Jacobi data seen from the identity rim state;
  and even fixed `eta_6(e)` together with the exact propagated retained triple
  still does **not** determine that reduced cyclic bulk object on the current
  bank
- and the new plaquette `beta = 6` identity-rim finite-Krylov bulk reduction
  theorem sharpens that bulk front one level further still: at fixed
  propagation depth, not even the whole `eta_6(e)`-cyclic object is the
  honest target, only the finite `eta_6(e)`-generated Krylov block matters;
  but the current propagated retained triple still does **not** determine even
  the first nontrivial `2 x 2` Krylov/Lanczos block on that front
- and the new global current-stack closure theorem closes the global question
  at current-stack level: sector-local PF theorems are exact, but one common
  sole-axiom global PF selector is still not derivable because the PMNS side
  still lacks a nontrivial descendant/projection law and the plaquette side
  still lacks determined operator-side beta data
- and the new step-3 compatibility-prerequisite audit theorem sharpens the
  program order one level further: common-state compatibility is still not
  theorem-ready because the current stack still lacks a global parent theorem,
  a Wilson-to-PMNS descendant law, fixed plaquette framework-point PF data,
  and matched positive-map hypotheses
- on the local marked side, the new rim-coupling boundary theorem shows that
  the theorem-grade local data are already exhausted by `exp[(beta/2)J]` and
  `D_beta^loc`
- and the new compressed rim-functional uniqueness theorem shows that the
  retained left boundary functional is already the universal Peter-Weyl
  evaluation functional `K(W)`, so the unresolved local object is now the full
  slice lift `B_6(W)`, not retained class-sector `W`-dependence

So the current exact stack supports:

- Perron-Frobenius reduction as a derived theorem once a concrete
  positive sector operator is fixed

and does not yet support:

- one unique common Perron-Frobenius state selected across sectors by the
  sole axiom alone

## Sector-local positive results already on `main`

### 1. Plaquette sector

From
[GAUGE_VACUUM_PLAQUETTE_PERRON_REDUCTION_THEOREM_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_PERRON_REDUCTION_THEOREM_NOTE.md):

- every finite Wilson `3 spatial + 1 derived-time` transfer operator with
  `beta > 0` is positivity-improving
- the spectral radius is simple
- there is one unique normalized strictly positive Perron vector
- the large-derived-time thermal state reduces exactly to that Perron state

This is a genuine sector-level Perron theorem from the current exact stack.

### 2. Strong-CP sector

From [STRONG_CP_THETA_ZERO_NOTE.md](./STRONG_CP_THETA_ZERO_NOTE.md):

- the retained topological-sector weights are non-negative
- the retained `theta`-deformed partition obeys `|Z(theta)| <= Z(0)`
- therefore the retained free energy is minimized at `theta = 0`

This is not phrased as a transfer-operator Perron theorem, but it is the
same positivity-selection logic on the retained action surface.

### 2a. Wilson parent/compression sector

From
[GAUGE_VACUUM_PLAQUETTE_PARENT_COMPRESSION_THEOREM_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_PARENT_COMPRESSION_THEOREM_NOTE_2026-04-17.md):

- the finite Wilson one-clock transfer object is the parent partition
  object on the Wilson surface
- the plaquette source-sector transfer law is a canonical descendant under
  source-sector compression
- the strong-CP `theta` law is a canonical descendant under topological
  Fourier weighting

So the Wilson surface now supports more than one exact PF-relevant
descendant construction from one parent object.

### 3. PMNS aligned transfer sector

From
[PMNS_TRANSFER_OPERATOR_DOMINANT_MODE_NOTE.md](./PMNS_TRANSFER_OPERATOR_DOMINANT_MODE_NOTE.md):

- the aligned `hw=1` active transfer kernel has one unique dominant
  symmetric mode
- the orthogonal complement is doubly degenerate
- the two eigenvalues reconstruct the aligned active seed pair exactly

So there is an exact native dominant-mode law on that aligned transfer
subsector.

## Why this is not yet a global sole-axiom selector theorem

### Boundary 1. The sole axiom still does not derive the nontrivial PMNS pack

The positive PMNS transfer theorem above is aligned-seed only.

From
[PMNS_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md](./PMNS_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md):

- if the nontrivial `hw=1` source/transfer pack is supplied, the retained
  PMNS lane closes exactly

But from
[PMNS_SOLE_AXIOM_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md](./PMNS_SOLE_AXIOM_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md):

- the strongest canonical sole-axiom `hw=1` source/transfer construction
  stays trivial
- the derived active/passive blocks are exactly `(I3, I3)`

So the current stack does not yet derive the nontrivial PMNS transfer data
that a common PF selector theorem would need.

### Boundary 2. The PMNS projection theorem is still missing

From
[GAUGE_VACUUM_PLAQUETTE_PARENT_COMPRESSION_THEOREM_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_PARENT_COMPRESSION_THEOREM_NOTE_2026-04-17.md):

- the Wilson parent object is now linked exactly to the plaquette and
  strong-CP descendants

But the repo still does **not** prove that the PMNS aligned transfer law is
also a canonical descendant of that same parent object.

### Boundary 3. Even the plaquette spatial-environment PF data are not yet uniquely forced

From
[GAUGE_VACUUM_PLAQUETTE_PERRON_JACOBI_UNDERDETERMINATION_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_PERRON_JACOBI_UNDERDETERMINATION_NOTE.md):

- the exact source operator `J` is fixed
- the transfer reduction to a unique Perron state for each admissible
  operator is fixed
- the normalized mixed-kernel local factor is fixed
- but distinct admissible positive residual environment operators can still
  induce different Perron moments for the same `J`

And from
[GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_TRANSFER_UNDERDETERMINATION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_TRANSFER_UNDERDETERMINATION_NOTE_2026-04-17.md):

- the live residual environment data already sit inside one explicit
  orthogonal-slice transfer / boundary law
- but distinct admissible positive `S_6^env` / `eta_6` pairs can still induce
  different `rho_(p,q)(6)` data and therefore different plaquette PF data

And from
[GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_CONSTRUCTION_BOUNDARY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_CONSTRUCTION_BOUNDARY_NOTE_2026-04-17.md):

- the earliest missing constructive datum on that branch is one level below
  `S_6^env`,
- namely the explicit orthogonal-slice kernel `K_6^env` and the rim map to
  `eta_6`,
- because once those are explicit, the transfer object, the boundary
  coefficients, and the plaquette PF data follow

And from
[GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_ONE_SLAB_ORTHOGONAL_KERNEL_INTEGRAL_BOUNDARY_SCIENCE_ONLY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_ONE_SLAB_ORTHOGONAL_KERNEL_INTEGRAL_BOUNDARY_SCIENCE_ONLY_NOTE_2026-04-17.md):

- the kernel side is now sharpened to its strongest honest current form:
  `K_beta^env` is one exact bulk Wilson/Haar one-slab integral,
- the marked-holonomy dependence is separated into a distinct local rim
  integral producing `eta_beta(W)`,
- but neither integral is yet explicitly evaluated in closed form at
  `beta = 6`

And from
[GAUGE_VACUUM_PLAQUETTE_FULL_SLICE_RIM_LIFT_INTEGRAL_BOUNDARY_SCIENCE_ONLY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FULL_SLICE_RIM_LIFT_INTEGRAL_BOUNDARY_SCIENCE_ONLY_NOTE_2026-04-17.md):

- the local side is now sharpened to its strongest honest current form:
  `B_beta(W)` is one exact local Wilson/Haar rim integral on the full slice
  Hilbert space,
- `eta_beta(W)` is its canonical compressed descendant,
- but explicit closed-form `beta = 6` evaluation of either object is still
  open

And from
[GAUGE_VACUUM_PLAQUETTE_BETA6_EVALUATION_SEAM_REDUCTION_SCIENCE_ONLY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_BETA6_EVALUATION_SEAM_REDUCTION_SCIENCE_ONLY_NOTE_2026-04-17.md):

- after the new integral-boundary theorems, the remaining explicit `beta = 6`
  plaquette PF seam is exactly evaluation of the class-sector matrix elements
  induced by `K_6^env` and `B_6(W)`,
- the compressed `W`-dependence is already canonical through the Peter-Weyl
  evaluation law,
- so no additional post-compression structural theorem is missing before
  explicit evaluation

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_EXACT_RADICAL_RECONSTRUCTION_MAP_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_EXACT_RADICAL_RECONSTRUCTION_MAP_NOTE_2026-04-17.md):

- on the first symmetric retained witness sector, the named three-sample matrix
  is already explicit in radicals,
- its inverse reconstruction map is already one exact algebraic formula,
- the `W_A` row decoupling is structurally explained by an antipodal torus
  eigenvalue pair,
- the remaining `chi_(1,1)` rows have opposite exact sign,
- so the remaining first retained plaquette task is exactly explicit evaluation
  of `Z_6^env(W_A)`, `Z_6^env(W_B)`, `Z_6^env(W_C)`, not matrix design

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_CURRENT_STACK_CONSTRAINT_BOUNDARY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_CURRENT_STACK_CONSTRAINT_BOUNDARY_NOTE_2026-04-17.md):

- the three named holonomies are pairwise neither conjugate nor
  inverse-conjugate, so current centrality and reality do not identify any
  pair of sample values,
- the exact three-sample matrix remains full-rank and irreducible on the first
  witness sector,
- and the connected-hierarchy, bridge-support, and observable-principle stack
  still does not add a holonomy-resolved linear relation among those three
  named same-surface values

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_LOCAL_WILSON_PARTIAL_EVALUATION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_LOCAL_WILSON_PARTIAL_EVALUATION_NOTE_2026-04-17.md):

- the current atlas already fixes the strongest exact local sample-side data on
  the named seam,
- namely the exact `J(W_i)`, exact half-slice factors `exp(3J(W_i))`, and
  exact normalized local one-plaquette values at `W_A, W_B, W_C`,
- so the branch now has one real exact first approximant on the three-sample
  seam,
- but those values are still only the local Wilson block, not the full
  environment amplitudes `Z_6^env(W_i)`

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_LOCAL_WILSON_RETAINED_POSITIVE_CONE_OBSTRUCTION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_LOCAL_WILSON_RETAINED_POSITIVE_CONE_OBSTRUCTION_NOTE_2026-04-17.md):

- that exact local Wilson three-sample block is already incompatible with the
  first symmetric retained positive-type environment cone,
- after exact radical reconstruction its adjoint-orbit coordinate is strictly
  negative,
- so the open `K_6^env / B_6(W)` completion is mathematically necessary before
  or at retained-sector closure, not just a precision upgrade of the local
  sample-side block

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_CONTROLLED_RETAINED_COEFFICIENT_WEDGE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_CONTROLLED_RETAINED_COEFFICIENT_WEDGE_NOTE_2026-04-17.md):

- the current coefficient-side control is now exact up to one open tail mass
  `Tau_(>1)`,
- namely `rho_(1,0)(6)` and `rho_(1,1)(6)` satisfy explicit `Tau`-controlled
  linear upper bounds and one exact outer wedge,
- but because `Tau_(>1)` itself is still open, this is a genuine bound theorem
  rather than a coefficient solve

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_UPPER_BOUND_NONDERIVATION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_UPPER_BOUND_NONDERIVATION_NOTE_2026-04-17.md):

- the branch now has one exact current-stack nonderivation theorem for
  `Tau_(>1)`,
- namely the presently landed seam constraints still admit a one-parameter
  family with arbitrarily large `Tau_(>1)`,
- so no finite theorem-grade upper bound on `Tau_(>1)` follows yet from the
  current exact seam bank alone,
- and the missing load-bearing input is now sharper than before:
  either one actual bound/evaluation on `Z_hat_6(e)` or the higher tail, or
  one genuine `K_6^env / B_6(W)` evaluation route

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_BOUND_ROUTE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_BOUND_ROUTE_NOTE_2026-04-17.md):

- the branch now has one exact sharp route boundary on the local-Wilson repair
  path,
- namely if the explicit local Wilson three-sample triple is forced into the
  first-seam truncation-envelope model with `rho_(1,0), rho_(1,1) >= 0` and
  `|R_i^(>1)| <= tau`, then necessarily
  `tau >= tau_* = 0.7015600400931378...`,
- and that lower bound is sharp, attained at
  `rho_(1,0)^* = 0.07110955014685417...`, `rho_(1,1)^* = 0`,
- so no `tau < 0.7` repair exists on the local-Wilson completion route

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_ENVIRONMENT_EVALUATOR_ROUTE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_ENVIRONMENT_EVALUATOR_ROUTE_NOTE_2026-04-17.md):

- the actual compressed-sector evaluator route is now theorem-grade and exact:
  the three named environment values factor through one common propagated
  beta-side vector and one fixed three-row sample operator,
- on the first symmetric witness sector that operator is exactly the radical
  matrix `F`,
- but the current exact stack still does not determine the common beta-side
  vector,
- so even the normalized three-sample environment triple is still not unique,
  and no actual evaluator is yet closed on the present science surface

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_IDENTITY_TAU_INSUFFICIENCY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_IDENTITY_TAU_INSUFFICIENCY_NOTE_2026-04-17.md):

- the branch now closes the next scalar shortcut exactly:
  even fixed `Z_hat_6(e)` together with fixed `Tau_(>1)` still leaves a full
  affine retained fiber
  `18 rho_(1,0)(6) + 64 rho_(1,1)(6) = C`,
- along that fiber the first symmetric retained three-sample triple varies
  nontrivially under the exact radical operator `F`,
- so identity-channel closure plus tail-mass closure is still not enough to
  determine the retained coefficient pair, the retained three-sample triple,
  or the full beta-side vector `v_6`

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_PROPAGATED_RETAINED_TRIPLE_FIXED_RETAINED_PAIR_TAU_NONCOLLAPSE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_PROPAGATED_RETAINED_TRIPLE_FIXED_RETAINED_PAIR_TAU_NONCOLLAPSE_NOTE_2026-04-17.md):

- the branch now closes the sharper coefficient-side shortcut too:
  even fixed
  `(rho_(1,0)(6), rho_(1,1)(6), Tau_(>1))`
  still does **not** determine the propagated retained triple on the present
  bank,
- already on the explicit higher-orbit two-slice `{(0,2), (0,3)}`, fixed
  `Tau_(>1)` leaves nontrivial variation in the propagated three-sample
  remainder because the two sample profiles are not proportional to their
  identity weights,
- so even exact first-retained coefficient closure together with tail-mass
  closure would still not evaluate the minimal propagated plaquette target

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_PROPAGATED_RETAINED_TRIPLE_OPERATOR_SIDE_NONCLOSURE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_PROPAGATED_RETAINED_TRIPLE_OPERATOR_SIDE_NONCLOSURE_NOTE_2026-04-17.md):

- the branch now closes the sharper operator-side clarification at that same
  finite target:
  even exact evaluation of the propagated retained three-sample triple still
  does **not** determine the full beta-side vector or the unresolved
  operator-side `beta = 6` data on the present bank,
- already on the explicit four-orbit higher slice
  `{(0,2), (0,3), (0,4), (0,5)}`, the propagated-triple map has nontrivial
  kernel by dimension alone,
- and the runner exhibits distinct nonnegative higher-orbit coefficient stacks
  with the same exact propagated retained triple,
- so the propagated retained triple is the correct next finite evaluator
  target, but still not an operator-side closure datum

And from
[GAUGE_VACUUM_PLAQUETTE_IDENTITY_PLUS_THREE_SAMPLE_HIGHER_ORBIT_UNDERDETERMINATION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_IDENTITY_PLUS_THREE_SAMPLE_HIGHER_ORBIT_UNDERDETERMINATION_NOTE_2026-04-17.md):

- the branch now closes the next finite-sample shortcut too:
  even if the full first sample packet
  `{Z_hat_6(e), Z_hat_6(W_A), Z_hat_6(W_B), Z_hat_6(W_C)}`
  were fixed exactly, the higher-orbit beta-side coefficients would still not
  be determined,
- already on a five-dimensional higher-orbit slice, those four scalars leave a
  nontrivial kernel by dimension alone,
- and because the identity row carries strictly positive orbit weights, that
  kernel supports positive distinct higher-orbit coefficient stacks with the
  same full first sample packet,
- so no finite first sample packet by itself closes the full beta-side vector
  `v_6`

And from
[GAUGE_VACUUM_PLAQUETTE_FINITE_SAMPLE_PACKET_NONCLOSURE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FINITE_SAMPLE_PACKET_NONCLOSURE_NOTE_2026-04-17.md):

- the branch now closes the sample-only strategy at full generality:
  for any finite marked-holonomy packet `P`, the corresponding finite summary
  map still has nontrivial higher-orbit kernel on a sufficiently large finite
  higher-orbit slice,
- because the identity functional has strictly positive orbit weights, that
  kernel supports positive nonunique higher-orbit coefficient stacks with the
  same full finite packet,
- so no finite sample packet, no matter how large, can by itself determine the
  full beta-side vector `v_6`

And from
[PERRON_FROBENIUS_PARENT_INTERTWINER_BOUNDARY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_PARENT_INTERTWINER_BOUNDARY_NOTE_2026-04-17.md):

- the branch now closes the correct theory-ordering statement for the global PF
  program:
  step 1 is exact on the Wilson gauge surface but not yet globally across the
  live retained sectors,
- so the next honest target is to strengthen the Wilson parent object into a
  true parent / intertwiner theorem,
- not to behave as if step 1 were already globally complete and only
  framework-point evaluation work remained

And from
[PERRON_FROBENIUS_THREE_STEP_GLOBAL_PROGRAM_BOUNDARY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_THREE_STEP_GLOBAL_PROGRAM_BOUNDARY_NOTE_2026-04-17.md):

- the branch now closes the full top-to-bottom work order for the global PF
  program:
  step 1 is only partially closed,
  step 2 is the actual bottleneck,
  and step 3 is still downstream and not yet available,
- so the next honest science target is not "global PF directly" but the
  missing Wilson-to-PMNS descendant / intertwiner theorem

And from
[PERRON_FROBENIUS_WILSON_TO_PMNS_DESCENDANT_BOUNDARY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_WILSON_TO_PMNS_DESCENDANT_BOUNDARY_NOTE_2026-04-17.md):

- the branch now closes the exact step-2 bottleneck statement:
  the missing theorem is specifically a Wilson-to-PMNS descendant /
  intertwiner theorem,
- internal PMNS support and transport exactness already exist, but they are not
  yet the missing cross-sector provenance law

And from
[PERRON_FROBENIUS_ALL_PATHS_ATTACK_PROGRAM_NOTE_2026-04-17.md](./PERRON_FROBENIUS_ALL_PATHS_ATTACK_PROGRAM_NOTE_2026-04-17.md):

- the branch now closes the exact all-paths work order for the PF lane:
  step-1 strengthening, step-2A Hermitian descendant, step-2B non-Hermitian
  current path, plaquette operator sidecar, then step-3 compatibility,
- the branch also now places the useful published math templates correctly:
  Luescher on step 1, Stinespring on step 2, Evans-Hoegh-Krohn on step 3
- and the new current-bank nonrealization theorem closes the remaining
  “maybe the bridge is already somewhere in the bank” loophole on step 2A

And from
[PERRON_FROBENIUS_PMNS_DESCENDANT_TARGET_DECOMPOSITION_NOTE_2026-04-17.md](./PERRON_FROBENIUS_PMNS_DESCENDANT_TARGET_DECOMPOSITION_NOTE_2026-04-17.md):

- the PMNS side of step 2 is no longer one undifferentiated target,
- the clean first codomain is the projected Hermitian response data
  `dW_e^H / H`,
- and the clean residual last-mile target is the native nontrivial current
  `J_chi`, if the Hermitian descendant theorem still leaves a PMNS-side gap

And from
[PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_REDUCTION_NOTE_2026-04-17.md](./PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_REDUCTION_NOTE_2026-04-17.md):

- the first honest step-2A theorem target is now sharper than
  `Wilson -> dW_e^H / H` abstractly,
- the exact charged-sector codomain is the Schur-pushforward chain
  `Wilson -> D_- -> dW_e^H -> H_e`,
- because once `dW_e^H` is known, the selected transport packet and the
  projected-source triplet channels are already downstream algorithmic

And from
[PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_CURRENT_STACK_BOUNDARY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_CURRENT_STACK_BOUNDARY_NOTE_2026-04-17.md):

- the current stack now also closes the exact step-2A boundary:
  codomain-side Hermitian structure is already exact once microscopic `D` is
  supplied,
- constructive existence and minimal sheet-selection reduction are already
  exact directly on `dW_e^H`,
- so the actual missing step-2A object is upstream provenance:
  a Wilson-to-`D_-` / Wilson-to-`dW_e^H` descendant theorem

And from
[PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_CURRENT_BANK_NONREALIZATION_NOTE_2026-04-17.md](./PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_CURRENT_BANK_NONREALIZATION_NOTE_2026-04-17.md):

- the branch now also closes the hidden-bridge loophole on step 2A:
  the current exact bank does **not** already contain that missing
  Wilson-to-`D_-` / Wilson-to-`dW_e^H` bridge under another name,
- existing Wilson, observable, support, and PMNS tools are exact on their own
  domains, but none yet realizes the missing cross-sector descendant law

And from
[PERRON_FROBENIUS_STEP2_MINIMAL_POSITIVE_COMPLETION_CLASS_NOTE_2026-04-17.md](./PERRON_FROBENIUS_STEP2_MINIMAL_POSITIVE_COMPLETION_CLASS_NOTE_2026-04-17.md):

- the branch now also closes the minimal positive target class for step 2:
  any future success must supply one new Wilson-to-Hermitian descendant law
  into `D_- / dW_e^H / H_e`,
- and if that Hermitian descendant still leaves a PMNS last mile, the
  remaining positive class is already reduced to one sector-sensitive
  inter-sector mixed bridge amplitude
  `B_red = a_sel (chi_N_nu - chi_N_e)`

And from
[PERRON_FROBENIUS_EXTERNAL_THEORY_PROOF_STANDARD_NOTE_2026-04-17.md](./PERRON_FROBENIUS_EXTERNAL_THEORY_PROOF_STANDARD_NOTE_2026-04-17.md):

- atlas rows are to be used only as index/navigation objects,
- published PF-adjacent mathematics is to be used only as a hypothesis
  template,
- and no external conclusion is to be imported at wider scope than the
  hypotheses actually matched on repo objects

And from
[PERRON_FROBENIUS_GLOBAL_SELECTOR_CURRENT_STACK_CLOSURE_NOTE_2026-04-17.md](./PERRON_FROBENIUS_GLOBAL_SELECTOR_CURRENT_STACK_CLOSURE_NOTE_2026-04-17.md):

- the branch now closes the global question exactly at current-stack level:
  the present exact bank supports sector-local PF / dominant-mode theorems,
  but it does **not** yet derive one common sole-axiom global PF selector,
- the PMNS-side blocker is structural:
  the strongest canonical sole-axiom `hw=1` pack is still trivial and there is
  still no Wilson-to-PMNS projection theorem,
- the plaquette-side blocker is also structural:
  the framework-point beta-side vector `v_6` is still not determined and no
  finite sample packet can determine it,
- so the remaining honest positive global route is operator-plus-projection,
  not sample-side closure or sector-local theorem restatement

And from
[GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_KERNEL_RIM_COMPRESSION_THEOREM_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_KERNEL_RIM_COMPRESSION_THEOREM_NOTE_2026-04-17.md):

- this is now theorem-grade at the pre-compression level,
- the class-sector objects `S_beta^env`, `eta_beta`, and `rho_(p,q)(beta)` are
  canonical compressions of one orthogonal-slice kernel plus one local rim map,
- so the remaining plaquette PF gap is explicitly pre-compression construction
  of `K_6^env / B_6`, not a new post-compression PF formalism

And from
[GAUGE_VACUUM_PLAQUETTE_RIM_COUPLING_BOUNDARY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_RIM_COUPLING_BOUNDARY_NOTE_2026-04-17.md):

- the theorem-grade local marked data are already fixed:
  `exp[(beta/2)J]` and `D_beta^loc`,
- the local rim lift `B_beta(W)` is now fixed at the integral-expression
  level,
- so the unresolved local issue is explicit framework-point evaluation of
  that already-identified local rim integral

And from
[GAUGE_VACUUM_PLAQUETTE_COMPRESSED_RIM_FUNCTIONAL_UNIQUENESS_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_COMPRESSED_RIM_FUNCTIONAL_UNIQUENESS_NOTE_2026-04-17.md):

- the retained left boundary functional is already explicit and universal:
  `K(W)`,
- retained class-sector `W`-dependence is therefore no longer open,
- so the remaining local issue is specifically explicit evaluation of the full
  slice lift `B_6(W)`, not another compressed boundary-functional theorem

And from
[GAUGE_VACUUM_PLAQUETTE_BETA6_SCALAR_VALUE_INSUFFICIENCY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_BETA6_SCALAR_VALUE_INSUFFICIENCY_NOTE_2026-04-17.md):

- even if the canonical same-surface plaquette value `P(6)` is fixed, that is
  still only one scalar framework-point constraint,
- one scalar value does **not** determine the class-sector vector `v_6`,
  equivalently it does not determine `rho_(p,q)(6)` or the full compressed
  boundary class function,
- so the live plaquette PF seam remains explicit matrix-element evaluation of
  `K_6^env` and `B_6(W)`, not reuse of one accepted scalar observable

And from
[GAUGE_VACUUM_PLAQUETTE_RETAINED_CLASS_SAMPLING_INVERSION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_RETAINED_CLASS_SAMPLING_INVERSION_NOTE_2026-04-17.md):

- on every retained finite marked class sector, the truncated coefficient vector
  is exactly reconstructible from enough generic marked-holonomy samples,
- too few samples remain underdetermined,
- so the finite retained constructive target is now explicit marked-holonomy
  sample evaluation, not abstract retained-sector ambiguity

And from
[GAUGE_VACUUM_PLAQUETTE_CONJUGATION_SYMMETRIC_RETAINED_SAMPLING_REDUCTION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_CONJUGATION_SYMMETRIC_RETAINED_SAMPLING_REDUCTION_NOTE_2026-04-17.md):

- the retained plaquette coefficients already satisfy conjugation symmetry,
- so the physical retained inversion dimension is conjugation-orbit count rather
  than full retained-basis size,
- for the first nontrivial four-weight witness, this drops the retained sample
  burden from four generic samples to three symmetric samples

And from
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_RECONSTRUCTION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_RECONSTRUCTION_NOTE_2026-04-17.md):

- the first symmetric retained coefficient triple is recoverable from three
  explicit regular rational-angle marked holonomies,
- one of those samples already annihilates the `chi_(1,1)` orbit contribution,
- so the next explicit plaquette PF target is now a short concrete list of
  three same-surface marked-holonomy sample values

So the current plaquette stack still does not force one unique framework-point
Perron measure at `beta = 6`, even after the spatial-transfer realization and
the Wilson parent/compression theorem, and even after one same-surface scalar
plaquette value is fixed.

## Theorem

**Theorem (current-stack PF selection boundary).** On the present exact
`Cl(3)` on `Z^3` stack, Perron-Frobenius selection is theorem-grade only
after a concrete positive operator has already been fixed. The current
stack fixes such an operator:

1. on each finite Wilson plaquette transfer surface, and
2. on the aligned `hw=1` PMNS transfer subsector.

But the current stack does **not** yet derive:

1. the nontrivial PMNS source/transfer pack from the sole axiom,
2. a canonical projection theorem linking the Wilson parent object to the
   PMNS aligned transfer law, or
3. one unique plaquette framework-point Perron measure after the residual
   environment operator is accounted for.

Therefore the present exact bank proves **sector-local** PF selection but
does **not** yet prove one **global common** sole-axiom PF selector across
the retained sectors.

## Priority order for the PF program

The current repo does **not** support attacking all sectors at once.
The most defensible PF-first program is:

1. **Wilson parent/compression theorem first.**
   This step is now closed on the Wilson surface:
   - the finite Wilson one-clock operator is the parent PF object,
   - the marked-plaquette source-sector operator is a canonical
     descendant under source-sector compression, and
   - the retained strong-CP `theta` law is a canonical descendant under
     topological Fourier weighting.

2. **Residual plaquette environment closure second.**
   On the already explicit Wilson compression ladder, the next concrete
   object is not "another PF theorem." It is the explicit `beta = 6`
   spatial-environment data:
   - the orthogonal-slice one-step kernel `K_6^env`,
   - explicit evaluation of the full local rim lift `B_6(W)` generating
     `eta_6`,
   - then the transfer operator `S_6^env`,
   - the rim boundary state `eta_6`,
   - equivalently the coefficients `rho_(p,q)(6)` or `z_(p,q)^env(6)`.

   The new spatial-environment underdetermination theorem sharpens this
   further: even after the transfer realization and the Wilson
   parent/compression theorem, that pair is still not forced. The new
   construction-boundary theorem sharpens it one level lower again: the
   earliest missing derivation target is explicit evaluation of `K_6^env`
   together with explicit evaluation of the local rim functional `B_6(W)`.
   The new one-slab orthogonal-kernel integral-boundary theorem sharpens the
   kernel side: `K_beta^env` is already fixed at the level of an exact
   Wilson/Haar slab integral, so what remains on that side is explicit
   evaluation, not kernel-class identification. The new full-slice rim-lift
   integral-boundary theorem does the same on the local side: `B_beta(W)` is
   already fixed at the level of an exact local Wilson/Haar rim integral, so
   what remains there is explicit framework-point evaluation, not local-object
   identification. The new kernel/rim compression theorem closes the
   downstream logic: once those pre-compression objects are explicitly
   evaluated, the compressed transfer data and the current plaquette
   Perron/Jacobi underdetermination disappear immediately. The new compressed
   rim-functional uniqueness theorem closes the retained left boundary
   functional too. The new current-stack constraint-boundary theorem closes one
   more tempting shortcut too: even after the exact radical reconstruction map,
   there is still no honest symmetry or current-source theorem reducing the
   first retained seam below explicit evaluation of the three named
   same-surface samples. The new local-Wilson retained positive-cone
   obstruction theorem closes another tempting shortcut: the exact local Wilson
   three-sample block already fails the retained positive-cone test, so the
   open nonlocal completion through `K_6^env / B_6(W)` is mathematically
   necessary. The new `Tau`-controlled retained-coefficient wedge theorem
   sharpens the coefficient side in the only honest current way: the first
   retained coefficients now satisfy exact linear bounds in terms of the still
   open tail mass `Tau_(>1)`. The new `Tau` upper-bound nonderivation theorem
   then closes the next exact negative statement: the current seam equalities
   and inequalities still admit arbitrarily large `Tau_(>1)`. The new
   `Tau`-bound route theorem sharpens the strongest local-Wilson repair path:
   no `tau < 0.7015600400931378...` route can repair the local Wilson triple
   inside the first-seam truncation model. And the new evaluator-route theorem
   identifies the actual compressed geometry more sharply than before: the
   three named values already factor through one fixed three-row operator, but
   the current stack still does not determine the common beta-side vector on
   which that operator must act. The new identity-`Tau` insufficiency theorem
   closes the next scalar shortcut too: even fixed `Z_hat_6(e)` together with
   fixed `Tau_(>1)` still leaves an affine retained fiber and does not
   determine the first retained coefficient pair or the retained three-sample
   triple. The new fixed-retained-pair/`Tau` noncollapse theorem sharpens that
   one level further: even fixed
   `(rho_(1,0)(6), rho_(1,1)(6), Tau_(>1))`
   still leaves nontrivial propagated-triple variation already on an explicit
   two-orbit higher-tail slice. And the new higher-orbit underdetermination
   theorem closes the next finite-sample shortcut: even the full first sample
   packet
   `{e, W_A, W_B, W_C}` still does not determine the full higher-orbit
   beta-side data. The new finite-sample-packet nonclosure theorem then closes
   the whole sample-only strategy: no finite marked-holonomy packet by itself
   can determine the full beta-side vector `v_6`. The new global current-stack
   closure theorem then states the exact global endpoint: sector-local PF is
   already theorem-grade, but one common sole-axiom global PF selector is
   still not derivable on the present bank. The new parent / intertwiner
   boundary theorem then sharpens the theory order one level further: step 1
   itself is only partially closed, exact on the Wilson surface but not yet
   globally, so the next honest target is a true parent / intertwiner theorem.

3. **PMNS only after the parent/compression lane is stronger.**
   The PMNS aligned dominant-mode theorem is real, but the nontrivial
   source/transfer pack is still not sole-axiom derived. So PMNS should
   not be sold yet as a canonical descendant of a common PF parent.

   The honest PMNS-side PF blocker remains:
   - first prove a Wilson-to-Hermitian descendant / intertwiner theorem into
     `dW_e^H / H`, or derive an equivalent nontrivial sole-axiom PMNS transfer
     construction strong enough to reach that codomain,
   - then, if the PMNS last mile still remains open, derive the non-Hermitian
     current `J_chi` or an equivalent right-sensitive descendant,
   - or prove a canonical PF projection from an already explicit parent
     operator once such a parent theorem exists.

## Corollary

The honest current classification is:

- **derived theorem, sector-local:** yes
- **single common physical-state selector from the sole axiom:** not yet

This is a boundary theorem, not an impossibility theorem. The missing
promotion would require one of the following:

1. an explicit full-space positive transfer operator together with a proof
   that the live sector transfer laws are its canonical projections, or
2. closure of the residual plaquette environment operator together with a
   Wilson-side parent/projection theorem strong enough to force the
   sector Perron data into one common projection structure that reaches
   PMNS as well

The current sharpened read is stricter than before:

- on the Wilson compressed route, the invariant missing object is now the
  matrix-source embedding `Phi_e`, more sharply the weaker Hermitian source
  embedding `Psi_e`, and sharpest of all the invariant Wilson compressed-
  resolvent block law `P_e S_W P_e |_(Ran(P_e)) ~= H_e`; while the finite
  response count is already known to be minimally `9`
- on the PMNS-native route, fixed-slice readout is now closed, so the
  remaining blocker is production of nonzero `J_chi`, not another collapse law;
  and on the strongest PMNS-native microscopic lane that blocker is now pinned
  more sharply to one active off-seed `5`-real packet beyond the fixed seed
  pair
- on the plaquette lane, the upstream operator-side target is now bulk
  class-sector data together with the identity rim datum `eta_6(e)`, and more
  sharply the compressed rim side is already explicit as
  `Z_beta^env(W) = <K(W), v_beta>` while the live bulk object is only the
  finite `eta_6(e)`-generated Krylov block of `S_6^env`; yet even exact
  evaluation of the propagated retained triple would still not by itself close
  even that first finite bulk block, much less the full operator-side
  `beta = 6` object
- on step 3, the compatibility theorem is still not ready to search because
  the prerequisite hypotheses are not all in hand

## What this closes

- the ambiguity between "there are no PF theorems here" and "PF selection
  is already globally derived"  
  the current exact answer is strictly in between
- the honest reviewer-level statement of the current PF status on `main`
- the next theorem object that would actually be needed to promote PF
  selection from sector theorem to global selector principle

## What this does not close

- an impossibility theorem against future PF unification
- a canonical projection theorem from the Wilson parent object to the
  PMNS aligned transfer law
- a sole-axiom production law for nonzero `J_chi`
- the residual plaquette bulk operator at `beta = 6`, even after the new
  identity-rim reduction
- a new canonical numeric repinning of the plaquette package
- a step-3 common-state compatibility theorem

## Atlas inputs used

- [GAUGE_VACUUM_PLAQUETTE_PERRON_REDUCTION_THEOREM_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_PERRON_REDUCTION_THEOREM_NOTE.md)
- [GAUGE_VACUUM_PLAQUETTE_PERRON_JACOBI_UNDERDETERMINATION_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_PERRON_JACOBI_UNDERDETERMINATION_NOTE.md)
- [GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_TRANSFER_UNDERDETERMINATION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_TRANSFER_UNDERDETERMINATION_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_CONSTRUCTION_BOUNDARY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_CONSTRUCTION_BOUNDARY_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_ONE_SLAB_ORTHOGONAL_KERNEL_INTEGRAL_BOUNDARY_SCIENCE_ONLY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_ONE_SLAB_ORTHOGONAL_KERNEL_INTEGRAL_BOUNDARY_SCIENCE_ONLY_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FULL_SLICE_RIM_LIFT_INTEGRAL_BOUNDARY_SCIENCE_ONLY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FULL_SLICE_RIM_LIFT_INTEGRAL_BOUNDARY_SCIENCE_ONLY_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_KERNEL_RIM_COMPRESSION_THEOREM_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_KERNEL_RIM_COMPRESSION_THEOREM_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_RIM_COUPLING_BOUNDARY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_RIM_COUPLING_BOUNDARY_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_COMPRESSED_RIM_FUNCTIONAL_UNIQUENESS_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_COMPRESSED_RIM_FUNCTIONAL_UNIQUENESS_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_EXACT_RADICAL_RECONSTRUCTION_MAP_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_EXACT_RADICAL_RECONSTRUCTION_MAP_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_CURRENT_STACK_CONSTRAINT_BOUNDARY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_CURRENT_STACK_CONSTRAINT_BOUNDARY_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_LOCAL_WILSON_RETAINED_POSITIVE_CONE_OBSTRUCTION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_LOCAL_WILSON_RETAINED_POSITIVE_CONE_OBSTRUCTION_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_CONTROLLED_RETAINED_COEFFICIENT_WEDGE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_CONTROLLED_RETAINED_COEFFICIENT_WEDGE_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_UPPER_BOUND_NONDERIVATION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_UPPER_BOUND_NONDERIVATION_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_BOUND_ROUTE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_BOUND_ROUTE_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_IDENTITY_TAU_INSUFFICIENCY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_IDENTITY_TAU_INSUFFICIENCY_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_IDENTITY_PLUS_THREE_SAMPLE_HIGHER_ORBIT_UNDERDETERMINATION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_IDENTITY_PLUS_THREE_SAMPLE_HIGHER_ORBIT_UNDERDETERMINATION_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FINITE_SAMPLE_PACKET_NONCLOSURE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FINITE_SAMPLE_PACKET_NONCLOSURE_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_PARENT_INTERTWINER_BOUNDARY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_PARENT_INTERTWINER_BOUNDARY_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_THREE_STEP_GLOBAL_PROGRAM_BOUNDARY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_THREE_STEP_GLOBAL_PROGRAM_BOUNDARY_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_WILSON_TO_PMNS_DESCENDANT_BOUNDARY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_WILSON_TO_PMNS_DESCENDANT_BOUNDARY_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_ALL_PATHS_ATTACK_PROGRAM_NOTE_2026-04-17.md](./PERRON_FROBENIUS_ALL_PATHS_ATTACK_PROGRAM_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_PMNS_DESCENDANT_TARGET_DECOMPOSITION_NOTE_2026-04-17.md](./PERRON_FROBENIUS_PMNS_DESCENDANT_TARGET_DECOMPOSITION_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_REDUCTION_NOTE_2026-04-17.md](./PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_REDUCTION_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_CURRENT_STACK_BOUNDARY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_WILSON_TO_HERMITIAN_DESCENDANT_CURRENT_STACK_BOUNDARY_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_STEP2_NINE_CHANNEL_MINIMALITY_NOTE_2026-04-17.md](./PERRON_FROBENIUS_STEP2_NINE_CHANNEL_MINIMALITY_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_EXTERNAL_THEORY_PROOF_STANDARD_NOTE_2026-04-17.md](./PERRON_FROBENIUS_EXTERNAL_THEORY_PROOF_STANDARD_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_GLOBAL_SELECTOR_CURRENT_STACK_CLOSURE_NOTE_2026-04-17.md](./PERRON_FROBENIUS_GLOBAL_SELECTOR_CURRENT_STACK_CLOSURE_NOTE_2026-04-17.md)
- [PERRON_FROBENIUS_STEP3_COMPATIBILITY_PREREQUISITE_AUDIT_NOTE_2026-04-17.md](./PERRON_FROBENIUS_STEP3_COMPATIBILITY_PREREQUISITE_AUDIT_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_ENVIRONMENT_EVALUATOR_ROUTE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_ENVIRONMENT_EVALUATOR_ROUTE_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_BETA6_IDENTITY_RIM_REDUCTION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_BETA6_IDENTITY_RIM_REDUCTION_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_BETA6_IDENTITY_RIM_CYCLIC_BULK_REDUCTION_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_BETA6_IDENTITY_RIM_CYCLIC_BULK_REDUCTION_NOTE_2026-04-17.md)
- [GAUGE_VACUUM_PLAQUETTE_TRANSFER_OPERATOR_CHARACTER_RECURRENCE_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_TRANSFER_OPERATOR_CHARACTER_RECURRENCE_NOTE.md)
- [GAUGE_VACUUM_PLAQUETTE_PARENT_COMPRESSION_THEOREM_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_PARENT_COMPRESSION_THEOREM_NOTE_2026-04-17.md)
- [PMNS_TRANSFER_OPERATOR_DOMINANT_MODE_NOTE.md](./PMNS_TRANSFER_OPERATOR_DOMINANT_MODE_NOTE.md)
- [PMNS_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md](./PMNS_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md)
- [PMNS_SOLE_AXIOM_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md](./PMNS_SOLE_AXIOM_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md)
- [PMNS_GRAPH_FIRST_FIXED_SLICE_TWO_HOLONOMY_COLLAPSE_NOTE_2026-04-17.md](./PMNS_GRAPH_FIRST_FIXED_SLICE_TWO_HOLONOMY_COLLAPSE_NOTE_2026-04-17.md)
- [DM_LEPTOGENESIS_NE_PROJECTED_SOURCE_TRIPLET_SIGN_THEOREM_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_NE_PROJECTED_SOURCE_TRIPLET_SIGN_THEOREM_NOTE_2026-04-16.md)
- [PMNS_RIGHT_POLAR_SECTION_NOTE.md](./PMNS_RIGHT_POLAR_SECTION_NOTE.md)
- [PMNS_C3_NONTRIVIAL_CURRENT_BOUNDARY_NOTE.md](./PMNS_C3_NONTRIVIAL_CURRENT_BOUNDARY_NOTE.md)
- [STRONG_CP_THETA_ZERO_NOTE.md](./STRONG_CP_THETA_ZERO_NOTE.md)

## Command

```bash
python3 scripts/frontier_perron_frobenius_selection_axiom_boundary.py
```
