# YT Observed-Mass Inversion No-Go Note

**Date:** 2026-05-01
**Status:** no-go / exact-negative-boundary for observed-comparator inversion
**Runner:** `scripts/frontier_yt_observed_mass_inversion_no_go.py`
**Certificate:** `outputs/yt_observed_mass_inversion_no_go_2026-05-01.json`

```yaml
actual_current_surface_status: no-go / exact-negative-boundary
conditional_surface_status: "Observed masses remain valid external comparators after a substrate calculation."
hypothetical_axiom_status: null
admitted_observation_status: "PDG top mass, Higgs mass, and y_t comparator values if used as proof inputs."
proposal_allowed: false
proposal_allowed_reason: "Inverting an admitted observed mass imports the target value rather than deriving it from Cl(3)/Z^3."
audit_required_before_effective_retained: true
bare_retained_allowed: false
```

## Question

Can PR #230 avoid production MC by using the observed top mass or accepted
`y_t(v)` value as the missing input, then reporting

```text
y_t = sqrt(2) m_t / v
```

as the retained derivation?

## Verdict

No.

That move is an external-observable calibration, not a substrate derivation.
It is allowed as a comparator/readout, but not as retained proof.

The runner records a simple diagnostic: direct pole-mass inversion gives

```text
sqrt(2) * 172.56 GeV / 246.282818 GeV = 0.991121...
```

which is not the running `y_t(v)` comparator near `0.9176`.  Therefore the
shortcut also cannot bypass the matching and running bridge.

## Boundary For PR #230

The direct correlator lane can measure the mass generated by a chosen heavy
top-sector mass parameter.  If that parameter is tuned to the observed top
mass, the lane is a calibrated readout.  For derivational status, PR #230 still
needs one of:

- production correlator evidence plus an independent substrate pin for the
  top-sector mass parameter;
- a separately derived UV selector for `y_t`;
- a re-audited Ward route if the framework chooses to permit it as a definition
  source.

Observed values remain comparators only.

## Verification

```bash
python3 scripts/frontier_yt_observed_mass_inversion_no_go.py
# SUMMARY: PASS=8 FAIL=0
```
