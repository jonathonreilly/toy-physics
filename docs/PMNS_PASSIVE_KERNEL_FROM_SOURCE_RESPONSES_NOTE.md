# PMNS Passive Kernel from Source Responses

Three source-response columns on the passive `hw=1` triplet determine the
passive projected kernel exactly. That kernel determines the passive monomial
block, and the recovered block determines `q` and `a_i`.

This is a lower-level derivation theorem: the theorem input is the response
column set, not `diag(a_i) P_q`.
