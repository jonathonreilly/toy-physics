# Koide Circulant Wilson-Target Note

**Date:** 2026-04-18
**Status:** support - structural or confirmatory support note
**Runner:** `scripts/frontier_koide_circulant_wilson_target.py`

## Inputs (cited authorities)

The load-bearing spectral bridge identity
`a_0^2 - 2 |z|^2 = 3 a^2 - 6 |b|^2` used in §"Theorem 3" is imported
verbatim from:

- [`KOIDE_CIRCULANT_CHARACTER_BRIDGE_NARROW_THEOREM_NOTE_2026-05-09.md`](KOIDE_CIRCULANT_CHARACTER_BRIDGE_NARROW_THEOREM_NOTE_2026-05-09.md)
  — the standalone polynomial-algebra identities `a_0 = sqrt(3) a`,
  `z = sqrt(3) b`, and `a_0^2 - 2 |z|^2 = 3 a^2 - 6 |b|^2` on a
  `C_3[111]`-circulant Hermitian. Self-contained (zero one-hop deps),
  so it provides the algebraic identity without importing any further
  upstream authority.

Companion structural inputs already in the one-hop deps:

- [`KOIDE_CYCLIC_WILSON_DESCENDANT_LAW_NOTE_2026-04-18.md`](KOIDE_CYCLIC_WILSON_DESCENDANT_LAW_NOTE_2026-04-18.md)
  — cyclic 3-response descendant law on `B0 = I`, `B1 = C + C^2`,
  `B2 = i(C - C^2)`.
- [`KOIDE_DWEH_CYCLIC_COMPRESSION_NOTE_2026-04-18.md`](KOIDE_DWEH_CYCLIC_COMPRESSION_NOTE_2026-04-18.md)
  — exact charged Hermitian source law compresses canonically to those
  three cyclic channels.

This note does not derive the spectral bridge; it imports the algebraic
identity from the narrow theorem above and pulls back the target-size
reduction to the `C_3`-covariant Hermitian subfamily. The selector
mechanism and charged-lepton readout primitive are explicitly out of
scope (see "What this does not yet close").

## Question

If we stop spending cycles on generic blocker language and ask for the smallest
positive upstream object that could actually realize the Koide charged-lepton
lane, what is it?

The generic Wilson-side constructive DM target now on the tree is:

- a `9`-real Hermitian source family reconstructing the full charged block
  `H_e ∈ Herm(3)`;
- sharpened further to one local adjacent-chain path-algebra embedding whose
  Hermitian shadow spans all of `Herm(3)`.

That is the right generic target. But it is larger than the Koide lane itself.

So the sharper constructive question is:

> what is the smallest structured Hermitian subfamily that already contains the
> whole Koide spectral target?

## Bottom line

The Koide lane does **not** need a generic `9`-real Hermitian target first.

It needs only the `C_3[111]`-covariant Hermitian subfamily on the retained
three-generation block:
```
H = a I + b C + b* C^2,
```
with:

- `a ∈ R`,
- `b ∈ C`,
- so real dimension `= 3`.

Equivalently, with
```
X_cyc = C + C^2,
Y_cyc = i(C - C^2),
```
the family is
```
H = a I + x X_cyc + y Y_cyc
```
with `(a, x, y) ∈ R^3`.

Then the Koide condition is only one further real equation:
```
3 a^2 = 6 |b|^2
      = 6 (x^2 + y^2),
```
so the actual Koide target is a **2-real family**:

- one overall scale,
- one phase / angular parameter.

So the constructive target size collapses:
```
Herm(3)        : 9 real
C_3-covariant  : 3 real
Koide cone     : 2 real
```

That is the path worth attacking.

## Exact relation to the new Wilson-side constructive notes

From the new current-`main` constructive DM notes:

- the generic charged-Hermitian target is `Herm(3)`, real dimension `9`;
- one adjacent two-edge path algebra already generates the full `Herm(3)`
  codomain.

For the Koide lane, we can sharpen this immediately:

> the adjacent-chain algebra already contains the **cyclic generator** `C` and
> its adjoint `C^2`, so it already contains the full `3`-real circulant
> Hermitian target family.

Therefore the Koide constructive problem is not:

- derive an arbitrary `9`-channel Hermitian descendant;

but:

- derive a **cyclic `3`-channel** Wilson descendant inside that local path
  algebra, then
- derive the **one real selector equation** that lands it on the Koide cone.

## Theorem 1: the `C_3`-covariant Hermitian subfamily is exactly `3` real dimensions

Let `C` be the retained `3 x 3` cycle on the charged-lepton triplet. Then the
Hermitian matrices commuting with `C` form exactly the family
```
H = a I + b C + b* C^2.
```

This is real dimension `3`, because:

- `a` is one real parameter,
- `b` contributes two real parameters.

The runner verifies this directly by solving the Hermitian commutant of `C`.

## Theorem 2: the adjacent-chain path algebra already contains the cycle generators

Let the local adjacent chain be generated by
```
E_12, E_23
```
and adjoints on `1 -> 2 -> 3`.

Then:
```
E_13 = E_12 E_23,
E_31 = E_32 E_21.
```

So the forward and backward cycle operators are already in the same local
algebra:
```
C   = E_21 + E_32 + E_13,
C^2 = E_12 + E_23 + E_31.
```

Hence:
```
I, C + C^2, i(C - C^2)
```
all already lie in the adjacent-chain algebra.

So one local adjacent-chain embedding already contains the entire `3`-real
circulant Hermitian family needed for the Koide spectral lane.

## Theorem 3: the Koide condition reduces the target to `2` real parameters

Write
```
H = a I + x (C + C^2) + y i(C - C^2)
  = a I + b C + b* C^2
```
with
```
b = x + i y.
```

Then:
```
|b|^2 = x^2 + y^2.
```

By the [KOIDE_CIRCULANT_CHARACTER_BRIDGE_NARROW_THEOREM_NOTE_2026-05-09](./KOIDE_CIRCULANT_CHARACTER_BRIDGE_NARROW_THEOREM_NOTE_2026-05-09.md)
exact algebraic identity `a_0^2 - 2 |z|^2 = 3 a^2 - 6 |b|^2`, the
spectral Koide condition
```
a_0^2 = 2 |z|^2
```
becomes, on the circulant coordinates,
```
3 a^2 = 6 |b|^2
      = 6 (x^2 + y^2).
```

So inside the `3`-real circulant family, Koide is one real codimension-1
constraint. The remaining target is therefore `2` real:

- one scale `a`,
- one phase `arg(b)`.

The runner also verifies numerically that the observed charged-lepton
`√m` triple lies on exactly this `2`-real family.

## Constructive consequence

The honest positive target is now much smaller than it looked.

Instead of first deriving a generic `9`-real charged Hermitian block and then
hoping Koide appears inside it, the sharper attack is:

1. realize one local adjacent-chain embedding on the Wilson side;
2. derive a descended **cyclic `3`-channel Hermitian law**
   landing on
   ```
   H = a I + b C + b* C^2;
   ```
3. derive the single real selector equation
   ```
   3 a^2 = 6 |b|^2;
   ```
4. then attach the remaining scale/readout identification.

That is a real path that can work. It avoids solving a larger problem than the
Koide lane itself asks for.

The companion
[KOIDE_CYCLIC_WILSON_DESCENDANT_LAW_NOTE_2026-04-18.md](./KOIDE_CYCLIC_WILSON_DESCENDANT_LAW_NOTE_2026-04-18.md)
now makes Step 2 explicit: the cyclic descendant is exactly the `3`-response
law on
`B0 = I`, `B1 = C + C^2`, `B2 = i(C - C^2)`, and the selector equation becomes
the response-space circle law `2 r0^2 = r1^2 + r2^2`. The companion
[KOIDE_DWEH_CYCLIC_COMPRESSION_NOTE_2026-04-18.md](./KOIDE_DWEH_CYCLIC_COMPRESSION_NOTE_2026-04-18.md)
now proves that the exact charged Hermitian source law compresses canonically
to those same three cyclic channels.

## What this does not yet close

This note does **not** yet derive:

- the microscopic Wilson law producing the cyclic responses;
- the selector mechanism enforcing `3 a^2 = 6 |b|^2`
  (equivalently `2 r0^2 = r1^2 + r2^2`);
- the final charged-lepton readout primitive.

It does fix the right **constructive target size and shape**.

## Bottom line

The Koide charged-lepton lane should now be attacked as:

- one **microscopic source law for the cyclic `3`-response descendant**,
- followed by one **1-equation** selector problem,

not as a generic `9`-channel Hermitian reconstruction first.
