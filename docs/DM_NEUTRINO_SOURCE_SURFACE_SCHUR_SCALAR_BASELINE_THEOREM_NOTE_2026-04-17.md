# Commutant-Class Structural Lemma (Schur Scalar Baseline) — CONDITIONAL ONLY

**Date:** 2026-04-17 (Option-A demotion after 2026-04-17 review pass)
**Status:** **CONDITIONAL commutant-class structural lemma only; NOT
a live-sheet promotion.** The Schur conclusion `D = m I_3` applies
exclusively to the hypothetical zero-source baseline that COMMUTES
with the retained three-generation algebra. The live Hermitian
curvature at the PMNS closure pin
`H_live = m_* I_3 + δ_* T_δ + q_+* T_q` with
`(m_*, δ_*, q_+*) = (0.657061, 0.933806, 0.715042)` does NOT satisfy
the Schur commutation premise — the runner Part 6 witness measures

```
‖[H_live, C_3]‖_F ≈ 3.96       (numerically non-zero)
‖[H_live, P_1]‖_F ≈ 2.35       (non-zero — projector is outside Z_3 circulant algebra)
‖[H_live, P_2]‖_F ≈ 1.06       (non-zero)
‖[H_live, P_3]‖_F ≈ 2.54       (non-zero)
```

Therefore the scalar-baseline curvature `Q = 6(δ² + q_+²)/m²` derived
from `D = m I_3` IS NOT the live-sheet curvature of the DM-neutrino
source surface. This note is retained only as a commutant-class
reference lemma.
**Script:** `scripts/frontier_dm_neutrino_source_surface_schur_scalar_baseline_theorem.py`
**Runner:** `PASS = 26, FAIL = 0` (Part 6 witness makes the non-
commutation explicit; algorithmic content unchanged from earlier
version).
**Framework convention:** "axiom" means only the single framework axiom `Cl(3)` on `Z^3`.

## Status demotion notice (2026-04-17, Option-A honest-label pass)

An earlier draft framed this note as a "partial closure" of the
selector gate via a promoted scalar-baseline curvature. Reviewer
feedback (commit 5c70c15d review) correctly identified that:

1. The Schur conclusion `D = m I_3` applies only to baselines commuting
   with the retained three-generation algebra ⟨P_1, P_2, P_3, C_3[111]⟩.
2. The live Hermitian curvature on the source-oriented sheet (either
   `H_base` alone, or the full H at the closure pin) does NOT commute
   with that algebra. The runner Part 6 witness verifies this on the
   full live H:

```
‖[H_live, C_3]‖_F ≈ 3.96          (non-zero; cyclic generator test)
‖[H_live, P_i]‖_F ≠ 0  for i=1,2,3 (projector tests outside Z_3 circulant algebra)
```

Therefore the Schur scalar baseline `D = m I_3` and the curvature
`Q = 6(δ² + q_+²)/m²` derived from it are NOT live-sheet curvature
objects. They characterise a different (commuting) baseline class
that is retained only as a reference structure for constructive
comparison.

The actual curvature object on the live source-oriented sheet is the
exact Hermitian affine chart
`H(m, δ, q_+) = H_base + m T_m + δ T_δ + q_+ T_q` directly; it does
NOT have a Schur-forced scalar-baseline reduction. The PMNS-as-f(H)
"closure" (basin-uniqueness under an imposed branch-choice rule) is a
conditional / support result, NOT a
retained live-sheet closure theorem. See the flagship closure review
note for the honest overall status.

Option B (derive, on the actual live sheet, why the relevant
zero-source baseline must commute with the retained three-generation
algebra) is a separate open item.

## Scope and discipline

This note is a **commutant-class structural lemma** about the Schur
rigidity of commuting operators on the retained irreducible space
`H_hw=1`. It does **not** close the selector gate, and it is **not**
the retained curvature of the live DM-neutrino source-oriented sheet.

The distinction matters for claim-boundary discipline:

- the scalar-baseline quadratic diagnostic in
 [DM_NEUTRINO_SOURCE_SURFACE_SCALAR_BASELINE_ACTIVE_QUADRATIC_DIAGNOSTIC_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_SCALAR_BASELINE_ACTIVE_QUADRATIC_DIAGNOSTIC_NOTE_2026-04-17.md)
 was bounded because **(i)** the baseline choice `D = m I_3` was not shown
 to be forced by the axiom, and **(ii)** the minimum-coupling selector
 principle was not shown to be forced by the axiom
- this note records a Schur argument that would force `D = m I_3`
 conditional on commuting with the retained three-generation algebra;
 the retained `H_base` does NOT satisfy that conditional (see the
 numerical commutator witnesses above)
- this note does **not** close (ii); the selector principle remains
 an atlas-level open object, and publication-grade selector closure
 for the DM-neutrino source sheet proceeds through PMNS-as-f(H)
 observational promotion (P3 lane), with basin-uniqueness retained
 via the Sylvester inertia-preservation theorem

## Theorem statement (Schur baseline on `H_hw=1`)

**Theorem (Canonical Scalar Baseline from Schur).**
Let `V = H_hw=1` be the retained 3-dimensional observable space of the
three-generation observable theorem, with exact retained operator algebra
generated by the sector projectors `{P_1, P_2, P_3}` and the cyclic map
`C_3[111]`. The R-algebra generated by these operators is all of `M_3(R)`,
and it acts absolutely irreducibly on `V` over `C`. By Schur's lemma on the
complex representation, any `C`-linear operator `D : V -> V` that commutes
with every retained generator is a scalar. Hermiticity restricts the scalar
to `R`, giving

```
D = m I_3, m in R.
```

On the commutant class, the zero-source baseline on `H_hw=1` is the
scalar matrix, and the corresponding reference curvature on the active
pair is:

```
K(X, Y) = (1/m^2) Tr(X Y),
Q(delta, q_+) = K(J_act, J_act) = 6 (delta^2 + q_+^2) / m^2.
```

## Why the previous "genuine promotion" claim is withdrawn

An earlier version of this note carried a "Why this is a genuine
promotion" section claiming that the scalar-baseline quadratic
`Q = 6(δ² + q_+²)/m²` had been upgraded from a bounded diagnostic to
"theorem-native source-response curvature" by the Schur conditional.
Under Option-A honest labeling, that claim is WITHDRAWN. The Schur
conditional only fires on the hypothetical scalar-commutant-class
baseline; the live H at the closure pin does NOT satisfy its premise
(Part 6 witness). So the "theorem-native" upgrade did not go through
on the live sheet.

What this note actually retains:

- the commutant-class Schur lemma itself (textbook, conditional on
  commutation with the retained algebra)
- an explicit numerical witness (Part 6) that the live H does NOT
  satisfy the Schur premise
- the chamber-boundary minimizer of the commutant-class reference
  curvature `(δ_*, q_+*) = (sqrt(6)/3, sqrt(6)/3)` — USEFUL ONLY AS
  A REFERENCE POINT on the commutant class, not as a live-sheet
  chamber minimum

What this note does NOT retain:

- that `Q = 6(δ² + q_+²)/m²` is the live-sheet source-response
  curvature (it is not; it is the commutant-class reference)
- that the chamber minimizer is on a theorem-native surface (it is
  on the commutant-class reference surface only)

## What this does **not** close

The Schur-baseline theorem defines a scalar-commutant-class *reference
curvature*. It does **not** promote the live-sheet curvature, and it
does **not** promote the *selector principle*.

Specifically, on the commutant class the Schur theorem establishes:

- the correct axiom-native quadratic functional on the active pair
- that this functional is isotropic in `(delta, q_+)` at quadratic order
- that the chamber-boundary minimizer of this functional is
 `(delta_*, q_+*) = (sqrt(6)/3, sqrt(6)/3)`

It does not establish:

- that the physical admissible source is the chamber minimizer of `Q`
- that minimum-coupling is an axiom-native selection principle

The minimum-information selector law in
[DM_LEPTOGENESIS_PMNS_MINIMUM_INFORMATION_SOURCE_LAW_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_PMNS_MINIMUM_INFORMATION_SOURCE_LAW_NOTE_2026-04-16.md)
is explicitly flagged as an **"invented post-axiom dynamical selector law"**,
and this note does not override that flag. Using the Schur theorem to
*evaluate* a selector Q at the physical point is well-defined; using it to
*derive* the physical point still requires an additional principle.

## Atlas inputs used

All retained / theorem-grade on current `main`:

- [OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md](./OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md)
 — axiom-native scalar generator `W[J] = log|det(D+J)| - log|det D|`
- [THREE_GENERATION_OBSERVABLE_THEOREM_NOTE.md](./THREE_GENERATION_OBSERVABLE_THEOREM_NOTE.md)
 — exact retained algebra `<P_1, P_2, P_3, C_3[111]>` acts irreducibly on
 `H_hw=1`
- [DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_AFFINE_POINT_SELECTION_BOUNDARY_NOTE_2026-04-16.md](./DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_AFFINE_POINT_SELECTION_BOUNDARY_NOTE_2026-04-16.md)
 — exact affine chart `H(m, delta, q_+)` and exact active generators
 `T_m, T_delta, T_q`
- [DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_HALF_PLANE_THEOREM_NOTE_2026-04-16.md](./DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_HALF_PLANE_THEOREM_NOTE_2026-04-16.md)
 — exact chamber `q_+ >= sqrt(8/3) - delta`
- [DM_NEUTRINO_SOURCE_SURFACE_Z3_DOUBLET_BLOCK_POINT_SELECTION_THEOREM_NOTE_2026-04-16.md](./DM_NEUTRINO_SOURCE_SURFACE_Z3_DOUBLET_BLOCK_POINT_SELECTION_THEOREM_NOTE_2026-04-16.md)
 — exact `Z_3` doublet-block readout
- [DM_NEUTRINO_SOURCE_SURFACE_SCALAR_BASELINE_ACTIVE_QUADRATIC_DIAGNOSTIC_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_SCALAR_BASELINE_ACTIVE_QUADRATIC_DIAGNOSTIC_NOTE_2026-04-17.md)
 — prior-bounded quadratic diagnostic this note promotes

No new axioms are introduced. The Schur step is a standard algebraic
lemma applied to an already-retained irreducible representation.

## Structural lemma: `Z_3`-circulant norm form

As a preparatory structural observation, we record the exact algebraic
identity for the scalar-baseline determinant of the active source family.
This is not itself the selector but it explains why the scalar diagnostic
is isotropic at quadratic order and right-sensitive at cubic order.

For `J_act = delta T_delta + q_+ T_q` and the scalar baseline `D = m I_3`,

```
det(m I + delta T_delta + q_+ T_q)
 = m^3 - 3 m (delta^2 + q_+^2) + 2 q_+ (q_+^2 - 3 delta^2)
 = m^3 - 3 m |w|^2 + 2 Re(w^3), w = q_+ + i delta
```

and this equals the determinant of the explicit `Z_3` circulant

```
[ m w w* ]
[ w* m w ]
[ w w* m ].
```

The cubic term `2 Re(w^3)` breaks the `w -> -w` symmetry to an exact
`Z_3` rotation symmetry `w -> exp(2 pi i / 3) w`. The chamber
`q_+ >= sqrt(8/3) - delta` is a half-plane cut that breaks this `Z_3`
to one physically accessible sector.

This structural observation is useful for future selector attempts along
the cubic-sensitive direction, but by itself it is not a selector law.

## Proof (runner-verifiable)

The formal argument:

1. **Generators are exact and real.** By the three-generation observable
 theorem, `P_1, P_2, P_3` are exact rank-1 sector projectors on `H_hw=1`
 with `P_1 + P_2 + P_3 = I_3`, and `C_3[111]` cycles them:
 `C_3 P_i C_3^{-1} = P_{i+1}` (mod 3). All four generators are real `3x3`.

2. **R-algebra equals `M_3(R)`.** Products of the generators (together
 with `C_3^2`) R-span all of `M_3(R)`. The runner verifies this
 numerically: iterative linear-independence closure gives exactly 9
 basis elements.

3. **Absolute irreducibility on `C^3`.** `M_3(R)` embeds in `M_3(C)` as
 a real form; it acts absolutely irreducibly on `C^3` iff the only
 `C`-linear commutant is scalar. The runner verifies this by solving
 `[A, G] = 0` for `A in M_3(C)` and each generator `G in {P_i, C_3}`;
 the `C`-nullspace has dimension 1 and is spanned by `I_3`.

4. **Schur's lemma over `C`.** Any `D : C^3 -> C^3` that commutes with
 the retained generators is `c I_3` for some `c in C`.

5. **Hermiticity restricts `c in R`.** A Hermitian `D = c I_3` has
 `D^dagger = bar{c} I_3 = c I_3`, forcing `c = bar{c}`, i.e. `c in R`.
 Denote this real scalar `m`.

6. **Observable-principle evaluation at `D = m I_3`.** The axiom-native
 generator is

 ```
 W[J_act] = log|det(m I + J_act)| - log|det(m I)|
  = log|1 - 3|w|^2/m^2 + 2 Re(w^3)/m^3|.
 ```

7. **Zero-source curvature.** Expanding `W` to second order in `J_act`
 at the origin gives

 ```
 W[J_act] = - 3 |w|^2 / m^2 + O(|w|^3 / m^3),
 ```

 whose Hessian bilinear is `K(X, Y) = (1/m^2) Tr(X Y)`. Using
 `Tr(T_delta^2) = Tr(T_q^2) = 6` and `Tr(T_delta T_q) = 0`:

 ```
 Q(delta, q_+) = - d^2 W = 6 (delta^2 + q_+^2) / m^2.
 ```

 The runner checks the Hessian bilinear numerically at four values of
 `m` and confirms the closed form.

8. **Chamber-boundary minimum.** On the exact chamber boundary
 `q_+ = sqrt(8/3) - delta`, the unconstrained minimum of
 `delta^2 + q_+^2` is at `delta = q_+ = sqrt(8/3)/2 = sqrt(6)/3`.

Steps 1–5 establish the Schur theorem. Steps 6–7 record the
commutant-class reference curvature. Step 8 records the chamber minimum
that the diagnostic note previously identified; here it is on the
commutant-class reference surface.

## Honest gap statement (Option-A — withdrawal of earlier "narrowed gap" claim)

An earlier version of this note claimed that the Schur conditional
had reduced the selector gate from `(baseline choice) AND (selector
principle)` to `(selector principle) only`. Under Option-A honest
labeling, that reduction is WITHDRAWN: the Schur conditional does
not fire on the live sheet, so the baseline-choice sub-objection is
NOT closed. Both the baseline-choice sub-objection and the selector
principle remain open at this note's scope.

Concretely, the open items that Schur on its own does not close:

**(0)** derive, on the actual live source-oriented sheet (NOT the
commutant class), why the relevant zero-source baseline must commute
with the retained three-generation algebra (Option B; required for
a genuine live-sheet promotion of the Schur conditional);

**(a)** a sole-axiom derivation of a minimum-coupling (or equivalent)
variational principle on the live sheet;

**(b)** a right-sensitive axiom-native functional on the chamber that
breaks the `δ ↔ q_+` isotropy (the cubic `Z_3`-circulant structure is
the natural target);

**(c)** a direct microscopic `Z_3` doublet-block law.

This note only retains the commutant-class Schur lemma as a reference.
It does not close (0), (a), (b), or (c).

## Position on publication surface

This note is **not** flagship publication-grade on its own, and after
the Option-A demotion it is not a prerequisite for any retained live-
sheet closure either. It sits as a commutant-class reference lemma
only.

Appropriate placement:

- atlas support row in
 `DERIVATION_ATLAS.md`
 under the DM neutrino source-surface family, labeled
 "commutant-class reference (conditional; NOT a live-sheet
 promotion)"
- the downstream PMNS-as-f(H) "closure" note
 (`PMNS_FROM_DM_NEUTRINO_SOURCE_H_DIAGONALIZATION_CLOSURE_THEOREM_NOTE_2026-04-17.md`)
 is itself conditional / support (see its Option-A demotion); it does
 NOT consume the scalar-baseline curvature `Q` from this note as a
 live-sheet input

This note must NOT be used to promote anything in the flagship paper
quantitative section. The honest overall status of the DM flagship
gate is conditional / support — see DM_FLAGSHIP_CLOSURE_REVIEW_NOTE_2026-04-17.md.

## Command

```bash
PYTHONPATH=scripts python3 scripts/frontier_dm_neutrino_source_surface_schur_scalar_baseline_theorem.py
```

Current expected: `PASS = 26, FAIL = 0` (including the Part 6
live-sheet non-commutation witness).

## What this file must never say

- that the Schur conditional fires on the LIVE source-oriented sheet
  (it does not; the Part 6 runner witness shows non-commutation)
- that `Q = 6(δ² + q_+²)/m²` is the live-sheet source-response
  curvature (it is NOT; it is the commutant-class reference)
- that **this note** closes the selector gate (it does not)
- that **this note** closes the DM flagship lane (it does not; the
  DM flagship lane is NOT declared closed on the current branch tip —
  its honest status is conditional / support; see the flagship
  closure review note)
- that this note "narrows the gap" from two unknowns to one (the
  Option-A demotion withdraws that narrative; the baseline-choice
  sub-objection is NOT closed on the live sheet)
- that the minimum-information selector has been promoted from post-axiom
- that the selector principle is a sole-axiom theorem

If any future revision tightens those boundaries, it must cite a
source on the live retained surface (Option B: derive the live-sheet
Schur commutation premise). Until then, the safe read is: **Schur is
a commutant-class conditional lemma only; the live sheet does NOT
satisfy its premise; the flagship lane is conditional / support, NOT
closed**.
