# `S^3` + Anomaly-Forced Time: Uniqueness Audit

**Date:** 2026-04-14  
**Branch:** `codex/review-active`  
**Role:** Route-2 uniqueness-theorem student  
**Scope:** uniqueness test for the retained `S^3 +` anomaly-forced-time stack

## Verdict

**Background uniqueness is real. Dynamics uniqueness is not.**

The retained stack does force a clean kinematic background candidate:

- `S^3` topology is closed as a retained `PL S^3` compactification
- anomaly-forced time is exact as a single-clock `d_t = 1` theorem

Together these select the unique clean background candidate

    PL S^3 x R

for a spacetime lift.

That is already a nontrivial uniqueness statement, but it is only a
**background uniqueness** statement. It does **not** yet force the GR
metric law, because the atlas does not contain an exact dynamics bridge
from that background to Einstein dynamics.

## What the route can and cannot force

### What it can force

The current retained inputs do force the following:

1. the spatial compactification surface is `S^3`
2. the temporal structure is single-clock
3. the combined background target is `PL S^3 x R`

This is the unique clean route-2 kinematic scaffold available on the
current atlas.

### What it cannot yet force

The retained stack does **not** yet force:

1. a unique tensor/metric action on `PL S^3 x R`
2. a unique Einstein equation from the anomaly/topology inputs alone
3. a unique large-scale dynamics law among all compatible tensor lifts

The missing object is not the background. The missing object is the exact
dynamics bridge.

## Why uniqueness still does not imply GR here

Route 2 is underdetermined at the dynamical level for a simple reason:
the current atlas supplies exact kinematics but not exact dynamics.

The retained tools give:

- a closed `S^3` compactification theorem
- an exact anomaly-forced-time theorem
- a reusable route-2 background candidate

But the atlas does **not** yet contain any of the following as an exact
theorem on this route:

1. an exact `S^3`-to-curvature law
2. an exact anomaly-to-Einstein derivation
3. an exact discrete variational action on `PL S^3 x R`
4. a uniqueness theorem saying the only compatible dynamics is GR

Without one of those, the route remains a **background theorem**, not a
full gravity theorem.

## Runnable result

The companion runner checks the route ingredients directly:

- `S^3` compactification theorem is closed
- anomaly-forced time theorem is exact
- the atlas contains both as reusable tools
- the atlas does not yet contain an exact dynamics bridge for this route

That is the sharp blocker.

## Sharp blocker

The route is blocked because the current retained stack can identify the
unique clean background candidate, but it cannot derive a unique compatible
metric law from that background.

So the exact uniqueness statement available today is:

> `PL S^3 x R` is uniquely selected as the route-2 background candidate,
> but GR dynamics are not uniquely forced by the current atlas.

## Bottom line

Route 2 is a clean axiom-first **kinematic uniqueness** result, not a GR
closure theorem.

To close the route, one would need at least one exact dynamics bridge:

1. exact spacetime-lift observable
2. exact discrete action on `PL S^3 x R`
3. exact uniqueness theorem for the allowed dynamics class

Until then, Route 2 stays blocked at the dynamics level.
