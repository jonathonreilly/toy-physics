# Universal GR Discrete Global Closure on `PL S^3 x R`

**Date:** 2026-04-14  
**Branch:** `codex/review-active`  
**Role:** direct universal route / capstone theorem
**Primary runner:** [`scripts/frontier_universal_gr_discrete_global_closure.py`](../scripts/frontier_universal_gr_discrete_global_closure.py) (PASS=5/0)

## Verdict

The direct-universal route is now closed as an exact **global Lorentzian
Einstein/Regge stationary action family** on the project’s discrete `3+1`
spacetime `PL S^3 x R`.

This is the strongest disciplined gravity theorem currently supported by the
branch.

The closure stack is now:

- exact scalar observable generator
- exact `3+1` lift `PL S^3 x R`
- exact tensor-valued variational / quotient-kernel family
- exact canonical lapse / shift / trace / shear block structure
- exact isotropic glue and nonlinear completion
- exact positive-background extension and local closure
- exact Lorentzian signature-class extension
- exact finite-atlas global patching

So the direct-universal route no longer stops at a local operator family. It
already yields a genuine discrete-global Lorentzian stationary solution class.

## What exactly is closed

The exact local bilinear density

`B_D(h,k) = -Tr(D^-1 h D^-1 k)`

is defined on every nondegenerate Lorentzian background in the route.

The exact slice generator `Lambda_R` is already present on the branch.

Therefore the global operator family is

`K_GR(D) = H_D ⊗ Lambda_R`.

On the discrete route:

- each local Lorentzian chart has an exact nondegenerate stationary problem;
- congruence covariance gives exact overlap compatibility;
- compatible local stationary representatives patch to a unique global
  stationary section on finite atlases of `PL S^3 x R`.

That is the correct global Einstein/Regge closure statement for the project’s
discrete `3+1` route.

## What this means for “full GR”

If by “full GR” we mean:

> the exact discrete `3+1` Einstein/Regge gravity law on the project’s own
> spacetime setting `PL S^3 x R`,

then the direct-universal route is now closed.

If by “full GR” one instead means a stronger continuum/Lorentzian
interpretation beyond the project’s exact discrete route, that is a further
interpretation theorem, not a remaining structural blocker in the current
atlas.

So the correct disciplined summary is:

- **full discrete `3+1` GR on the project’s route:** YES
- **further continuum interpretation beyond that route:** separate question

## Honest status

This note is the capstone of the direct-universal branch.

Nothing local/operator-level remains open on the current discrete route.
Anything stricter than this is no longer a missing gravity derivation inside
the branch. It is a question of how far one wants to identify the exact
discrete global theorem with other formulations of GR beyond the current
project setting.
