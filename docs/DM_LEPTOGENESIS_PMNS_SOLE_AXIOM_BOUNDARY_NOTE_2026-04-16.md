# DM Leptogenesis PMNS Sole-Axiom Boundary

**Date:** 2026-04-16  
**Status:** support - structural or confirmatory support note
**Script:** `scripts/frontier_dm_leptogenesis_pmns_sole_axiom_boundary.py`

## Question

Does the sole axiom

`Cl(3)` on `Z^3`

already fix a unique PMNS-assisted flavored-DM repair value once the
microscopic route has been reduced to the active `D`-level last mile?

## Bottom line

No.

On the current branch:

- transport is already closed
- flavored-column selection is already closed
- the microscopic-to-packet map is already closed
- the aligned seed pair is already native

but the off-seed active `5`-real microscopic source is still not fixed.

That exact remaining object is:

`(xi_1, xi_2, eta_1, eta_2, delta)`.

## Exact content

### 1. Same seed pair, different off-seed source

Two exact active microscopic points can share the same native seed pair
`(xbar,ybar)` while carrying different breaking-source data
`(xi_1,xi_2,eta_1,eta_2,delta)`.

So the native seed data do not by themselves determine a unique active
microscopic point.

### 2. Different off-seed sources give different PMNS-assisted `eta`

Those different off-seed sources induce:

- different flavored packets
- different selected transport columns
- different `eta / eta_obs`

on the same exact DM branch.

So the sole axiom does not yet fix a unique PMNS-assisted flavored repair
value.

### 3. Once the off-seed source is supplied, the rest is algorithmic

Once the active `5`-real source is supplied, the remaining path is exact:

`(xbar,ybar,xi_1,xi_2,eta_1,eta_2,delta)`
`-> D_act`
`-> H_e`
`-> |U_e|^2^T`
`-> selected column`
`-> eta`.

On the canonical charged-lepton-active sample this reproduces the same
near-closing PMNS-assisted value as the earlier reduction notes.

## Consequence

The PMNS-assisted flavored-DM route is now closed as far as it can honestly go
on the sole-axiom surface:

- the carrier is known
- the aligned seed surface is known
- the microscopic-to-transport map is known

but the sole axiom does **not** yet fix the off-seed active `5`-real source.

So the remaining sole-axiom boundary is no longer transport, no longer
projector selection, and no longer the full microscopic operator again.

It is only the active off-seed microscopic source law.

## Command

```bash
python3 scripts/frontier_dm_leptogenesis_pmns_sole_axiom_boundary.py
```
