# Gauge-Vacuum Plaquette Beta=6 Fixed-Depth Minimal Certificate

**Date:** 2026-04-18  
**Status:** exact fixed-depth plaquette packaging theorem; the whole non-Wilson
operator lane is one minimal `Jacobi + K` certificate, and the current bank
still fails already at the first Jacobi layer  
**Script:** `scripts/frontier_gauge_vacuum_plaquette_beta6_fixed_depth_minimal_certificate_2026_04_18.py`

## Question

After the compressed rim-evaluation theorem and the identity-rim finite-Jacobi
bulk reduction, can the fixed-depth plaquette `beta = 6` operator lane be
stated as one finite hard-review-safe certificate, rather than a loose list of
bulk and rim objects?

## Answer

Yes.

At fixed propagation depth `d = L_perp - 1`, the whole plaquette operator lane
is exactly one minimal two-layer certificate:

1. one finite identity-rim Jacobi packet for the `eta_6(e)`-generated bulk
   Krylov compression;
2. one already-explicit downstream boundary evaluation law
   `Z_beta^env(W) = <K(W), v_beta>`.

More concretely, let `J_d` be the finite Jacobi matrix of the bulk compression
in the Lanczos basis generated by the normalized identity rim state, with
`e_1` the first basis vector. Then

`v_d = Q J_d^d e_1`

and every marked-holonomy amplitude is already downstream through the fixed
class function `K(W)`:

`Z_beta^env(W) = <K(W), v_d>`.

So the sharp fixed-depth plaquette lane is not:

- the whole class-sector operator `S_6^env`,
- nor even the whole cyclic bulk object,
- nor an arbitrary finite Krylov matrix block.

It is one finite **identity-rim Jacobi packet**, plus one already-explicit
boundary evaluation law.

But the current bank still fails already at the first certificate layer:
the propagated retained triple does **not** determine even the first
nontrivial Jacobi packet.

## Setup

From
[GAUGE_VACUUM_PLAQUETTE_COMPRESSED_RIM_EVALUATION_THEOREM_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_COMPRESSED_RIM_EVALUATION_THEOREM_NOTE_2026-04-17.md):

- after compression the boundary law is already explicit as
  `Z_beta^env(W) = <K(W), v_beta>`.

From
[GAUGE_VACUUM_PLAQUETTE_BETA6_IDENTITY_RIM_FINITE_JACOBI_PACKET_REDUCTION_NOTE_2026-04-18.md](./GAUGE_VACUUM_PLAQUETTE_BETA6_IDENTITY_RIM_FINITE_JACOBI_PACKET_REDUCTION_NOTE_2026-04-18.md):

- the sharp fixed-depth bulk datum is the finite identity-rim Jacobi packet of
  the `eta_6(e)`-generated Krylov compression,
- and the current bank still does not determine even its first nontrivial
  packet from the propagated retained triple.

The new point is only the packaging:

- the whole fixed-depth plaquette operator lane is now one minimal finite
  certificate,
- and the current bank already fails at its first layer.

## Theorem 1: exact fixed-depth `Jacobi + K` certificate form of the plaquette lane

Fix propagation depth `d = L_perp - 1`.

Then fixed-depth class-sector plaquette closure is equivalent to:

1. one finite identity-rim Jacobi packet
   `(alpha_0, ..., alpha_r-1; beta_1, ..., beta_r-1)` for the
   `eta_6(e)`-generated Krylov compression of the bulk operator, with
   `r <= d + 1`;
2. the already-explicit compressed boundary evaluation law
   `Z_beta^env(W) = <K(W), v_d>`.

Indeed, the Jacobi packet determines the propagated bulk vector

`v_d = Q J_d^d e_1`,

and the boundary law then evaluates every marked-holonomy amplitude from that
same `v_d`.

So the whole fixed-depth plaquette lane is exactly one minimal `Jacobi + K`
certificate.

## Corollary 1: the current bank still fails at the first certificate layer

The finite-Jacobi reduction theorem already gives two positive bulk witnesses
with:

- the same exact propagated retained triple,
- but different first nontrivial Jacobi packets.

Therefore the current bank still does **not** realize the full fixed-depth
plaquette certificate, because it already fails at the first Jacobi layer,
before any downstream `K(W)` evaluation issue even arises.

## What this closes

- one exact minimal-certificate packaging of the fixed-depth plaquette
  operator-side lane
- exact clarification that the remaining non-Wilson PF seam is not a generic
  operator-evaluation problem but one finite `Jacobi + K` certificate
- exact current-bank failure location: the bank already fails at the first
  Jacobi layer

## What this does not close

- the true explicit Jacobi packet of the actual `beta = 6` bulk object
- the explicit identity rim datum `eta_6(e)`
- the explicit plaquette framework-point PF data
- the global sole-axiom PF selector theorem

## Why this matters

This puts the non-Wilson PF frontier in the same hard-review-safe form as the
Wilson side.

The branch can now say:

- the Wilson positive reopening route is one local `2-edge + 3` certificate,
- the fixed-depth plaquette non-Wilson route is one minimal `Jacobi + K`
  certificate,
- and the current bank already fails at the first constructive layer on both.

## Command

```bash
python3 scripts/frontier_gauge_vacuum_plaquette_beta6_fixed_depth_minimal_certificate_2026_04_18.py
```
