# PMNS Selector from Response Observables

The selector outputs `tau` and `q` are derivable in the same lower-level chain
from the response-observable packs on the neutral and charge `-1` supports.

- `tau` is derived by active/passive family classification of the
  response-derived blocks.
- `q` is derived from the response-derived passive block.
