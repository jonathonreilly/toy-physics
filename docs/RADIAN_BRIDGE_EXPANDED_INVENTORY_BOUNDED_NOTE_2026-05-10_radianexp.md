# Radian-Bridge Primitive `P` From EXPANDED Retained Inventory (Bounded, Sharpened)

**Date:** 2026-05-10
**Type:** bounded_theorem (sharpened obstruction; does not close `P`)
**Claim type:** bounded_theorem
**Intended audit claim class:** bounded_theorem
**Status authority:** independent audit lane only. This source note does
not set or predict an audit outcome; later status is generated by the
audit pipeline after independent review.
**Loop:** radian-bridge-expanded-inventory-20260510
**Primary runner:** [`scripts/cl3_radian_bridge_expanded_inventory_2026_05_10_radianexp.py`](../scripts/cl3_radian_bridge_expanded_inventory_2026_05_10_radianexp.py)
**Cached output:** [`logs/runner-cache/cl3_radian_bridge_expanded_inventory_2026_05_10_radianexp.txt`](../logs/runner-cache/cl3_radian_bridge_expanded_inventory_2026_05_10_radianexp.txt)

## Authority disclaimer

This is a source-note proposal. Pipeline-derived status is generated
only after the independent audit lane reviews the claim, dependency
chain, and runner. This note does not write audit verdicts and does not
promote any downstream theorem.

## Naming convention

- **`P`** = radian-bridge primitive. Per
  [`KOIDE_Q_DELTA_LINKING_RELATION_THEOREM_NOTE_2026-04-20.md`](KOIDE_Q_DELTA_LINKING_RELATION_THEOREM_NOTE_2026-04-20.md)
  §4: identification of the dimensionless character-algebra ratio
  `2/d² = 2/9` with the literal radian value `2/9 rad`.
- **Probe 30** =
  [`KOIDE_BAE_PROBE_RADIAN_FROM_DIMENSIONS_BOUNDED_NOTE_2026-05-09_probe30.md`](KOIDE_BAE_PROBE_RADIAN_FROM_DIMENSIONS_BOUNDED_NOTE_2026-05-09_probe30.md):
  Buckingham-Pi closure on the 7 dimensional constants
  `(a_s, a_τ, v_LR, M_Pl, <P>, g_bare, ħ)`; concluded `P` not in dimensional
  inventory.

## Distinct angle (vs Probe 30)

Since 2026-05-09, the framework's retained dimensionless content has
grown substantially. Probe 30 was constructed against an inventory of 7
dimensional constants plus a few rationals (`2/9`, `7/8`, `8/9`, `7/18`).
**This work** redoes the Buckingham-Pi-style closure analysis against
the full **2026-05-10** retained dimensionless content, including
several recent additions whose rational-coefficient structure or
exponent structure was hypothesized to enable new combinations
producing `2π`:

- **SU(3) NLO C-iso coefficient** `+7/9` (retained per PR #685);
- **SU(3) NNLO/NNNLO/N4LO/N5LO Wilson series coefficients** in
  `<P>_W_SU(3)(s_t)`: `-13/3, -37/2, -3071/36, -20873/48` over
  `β_W^k` (retained as numerically-identified rationals per
  [`C_ISO_SU3_NNLO_CLOSURE_BOUNDED_NOTE_2026-05-10_su3nnlo.md`](C_ISO_SU3_NNLO_CLOSURE_BOUNDED_NOTE_2026-05-10_su3nnlo.md));
- **SU(3) C-iso heat-kernel - Wilson discrepancy at NNLO+**:
  `-5/9, -47/486, -16891/43740, -308999/524880` (PR #857);
- **SU(3) heat-kernel exact rationals** `(-1)^(n+1)·(4/3)^n / n!`
  (closed-form per PR #685, NNLO note);
- **QCD `β_0 = 7, β_1 = 26` at `N_f = 6`** (retained closed forms,
  Casimir-derived, per
  [`ALPHA_S_4LOOP_RUNNING_DERIVATION_*_NOTE_2026-05-10_4loop.md`](ALPHA_S_4LOOP_RUNNING_DERIVATION_NOTE_2026-05-10_4loop.md)
  if present, or analytically derivable from `b_3 = -7` etc.);
- **Wilson chain exponent `18 = 16 + 2`** for `m_τ` (retained per
  [`KOIDE_BAE_PROBE_WILSON_CHAIN_MASS_SHARPENED_NOTE_2026-05-09_probe19.md`](KOIDE_BAE_PROBE_WILSON_CHAIN_MASS_SHARPENED_NOTE_2026-05-09_probe19.md));
- **`φ_dimensionless = 2/9`** as character-algebra rational (Step 1 of
  [`KOIDE_BAE_PROBE_PHI_FROM_Z3_CHARACTER_SHARPENED_NOTE_2026-05-09_probe24.md`](KOIDE_BAE_PROBE_PHI_FROM_Z3_CHARACTER_SHARPENED_NOTE_2026-05-09_probe24.md));
- **C-iso `ε_witness ~ 3×10⁻⁴`** as the retained engineering tolerance
  on single-plaquette truncation residual.

The hypothesis under test: with this dramatically expanded retained
dimensionless inventory, does the Buckingham-Pi closure space contain
combinations that derive radian-equivalent angles, fully closing `P`?

## Question

Does the EXPANDED retained Cl(3)/Z³ dimensionless inventory (post-2026-05-09
additions through 2026-05-10) supply a derivation chain to the literal
scalar `2π` (or any rational multiple thereof) such that the `2/9`
Plancherel-Frobenius rational becomes `2/9 rad` by retained closure?

## Answer

**STRUCTURAL OBSTRUCTION (sharpened).**

1. **POSITIVE FINDING (expanded inventory enumerated).** The retained
   dimensionless content as of 2026-05-10 comprises ~25+ named items
   in 14+ groups. Every NEW item added since Probe 30 is a **pure
   rational** (group-theoretic, character-algebra, integer count, or
   numerically-identified rational from Weyl integration over the SU(3)
   Cartan torus). Explicit list:

   | Group | Sample values | Source |
   |---|---|---|
   | Cl(3)/Z³ counts | `1/3, 2/3, 2/9, 8/9, 1/9, 1/2, 1/4, 1/5` | character-algebra |
   | APBC fourth-power | `7/8` | hierarchy theorem |
   | NLO C-iso | `7/9` | PR #685 |
   | taste_weight | `7/18 = (7/8)·T_F·R_conn` | retained chain |
   | Casimir | `4/3 = C_2(SU(3) fund.)` | group theory |
   | Wilson NNLO/N4LO/N5LO | `-13/3, -37/2, -3071/36, -20873/48` | PR #857 |
   | C-iso discrepancy | `-5/9, -47/486, -16891/43740` | PR #857 |
   | SM β | `41/10, -19/6, -7` | retained |
   | Wilson chain exp | `16, 18` | PR #799 |
   | QCD `N_f=6` β | `β_0 = 7, β_1 = 26` | retained closed forms |

   plus the lattice-MC `<P> = 0.5934 ± 0.0001` and its `u_0 = <P>^{1/4}`.

2. **STRUCTURAL OBSTRUCTION (Lindemann-Weierstrass holds on the
   expanded basis).** All retained dimensionless content NEW since
   Probe 30 is rational. The set of finite Q-linear / Q-algebraic /
   Q-rational combinations of rationals is a subset of `Q ⊂ R`. The
   number `π` is transcendental over `Q` (Lindemann 1882, sharpened by
   Weierstrass 1885). Therefore `π ∉ Q` and consequently `2π ∉
   <expanded rational basis>_Q` for any finite Q-algebraic span.

   **The Probe 30 transcendentality argument is RECONFIRMED on the
   strictly larger retained basis.** No item added since 2026-05-09
   contains a transcendental ingredient, so the Lindemann-Weierstrass
   blocker is preserved.

3. **NEW SHARPENING (αbar pi-inheritance is convention re-entry).** The
   expanded inventory does include items that contain `π` explicitly:

   - `α_bare = 1/(4π)` — QED canonical normalization;
   - `α_LM = α_bare/u_0 = 1/(4π · u_0)` — derived from `α_bare`;
   - `α_s(v) = α_bare/u_0² = 1/(4π · u_0²)` — derived from `α_bare`.

   Trivially `1/α_bare = 4π`, and `1/(α_LM · u_0) = 4π`. The numerical
   search at 50 dps confirms these as exact-tier matches to `4π` (and
   their multiples to `2π, π, π/2, ...`).

   **CRITICAL DIAGNOSTIC**: this is **not a derivation of `π`**.
   `α_bare = 1/(4π)` is the **continuum-momentum-integration normalization
   convention** entering through the `(2π)^d` phase-space factor of the
   gauge propagator at the action level, fixed by matching the discrete
   Brillouin-zone Riemann sum to the continuum loop integral. The `4π`
   factor IS the period-`2π` convention (the same `P` Probe 30
   identified) re-entering through the gauge-normalization channel.

   In particular, on the retained discrete `Cl(3)/Z³` lattice, momentum
   sums use `(1/V)·Σ_k`, NOT `(2π)^{-d}·∫d^d k`. The `4π` of `α_bare`
   thus appears at the matching stage, where the discrete-to-continuum
   correspondence sets `1/(2π)^d ↔ 1/V` modulo periodic identifications.
   The matching IS the period-2π identification. **Using `α_bare` to
   derive `π` is therefore circular**: `α_bare` imports `π` via the
   period convention `P`.

4. **NEW SHARPENING (heat-kernel-`e` is not `π`).** The retained SU(3)
   heat-kernel closed form `<P>_HK_SU(3)(s_t) = 1 - exp(-(4/3) s_t)` is
   transcendental in `e`, not `π`. By Nesterenko's theorem on `(e^π, π)`,
   `e` and `π` are algebraically independent for the cases of interest.
   Therefore evaluating the heat-kernel at any retained `s_t` gives a
   number transcendental in `e` but not a rational multiple of `π`.
   The heat-kernel route does not supply `2π` as derived scalar.

5. **NUMERICAL NEAR-COINCIDENCE DIAGNOSTICS.** The high-precision search
   (50 dps, mpmath; pair products + small-integer-coefficient sums over
   the expanded inventory) finds non-trivial near-misses to `π`-related
   targets at relative error ~10⁻⁴, e.g.:

   - `9·(16891/43740) - 1/3 = 15271/4860 ≈ 3.14218`, vs. `π = 3.14159`,
     rel.err `1.87×10⁻⁴`;
   - `8·(2/9) - 2·α_s(v) ≈ 1.5712`, vs. `π/2 = 1.5708`, rel.err
     `2.4×10⁻⁴`;
   - `9·u_0 + 6·(7/9) ≈ 12.566`, vs. `4π = 12.5664`, rel.err `4.5×10⁻⁵`.

   These are **numerical coincidences**, not exact. Their relative
   error scale (`~10⁻⁴`) is interestingly close to the C-iso engineering
   `ε_witness ~ 3×10⁻⁴` — but this is itself not a closure of `P`,
   merely a "rational approximation to `π` of denominator `4860` at
   `1.9×10⁻⁴`" type of fact. By a standard continued-fraction estimate,
   approximations of `π` at error `1/N²` exist trivially; finding one
   in the retained inventory at this accuracy is statistically expected
   for an inventory of `>20` rationals, and does not constitute
   derivation.

   **The exact-tier matches all involve `α_bare` (or `α_LM`,
   `α_s(v)`) and reduce algebraically to `4π = 1/α_bare` — which is the
   period-convention re-entry diagnosed in (3).**

## What this DOES close (positively)

**Expanded-inventory closure of the radian-bridge no-go.** The retained
dimensionless inventory as of 2026-05-10 is enumerated and tested.
Specifically:

- `~25+ retained dimensionless items in 14+ groups` are inventoried with
  explicit retained provenance;
- the Q-rational subset of these is closed under finite arithmetic and
  cannot produce `π` (Lindemann-Weierstrass);
- the only π-containing items (`α_bare`, `α_LM`, `α_s(v)`) inherit `π`
  from the QED continuum-normalization convention, which is itself the
  period-`2π` convention `P`;
- numerical search at 50 dps finds no exact match to `π`-targets except
  through `α_bare`-relabeling.

This positively closes the question "does the EXPANDED retained
dimensionless inventory derive `2π`?" with a **negative answer**.

## What this DOES NOT close

**The radian-bridge primitive `P` itself remains bounded.** This work
adds one more no-go to the campaign's running tally: dimensional/
dimensionless analysis on the EXPANDED retained dimensionless content
through 2026-05-10 does **not** supply `P`. The minimal closing inputs
(a), (b), (c) named in
[`KOIDE_Z3_QUBIT_RADIAN_BRIDGE_NO_GO_NOTE_2026-04-20.md`](KOIDE_Z3_QUBIT_RADIAN_BRIDGE_NO_GO_NOTE_2026-04-20.md)
§4 remain candidate routes (lattice propagator radian quantum, 4×4
hw=1+baryon Wilson holonomy, Z_3-orbit Wilson-line `d²`-power
quantization). None currently retained.

## Why dimensionless analysis fails on the radian even with the expanded inventory

### A. The Lindemann-Weierstrass blocker is irreducible

The transcendentality of `π` over `Q` is a theorem (Lindemann 1882).
No finite Q-algebraic combination of rationals can equal a
transcendental. Adding more rationals to the basis enlarges the
Q-algebraic span, but the span remains a subset of Q-algebraic numbers,
which by Lindemann does not contain `π`. The expanded inventory does
not change this.

### B. The expanded inventory adds rationals only, no new transcendentals

A close audit of the post-2026-05-09 retained additions:

| Item | Algebraic over Q? | Transcendental? |
|---|---|---|
| SU(3) NLO `7/9` | yes (rational) | no |
| Wilson NNLO `-13/3` etc. | yes (rational) | no |
| C-iso disc. `-5/9` etc. | yes (rational) | no |
| heat-kernel `(4/3)^n/n!` | yes (rational) | no |
| Casimir `4/3` | yes (rational) | no |
| `β_0 = 7, β_1 = 26` | yes (rational) | no |
| Wilson exp `16, 18` | yes (rational integer) | no |
| `φ_dim 2/9` | yes (rational) | no |
| `ε_witness ~ 3×10⁻⁴` | numerical bound | no (a tolerance, not a constant) |

Every NEW item is rational. The inventory enlarges as a Q-vector
space but does not gain a transcendental dimension. The
Lindemann-Weierstrass argument ports unchanged.

### C. The `π` in `α_bare` is convention re-entry of `P`

`α_bare = 1/(4π)` is widely treated as a "derived" quantity in the
retained chain, with provenance "`g_bare = 1` plus QED canonical
normalization gives `α_bare = g_bare²/(4π) = 1/(4π)`". The `4π` here
arises from the gauge-propagator phase-space factor `d^4k/(2π)^4`
under continuum matching. On the discrete retained Cl(3)/Z³
substrate, the BZ momentum measure is `(1/V)·Σ_k`; matching to
`(2π)^{-d}·∫d^d k` is a **convention** for normalizing the continuum
limit, not a derivation of `π`. The `(2π)^d` factor in the matching
IS the period-`2π` identification `P` operating at the gauge
normalization stage.

This is the same primitive Probe 30 named, now diagnosed as also
entering through the gauge-normalization channel (in addition to the
direct-cosine channel of the Brannen circulant). **Closing `P` via
`α_bare` is structurally circular**: `α_bare` imports `P` rather than
deriving it.

### D. Numerical near-coincidences are statistical, not structural

The search produced near-coincidences at relative error `~10⁻⁴` (e.g.,
`9·(16891/43740) - 1/3 ≈ π` at `1.9×10⁻⁴`). For an inventory of `~25+`
items combined under pair-products and small-integer-coefficient sums,
the expected near-match density to a fixed real target at error `<ε`
scales as `O(N²/ε)` — for `N=25, ε=10⁻⁴` this expectation is `~6.25`
matches per target, which is consistent with the observed match count.
**The near-misses are statistically expected and do not constitute a
derivation.**

In particular: `15271/4860 ≈ π` is the rational `15271/4860` happening
to lie within `1.9×10⁻⁴` of `π`. This is qualitatively the same as the
classical numerical coincidence `355/113 ≈ π` at `2.7×10⁻⁷` (Tsu
Ch'ung Chih, c. 480 CE) or `22/7 ≈ π` at `4.0×10⁻⁴`. None of these
constitute a derivation of `π`; they are continued-fraction
convergents (or near-convergents) of a fixed transcendental. Finding
one in the retained inventory does not change `π`'s transcendentality.

## Setup

### Retained dimensionless content (expanded)

#### Pure rationals (Q):

| Symbol | Value | Origin | Authority |
|---|---|---|---|
| `1/3, 2/3` | rationals | `Z₃` group/Casimir | character algebra |
| `2/9` | `2/d² = n_eff/d²` | Plancherel-Frobenius | Probe 24 Step 1 |
| `8/9 = R_conn` | `(N_c²-1)/N_c²` | 1/N_c expansion | `RCONN_DERIVED_NOTE.md` |
| `1/2 = T_F` | Casimir T_F SU(3) | group theory | retained |
| `1/4 = g_2²(bare)` | `1/(d+1)` | Z_2 bipartite | retained |
| `1/5 = g_Y²(bare)` | `1/(d+2)` | chirality sector | retained |
| `7/8` | APBC fourth-power | hierarchy theorem | retained |
| `7/9` | NLO C-iso | SU(3) NLO closure | PR #685 |
| `7/18 = taste_weight` | `(7/8)·T_F·R_conn` | retained chain | retained |
| `4/3 = C_2(SU(3) fund.)` | Casimir | group theory | retained |
| `13/3, 37/2, 3071/36, 20873/48` | Wilson NNLO+ coefficients | `<P>_W_SU(3)` | PR #857 |
| `5/9, 47/486, 16891/43740` | C-iso discrepancy NNLO+ | `(P_W − P_HK)_SU(3)` | PR #857 |
| `41/10, -19/6, -7` | SM 1-loop β | derived gauge content | retained |
| `16, 18` | Wilson chain exponents | hierarchy theorem | retained |
| `7, 26` | QCD `β_0, β_1` at `N_f=6` | retained closed forms | retained |

#### Numerical (lattice MC):

| Symbol | Value | Origin |
|---|---|---|
| `<P>` | `0.5934 ± 0.0001` | SU(3) lattice MC at β=6 |
| `u_0 = <P>^{1/4}` | `0.8776` | tadpole improvement |

#### Contains `π` (convention inheritance):

| Symbol | Value | Origin |
|---|---|---|
| `α_bare = 1/(4π)` | `0.07958` | QED canonical normalization (CONVENTION) |
| `α_LM = α_bare/u_0` | `0.09066` | inherits `1/(4π)` from `α_bare` |
| `α_s(v) = α_bare/u_0²` | `0.1033` | inherits `1/(4π)` from `α_bare` |

Engineering bound (not a constant):

| Symbol | Value | Role |
|---|---|---|
| `ε_witness` | `~ 3×10⁻⁴` | C-iso single-plaquette truncation tolerance |

### Forbidden imports

- NO PDG observed values consumed (PDG appears only as falsifiability
  comparator in Section 9 of the runner).
- NO literature numerical comparators consumed beyond standard
  mathematical-constant precision.
- NO fitted selectors consumed.
- NO admitted unit conventions load-bearing on retention. The `α_bare`
  convention IS analyzed and shown to inherit the `P` primitive rather
  than derive it.
- NO same-surface family arguments.
- NO new axioms.
- NO new admissions.

## Derivation chain

### Step 1 (positive): expanded inventory enumerated

The retained dimensionless content as of 2026-05-10 is catalogued
explicitly. Each item has retained provenance pointing to a cited
source-stack note. The inventory comprises ~25+ items in 14+ groups.

### Step 2 (structural obstruction): Lindemann-Weierstrass on expanded basis

Every NEW item added since Probe 30 is **rational**. The Q-algebraic
span of the rational subset is a subset of `Q-algebraic` numbers, which
by Lindemann (1882) does not contain `π`. The Probe 30 argument ports
unchanged: no rational/Q-algebraic combination of the expanded basis
produces `2π` as derived scalar.

### Step 3 (new sharpening): `α_bare` route is convention re-entry

The expanded basis contains `α_bare = 1/(4π)` (and derivatives `α_LM`,
`α_s(v)`). These DO contain `π` explicitly, and trivially
`1/α_bare = 4π`. However, this is **convention re-entry**, not
derivation:

- `α_bare = g_bare²/(4π)` is the QED canonical normalization, where the
  `4π` arises from the gauge-propagator continuum phase-space factor
  `d^4k/(2π)^4`. On the discrete `Cl(3)/Z³` substrate, the BZ momentum
  measure is the discrete Riemann sum `(1/V)·Σ_k`, NOT the continuum
  `(2π)^{-d}·∫d^d k`. The `(2π)^d` factor enters only at the
  discrete-to-continuum matching stage, where it IS the period-`2π`
  identification.
- Therefore `α_bare = 1/(4π)` IMPORTS `π` via the period-`2π`
  convention, which is the same primitive `P` Probe 30 identified.
- Using `1/α_bare = 4π` to "derive" `π` is structurally circular: the
  derivation chain `α_bare → 4π → 2π → P` returns the input `P` it was
  supposed to derive.

### Step 4 (new sharpening): heat-kernel `e` is not `π`

The retained SU(3) heat-kernel `<P>_HK_SU(3)(s_t) = 1 - exp(-(4/3)s_t)`
is transcendental in `e`, not `π`. Nesterenko's theorem on `(e^π, π)`
establishes `e` and `π` as algebraically independent for relevant
cases. Therefore the heat-kernel evaluation at any retained `s_t`
gives a number transcendental in `e` but not a rational multiple of `π`.
This route does not supply `2π`.

### Step 5 (numerical exhaustion): high-precision search at 50 dps

The runner performs a brute search over:

- single-atom matches to `{π, 2π, π/2, π/3, π/9, 4π, 1/(2π), (2/9)·2π}`;
- pair products and ratios of inventory atoms;
- small-integer-coefficient sums `c₁·X + c₂·Y` for
  `c₁, c₂ ∈ {-3, -2, -1, 1, 2, 3, 4, 6, 8, 9}`.

**Findings:**

- exact-tier matches (rel.err `< 10⁻³⁰`): all involve `α_bare` or its
  derivatives. Examples: `1/(4·α_bare) = π`, `2·α_bare = 1/(2π)`. All
  reduce to `α_bare`-relabeling.
- non-α_bare near-misses bottom out at rel.err `~10⁻⁴`:
  - `9·(16891/43740) - 1/3 ≈ π` at `1.87×10⁻⁴`;
  - `8·(2/9) - 2·α_s(v) ≈ π/2` at `2.4×10⁻⁴`;
  - `9·u_0 + 6·(7/9) ≈ 4π` at `4.5×10⁻⁵`.

  These are statistically-expected numerical coincidences for an
  inventory of `~25+` rationals under pair sums (continued-fraction
  convergent density argument). They are not derivations.

### Step 6 (PDG match exhaustion): expanded candidates tested

The Brannen circulant `cos((2/9)·α + 2πk/3)` is tested for each
candidate identification of `α` (`= 1, 2π, 4π, π, 4π=u_0/α_LM` (sic),
`4/3, 7/8, 7/9, 16, 18, 26, α_LM, α_LM²`, `<P>`):

- `α = 1` (literal `P`): max-dev vs PDG `~3×10⁻⁵` ✓ (recovers Probe 30 result)
- All other expanded candidates: max-dev ≥ `O(0.5)`, no PDG match.

No NEW PDG-passing dimensional/dimensionless candidate beats `α = 1`.

### Step 7 (verdict): structural obstruction sharpened

Combining Steps 1-6:

- expanded inventory enumerated (Step 1, positive);
- Lindemann-Weierstrass forbids Q-algebraic → `π` (Step 2);
- `α_bare` route is convention re-entry, not derivation (Step 3);
- heat-kernel route is `e`-transcendental, not `π` (Step 4);
- numerical search at 50 dps finds no exact match outside `α_bare`-relabeling
  (Step 5);
- PDG match exhaustion: only `α = 1` (literal `P`) passes (Step 6).

**Verdict: STRUCTURAL OBSTRUCTION (sharpened).**

The radian-bridge primitive `P` is NOT closed by the expanded retained
dimensionless inventory. Probe 30's negative result is reconfirmed on
a strictly larger basis, with two new sharpenings:

(i) `α_bare = 1/(4π)` is diagnosed as a convention re-entry of `P`
    through the gauge-normalization channel (not previously emphasized
    in Probe 30);
(ii) numerical near-coincidences at rel.err `~10⁻⁴` are catalogued and
     attributed to standard rational-approximation density, not
     derivation.

## Status delta vs Probe 30

| | Probe 30 (PR #826) | This work (2026-05-10) |
|---|---|---|
| Inventory size | 7 dim. + ~5 rationals | ~25+ items in 14+ groups |
| Verdict | STRUCTURAL OBSTRUCTION (bounded) | STRUCTURAL OBSTRUCTION (sharpened) |
| `P` status | bounded admission | bounded admission (unchanged) |
| New diagnostic | none | `α_bare = 1/(4π)` is convention re-entry of `P` |
| Lindemann-Weierstrass | invoked | reconfirmed on larger basis |
| Numerical search | none beyond ratio enumeration | 50 dps mpmath; pair-products + small sums |
| Near-coincidence catalog | none | `~10⁻⁴` rel.err near-misses tabulated |

## Sharpened residue

The BAE-campaign admission count is unchanged:

- **(BAE)**: `|b|²/a² = 1/2`. Independent admission. UNCHANGED.
- **(P, radian-bridge)**: literal-rational-as-radian convention.
  CONFIRMED bounded by this probe — neither dimensional analysis nor
  expanded dimensionless analysis derives `P`.

After this work, the campaign's terminal residues remain `BAE + P = 2`.
The number of independent no-go routes against `P` increases from 5
(Probe 30) to 6:

1. `Z_3` qubit Pancharatnam-Berry (Probe 20);
2. selected-line local Berry (selected-line no-go 2026-04-20);
3. irreducibility audit (Probe campaign 2026-04-24);
4. native-angle exhaustion (Probe 24);
5. **dimensional-inventory exhaustion (Probe 30)**;
6. **expanded-dimensionless-inventory exhaustion (THIS work)**.

## What is positively closed

**Expanded-inventory closure of the no-go.** The dimensionless
inventory accumulated through 2026-05-10 does not derive `P`. This is
a positive structural theorem in the sense of being an exhaustive
inventory: every retained dimensionless ingredient through 2026-05-10
has been tested.

Furthermore, the `α_bare = 1/(4π)` route — which superficially looks
like a closure path because it does literally contain `π` — is
diagnosed as **convention re-entry** of `P` through the gauge-
normalization channel. This is a NEW sharpening relative to Probe 30,
which did not explicitly catalog the gauge-normalization channel as a
re-entry point.

## What remains bounded

**The radian-bridge primitive `P`** itself remains the load-bearing
missing input for closing `δ = 2/9 rad` and (per Probe 24) the BAE
"magic angle" `φ = 2/9 rad`. To close `P`, one of the three structural
inputs from
[`KOIDE_Z3_QUBIT_RADIAN_BRIDGE_NO_GO_NOTE_2026-04-20.md`](KOIDE_Z3_QUBIT_RADIAN_BRIDGE_NO_GO_NOTE_2026-04-20.md)
§4 must be supplied:

- **Input (a):** Lattice propagator radian quantum
  `G_{C_3}(1) = exp(i · 2/d²) · G_0`.
- **Input (b):** 4×4 hw=1+baryon non-uniform Wilson holonomy where the
  baryon-projected line gives Wilson phase `2/d²` rad.
- **Input (c):** Z_3-orbit Wilson-line `d²`-power quantization
  `W_{Z_3}^{d²} = exp(2i) · 𝟙`.

None currently retained.

## Strategic options (this note ENDORSES NONE)

1. **Accept `P` as a permanent named bounded admission.** Six independent
   no-go probes now agree that `P` is not derivable from currently
   cited source-stack content.
2. **Continue closure hunt via Inputs (a), (b), (c).** Wilson-chain
   structure (Probe 19's `m_τ` finding) hints toward (a) or (c).
3. **Pivot to a dimensionless-only readout law.** Reformulate the
   Brannen circulant to take dimensionless inputs directly, bypassing
   the radian unit.
4. **Audit `α_bare = 1/(4π)` as a named admission.** This work newly
   diagnoses `α_bare` as a convention re-entry of `P`. The audit lane
   may consider whether `α_bare` should be reclassified from "derived"
   to "convention-bounded" pending closure of `P`. (This note does not
   endorse the reclassification — only flags the diagnostic.)

## What this DOES NOT do

This note explicitly does **NOT**:

1. **Close the radian-bridge primitive `P`.** `P` remains a named
   bounded admission.
2. **Promote any retained theorem.** No retained theorem is modified.
3. **Add a new axiom.** The physical `Cl(3)` local algebra plus `Z^3`
   spatial substrate baseline still suffices.
4. **Use PDG values as derivation input.** PDG appears only as
   falsifiability comparator in Section 9 of the runner.
5. **Modify the BAE admission status.** BAE remains independently
   bounded.
6. **Reclassify `α_bare`.** The diagnostic that `α_bare` inherits `π`
   via the period convention is reported but the formal classification
   of `α_bare` as bounded-or-derived is left to the audit lane.
7. **Resolve any sister bridge gap** (L3a, L3b, C-iso truncation, W1.exact engineering frontier).

## Forbidden imports check

- No PDG observed values consumed.
- No literature numerical comparators consumed beyond standard
  mathematical-constant precision.
- No fitted selectors consumed.
- No admitted unit conventions load-bearing on retention. The `α_bare`
  convention is ANALYZED, not admitted.
- No same-surface family arguments.

## Validation

Primary runner:
[`scripts/cl3_radian_bridge_expanded_inventory_2026_05_10_radianexp.py`](../scripts/cl3_radian_bridge_expanded_inventory_2026_05_10_radianexp.py)

Verifies:

1. Expanded retained dimensionless inventory enumerated (~25+ items).
2. All NEW items since Probe 30 are pure rationals (no transcendentals
   added beyond what Probe 30 already had).
3. Lindemann-Weierstrass holds on the expanded Q-algebraic basis.
4. `α_bare = 1/(4π)` exactly: the only `π`-containing retained item
   is the QED-convention `α_bare`; numerical exact-tier matches in the
   search all involve `α_bare` and reduce to `α_bare`-relabeling.
5. Heat-kernel `1 - exp(-(4/3)s_t)` is transcendental in `e`, not `π`.
6. Wilson series partial sums at retained `s_t` are rational; no
   `π`-target match.
7. C-iso discrepancy series at retained `s_t` is rational; no
   `π`-target match.
8. `α_LM^k` for `k ∈ {1, 2, ..., 36}` does not produce `π`-related
   target.
9. PDG match exhaustion: only `α = 1` (literal `P`) reproduces the
   PDG charged-lepton triplet.
10. PDG firewall: PDG values appear only as Section 9 comparator,
    never as derivation input.

Target: `=== TOTAL: PASS=N, FAIL=0 ===` (achieved: PASS=31).

## Cross-references

- [`KOIDE_BAE_PROBE_RADIAN_FROM_DIMENSIONS_BOUNDED_NOTE_2026-05-09_probe30.md`](KOIDE_BAE_PROBE_RADIAN_FROM_DIMENSIONS_BOUNDED_NOTE_2026-05-09_probe30.md)
  — direct predecessor; this work extends to expanded dimensionless inventory.
- [`KOIDE_BAE_PROBE_PHI_FROM_Z3_CHARACTER_SHARPENED_NOTE_2026-05-09_probe24.md`](KOIDE_BAE_PROBE_PHI_FROM_Z3_CHARACTER_SHARPENED_NOTE_2026-05-09_probe24.md)
  — identifies `P` with BAE-level magic-angle admission.
- [`KOIDE_BAE_PROBE_WILSON_CHAIN_MASS_SHARPENED_NOTE_2026-05-09_probe19.md`](KOIDE_BAE_PROBE_WILSON_CHAIN_MASS_SHARPENED_NOTE_2026-05-09_probe19.md)
  — Wilson chain exponent `18 = 16 + 2` for `m_τ`.
- [`C_ISO_SU3_NNLO_CLOSURE_BOUNDED_NOTE_2026-05-10_su3nnlo.md`](C_ISO_SU3_NNLO_CLOSURE_BOUNDED_NOTE_2026-05-10_su3nnlo.md)
  — SU(3) NNLO/N5LO Wilson coefficients and C-iso discrepancy series.
- [`COMPLETE_PREDICTION_CHAIN_2026_04_15.md`](COMPLETE_PREDICTION_CHAIN_2026_04_15.md)
  — `<P>`, `u_0`, `α_bare`, `α_LM`, `α_s(v)` provenance.
- [`MINIMAL_AXIOMS_2026-05-03.md`](MINIMAL_AXIOMS_2026-05-03.md)
  — physical `Cl(3)` local algebra plus `Z^3` spatial substrate
  baseline (legacy aliases `A1`/`A2`).
- [`G_BARE_HILBERT_SCHMIDT_RIGIDITY_THEOREM_NOTE_2026-05-07.md`](G_BARE_HILBERT_SCHMIDT_RIGIDITY_THEOREM_NOTE_2026-05-07.md)
  — `g_bare = 1` retained.
- [`KOIDE_Q_DELTA_LINKING_RELATION_THEOREM_NOTE_2026-04-20.md`](KOIDE_Q_DELTA_LINKING_RELATION_THEOREM_NOTE_2026-04-20.md)
  §4 — names primitive `P`.
- [`KOIDE_Z3_QUBIT_RADIAN_BRIDGE_NO_GO_NOTE_2026-04-20.md`](KOIDE_Z3_QUBIT_RADIAN_BRIDGE_NO_GO_NOTE_2026-04-20.md)
  — closing-input list (a)/(b)/(c).
- [`KOIDE_A1_RADIAN_BRIDGE_IRREDUCIBILITY_AUDIT_NOTE_2026-04-24.md`](KOIDE_A1_RADIAN_BRIDGE_IRREDUCIBILITY_AUDIT_NOTE_2026-04-24.md)
  — period-1-rad vs canonical period-2π-rad convention.

## Cited dependencies

Load-bearing for Step 1 (expanded inventory enumeration):

1. `MINIMAL_AXIOMS_2026-05-03.md` (physical `Cl(3)` local algebra
   plus `Z^3` spatial substrate baseline; legacy aliases `A1`/`A2`);
2. `COMPLETE_PREDICTION_CHAIN_2026_04_15.md` (`<P>`, `u_0`, `α_bare`,
   `α_LM`, `α_s(v)`, β coefficients, hierarchy theorem);
3. `C_ISO_SU3_NNLO_CLOSURE_BOUNDED_NOTE_2026-05-10_su3nnlo.md` (NNLO/N5LO
   Wilson and C-iso discrepancy rationals);
4. `KOIDE_BAE_PROBE_WILSON_CHAIN_MASS_SHARPENED_NOTE_2026-05-09_probe19.md`
   (m_τ exponent 18);
5. `KOIDE_BAE_PROBE_PHI_FROM_Z3_CHARACTER_SHARPENED_NOTE_2026-05-09_probe24.md`
   (`φ_dim = 2/9`).

Load-bearing for Step 2-4 (negative argument):

6. **Lindemann-Weierstrass theorem**: standard mathematical theorem on
   transcendentality of `π` over Q (theorem-grade external reference).
7. **Nesterenko's theorem on (e^π, π)**: standard mathematical theorem
   on algebraic independence of `e^π` and `π` (theorem-grade external
   reference).

Load-bearing for Step 6 (PDG match):

8. `KOIDE_CIRCULANT_CHARACTER_DERIVATION_NOTE_2026-04-18.md` R3 — Brannen
   circulant eigenvalue formula.

## Bottom line

**Verdict: STRUCTURAL OBSTRUCTION (sharpened).**

The retained Cl(3)/Z³ framework's dimensionless inventory as of
2026-05-10 — comprising ~25+ named retained items in 14+ groups,
including SU(3) NNLO/N5LO Wilson coefficients, QCD `β_0/β_1` at
`N_f=6`, Wilson chain exponent `18 = 16 + 2`, character-algebra
rationals `2/9, 7/8, 7/9, 7/18, 4/3`, and lattice-MC `<P>, u_0` —
does NOT supply `2π` as a derived scalar.

Lindemann-Weierstrass holds on the expanded basis: every NEW retained
item is rational, and the Q-algebraic span of rationals cannot reach
the transcendental `2π`. The only `π`-containing items
(`α_bare = 1/(4π)`, `α_LM`, `α_s(v)`) inherit `π` via the QED
continuum-momentum-integration convention, which IS the period-`2π`
identification — the same primitive `P` Probe 30 named. Using `α_bare`
to derive `π` is structurally circular.

Numerical near-coincidences at rel.err `~10⁻⁴` (e.g.,
`9·(16891/43740) − 1/3 ≈ π` at `1.87×10⁻⁴`) are catalogued and
attributed to standard rational-approximation density on an inventory
of `~25+` items. They do NOT constitute derivation.

The framework's open-import count for the BAE/Koide closure remains
unchanged at 2: `BAE + P`. After this work, `P` is closed negatively
against six independent no-go routes; the dimensional-inventory
expansion sharpens but does not close the obstruction.

The honest answer to the user's hypothesis "maybe the EXPANDED inventory
contains combinations that DERIVE radian-equivalent angles" is:

> **NO.** The expansion does not change the Lindemann-Weierstrass
> blocker. The only `π`-containing retained item is `α_bare`, whose
> `π` is itself an import of the period convention `P`. The radian
> primitive is not derivable from retained dimensionless content.
