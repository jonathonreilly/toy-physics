# DM Wilson-to-`dW_e^H` Local Chain Path-Algebra Target

**Date:** 2026-04-18  
**Status:** exact current-`main` target theorem sharpening the generic
Wilson-side `9`-channel source-family target to one structured local algebra
generator target upstream of the DM selector  
**Script:** `scripts/frontier_dm_wilson_to_dweh_local_chain_path_algebra_target_2026_04_18.py`

## Question

After fixing the weakest honest constructive DM target as one Wilson-side
Hermitian source family with `9` real channels for

`dW_e^H = Schur_Ee(D_-)`,

is the next positive theorem really about `9` unrelated Wilson probes?

Or is there already a sharper structured local object whose Hermitian shadow
produces that same `9`-channel codomain?

## Bottom line

There is a sharper structured local object.

Let `A_chain` be the local `*`-algebra generated by one adjacent directed
nearest-neighbor two-edge chain

- `E_12`,
- `E_23`,

and their adjoints on `1 -> 2 -> 3`.

Equivalently, `A_chain` is the span of the `9` matrix units

`E_11, E_22, E_33, E_12, E_21, E_23, E_32, E_13, E_31`,

with

- `E_11 = E_12 E_21`,
- `E_22 = E_21 E_12 = E_23 E_32`,
- `E_33 = E_32 E_23`,
- `E_13 = E_12 E_23`,
- `E_31 = E_32 E_21`.

Then the sharpest structured Wilson-side target upstream of the DM selector is
not `9` unrelated channels. It is:

1. one local unital `*`-monomorphism
   `Phi_chain : A_chain -> End(H_W)`;
2. one descended Wilson first-variation law on the Hermitian image.

The generic `9`-channel Hermitian source-family target is then just the
Hermitian shadow of this local chain-algebra object.

So the next constructive DM step is best read as:

- realize one local adjacent-chain path-algebra embedding,
- then prove its descended first-variation law is exactly `dW_e^H`.

## What current `main` already fixes

### 1. `dW_e^H` is already the live compressed codomain

From
[DM_LEPTOGENESIS_NE_PROJECTED_SOURCE_LAW_DERIVATION_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_NE_PROJECTED_SOURCE_LAW_DERIVATION_NOTE_2026-04-16.md)
and
[DM_LEPTOGENESIS_NE_CHARGED_SOURCE_RESPONSE_REDUCTION_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_NE_CHARGED_SOURCE_RESPONSE_REDUCTION_NOTE_2026-04-16.md):

- the live remaining PMNS-assisted DM object is the charged-lepton projected
  Hermitian source law `dW_e^H`;
- once `dW_e^H` is known, `H_e`, the charged packet, and the selected flavored
  transport column are already downstream algorithmic.

### 2. A generic Hermitian reconstruction route needs `9` real channels

From
[DM_WILSON_TO_DWEH_HERMITIAN_SOURCE_FAMILY_TARGET_NOTE_2026-04-18.md](./DM_WILSON_TO_DWEH_HERMITIAN_SOURCE_FAMILY_TARGET_NOTE_2026-04-18.md):

- `dim_R Herm(3) = 9`;
- a generic `dW_e^H` reconstruction route therefore needs `9` real response
  channels;
- but that note did not yet identify the sharpest structured generator target.

This note sharpens exactly that point.

## Theorem 1: one adjacent two-edge chain already generates the full `Herm(3)` target

Define the Hermitian chain-generated basis elements

- `D_1 := E_11`,
- `D_2 := E_22`,
- `D_3 := E_33`,
- `X_12 := E_12 + E_21`,
- `Y_12 := -i E_12 + i E_21`,
- `X_23 := E_23 + E_32`,
- `Y_23 := -i E_23 + i E_32`,
- `X_13 := E_13 + E_31`,
- `Y_13 := -i E_13 + i E_31`.

Then these `9` Hermitian elements form a real basis of `Herm(3)`.

### Proof

The diagonal terms are Hermitian and linearly independent.

The four nearest-neighbor Hermitian terms come directly from the edge
generators and their adjoints.

The long-corner Hermitian terms come from chain products because

- `E_13 = E_12 E_23`,
- `E_31 = E_32 E_21`.

So every displayed Hermitian basis element lies in the Hermitian part of the
chain-generated algebra `A_chain`.

Because the `9` underlying matrix units are linearly independent and the
displayed Hermitian combinations are the standard Hermitian coordinate basis of
`Mat_3(C)`, they form a real basis of `Herm(3)`.

Therefore one adjacent two-edge chain already generates the full Hermitian
codomain needed for `dW_e^H`.

## Theorem 2: a local chain path-algebra embedding canonically induces the full `9`-channel Hermitian source-family target

Assume one local unital `*`-monomorphism

`Phi_chain : A_chain -> End(H_W)`.

Restrict it to the Hermitian basis displayed above. Then the `9` Wilson-side
Hermitian operators

- `Phi_chain(D_1)`,
- `Phi_chain(D_2)`,
- `Phi_chain(D_3)`,
- `Phi_chain(X_12)`,
- `Phi_chain(Y_12)`,
- `Phi_chain(X_23)`,
- `Phi_chain(Y_23)`,
- `Phi_chain(X_13)`,
- `Phi_chain(Y_13)`

form a canonical Wilson Hermitian source family spanning a copy of `Herm(3)`.

If `dW_W` is any real-linear Wilson first-variation law on the Hermitian image,
then

`X -> dW_W(Phi_chain(X))`

is a real-linear Hermitian source law on `Herm(3)`.

In particular, if

`dW_W o Phi_chain = dW_e^H`,

then the full charged Hermitian block `H_e` is fixed, and the downstream DM
chain is already algorithmic.

### Proof

Because `Phi_chain` is `*`-preserving, Hermitian elements of `A_chain` map to
Hermitian Wilson-side operators. Because `Phi_chain` is injective, the images
of the `9` Hermitian basis elements remain real-linearly independent.

So the restriction of `Phi_chain` to the Hermitian part of `A_chain` gives a
canonical `9`-channel Wilson Hermitian source family.

Composing any real-linear Wilson first-variation law `dW_W` with that
restriction gives a real-linear functional on `Herm(3)`.

If that composed functional equals `dW_e^H`, then by the current exact DM
reduction `H_e` is fixed, and the packet / selected transport column follow
algorithmically.

Therefore the generic `9`-channel target is exactly the Hermitian shadow of
one local chain path-algebra embedding plus one descended first-variation law.

## Corollary: the next positive DM attack splits into two exact layers

The sharpest structured current-`main` Wilson attack is now:

1. **generator layer**:
   realize one local adjacent-chain path-algebra embedding `Phi_chain`;
2. **descended-response layer**:
   prove the descended first-variation identity
   `dW_W o Phi_chain = dW_e^H`.

This is a better target than:

- searching for `9` unrelated Wilson probes,
- or returning to internal selector functionals on `H(m, delta, q_+)`.

## What this closes

- the target-shape question between a generic `9`-probe Wilson family and a
  structured local generator target
- an exact algebraic reason the PF-style local adjacent-chain route is the
  right sharpening of the Wilson attack
- an exact two-layer decomposition of the constructive Wilson-to-`dW_e^H`
  problem on current `main`

## What this does not close

- a positive realization of `Phi_chain`
- a positive theorem that the descended Wilson first variation equals
  `dW_e^H`
- the DM microscopic selector law
- the DM flagship gate

## Why this matters

This note changes the practical attack surface.

The constructive target is no longer best read as:

- `9` unrelated Wilson response channels.

It is now best read as:

- one local adjacent-chain path algebra carrying the full Hermitian codomain,
- plus one descended first-variation identity into `dW_e^H`.
