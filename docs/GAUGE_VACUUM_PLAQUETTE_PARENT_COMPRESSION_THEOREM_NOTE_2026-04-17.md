# Gauge-Vacuum Plaquette Parent / Compression Theorem

**Date:** 2026-04-17  
**Status:** exact structural theorem on the Wilson parent partition object and its canonical plaquette / `theta` descendants on the retained `3 spatial + 1 derived-time` surface  
**Script:** `scripts/frontier_gauge_vacuum_plaquette_parent_compression_theorem.py`  
**Framework convention:** "axiom" means only the single framework axiom `Cl(3)` on `Z^3`.

## Question

Before attempting a global Perron-Frobenius selector theorem, is there already
one exact parent object on the retained Wilson surface from which multiple
PF-relevant descendant laws are canonically obtained?

## Answer

Yes, on the Wilson gauge surface.

The exact one-clock Wilson object

`Z_(L_s,L_t)(beta) = Tr[T_(L_s,beta)^(L_t)]`

already serves as the parent partition object on that surface.

From that one parent object, the repo already has two canonical descendants:

1. **Plaquette source-sector compression.**  
   Compress to the marked-plaquette source-cyclic class-function sector
   generated by repeated applications of the explicit plaquette source operator
   `J = (chi_(1,0) + chi_(0,1)) / 6`. On that sector, the transfer law reduces
   to the exact source-sector object whose large-derived-time state is the
   plaquette Perron state.

2. **Topological Fourier descendant.**  
   Decompose the same retained Wilson-plus-staggered partition into
   topological sectors `Z_Q >= 0`, then form

   `Z(theta) = sum_Q Z_Q e^(i theta Q)`.

   The strong-CP `theta = 0` minimum is therefore a canonical Fourier-sector
   descendant of the same positive Wilson parent partition object.

So the repo already supports the first half of the PF consistency program:
there is one exact parent object on the Wilson surface with more than one
canonical descendant construction.

What it does **not** yet support is the stronger global claim that every live
PF sector in the repo, especially PMNS, is already a canonical projection of
that same parent object.

## Setup

### 1. Exact Wilson parent object

From
[GAUGE_VACUUM_PLAQUETTE_TRANSFER_OPERATOR_CHARACTER_RECURRENCE_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_TRANSFER_OPERATOR_CHARACTER_RECURRENCE_NOTE.md):

- the finite Wilson partition function on the retained `3+1` source surface
  factors as one exact positive self-adjoint one-clock transfer operator
  `T_(L_s,beta)`
- the local marked plaquette source is the explicit self-adjoint class-function
  operator `J = (chi_(1,0) + chi_(0,1)) / 6`
- the source-cyclic class-function sector generated by repeated application of
  `J` is the exact sector on which the plaquette generating object is realized

### 2. Exact plaquette source-sector descendant

From
[GAUGE_VACUUM_PLAQUETTE_SOURCE_SECTOR_MATRIX_ELEMENT_FACTORIZATION_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_SOURCE_SECTOR_MATRIX_ELEMENT_FACTORIZATION_NOTE.md):

- the exact source-sector transfer law is

  `T_src(beta) = exp[(beta / 2) J] D_beta exp[(beta / 2) J]`

- at the framework point,

  `T_src(6) = exp(3 J) D_6 exp(3 J)`

So the plaquette PF object is not an unrelated transfer law. It is the
canonical source-sector descendant of the same Wilson parent object.

### 3. Exact `theta` descendant

From [STRONG_CP_THETA_ZERO_NOTE.md](./STRONG_CP_THETA_ZERO_NOTE.md):

- the same retained Wilson-plus-staggered partition admits a topological
  decomposition

  `Z = sum_Q Z_Q`

- every sector weight satisfies `Z_Q >= 0`
- the `theta`-deformed partition is

  `Z(theta) = sum_Q Z_Q e^(i theta Q)`

So the strong-CP positivity law is a canonical Fourier descendant of the same
retained Wilson partition object.

## Theorem 1: Wilson parent object

On the retained Wilson `3 spatial + 1 derived-time` surface, the exact one-clock
transfer object `T_(L_s,beta)` is the parent partition object for the current
plaquette PF lane.

Its exact descendants already present on `main` are obtained by two canonical
operations:

1. orthogonal compression to the marked-plaquette source-cyclic class-function
   sector;
2. Fourier weighting of the topological-sector decomposition of the same
   partition function.

## Corollary 1: plaquette Perron reduction is a parent-descendant theorem

The current plaquette Perron theorem is therefore not merely:

- a positive operator theorem on an isolated reduced object.

It is more specifically:

- the Perron reduction of a canonical descendant of the Wilson parent object.

So the large-derived-time plaquette PF state already sits inside a genuine
parent/compression structure on the Wilson surface.

## Corollary 2: strong-CP `theta = 0` is a parent-descendant positivity theorem

The strong-CP result is therefore not merely:

- an unrelated positivity argument on a separate measure family.

It is more specifically:

- the positivity-selection law of the topological Fourier descendant of the
  same retained Wilson parent partition object.

So the Wilson surface already exhibits more than one exact PF-relevant
descendant construction.

## What this closes

- the first Wilson-side step of the PF consistency program:
  there is one exact parent partition object with multiple canonical
  descendants already present on `main`
- the clarification that plaquette PF reduction and strong-CP positivity are
  not parallel slogans but descendant constructions from the same Wilson
  surface
- the scientific case for pursuing a Wilson parent/compression theorem before
  trying to pull PMNS into a common PF selector

## What this does not close

- a full common PF parent/projection theorem across plaquette, strong-CP, and
  PMNS
- a canonical PMNS projection from the Wilson parent object
- the explicit `beta = 6` residual environment data `S_6^env` / `rho_(p,q)(6)`
- a global sole-axiom PF selector

## Why this matters for the PF program

This note advances the PF program in the right order.

It shows that the repo is no longer at the stage:

- "PF appears in unrelated isolated notes."

It is now at the stage:

- "the Wilson surface already has one parent object and two exact descendant
  PF-relevant laws."

The next two science questions are therefore sharper:

1. can the Wilson parent/compression structure be strengthened into a true
   parent/projection theorem?
2. once that is done, can the PMNS aligned transfer law be shown to be a
   canonical descendant too?

## Atlas inputs used

- [GAUGE_VACUUM_PLAQUETTE_TRANSFER_OPERATOR_CHARACTER_RECURRENCE_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_TRANSFER_OPERATOR_CHARACTER_RECURRENCE_NOTE.md)
- [GAUGE_VACUUM_PLAQUETTE_SOURCE_SECTOR_MATRIX_ELEMENT_FACTORIZATION_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_SOURCE_SECTOR_MATRIX_ELEMENT_FACTORIZATION_NOTE.md)
- [GAUGE_VACUUM_PLAQUETTE_PERRON_REDUCTION_THEOREM_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_PERRON_REDUCTION_THEOREM_NOTE.md)
- [STRONG_CP_THETA_ZERO_NOTE.md](./STRONG_CP_THETA_ZERO_NOTE.md)

## Command

```bash
python3 scripts/frontier_gauge_vacuum_plaquette_parent_compression_theorem.py
```
