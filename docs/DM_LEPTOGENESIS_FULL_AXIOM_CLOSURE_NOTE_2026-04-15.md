# DM Leptogenesis Full Axiom Closure

**Date:** 2026-04-15
**Branch:** `codex/dm-main-refresh`
**Script:** `scripts/frontier_dm_leptogenesis_full_axiom_closure.py`
**Framework convention:** "axiom" means only `Cl(3)` on `Z^3`

## Exact theorem-native chain now closed on branch

- exact source package:
  - `gamma = 1/2`
  - `E1 = sqrt(8/3)`
  - `E2 = sqrt(8)/3`
- exact denominator/projection:
  - `physical denominator = K00 = 2`
- exact coherent kernel:
  - `epsilon_1 = 2.4576198795840222e-6`
  - `epsilon_1 / epsilon_DI = 0.9276209209197268`
- exact effective mass implied by the same denominator:
  - `m_tilde = 0.10116572813579411 eV`

## End state

This does **not** yet land at `FULL THEOREM CLOSURE`.

With the remaining explicit retained transport map

- `T_rad(K) = 7.04 * C_sph * d_th * kappa_fit(K)`

the current branch gives

- `eta / eta_obs = 0.557919848420251`

Therefore the honest end state is:

- `FINAL EXACT BOUNDARY`

with one remaining non-axiom ingredient:

- `T_rad(K)`

and no silent benchmark dependence left in the final authority path.
