# Route 2 Spacetime Tensor Primitive Candidate on `PL S^3 x R`

**Date:** 2026-04-14  
**Purpose:** define the smallest plausible spacetime-level tensor carrier that
mediates between the bounded support-side tensor data `Theta_R^(0)` and the
exact slice dynamics generated by `Lambda_R` on the exact route-2 background
`PL S^3 x R`

## Verdict

The exact tensor-valued support observable is still missing.

But the current route-2 stack is now clean enough to build a concrete **bounded
spacetime tensor primitive candidate** that uses exactly the inputs the route
has already earned:

- exact background `PL S^3 x R`
- exact slice generator `Lambda_R`
- bounded source-side tensor pair `Theta_R^(0)(q) = (gamma_E(q), gamma_T(q))`

The smallest plausible carrier is the rank-1 spacetime tensor

> `Xi_R^(0)(t; q) := Theta_R^(0)(q) \otimes V_R(t)`

with

> `V_R(t) := exp(-t Lambda_R) u_*`

where `u_*` is the canonical normalized slice seed.

This candidate is bounded, not exact. It is the first spacetime-level object
that actually combines the support-side bright data with the exact route-2
slice semigroup without inventing a new tensor structure.

## Exact ingredients already in hand

### Background

The route-2 background remains exact:

- `PL S^3 x R`

from:

- [`S3_GENERAL_R_DERIVATION_NOTE.md`](/Users/jonreilly/Projects/Physics/docs/S3_GENERAL_R_DERIVATION_NOTE.md)
- [`ANOMALY_FORCES_TIME_THEOREM.md`](/Users/jonreilly/Projects/Physics/docs/ANOMALY_FORCES_TIME_THEOREM.md)

### Slice generator

The exact microscopic shell action supplies the exact slice generator:

- `I_R(f ; j) = 1/2 f^T Lambda_R f - j^T f`

from:

- [`OH_SCHUR_BOUNDARY_ACTION_NOTE.md`](/Users/jonreilly/Projects/Physics/docs/OH_SCHUR_BOUNDARY_ACTION_NOTE.md)

The induced transfer operator is the bounded one-step law:

- `T_R = exp(-Lambda_R)`

### Bounded source-side tensor pair

The best current source-side bright pair is:

- `Theta_R^(0)(q) = (gamma_E(q), gamma_T(q))`

on the microscopic support block:

- `A1 x {E_x, T1x}`

with the exact projective support scalar:

- `delta_A1(r) = 1 / (6 (1 + sqrt(6) r))`

## Candidate spacetime tensor primitive

Define the canonical route-2 slice seed

> `u_* := 1 / ||1||`

on the route-2 slice trace space, and let

> `V_R(t) := exp(-t Lambda_R) u_*`.

Then the spacetime tensor carrier candidate is

> `Xi_R^(0)(t; q) := Theta_R^(0)(q) \otimes V_R(t)`.

This is the minimal plausible route-2 spacetime tensor primitive because:

1. the first factor is the bounded bright-channel source data already found on
   the support side
2. the second factor is the exact route-2 slice semigroup generated by
   `Lambda_R`
3. the outer product keeps the support channels and the slice-time evolution
   visible at the same time
4. no new exact tensor observable is assumed

So `Xi_R^(0)` is the first spacetime-level carrier that truly mediates between
the support-side bright data and the slice dynamics.

## Why this is only a candidate

The retained exact support machinery is still scalar/rank-one on the current
`A1` block.

That means:

- there is still no exact tensor-valued support observable on
  `A1 x {E_x, T1x}`
- `Theta_R^(0)` remains a bounded staging object
- the current carrier is therefore not theorem-grade GR dynamics

So `Xi_R^(0)` is the smallest plausible spacetime tensor primitive candidate,
not an exact closure theorem.

## Runnable interpretation

The candidate can be checked by the following route-2 sanity conditions:

1. `Lambda_R` is symmetric positive definite on the audited source classes
2. `V_R(t)` is contractive for `t >= 0`
3. `Xi_R^(0)(0; q) = Theta_R^(0)(q) \otimes u_*`
4. `||Xi_R^(0)(t; q)||` decreases with `t`
5. the candidate remains bounded on the canonical `A1` family and the audited
   exact local `O_h` / finite-rank baselines

Those are the right checks for a candidate carrier; they are not proof of a
full dynamics theorem.

## Sharp blocker

The exact blocker is unchanged:

> there is still no exact tensor-valued support observable on
> `A1 x {E_x, T1x}` in the current retained support-side machinery.

Without that exact carrier, `Xi_R^(0)` remains a bounded spacetime primitive
candidate rather than a theorem-grade Einstein/Regge dynamics law.

## Bottom line

Route 2 now has the smallest plausible spacetime tensor carrier we can build
from the existing stack:

- exact background: `PL S^3 x R`
- exact slice generator: `Lambda_R`
- bounded support tensor pair: `Theta_R^(0)(q)`
- bounded spacetime tensor candidate: `Xi_R^(0)(t; q)`

That is the correct next tool to carry forward while the exact tensor
observable remains missing.
