# Next Chunk Recommendation

**Date:** 2026-04-08  
**Status:** working recommendation after the direct-`dM` fine-`H` family-pair closure and the overnight continuum/Kubo downgrades

The next highest-value chunk is:

> exact comparator work on the wave-retardation continuum lane.

## Why this is the top chunk

- The direct-`dM` fine-`H` surface is now controlled on both retained families.
- The next ceiling-lifting result is no longer another same-resolution direct
  `dM` hardening pass; it is restoring a trustworthy continuum baseline on the
  retardation/comparator lane.
- It has much higher theoretical leverage than another narrow direct-`dM`
  synthesis note or a fourth-point `dM(v)` negative.

## Comparison

1. Exact comparator.
   - Highest long-run leverage on the wave-retardation magnitude story.
   - Now also the best immediate chunk because the direct-`dM` family-pair
     control gap is closed.

2. `dM(v)` at `H = 0.20`.
   - Useful as a refinement check.
   - Lower priority because the lane is already a retained negative at
     `H = 0.25`; this would mostly sharpen a downgraded claim rather than
     open a new one.

3. Born derivation write-up.
   - Cheap and worth doing eventually.
   - Lowest marginal scientific upside of the four because it is mostly
     documentation/derivation hygiene, not a new retained observable.

4. Additional direct-`dM` synthesis notes.
   - Cheap, but now lower value.
   - The retained direct-`dM` surface is already strong enough that another
     narrow compression note does not move the scorecard much.

## Honest read

If we want one active chunk now, return to the exact comparator.

If we want a secondary cheap hardening lane after that, do the Born write-up
or a single `dM(v)` H=0.20 extension, not more same-resolution direct-`dM`
control work.

That keeps the current program ordered by return:

- push the exact comparator on the continuum lane
- keep `dM(v)` and Born work as secondary hardening
- treat direct-`dM` synthesis as tertiary unless a new family or observable is
  introduced
