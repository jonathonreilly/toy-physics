# 3D Dense Spent-Delay `z=2..5` Support Note

---

**This is a salvage / observation-cluster note for a narrowed
finite-window observation. It does not establish any retained claim.**
For retained claims on the 3D dense spent-delay surface, see the
per-claim notes referenced from the `## Audit scope` block below.

---

**Date:** 2026-04-30
**Status:** support / observation-cluster salvage record only — does not propagate retained-grade
**Claim type:** meta
**Claim scope:** support / observation-cluster salvage record only — does not propagate retained-grade
**Audit authority:** independent audit ledger only; this source does not set an audit verdict.
**Propagates retained-grade:** no
**Proposes new claims:** no

Salvage note for the runner-supported portion of the 3D dense
spent-delay wrapper after its wider attractive-window claim failed
audit.

## Audit scope (relabel 2026-05-10)

This file is a **finite-window observation-cluster salvage note**
for the `z=2..5` portion of the archived 3D dense spent-delay
wrapper. It is **not** a single retained theorem and **must not** be
audited as one. The audit ledger row for
`lattice_3d_dense_spent_delay_z2_z5_support_note_2026-04-30`
classified this source as conditional/positive_theorem with auditor's
repair target:

> register a runner/proof note for the load-bearing step or cite an
> audited retained dependency.

The minimal-scope response in this PR is to **relabel** this document
as an observation-cluster salvage note rather than to register a
primary runner or cite an audited retained dependency here. Those
steps belong in dedicated review-loop or per-z-row audit passes.
Until that work is done:

- This file makes **no** retained-claim assertions of its own.
- The "hierarchy-aligned read is support-side positive on `z=2,3,4,5`"
  observation, finite same-family diagnostic framing, and ordered-
  lattice dense spent-delay branch context below are **historical
  observation memory only**.
- The note explicitly excludes the archived wrapper's `z=6` row, any
  all-distance claim, any asymptotic claim, and any continuum/RG
  reading.
- The retained-status surface for any 3D dense spent-delay,
  attractive-window, or ordered-lattice sub-claim is the audit
  ledger (`docs/audit/AUDIT_LEDGER.md`) plus the per-claim notes,
  **not** this salvage record.
- Retained-grade does **NOT** propagate from this salvage record to
  any z-row observation, finite-window verdict, or successor wrapper.

For any retained claim about 3D dense spent-delay content, audit the
corresponding dedicated note (or its narrowed replacement) and its
runner as a separate scoped claim — not this observation-cluster
salvage record.

---

## 0. Provenance

The source wrapper
[`LATTICE_3D_DENSE_SPENT_DELAY_NOTE.md`](../archive_unlanded/lattice-dense-spent-delay-window-salvage-2026-04-30/LATTICE_3D_DENSE_SPENT_DELAY_NOTE.md)
is archived under recovery tag
`archive_unlanded/lattice-dense-spent-delay-window-salvage-2026-04-30/`.
The audit rejected the wrapper because its load-bearing attractive-window
claim included `z=6`, while the restricted live runner and checked log only
covered `z=2,3,4,5`.

## 1. Surviving observations

The restricted audit preserved a narrower finite-window observation:

- the named live runner/check surface covers `z=2,3,4,5`;
- on that checked window, the hierarchy-aligned read is support-side positive
  for the four tested points;
- the result is a finite same-family diagnostic for the ordered-lattice
  dense spent-delay branch.

## 2. Boundary

This note does not preserve the archived wrapper's `z=6` row, any all-distance
claim, any asymptotic claim, or any continuum/RG reading. Future promotion
requires a runner/log surface that includes the missing row or narrows the
source claim before audit.
