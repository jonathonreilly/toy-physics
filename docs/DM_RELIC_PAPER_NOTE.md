# DM Relic Paper Note

**Date:** 2026-04-12
**Branch:** `codex/dm-main-derived`
**Lane:** DM relic mapping
**Responding to:** Codex retain audit on `codex/review-active`

---

## Status

**BOUNDED / OPEN** -- one-input theorem boundary.

This note now sits downstream of the stronger 2026-04-14 authority surface:
the DM numerator is materially stronger than this note originally assumed,
the Coulomb shape is now native, and `sigma_v` is now best described through
the direct-observable `H -> T-matrix -> sigma_v` route. The old
"one-parameter window in `g_bare`" language is no longer the right headline
for the branch.

The clean current paper boundary is:

- the framework fixes the dark-sector relic structure much more strongly than
  before
- if the gauge-normalization rigidity theorem survives review, the old
  `g_bare` scan is no longer the live paper surface
- the honest unresolved object is the baryon asymmetry `eta`
- more sharply, the denominator is no longer blocked by "missing local
  charge-`2` source-family existence" once the new Nambu source principle is
  admitted; the genuinely new one-generation source increment is now also
  fixed to a pure-pairing ray up to rephasing, and the admitted
  Pfaffian/Nambu source family now has an exact no-stationary-scale theorem,
  but the branch now also has the exact local quadratic comparator
  `Q_2 = ||s||^2` and the exact background-normalized local response curve
  `W_rel = (1/2) log(1 + rho^2)`, `Q_rel = rho^2`; the branch now also has
  the exact local axis-exchange fixed point `rho = 1` on that normalized
  curve, with `W_rel = (1/2) log 2`, `Q_rel = 1`, once the selector is taken
  to be covariant under the exact canonical exchange of the retained normal
  and pairing axes on the admitted local block. So the remaining blocker is
  no longer comparator/background existence or local finite-point selection
  but the absolute staircase-embedding law for that now-selected local
  Majorana point; more sharply, the new self-dual staircase-lift obstruction
  theorem now shows the selected local point is still only projective on the
  present stack, so the missing object is a genuinely new non-homogeneous
  local-to-generation bridge or absolute-scale datum beyond the current
  homogeneous `Z_3` texture class. More sharply still, the new
  algebraic/spectral bridge obstruction theorem now closes the next obvious
  finite escape hatch too: the current bridge class built from block assembly,
  Schur complements, and obvious spectral data remains homogeneous or
  scale-free, so the missing object must go beyond that class as well.
  More sharply still, the new scalar-datum transplant obstruction theorem
  closes the obvious atlas-reuse escape hatch on the absolute-scale side:
  the current exact scalar atlas datums remain only fixed multiplicative
  coefficients on this lane, so they do not supply the missing absolute-scale
  datum either. More sharply still, the new source-response matching
  obstruction theorem closes the obvious matching escape hatch too: the exact
  local self-dual values do not anchor the current generation-side source-
  response observables absolutely either. More sharply still, the new tensor-
  variational transplant obstruction theorem now proves that the strongest
  gravity/atlas rescue route fails too: feeding the current self-dual
  Majorana ray into the exact direct-universal tensor/local-closure family
  still leaves the stationary family homogeneous, so it does not fix an
  absolute staircase anchor either. More sharply still, the new partition/
  projective transplant obstruction theorem now proves that the strongest
  QG/measure rescue route fails too: feeding the same ray into the exact
  universal partition/projective/refinement family still leaves the
  measure-compensated density monotone in the same scale, so it does not fix
  an absolute staircase anchor either. More sharply still, the new
  continuum-bridge transplant obstruction theorem now proves that the last
  inverse-limit/QG loophole fails too: a bare reinterpretation of that same
  compatible discrete family still does not fix an absolute staircase anchor
- therefore the strongest branch-safe theorem surface is `R(eta)`, with
  `R = 5.48` at the observed `eta`

**Update after the new midpoint/placement pass (2026-04-15):**

The denominator boundary above is now narrower on the branch's minimal
constructive bridge. The branch no longer lacks:

- an absolute staircase anchor
- a singlet/doublet placement law

It now has:

- exact doublet anchor `k_B = 8` on the endpoint-exchange bridge
- exact singlet/doublet placement `k_A = 7`, `k_B = 8` on the minimal
  adjacent lift

So the remaining denominator blocker is now the amplitude law inside the
already-fixed `A/B/epsilon` texture, especially `eps/B`, not the staircase

**Update after the split / atmospheric-scale pass (2026-04-15):**

The denominator boundary above is narrower again on the same minimal bridge.
The branch no longer lacks:

- a split law inside the fixed `A/B/epsilon` texture
- the reduced democratic `1/3` overlap factor
- an atmospheric benchmark value for `m_3`

It now has:

- exact split law `eps/B = alpha_LM/2` on the minimal symmetric bridge
- exact democratic overlap factor `1/3` on the retained `Z_3` basis bridge
- exact benchmark prediction `m_3 = 5.06e-2 eV`,
  `Dm^2_31 = 2.54e-3 eV^2` on the current diagonal chain

So the remaining denominator blocker is no longer a fitted texture input. It
is the exact CP-asymmetry kernel of leptogenesis on the fixed three-generation
`Z_3` texture, especially the relative sign/interference of the `N_2` and
`N_3` terms and the full-matrix flavor closure beyond the current reduced
estimate.

More sharply still, the new universal-Yukawa no-go theorem closes the last
soft phrasing loophole on that denominator surface: if the Dirac neutrino
Yukawa remains exactly universal `Y = y_0 I`, then every unitary basis change
preserves `Y^dag Y = y_0^2 I`, so the standard leptogenesis CP tensor is
exactly zero. So the remaining denominator blocker is not "polish the same
reduced epsilon estimate." It is derive the non-universal Dirac flavor
texture (or equivalent extra structure) that makes a nonzero CP kernel
possible on the fixed three-generation `Z_3` surface.
More sharply still, the new CKM-texture transfer no-go theorem closes the
next obvious flavor-atlas escape hatch: the current CKM/NNI tools do not
transfer to the exact universal neutrino bridge because its singular spectrum
is still triple-degenerate, the mass-basis hierarchy factors collapse to `1`,
and phase-only companions still leave `Y^dag Y` proportional to `I`.
More sharply still, the new CP-kernel deformation-necessity theorem now
specifies the shape of the missing object: it cannot be just another unitary
basis choice or a purely diagonal Yukawa split. The denominator now needs a
non-unitary off-diagonal flavor-breaking deformation that makes `Y^dag Y`
itself non-diagonal on the three-generation neutrino lane.
More sharply still, the new minimal `Z_3` circulant CP tool identifies the
exact algebraic target inside that class: the current weak-axis `1+2` split
already lifts to the even circulant slice `mu I + nu(S + S^2)`, but that
slice is still CP-degenerate. The minimal exact `Z_3`-covariant extension
that can support a nonzero leptogenesis kernel is the odd circulant
generator `i(S - S^2)`.
More sharply still, the new two-Higgs right-Gram bridge theorem now shows
that this CP-supporting odd-circulant target is not alien to the local
neutrino atlas: the canonical local two-Higgs neutrino lane already contains
it on an exact admissible subcone `d >= 2 r`. So the live denominator blocker
is no longer "invent a flavor mechanism from scratch."
More sharply still, the new DM two-Higgs minimality theorem now shows that
once nonzero local DM CP support is required, there is no smaller exact local
escape left: single-Higgs and repeated-charge lanes are CP-empty at the
Hermitian-kernel level, and the canonical distinct-charge two-Higgs class is
the unique minimal exact local escape.
More sharply still, the new DM continuity sheet theorem now removes the
residual local two-Higgs sheet ambiguity on the DM circulant route: the only
remaining local swap `x <-> y` is fixed intrinsically by continuity to the
retained universal bridge `Y = y_0 I`, selecting the physical `x >= y` sheet.
More sharply still, the new odd-circulant residual-`Z_2` slot theorem now
shows that the remaining local coefficient target is exactly one odd slot
`c_odd`, and the new odd-circulant current-stack zero law shows that the
present retained local bank assigns
`c_odd,current = 0`.
So the live denominator blocker is now sharper than a generic coefficient law:
full DM closure needs a genuinely new residual-`Z_2`-odd bridge or activator
beyond the retained local stack.
More sharply still, the new odd-slot minimal mixed-bridge extension theorem now
shows that the remaining positive extension class is itself highly constrained:
it must be a residual-`Z_2`-odd non-additive mixed bridge with one real
amplitude on the unique odd class `i(S-S^2)`.
And there is now a strongest current axiom-native candidate family on branch:
the `Z_3` phase-lift mixed bridge
`K_lambda = d I + r (e^{i lambda delta_src} S + e^{-i lambda delta_src} S^2)`
with exact weak-only source `delta_src = 2pi/3`.
That candidate turns on the missing odd slot without changing the local
two-Higgs admissible data, but `lambda` is not yet retained as a theorem, so
this is still a candidate route rather than a closure claim.

This is NOT "DM relic abundance derived from the lattice axioms alone."

**Current numerator authority on this branch:**

- `docs/DM_DIRECT_OBSERVABLE_NOTE.md`
- `docs/DM_COULOMB_FROM_LATTICE_NOTE.md`
- `docs/DM_FINAL_GAPS_NOTE.md`

---

## Theorem / Claim

The Cl(3) lattice structure fixes the DM freeze-out structure and therefore
determines the dark-to-baryon ratio as a function of the baryon asymmetry:

`R = R(eta)`.

At the observed baryon asymmetry `eta_obs = 6.12e-10`, the branch gives

`R(eta_obs) = 5.48`,

matching `R_obs = 5.47` to 0.25%.

This is a strong one-input theorem surface. It is not yet a zero-import
theorem because `eta` is not derived uniquely from the framework.

---

## Assumptions

1. The gauge-normalization issue is now better framed by
   `G_BARE_RIGIDITY_THEOREM_NOTE.md`: if that theorem candidate survives
   review, the framework no longer carries an independent bare coupling
   parameter. Until that authority is fully integrated, keep the
   normalization issue explicit and bounded.
2. The Coulomb potential V(r) = -C_F * alpha_s / r has a native lattice
   Green's-function route on the current branch (`DM_COULOMB_FROM_LATTICE_NOTE.md`).
3. sigma_v = C * alpha_s^2 / m^2 has a native direct-observable route through
   the lattice T-matrix / optical theorem on the current branch
   (`DM_DIRECT_OBSERVABLE_NOTE.md` and `scripts/frontier_dm_sigma_v_lattice.py`).
   The branch-safe claim is the functional form and direct-observable surface,
   not a declaration that the entire numerator chain is now exact.
4. The thermodynamic limit (lattice master equation -> Boltzmann equation)
   is strengthened on the current branch but still needs honest handling at
   theorem grade for the full relic bridge.

---

## Provenance Chain

The detailed chain below should be read as a numerator-side provenance sketch.
It no longer defines the branch headline claim boundary by itself: the live
paper surface is now `R(eta)`, not "scan a free `g_bare` window."

```
R = (3/5) * (f_vis / f_dark) * S_vis

             NATIVE quantities (7)
             ----------------------
             mass ratio 3/5          Hamming weights on Cl(3) bit strings
             C_F(SU3) = 4/3          group theory of lattice gauge group
             dim_adj(SU3) = 8        group theory
             C2(SU2) = 3/4           group theory
             dim_adj(SU2) = 3        group theory
             g_* = 106.75            taste spectrum: 28 bosonic + 7/8 * 90 fermionic
             n_eq ~ exp(-m/T)        heat kernel on lattice

             DERIVED equations (5)
             ----------------------
             Boltzmann equation       thermodynamic limit of lattice master eq.
             Friedmann equation       Poisson coupling + spectral energy density
             H > 0                    spectral gap + 2nd law (boundary condition)
             x_F = 25                 lattice Boltzmann, Gamma_ann = H condition
             Sommerfeld formula       Schrodinger eq. / lattice Green's fn ratio

             NORMALIZATION boundary (1)
             --------------------------
             canonical gauge basis      see G_BARE_RIGIDITY_THEOREM_NOTE.md;
                                        do not headline this as a free scan

             DIRECT-OBSERVABLE / BRIDGE ITEMS (2)
             ------------------------------------
             sigma_v = C*alpha^2/m^2   lattice T-matrix + optical theorem
             V(r) = -C_F*g^2*G(r)      lattice Green's function / Poisson
```

### Provenance flow diagram

```
  Cl(3) bit strings -----> mass ratio 3/5 --------\
                                                     \
  SU(3) x SU(2) --------> Casimir channels --------+--> R_base = 31/9
  (lattice gauge group)    f_vis, f_dark            /
                                                   /
  Plaquette action -------> alpha_plaq = 0.092 ---+
       |                        |                  |
  [ASSUMED: g_bare=1]           |                  |
                                v                  |
  [DERIVED: V(r)=-C_F*g^2*G(r)] --> Coulomb Sommerfeld --> S_vis = 1.592
                                ^                  |
  Taste spectrum --> g_*=106.75 |                  |
  Heat kernel ----> n_eq        |                  |
  Lattice master eq ----------> Boltzmann eq.      |
  Poisson coupling -----------> Friedmann eq.      |
  Spectral gap + 2nd law -----> H > 0              |
       |                        |                  |
       +-----> x_F = 25 -------+                  |
       |                                           |
  [DIRECT-OBSERVABLE: sigma_v=C*a^2/m^2] (enters x_F)  |
                                                   v
                                            R = R_base * S_vis = 5.48
```

---

## What Is Actually Proved

1. The structural backbone R_base = (3/5) * (155/27) = 31/9 = 3.444
   is NATIVE: it follows from Hamming weights and Casimir algebra
   with zero free parameters.

2. The Sommerfeld enhancement S_vis = 1.592 is DERIVED from the
   Coulomb scattering formula (lattice Green's function ratio).
   On the current branch, the Coulomb potential shape is no longer best
   described as an imported ingredient; the remaining honest dependency is
   the framework-defining `g_bare = 1` surface.

3. The freeze-out temperature x_F = 25 is DERIVED from the lattice
   Boltzmann equation. On the current branch, `sigma_v` has a native
   direct-observable route via the lattice T-matrix / optical theorem, but
   the finite-lattice / Born-level / thermodynamic-limit boundary still needs
   to be carried honestly. The result remains log-insensitive to `sigma_v`
   (changing it by 2x shifts x_F by ~2 units, shifting R by ~1%).

4. g_* = 106.75 is NATIVE from the taste spectrum decomposition.

5. The Boltzmann and Friedmann equations are DERIVED from lattice
   quantities in the thermodynamic limit (demonstrated numerically,
   not proved as a theorem). This is a genuine advance over importing
   them as external cosmological machinery.

---

## Claim Boundary and Historical `g` Window

The old `g in [0.9, 1.1]` scan should now be treated as historical bounded
support, not as the main paper surface. If the gauge-normalization rigidity
theorem lands, the sharper uncertainty boundary is no longer "what free
`g_bare` should we scan?" but "is `eta` derived or observed?"

The safer presentation is therefore:

- primary claim: `R(eta)` is structurally determined by the framework
- numerical consistency check: `R(eta_obs) = 5.48`
- historical support only: earlier `g_bare` window studies showed the
  observation sat inside a natural-coupling band, but that is no longer the
  preferred branch framing

---

## What Remains Open

### To upgrade from BOUNDED to CLOSED:

1. **Integrate or refute the gauge-normalization rigidity theorem.**
   If that theorem stands, stop presenting `g_bare` as the paper's free
   parameter. If it fails, the branch still needs a theorem-grade response
   to the normalization objection.

2. **Tighten the direct-observable `sigma_v` route into one final authority
   surface.**
   The branch now has a lattice T-matrix / optical-theorem route, but the
   claim boundary still needs to be normalized across note, script, and
   downstream summary surfaces.

3. **Derive `eta`, or narrow the flagship claim explicitly to `R(eta)`.**
   The denominator is now the honest live blocker. If denominator work
   resumes, the current harsh route ranking is:
   - publication fallback: `R(eta)`
   - research mainline: leptogenesis via the taste staircase
   - sharpest sub-blocker inside leptogenesis: derive the absolute
     staircase-embedding law for the now-selected self-dual point on the
     already background-normalized Majorana response curve; the retained local Dirac lane now selects
     `y_nu^eff = g_weak^2 / 64` and points sharply to `k_B ~= 8`, the
     right-handed-neutrino source direction and local response normalization
     are both now exact, the local finite-point selector is now exact on the
     admitted block, the current atlas observable-principle toolkit is
     explicitly exhausted as a rescue path, but the admitted local response
     curve is still monotone and the full `Z_3` texture is not yet
     axiom-closed
   - not promotable at theorem bar: current EWBG / transport

4. **Prove the thermodynamic limit.**
   Show rigorously that the lattice master equation converges to the
   Boltzmann equation as N -> infinity for the Z^3 graph family.
   This, together with the radiation-era expansion bridge, is now the
   remaining numerator-side wording problem.

Not all of these are achieved on one final theorem-grade surface yet.

---

## How This Changes The Paper

### Replace the current DM section claim with:

> The Cl(3) lattice structure fixes the dark-sector freeze-out structure and
> therefore determines the dark-to-baryon ratio as a function of the baryon
> asymmetry `eta`. The structural factors (mass ratio `3/5`, Casimir channel
> ratio `155/27`, lattice-native Coulomb enhancement, and the strengthened
> direct-observable annihilation route) are fixed by the framework. At the
> observed baryon asymmetry `eta = 6.12e-10`, the framework gives
> `R = Omega_DM / Omega_b = 5.48`, matching the observed `R = 5.47` to 0.2%.
>
> The overall relic bridge remains bounded because the full numerical claim
> still depends on the baryon asymmetry `eta`, which is not yet uniquely
> derived from the framework.

### Do NOT write:

- "DM relic abundance derived from the lattice axioms alone"
- "Zero-parameter prediction"
- "The paper headline is a one-parameter `g_bare` scan"
- "Zero imported cosmological equations" (the synthesis script line 674
  overclaims this)
- "Gate closed" for this lane

---

## Commands Run

```
python scripts/frontier_dm_relic_paper.py
```

---

## Scripts and Notes Reviewed

- `scripts/frontier_dm_relic_mapping.py` (G_BARE = 1.0 at line 86)
- `scripts/frontier_dm_relic_gap_closure.py` (G_BARE = 1.0 at line 89)
- `scripts/frontier_dm_relic_synthesis.py` (G_BARE = 1.0 at line 88)
- `docs/CODEX_DM_RESPONSE.md` (provenance chain and objection verdicts)
- `review.md` (Codex audited state)
