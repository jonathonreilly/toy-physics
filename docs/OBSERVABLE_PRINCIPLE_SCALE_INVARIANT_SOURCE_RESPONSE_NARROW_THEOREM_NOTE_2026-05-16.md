# Observable Principle — Scale-Invariant Source-Response Narrow Theorem

**Date:** 2026-05-16
**Type:** bounded_theorem
**Claim type:** bounded_theorem
**Claim scope:** the standalone algebraic statement that, on the
admissibility class (X2) and invertible real anti-Hermitian Dirac block
hypotheses of
[`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md),
the residual overall-scale degree of freedom `c` in the block-local
uniqueness conclusion `F[J] = c · W[J]` (with
`W[J] := log |det(D + J)| − log |det D|`) is the only free parameter
remaining, and it cancels exactly in every normalized source-response
observable formed from ratios or relative differences of source
derivatives. As an algebraic corollary, the choice `c = 1` is a
convention with no propagation to physical source-response content.
This is a purely algebraic statement on the admissibility class (X2)
of the cited retained narrow theorem; no new physical-principle premise
is admitted, no new framework axiom is introduced, and the audit-status
of the broad parent `observable_principle_from_axiom_note` is not
promoted by this note.

**Status authority:** independent audit lane only. This source note does
not set or predict an audit outcome; later status is generated by the
audit pipeline after independent review.

**Runner:** [`scripts/audit_companion_observable_principle_scale_invariant_source_response_exact_2026_05_16.py`](../scripts/audit_companion_observable_principle_scale_invariant_source_response_exact_2026_05_16.py)

**Authority role:** Pattern A narrow bridge derivation closing the
"P1 scalar additivity + c=1 source-normalization" repair target named
in the live audit ledger for `observable_principle_from_axiom_note`
(see `notes_for_re_audit_if_any`). The P1 (scalar additivity) input
is supplied by the retained-bounded admissibility class (X2) of
[`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md);
this note closes the c=1 source-normalization step by showing it has no
physical content on the admissibility class.

## Statement

Let `D` be an invertible real anti-Hermitian operator on a finite-dimensional
complex Hilbert space `V = C^N` (with `N` even), let `U_0 ⊂ Sym_N(R)` be
an open neighborhood of `J = 0` on which `det(D + J) ≠ 0`, and let
the admissibility class (X2) and the candidate generator

```text
W[J]  :=  log |det(D + J)|  −  log |det D|                              (W)
```

be as in
[`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md).
By the retained block-local uniqueness conclusion (U1) of that note,
every admissible `F` in class (X2) equals `c · W` on `U_0` for some
real constant `c`. The present narrow theorem isolates the algebraic
content of that residual scale `c`.

**Definition (normalized source-response observables).** For any
admissible `F` with `F[J] = c · W[J]` (`c ∈ R`) and any pair of
sites `x, y, z, w ∈ Λ` with `∂² W / ∂J_x ∂J_y |_{J = 0} ≠ 0`, define
the normalized source-response observable

```text
R^{(F)}_{xy ; zw}  :=  ( ∂² F / ∂J_z ∂J_w |_{J = 0} )
                       /
                       ( ∂² F / ∂J_x ∂J_y |_{J = 0} ).                  (R)
```

When the normalizing denominator at `(x, y)` is nonzero, the
normalized source-response is a well-defined real number that compares
the second-order curvature at `(z, w)` to a chosen reference pair
`(x, y)`. (For a single-site source the reduction is the same; the
two-source presentation is for generality.)

### Conclusion

Under the hypotheses of
[`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md),
for every pair of admissible generators `F_1, F_2` in class (X2) with
`F_i[J] = c_i · W[J]` (`c_1, c_2 ∈ R`, `c_1, c_2 ≠ 0`), and every
quadruple of sites `(x, y, z, w)` with `∂² W / ∂J_x ∂J_y |_{J = 0} ≠ 0`:

**(S1) Source-response ratio invariance.**

```text
R^{(F_1)}_{xy ; zw}  =  R^{(F_2)}_{xy ; zw}
                    =  R^{(W)}_{xy ; zw}.                               (S1)
```

That is, the normalized second-order source-response observable is
identical for every admissible `F` and equals the value computed from
the `c = 1` representative `W`. The overall scale `c` cancels exactly.

**(S2) Source-response sign invariance.** When `c_1, c_2 > 0`,

```text
sign( ∂² F_1 / ∂J_x ∂J_y |_{J=0} )
   =  sign( ∂² F_2 / ∂J_x ∂J_y |_{J=0} )
   =  sign( ∂² W / ∂J_x ∂J_y |_{J=0} ).                                 (S2)
```

When `c_1 > 0` and `c_2 < 0`, the signs flip but the absolute values
scale exactly by `|c_2 / c_1|`; the source-response ratios `R` are
unaffected by either rescaling.

**(S3) c = 1 is conventional.** The unique choice `c = 1` is the
representative for which `F = W` itself, i.e. the natural-log baseline
on the determinant ratio `r(J) = |det(D + J)| / |det D|`. Under (S1)
this choice has no propagation to any normalized source-response
observable on the admissibility class (X2); under (S2) it fixes the
overall sign positively. The choice is a unit convention on the scalar
generator, in the same sense that `log r` and `α · log r` (`α > 0`)
give identical normalized source-response content. No physical
selection premise beyond admissibility class (X2) is consumed by
fixing `c = 1`.

## Proof

Pure algebraic substitution closed by linearity of differentiation.

**Step 1 — uniqueness up to scale on the block.** By the retained
block-local uniqueness conclusion (U1) of
[`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md),
for every admissible `F` in class (X2) there exists a real constant
`c` such that `F[J] = c · W[J]` on `U_0`. This is the live input; the
present note does not re-derive it.

**Step 2 — linearity of differentiation through the scalar.** For
any twice-differentiable `W` and any constant `c`, the elementary
calculus identity

```text
∂² (c · W) / ∂J_z ∂J_w  =  c · ( ∂² W / ∂J_z ∂J_w )                     (L)
```

holds pointwise on `U_0`, in particular at `J = 0`. This is the
linearity of the differential operator `∂² / ∂J_z ∂J_w` over the real
scalar field.

**Step 3 — cancellation of `c` in the ratio.** Substituting Step 1
into the normalized source-response (R):

```text
R^{(F)}_{xy ; zw}
  =  ( ∂² F / ∂J_z ∂J_w |_{J = 0} )
     /
     ( ∂² F / ∂J_x ∂J_y |_{J = 0} )
  =  ( c · ∂² W / ∂J_z ∂J_w |_{J = 0} )
     /
     ( c · ∂² W / ∂J_x ∂J_y |_{J = 0} )                                 (use L)
  =  ( ∂² W / ∂J_z ∂J_w |_{J = 0} )
     /
     ( ∂² W / ∂J_x ∂J_y |_{J = 0} )                                     (c ≠ 0)
  =  R^{(W)}_{xy ; zw}.                                                 (R for W)
```

The denominator is nonzero by hypothesis (`∂² W / ∂J_x ∂J_y |_{J=0} ≠ 0`
and `c ≠ 0`), so the cancellation is legal. Applying the same
substitution with `F = F_2`, `c = c_2` proves (S1).

**Step 4 — sign of `∂² F / ∂J ∂J` at `J = 0`.** By Step 2 with
`F = c · W`, the second derivative at `J = 0` is
`c · ∂² W / ∂J_x ∂J_y |_{J = 0}`. The sign is `sign(c) · sign(∂² W
/ ∂J_x ∂J_y |_{J = 0})`, which gives (S2).

**Step 5 — conventional status of `c = 1`.** The choice `c = 1`
selects the admissible representative `F = W` itself. By (S1), no
normalized source-response observable distinguishes this choice from
`c = α > 0` (which gives `F = α · W`). By (S2), the sign is fixed
positive for `c = 1`. No further physical content is determined by
the choice. Therefore `c = 1` is a unit convention on the scalar
generator, in the same algebraic sense that "natural log" vs
"log base 10" is a unit convention on the logarithm (they differ by
a positive constant prefactor that cancels in ratios). ∎

## Derivable corollaries

**(D1) Higher-order ratios cancel `c` as well.** For any
multi-index `(x_1, ..., x_k)` with the all-order partial derivative
`∂^k W / ∂J_{x_1} ... ∂J_{x_k} |_{J = 0}` well-defined (real-analytic
by Step 3 of the retained narrow theorem) and any reference
multi-index `(y_1, ..., y_k)` with the corresponding partial
derivative nonzero,

```text
( ∂^k F / ∂J_{x_1} ... ∂J_{x_k} |_{J = 0} )
    /
( ∂^k F / ∂J_{y_1} ... ∂J_{y_k} |_{J = 0} )
  =  ( ∂^k W / ∂J_{x_1} ... ∂J_{x_k} |_{J = 0} )
     /
     ( ∂^k W / ∂J_{y_1} ... ∂J_{y_k} |_{J = 0} ).                       (D1)
```

The proof is identical: a factor of `c^k = c · ... · c` appears in
numerator and denominator and cancels. (Strictly, by linearity of the
k-th total derivative, `∂^k (c W) = c · ∂^k W` for any constant `c`
and any twice-or-higher differentiable `W`.)

**(D2) Additive constant in `F` is invisible to all source
derivatives.** Adding an arbitrary additive constant `k` to `F`
(`F̃ := F + k`) leaves all source derivatives at any order unchanged:
`∂ F̃ / ∂J_x = ∂ F / ∂J_x` exactly. This recovers the §"Runner-local
derivation of P4" content of the broad parent
[`OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md`](OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md)
inside the present narrow scope: the zero-source-baseline subtraction
in (W) is a normalization convention that does not propagate to
observables. Combined with (D1), both the overall scale `c` and the
additive constant `k` are conventions; only the source-response shape
(ratios at every order) is physical content.

## What this claims

- (S1) The normalized second-order source-response observable is
  identical for every admissible `F` in class (X2) and equals the
  `c = 1` value computed from `W`.
- (S2) The source-response sign is fixed once `c > 0` is chosen;
  reversing `sign(c)` flips all source derivatives but not their
  ratios.
- (S3) The choice `c = 1` is a unit convention with no
  observable-distinguishing content on class (X2).
- (D1) Higher-order source-response ratios also cancel `c`.
- (D2) The additive constant in `W` is invisible to all source
  derivatives.

## What this does NOT claim

- Does **not** derive the admissibility class (X2) of
  [`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md).
  The (A) block-additive criterion (the P1 scalar-additivity content),
  the (C) CPT-even determinant-ratio criterion, the (R) continuity
  criterion, and the (N) zero-source normalization criterion are all
  imported as the admissibility class of the cited retained narrow
  theorem; they remain selection premises on the broad parent
  [`OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md`](OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md).
- Does **not** derive the block-local uniqueness conclusion (U1) of
  [`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md).
  That uniqueness up to scale `c` is the live retained input.
- Does **not** promote the audit status of
  `observable_principle_from_axiom_note`. That row's `effective_status`
  remains whatever the live ledger records and updates only on next
  audit pass; this narrow theorem is a separate, narrower row.
- Does **not** claim absolute scale calibration of `W` against any
  external observable. The normalization `c = 1` is a unit convention,
  not an empirical calibration. No measurement comparator is consumed.
- Does **not** identify `D` with any specific physical Hamiltonian or
  consume any numerical comparator. No PDG values, no fitted constants,
  no admitted unit conventions enter the load-bearing claim beyond
  those already imported in the cited retained narrow theorem.
- Does **not** extend beyond the admissibility class (X2). A
  generator outside class (X2) (e.g., a non-additive functional of
  `Z[J]`, or a phase-sensitive observable on a non-real-D block) is
  outside scope.

## Relation to the missing-bridge repair target

The live audit ledger row for `observable_principle_from_axiom_note`
records under `notes_for_re_audit_if_any`:

> missing_bridge_theorem — derive P1 scalar additivity and the
> c=1/source-normalization bridge from retained primitives, then
> rerun an audit with the required direct dependencies wired.

This narrow theorem addresses the named repair target in two parts:

1. **P1 scalar additivity (block-additive on independent direct
   sums).** Imported as the (A) criterion of the admissibility class
   (X2) of the retained-bounded narrow theorem
   [`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md).
   That retained narrow theorem packages the block-additivity hypothesis
   as part of an explicit class on the real-D block, paired with the
   Cauchy–Erdős multiplicative-to-additive closure to give uniqueness
   up to scale `c`. The P1 content is therefore retained at the
   admissibility-class level of the cited narrow theorem.

2. **c=1 / source-normalization bridge.** Closed by the present
   narrow theorem: the algebraic content of the residual scale `c`
   on the admissibility class (X2) is that it is a unit convention
   with no propagation to normalized source-response observables.
   The c=1 choice is the natural-log representative; it is the choice
   for which `F = W` itself, and it is observationally indistinguishable
   from `F = α W` for any `α > 0` on the admissibility class via
   (S1) and (D1).

Together with the live retained inputs
[`CPT_EXACT_REAL_ANTI_HERMITIAN_D_NARROW_THEOREM_NOTE_2026-05-10.md`](CPT_EXACT_REAL_ANTI_HERMITIAN_D_NARROW_THEOREM_NOTE_2026-05-10.md)
(real-D structural fact) and
[`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md)
(block-local uniqueness up to scale on the admissibility class), the
chain from retained primitives to the axiom-to-observable map is

```text
retained CPT_EXACT real-D structural fact
   →  retained OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS narrow theorem
       (admissibility class (X2) including (A) block-additive,
        Cauchy–Erdős closure → uniqueness up to scale c)
   →  the present narrow theorem
       (c is conventional under (X2); c = 1 is the natural-log unit choice)
   →  axiom-to-observable map content of the broad parent
       OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE retained at narrow level.
```

The audit lane decides whether this chain re-audits cleanly under the
narrow-theorem framing. This source note does not predict that
verdict.

## Hypothesis set used

- (X1) Invertible real anti-Hermitian Dirac block — imported from
  [`CPT_EXACT_REAL_ANTI_HERMITIAN_D_NARROW_THEOREM_NOTE_2026-05-10.md`](CPT_EXACT_REAL_ANTI_HERMITIAN_D_NARROW_THEOREM_NOTE_2026-05-10.md)
  via the cited retained narrow theorem.
- (X2) Admissibility class on the real-D block — imported from
  [`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md).
- (U1) Block-local uniqueness up to overall scale `c` — imported from
  the same retained narrow theorem.
- Elementary linearity of differentiation `∂^k (c · W) = c · ∂^k W`
  for constants `c` and twice-or-higher differentiable `W` (standard
  one-variable calculus).
- Real-analyticity of `W` on `U_0` (Step 3 of the cited retained
  narrow theorem) — used only to guarantee the partial derivatives
  exist at `J = 0`.

No fitted parameters. No observed values. No physics conventions
admitted beyond the retained inputs above plus elementary calculus.

## Audit boundary

This note should seed as `bounded_theorem`. It does not write an audit
verdict, an effective status, or a retained-grade closure claim. The
independent audit lane must decide whether the now-available retained
upstream (X1) + retained block-local uniqueness narrow theorem + this
narrow scale-invariance closure together close the
`observable_principle_from_axiom_note` "missing_bridge_theorem"
repair target cleanly on the next audit pass.

## Cited dependencies

- [`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md)
  — supplies (X1) real-D structural fact via further-upstream import,
  (X2) admissibility class on the real-D block, and (U1) block-local
  uniqueness up to overall scale `c`. Live effective status at the
  time of writing: `retained_bounded` (audit_status `audited_clean`).

Context-only references (not load-bearing dependencies of this note;
named by `claim_id` in backticks rather than via markdown link, so the
citation graph does not register them as outgoing edges from this
narrow theorem):

- `observable_principle_from_axiom_note` — broad parent whose
  `notes_for_re_audit_if_any` names this repair target. Its audit
  verdict (`audited_conditional`) is unaffected by this row.
- `cpt_exact_real_anti_hermitian_d_narrow_theorem_note_2026-05-10`
  — further-upstream retained narrow theorem supplying the real-D
  structural fact, inherited through the markdown-linked dependency
  above.

## Forbidden imports check

- No PDG observed values consumed.
- No literature numerical comparators consumed.
- No fitted selectors consumed.
- No admitted unit conventions load-bearing on the claim (`c = 1` is
  shown to be observationally vacuous on the admissibility class, not
  admitted as a load-bearing premise).
- No same-surface family arguments.

## Validation

Primary runner:
[`scripts/audit_companion_observable_principle_scale_invariant_source_response_exact_2026_05_16.py`](../scripts/audit_companion_observable_principle_scale_invariant_source_response_exact_2026_05_16.py)
verifies at exact sympy precision on the same test blocks used by the
cited retained narrow theorem's runner:

1. (X1) is in effect on the test blocks: `D` real, anti-Hermitian,
   invertible on the source neighborhood (re-verified locally for
   self-containment of the runner).
2. (S1) ratio invariance: for the candidate `F = c · W` with several
   values of `c` (including `c = 1, 2, -1, 1/3`), the normalized
   second-order source-response ratio at any two test pairs of source
   sites is exactly identical to the `c = 1` value, at exact sympy
   precision.
3. (S2) sign behavior: for `c > 0`, the second-derivative sign at
   `J = 0` matches `W`; for `c < 0`, it flips; the ratio is
   unaffected in both cases.
4. (S3) the `c = 1` choice is the `F = W` representative, and the
   runner exhibits explicitly that `F[J] − W[J] = 0` for that choice
   on the test instance.
5. (D1) higher-order (3rd, 4th order on a small test instance)
   source-response ratios also cancel `c` exactly.
6. (D2) additive-constant invariance: shifting `F → F + k` for
   `k` a constant leaves every source derivative at every checked
   order identical to the unshifted value.
7. Counterfactual: a non-admissible perturbation
   `F[J] = W[J] + ε · J_{x} · J_{y}` (with `ε ≠ 0`, a local
   source-coupling on the reference pair `(x, y)` that is NOT a
   function of the determinant ratio `r(J) = |det(D + J)| / |det D|`)
   is shown to lie outside class (X2) (it fails (C), the CPT-even
   determinant-ratio dependence criterion) and to change the
   normalized source-response ratio, confirming class (X2) is
   load-bearing for (S1).

Expected scorecard: all assertions PASS at exact sympy precision on
the test blocks; the counterfactual is exhibited as a non-admissible
perturbation that violates (C) and thereby leaves class (X2).

## Cross-references

Load-bearing dependency (markdown-linked, picked up by the citation
graph and verified retained on the live ledger at the time of writing):

- [`OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md`](OBSERVABLE_PRINCIPLE_REAL_D_BLOCK_UNIQUENESS_NARROW_THEOREM_NOTE_2026-05-10.md)
  — retained-bounded upstream supplying (X1), (X2), and (U1).

Context-only references (named by `claim_id` in backticks; not
registered as outgoing edges in the citation graph):

- `observable_principle_from_axiom_note` — broad parent whose
  `notes_for_re_audit_if_any` names the repair target this narrow
  theorem addresses.
- `cpt_exact_real_anti_hermitian_d_narrow_theorem_note_2026-05-10`
  — further-upstream retained narrow theorem, inherited through the
  markdown-linked dependency above.
