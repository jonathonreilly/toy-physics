# alpha_s Derived Narrow Theorem (Tadpole-Power Coupling-Chain Identity)

**Date:** 2026-05-10
**Type:** positive_theorem
**Claim scope:** the standalone algebraic-substitution implication that, given
(i) the retained Combined Mean-Field Theory (CMT) change-of-variables
identity `<O(U)> = u_0^{n_link} <O_V(V)>_eff` supplied by
[`YT_EW_COLOR_PROJECTION_THEOREM.md`](YT_EW_COLOR_PROJECTION_THEOREM.md)
(D14, retained `yt_ew_color_projection_theorem`, audited_clean /
retained_bounded), (ii) the retained `n_link = 1` per single-vertex
gauge-coupling identity (D15, same retained authority), and (iii) the
retained `n_link = 2` per vacuum-polarization identity (D15, same retained
authority), the two canonical-surface coupling definitions

```text
alpha_LM    :=  alpha_bare / u_0,                                      (D1)
alpha_s(v)  :=  alpha_bare / u_0^2,                                    (D2)
```

algebraically satisfy the exact coupling-chain identity

```text
alpha_LM^2  =  alpha_bare * alpha_s(v),                                (P1)
```

and the exact tadpole-ratio identity

```text
alpha_s(v) / alpha_LM  =  1 / u_0.                                     (P2)
```

This is purely a statement about algebraic substitution in the tadpole
powers; no `u_0` value, no plaquette analytic evaluation, no low-energy
running bridge, no Wilson-loop static potential measurement, and no
Standard-Model identification enters. The narrow theorem is the
load-bearing class-(A) algebra at the heart of the `alpha_s_derived_note`
chain that survives audit independently of the upstream `u_0` evaluation
and the downstream `M_Z` running bridge.
**Status authority:** independent audit lane only. This source note does
not set or predict an audit outcome; later status is generated by the
audit pipeline after independent review.
**Primary runner:** [`scripts/audit_companion_alpha_s_tadpole_power_identity.py`](./../scripts/audit_companion_alpha_s_tadpole_power_identity.py)
**Authority role:** Pattern A narrow rescope of the load-bearing algebraic
core of the parent `alpha_s_derived_note` row. The broad parent additionally
bundles the upstream `u_0` plaquette evaluation (currently `unaudited` on
`plaquette_self_consistency_note`), the downstream running bridge
(`qcd_low_energy_running_bridge_note_2026-05-01`, currently `unaudited`),
and the numerical `alpha_s(M_Z) = 0.1181` readout. This narrow theorem
isolates only the exact algebraic tadpole-power substitution.

## Statement

Let `alpha_bare, u_0` be abstract positive real symbols. Suppose the
three retained input identities

```text
CMT(O,n)    :  <O(U)>  =  u_0^n * <O_V(V)>_eff,                        (I1)
n_link(g)   :  n_link = 1  for the single-vertex gauge coupling,       (I2)
n_link(vp)  :  n_link = 2  for the vacuum-polarization correlator,     (I3)
```

are supplied as cited inputs from the retained
`yt_ew_color_projection_theorem` D14-D15 chain (audited_clean /
retained_bounded, per `docs/audit/data/audit_ledger.json` at the date of
this note). Define the two canonical-surface couplings

```text
alpha_LM    :=  alpha_bare / u_0,                                      (D1)
alpha_s(v)  :=  alpha_bare / u_0^2.                                    (D2)
```

`(D1)` is the CMT-rescaled coupling at `n_link = 1`; `(D2)` is the
CMT-rescaled coupling at `n_link = 2`. Both definitions consume only the
cited retained CMT identity `(I1)` plus the cited `n_link` identities
`(I2)`, `(I3)`. Then the two algebraic identities

```text
alpha_LM^2          =  alpha_bare * alpha_s(v),                        (P1)
alpha_s(v) / alpha_LM  =  1 / u_0,                                     (P2)
```

hold exactly as identities in the polynomial ring in
`(alpha_bare, u_0, 1/u_0)`.

## Proof

Pure algebraic substitution.

`(P1)`. From `(D1)` and `(D2)`:

```text
alpha_LM^2  =  (alpha_bare / u_0)^2
            =  alpha_bare^2 / u_0^2
            =  alpha_bare * (alpha_bare / u_0^2)
            =  alpha_bare * alpha_s(v).
```

`(P2)`. From `(D1)` and `(D2)`:

```text
alpha_s(v) / alpha_LM  =  (alpha_bare / u_0^2) / (alpha_bare / u_0)
                       =  (alpha_bare / u_0^2) * (u_0 / alpha_bare)
                       =  u_0 / u_0^2
                       =  1 / u_0.
```

∎

## Derivable corollaries

```text
alpha_s(v)^2 / alpha_LM^4         =  1 / alpha_bare^2
                                       (pure tadpole-power consequence)
alpha_LM / alpha_bare              =  1 / u_0     (= sqrt(alpha_s(v) / alpha_bare))
alpha_s(v) / alpha_bare            =  1 / u_0^2   (D2 restated as ratio)
alpha_LM^2 / alpha_s(v)            =  alpha_bare   (P1 rearranged)
```

All follow algebraically from `(D1)` and `(D2)`.

## What this claims

- The exact algebraic coupling-chain identity `(P1)` and tadpole-ratio
  identity `(P2)`, **conditional on** the three cited retained CMT
  inputs `(I1)`-`(I3)`.
- Four derivable corollary identities that follow algebraically.

## What this does NOT claim

- Does **not** derive the CMT identity `(I1)`. That is the retained
  `yt_ew_color_projection_theorem` D14 / CMT change-of-variables
  identity (audited_clean / retained_bounded).
- Does **not** derive the `n_link` counts `(I2)`-`(I3)`. Those are
  retained content in the same authority (D15).
- Does **not** supply a value of `u_0`. The plaquette evaluation
  `u_0 = <P>^{1/4}` from `plaquette_self_consistency_note` is currently
  `unaudited`, and the analytic `beta = 6` insertion of that note is
  open work; the narrow theorem is independent of any specific `u_0`
  value, only of the abstract positive real symbol.
- Does **not** supply a value of `alpha_s(M_Z)`. The downstream
  low-energy running bridge in
  `qcd_low_energy_running_bridge_note_2026-05-01` (currently `unaudited`)
  is required to transfer the `v` readout to the `M_Z` scale; the narrow
  theorem is independent of any running step.
- Does **not** identify the framework's `alpha_s(v)` with the
  Standard-Model strong coupling observable. The PDG comparator
  `alpha_s(M_Z) = 0.1179 +- 0.0010` and the comparison `+0.2%` deviation
  are explicitly downstream and do not appear in the narrow note.
- Does **not** consume any PDG observed value, literature numerical
  comparator, fitted selector, or admitted unit convention.
- Does **not** claim the upstream plaquette analytic insertion is closed;
  it remains explicitly open work on `plaquette_self_consistency_note`.

## Relation to the parent `alpha_s_derived_note` row

The older broad alpha_s row bundles the algebraic substitution above with
several distinct downstream items:

1. The retained CMT identities `(I1)`-`(I3)` themselves, supplied by
   `yt_ew_color_projection_theorem`.
2. The upstream `u_0` plaquette evaluation `u_0 = <P>^{1/4}` with
   `<P> = 0.5934` from `plaquette_self_consistency_note` (currently
   `unaudited`; analytic `beta = 6` insertion explicitly open work).
3. The downstream `v -> M_Z` running bridge in
   `qcd_low_energy_running_bridge_note_2026-05-01` (currently `unaudited`,
   imports standard SM 2-loop RGE plus PDG quark-mass threshold matching).
4. The numerical `alpha_s(v) = 0.1033` and `alpha_s(M_Z) = 0.1181`
   readouts.
5. The pure algebraic tadpole-power substitution `(P1)`, `(P2)` and
   corollaries.
6. (Out-of-scope of this narrow note) Identification with the PDG
   `alpha_s(M_Z) = 0.1179` observed value.

This narrow theorem isolates item 5 from items 2-4 and 6. The
substitution is reviewable independently of the upstream plaquette
evaluation, the downstream running bridge, the numerical readouts, and
the PDG identification.

## Cited dependencies

- [`YT_EW_COLOR_PROJECTION_THEOREM.md`](YT_EW_COLOR_PROJECTION_THEOREM.md)
  — retained CMT change-of-variables identity D14 (`(I1)`) and `n_link`
  identities D15 (`(I2)`, `(I3)`). Retained authority
  (`yt_ew_color_projection_theorem`, effective_status=retained_bounded,
  audit_status=audited_clean per `docs/audit/data/audit_ledger.json` row
  at the date of this note).

## Forbidden imports check

- No PDG observed values consumed.
- No literature numerical comparators consumed.
- No fitted selectors consumed.
- No admitted unit conventions load-bearing on the claim (the canonical
  surface choice is upstream of this note's input identities, not part of
  the narrow substitution).
- No same-surface family arguments.

## Validation

Primary runner:
[`scripts/audit_companion_alpha_s_tadpole_power_identity.py`](./../scripts/audit_companion_alpha_s_tadpole_power_identity.py)
verifies symbolically (sympy `simplify` to 0) that:

1. `(P1)`: `alpha_LM^2 - alpha_bare * alpha_s(v)` reduces to 0 under `(D1)`,
   `(D2)` parametrically in `(alpha_bare, u_0)`.
2. `(P2)`: `alpha_s(v) / alpha_LM - 1/u_0` reduces to 0 under `(D1)`, `(D2)`
   parametrically.
3. Four corollary identities reduce to 0 parametrically.
4. The `(P1)` LHS retains the expected polynomial form
   `alpha_bare^2 / u_0^2` after `simplify`, confirming the algebraic
   pathway.
5. The `(P2)` LHS retains the expected `1/u_0` after `simplify`,
   independent of `alpha_bare`.
6. The free-symbol set of the LHS - RHS of `(P1)` is empty after
   substitution (no residual dependence).
7. Numerical sanity at one independent random sample
   `(alpha_bare, u_0) = (0.23, 0.876)`: confirms `(P1)` to machine
   precision (this is an independent FP-cross-check; the algebraic
   identity is the load-bearing content).
8. Counterfactual probe: at `n_link = 0` for the vacuum polarization
   (instead of 2), `(P1)` fails parametrically; at `n_link = 1` for
   both, `(P1)` reduces to `alpha_bare = u_0`, which is a non-identity
   on `(alpha_bare, u_0)`, confirming the `n_link = (1, 2)` split is
   load-bearing.

## Cross-references

- `alpha_s_derived_note` (parent broad note that bundles the algebraic
  substitution above with the upstream `u_0` evaluation, downstream
  running bridge, numerical readouts, and PDG identification; currently
  `unaudited`).
- `yt_ew_color_projection_theorem` (retained source of the CMT
  identities and `n_link` counts; `audited_clean`).
- `plaquette_self_consistency_note` (upstream plaquette `u_0` evaluation;
  currently `unaudited`; not consumed by this narrow theorem).
- `qcd_low_energy_running_bridge_note_2026-05-01` (downstream running
  bridge; currently `unaudited`; not consumed by this narrow theorem).
- `alpha_s_direct_wilson_loop_derivation_theorem_note_2026-04-30`
  (sibling alpha_s authority via Wilson-loop static potential;
  `audited_conditional`; not consumed by this narrow theorem).
