# PR230 Taste-Radial Canonical `O_H` Selector Gate

**Date:** 2026-05-06
**Status:** exact-support / degree-one taste-radial uniqueness with exact
negative boundary for symmetry-only canonical `O_H` selection
**Claim type:** support_boundary
**Runner:** `scripts/frontier_yt_pr230_taste_radial_canonical_oh_selector_gate.py`
**Certificate:** `outputs/yt_pr230_taste_radial_canonical_oh_selector_gate_2026-05-06.json`

```yaml
actual_current_surface_status: exact-support / degree-one taste-radial uniqueness with exact negative boundary for symmetry-only canonical O_H selection
conditional_surface_status: conditional-support if a same-surface EW/Higgs action or canonical operator theorem proves canonical O_H is the degree-one radial fluctuation in span{S0,S1,S2}
hypothetical_axiom_status: null
admitted_observation_status: null
proposal_allowed: false
bare_retained_allowed: false
```

## Purpose

The two-source taste-radial chart and action certificates now give PR230 a
real same-surface second source axis.  The remaining non-chunk question is
whether that source can be identified with canonical `O_H` by the current
`Cl(3)/Z^3` taste symmetries alone.

This gate separates the part that is exact from the part that remains missing.

## Exact Support

Let

```text
S0 = sigma_x I I
S1 = I sigma_x I
S2 = I I sigma_x
```

on `C^8 = (C^2)^{otimes 3}`.  Inside the degree-one source-axis space
`span{S0,S1,S2}`, the cyclic `Z3` action has a one-dimensional invariant
subspace.  Its normalized generator is

```text
E1 / ||E1||,  E1 = S0 + S1 + S2.
```

Therefore, if a future same-surface EW/Higgs action proves that canonical
`O_H` is the degree-one radial fluctuation in this taste-axis space, the
taste-radial source already implemented in the harness is the unique target.

## Boundary

The current surface does not derive that degree-one premise.  In the full
commuting taste algebra generated by the same axes, the trace-zero cyclic
invariant subspace has rank 3:

```text
E1 = S0 + S1 + S2
E2 = S0 S1 + S1 S2 + S2 S0
E3 = S0 S1 S2
```

After Hilbert-Schmidt normalization, `E1`, `E2`, `E3`, and mixtures such as
`(E1 + E2)/sqrt(2)` all pass the basic current-surface filters:
Hermitian, trace-zero, source-orthogonal, normalized, and cyclically invariant.
They are spectrally distinct, so this is not a notation-only degeneracy.

Thus `Z3` symmetry plus trace-zero taste algebra does not select canonical
`O_H`.  The route still needs one of:

- a same-surface EW/Higgs action or canonical operator theorem proving the
  degree-one radial-Higgs premise;
- production `C_sx/C_xx` rows plus a source-overlap/pole bridge;
- a same-source physical-response bypass such as W/Z rows with strict
  `g2`, covariance, and `delta_perp` authority.

## Non-Claims

This block does not claim retained or `proposed_retained` PR230 closure.  It
does not define `O_H` by notation, does not set `kappa_s = 1`, does not use
`H_unit`, does not use `yt_ward_identity`, and does not use observed
`y_t`/`m_t` targets as selectors.

## Verification

```bash
python3 scripts/frontier_yt_pr230_taste_radial_canonical_oh_selector_gate.py
# SUMMARY: PASS=17 FAIL=0
```
