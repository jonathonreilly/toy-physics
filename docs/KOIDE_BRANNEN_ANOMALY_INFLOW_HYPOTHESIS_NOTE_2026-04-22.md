# Koide Brannen-Phase Anomaly-Inflow Hypothesis

**Date:** 2026-04-22
**Lane:** Charged-lepton Koide phase δ = 2/9 — new-physics direction.
**Status:** SPECULATIVE HYPOTHESIS with specific testable content.
**Scope:** review package only (companion to KOIDE_BRANNEN_WILSON_DSQ_QUANTIZATION_THEOREM_NOTE_2026-04-22.md). Not landed on main.

## 0. Motivation

All retained numerical derivations of 2/9 (ABSS η, Brannen conjugate-pair
δ_per_step = n_eff/d², quark LH Y³ anomaly per generation
`Tr[Y³]_q_L = 2d·(1/d)³ = 2/d²`) give the same dimensionless rational.
The rigid-triangle rotation theorem (§7 of companion note) upgrades δ to
a clean Euclidean rotation angle, eliminating the "radian vs
dimensionless" convention concern at the LHS. What remains: WHY does
the rotation at the physical charged-lepton point m_* equal exactly 2/9 rad?

**Physics hypothesis (H_anomaly-inflow)**: The charged-lepton Brannen
phase δ(m_*) inherits its value from the quark-LH Y³ anomaly density
via Z_3-spatial-orbifold → one-clock-time descent within the retained
Cl(3)/Z³ 3+1 structure. The parameter m in H_sel(m) is NOT a spacetime
coordinate but a modulus of the Hermitian source family; the "inflow"
is from the spatial Z_3 anomaly onto the single-clock evolution
generated by exp(H_sel(m)) through the retained one-clock grammar
(ANOMALY_FORCES_TIME).
Specifically:

```text
δ(m_*) = Tr[Y³]_quark_LH_per_generation  [in natural one-clock units]
       = (2d)·(1/d)³                      [structural formula]
       = 2/d²                             [retained]
       = 2/9                              [at d = 3]
```

## 1. Cross-sector identity

The empirical/structural fact:

| Quantity | Value | Source |
|----------|-------|--------|
| δ_Brannen (charged-lepton) | 2/9 | PDG fit + framework Berry theorem |
| Tr[Y³]_q_L per gen | 2/9 | Anomaly arithmetic (retained in ANOMALY_FORCES_TIME) |
| η_APS (Z_3 with weights (1,2)) | 2/9 | ABSS equivariant fixed-point |

These are three distinct computations over THREE different sectors
(charged lepton Berry phase, quark hypercharge anomaly, ambient Z_3
orbifold spectral asymmetry). They converge numerically at exact 2/9.

**A cross-sector identity is suggestive of a unifying mechanism.**

## 2. The proposed physics mechanism

### 2.1 Spatial Z_3 anomaly → temporal one-clock descent

Quark LH fermions in the Cl(3)/Z³ framework (retained per anomaly-
forces-time): 2·3 = 6 states per generation with hypercharge Y_q = 1/3.
Per-generation Y³ anomaly coefficient:

```text
A_quark_LH = N_q · Y_q³ = 6·(1/27) = 2/9
```

In the retained 3+1 structure (signature (3,1), single temporal clock),
this spatial anomaly can descend via the Z_3 generation action to
source a topological Wess-Zumino-like term on the single-clock time
direction. Following Callan-Harvey 1985 / Kaplan 1992, applied to
retained Cl(3)/Z³ 3+1 with the specific single-clock codimension-1
evolution grammar. Specifically, if the bulk
Dirac operator has non-zero index on a submanifold, the boundary
inherits a topological term with coefficient = bulk anomaly coefficient.

### 2.2 Proposed inflow channel

**Hypothesis**: The quark-LH Y³ spatial anomaly descends onto the
one-clock time direction via the retained C_3[111] generation action:

1. The Cl(3)/Z³ C_3 action couples quark and lepton sectors via the
   generation index (retained three-generation structure from
   THREE_GENERATION_OBSERVABLE_THEOREM).
2. The 3+1 spatial 3D anomaly density `Tr[Y³]_q_L = 2/9` is integrated
   over the 3 retained spatial directions of Cl(3) to give a
   one-clock-time effective coupling.
3. On the single-clock time direction (retained from
   ANOMALY_FORCES_TIME), this induces a topological WZ-like phase with
   coefficient 2/9 rad per natural time step.
4. The Hermitian moduli family H_sel(m) generates the Koide amplitude
   via one natural time step exp(H_sel). The parameter m traces out
   the physical first branch in moduli space.
5. The ENTIRE first-branch arc corresponds to ONE unit of inflow
   time-integration; hence Berry phase accumulated = 2/9 rad.

**Important clarification (3+1 not 4D)**: This is explicitly NOT a 4D
dimensional reduction. The retained framework's 3+1 single-clock
structure is load-bearing: the Z_3 generation action (on the spatial
Z³ lattice) sources the one-clock CS phase without breaking the (3,1)
Lorentzian signature.

### 2.3 Required lemmas for closure

For this hypothesis to become a closure theorem:

- **L1**: Identify the specific anomaly-inflow map from 3+1 quark sector
  to 1D charged-lepton selected-line within the retained framework.
- **L2**: Show that the integrated CS phase on the first-branch arc
  (from m_0 to m_*) equals exactly 2/9 rad.
- **L3**: Verify that m_* corresponds to one "full inflow cycle" (one
  natural unit of one-clock time integration).

None of L1, L2, L3 is currently proven. This note records the
hypothesis and provides a framework for rigorous derivation.

## 3. Testable predictions (if hypothesis is correct)

1. Under chiral rotation on the selected-line, the Koide amplitude's
   phase should shift by exactly the anomaly coefficient.
2. The arc length from m_0 to m_* should equal 1 natural one-clock
   unit (not a general real).
3. The ratio of charged-lepton Brannen phase to quark-LH hypercharge
   anomaly per generation should be EXACTLY 1 (both 2/9, no free
   normalization).

## 4. Numerical verification of prediction (1)

Standard chiral anomaly: under ψ → exp(iα·γ_5)ψ, path integral measure
transforms as det → det · exp(i·α·A_anomaly). For A_anomaly = 2/9 and
α = 1 (unit chiral rotation), the det phase shift is 2/9 rad.

This matches the Brannen phase δ(m_*) = 2/9 rad. (Runner
implementation pending for the specific Cl(3)/Z³ chiral transformation.)

## 5. Remaining obstruction

The honest reviewer position: **this hypothesis provides a physics
mechanism consistent with the numerics, but does not yet constitute a
derivation**. Rigorous closure requires:

- Explicit construction of the inflow map on retained Cl(3)/Z³ structure.
- Verification that the charged-lepton selected-line is the natural
  inflow boundary (vs. another sector or no inflow).
- Derivation that the arc m_0 → m_* corresponds to exactly 1 natural
  time unit of inflow evolution.

The existing framework's anomaly-forces-time theorem gives the 3+1
structure and one-clock evolution. The missing piece is the SPECIFIC
INFLOW CHANNEL from quark LH to charged-lepton selected-line.

## 6. Path forward

1. Explicitly construct the 3+1 Dirac operator on Cl(3)/Z³ including
   both quark and lepton sectors.
2. Compute the Pontryagin/Chern density sourced by the quark LH Y³
   anomaly.
3. Integrate over 3 spatial directions to obtain the 1D effective action
   on the selected-line.
4. Show the coefficient = 2/9 and the natural-time unit makes the
   Berry phase exactly 2/9 rad.

This is substantial new theoretical work beyond the iteration scope.

## 7. Relation to existing closure candidates

This hypothesis is COMPLEMENTARY to:
- Route 3 Wilson-line quantization (companion note) — geometric.
- Qubit-lattice-dim closure — algebraic.

All three would be derived from the same inflow picture if the physics
mechanism holds.

## 8. Status

- 5 retained routes converge at 2/9 (ABSS, anomaly Y³, Brannen
  conjugate-pair, Wilson d²-quantization, rigid-triangle rotation).
- The missing link: explicit anomaly-inflow derivation from 3+1 to 1D.
- Numerical verification of all structural building blocks: 20/20 PASS
  in companion runner.

This note serves as a **targeted research proposal** for the remaining
theoretical work to close P unconditionally.
