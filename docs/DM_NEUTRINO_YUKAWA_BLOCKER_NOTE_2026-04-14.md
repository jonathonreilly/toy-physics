# DM Neutrino Yukawa Blocker Note

**Date:** 2026-04-14  
**Branch:** `codex/dm-main-derived`

---

## Status

**BOUNDED / DOWNSTREAM-OPEN**

The leptogenesis denominator is no longer blocked by a vague neutrino-Yukawa
objection. The branch now has a much sharper read:

- the direct post-EWSB operator is fixed as `Gamma_1`
- the physical bosonic base normalization is fixed as `g_weak/sqrt(2)`
- the exact retained local second-order Schur coefficient is fixed as
  `y_nu^eff = g_weak^2 / 64`
- on the present seesaw calibration, that lands at `k_eff ~= 8.01`

So the blocker is no longer "derive the Dirac suppression law." The live
problem is now downstream:

> derive or rule out the Majorana / `Z_3` activation law that turns on the
> unique charge-`2` source and fixes the three-generation `A/B/epsilon`
> structure without fitted leftovers.

**Update after the midpoint and adjacent-placement theorems (2026-04-15):**

The branch has now moved one step further downstream than the paragraph above.
On the minimal non-homogeneous bridge already admitted by the branch:

- the absolute doublet anchor is fixed as `k_B = 8`
- the minimal adjacent lift fixes the singlet/doublet placement as
  `k_A = 7`, `k_B = 8`

So the live denominator blocker is no longer:

- the absolute staircase anchor
- the singlet/doublet placement

It is now the remaining amplitude law inside the already-shaped texture,
especially the doublet splitting `eps/B`, together with the downstream
`m_3` / texture-factor inputs that still enter leptogenesis.

The new exact residual-ratio obstruction theorem sharpens that one step
further: once `k_A = 7`, `k_B = 8` are fixed, `eps/B` is still a residual
dimensionless deformation of the same exact `Z_3` texture class. So the live
blocker is no longer the staircase placement or even a generic texture class.
It is specifically the `eps/B` law and the amplitudes that depend on it.

The next obvious rescue path now closes negatively too: the new doublet-block
matching obstruction theorem shows that the obvious normalized `2 x 2`
generation-doublet block `r I + sigma_x` cannot match the exact local
self-dual Majorana block in any perturbative regime. Its quadratic match
forces `r = 1`, and its bosonic log-response match forces `r = sqrt(3)`.
So the missing `eps/B` law is not the obvious normalized local-to-doublet
block match either.

**Update after the residual-sharing / atmospheric-scale pass (2026-04-15):**

The branch has now moved one step further downstream again on the minimal
bridge:

- the minimal symmetric residual-sharing theorem fixes `eps/B = alpha_LM/2`
- the canonical `Z_3` bridge fixes the reduced texture factor exactly as `1/3`
- the exact retained Dirac coefficient together with
  `k_A = 7`, `k_B = 8`, `eps/B = alpha_LM/2` predicts
  `m_3 = 5.06e-2 eV` and `Dm^2_31 = 2.54e-3 eV^2` on the current diagonal
  benchmark

So the live denominator blocker is no longer:

- the staircase anchor
- the singlet/doublet placement
- the split law `eps/B`
- the atmospheric benchmark scale `m_3`
- the reduced democratic `1/3` overlap factor

It is now the exact leptogenesis CP-asymmetry kernel on the three-generation
`Z_3` texture, especially the relative sign/interference of the `N_2` and
`N_3` contributions and the full-matrix flavor closure beyond the reduced
DI + `U_Z3` estimate.

The new universal-Yukawa no-go theorem sharpens that one step further still:
if the Dirac neutrino Yukawa remains exactly universal `Y = y_0 I`, then every
unitary basis change preserves `Y^dag Y = y_0^2 I`, so the standard
leptogenesis CP tensor vanishes identically. So the live blocker is no longer
"just compute epsilon_1 more carefully on the same universal bridge." It is:

> derive the non-universal Dirac flavor texture (or equivalent extra
> structure) that makes the leptogenesis CP kernel nonzero on the fixed
> three-generation `Z_3` surface.

The next obvious rescue path closes negatively too: the new CKM-texture
transfer no-go theorem shows that the current CKM/NNI atlas tools do not
transfer to this lane as-is. On the exact universal Dirac bridge the singular
spectrum stays triple-degenerate, so:

- the GST / mass-hierarchy route has no nontrivial hierarchy parameter
- the mass-basis NNI normalization collapses to the identity map
- the Schur-complement identity can at most reorganize `O(1)` coefficients,
  not generate Wolfenstein-like suppression
- phase-only companions still leave `Y^dag Y` proportional to `I`

So the live blocker is not "reuse the current CKM flavor package on the
neutrino side." It is still a genuinely new non-universal neutrino Dirac
texture or equivalent exact extra structure.

The new CP-kernel deformation-necessity theorem sharpens that target one step
further: the missing object is not just "some non-universal texture." Pure
unitary basis mismatch still leaves `Y^dag Y = y_0^2 I`, and pure diagonal
non-universal rescaling still leaves `Y^dag Y` diagonal. So any successful
future texture must be a non-unitary off-diagonal flavor-breaking deformation
that makes the Hermitian kernel itself non-diagonal.

The new minimal `Z_3` circulant CP tool sharpens it one step further again:
the current exact weak-axis `1+2` split already lifts to an exact even
circulant texture on the generation space, but that slice is still
CP-degenerate. The minimal exact `Z_3`-covariant extension that can support a
nonzero leptogenesis kernel is the unique odd circulant generator
`i(S - S^2)`. So the live theorem target is no longer an unspecified flavor
deformation. It is derive that odd circulant generator, or an equivalent
object, from the axiom surface.

The new two-Higgs right-Gram bridge theorem sharpens that again in a positive
direction. The odd-circulant CP-supporting family is not alien to the local
neutrino atlas: the canonical local two-Higgs neutrino lane already contains
it on an exact admissible subcone. After canonical rephasing, the circulant
target is realized on that lane iff its common diagonal/off-diagonal data
obey `d >= 2 r`. So the live blocker is no longer "invent any CP-supporting
Dirac flavor texture somehow." It is:

> derive or justify the actual axiom-side activation / coefficient law for the
> canonical distinct-charge two-Higgs neutrino lane, fixing its physical
> CP-supporting circulant data `(d,r,delta)` (equivalently `(x,y,delta)` on the
> physical `x >= y` sheet) rather than merely inventing a flavor texture.

The new DM two-Higgs minimality theorem sharpens that one step further:
once nonzero local DM CP support is required, there is no smaller exact local
escape left. Single-Higgs and repeated-charge lanes stay CP-empty at the
Hermitian-kernel level, while the canonical distinct-charge two-Higgs class is
the unique minimal exact local escape and already carries the admissible
odd-circulant target.

The new DM continuity sheet theorem sharpens the local-data side too. On the
DM circulant admissible subcone, the residual local ambiguity is only the swap
`x <-> y`, and continuity to the retained universal bridge `Y = y_0 I`
selects the physical `x >= y` sheet explicitly. So the live blocker is no
longer the local sheet either. What remains is the actual activation and
coefficient law for that now-isolated branch.

The new odd-circulant residual-`Z_2` slot theorem sharpens the coefficient
target once more: on the DM Hermitian circulant family the odd coefficient
`c_odd` is the unique residual-`Z_2`-odd local slot, and the standard
leptogenesis CP tensor is proportional to `c_even c_odd`. So the coefficient
problem is no longer generic even on that branch. It is one exact odd slot.

The new odd-circulant current-stack zero law then gives the harsh present-tense
answer: the retained local DM bank is residual-`Z_2` even, so
`c_odd,current = 0` exactly. The live blocker is therefore no longer “derive a
coefficient law somehow” on the current stack. It is:

> derive a genuinely new residual-`Z_2`-odd bridge or activator that turns on
> the unique odd circulant coefficient away from its exact current-stack zero
> law.

The new odd-slot minimal mixed-bridge extension theorem makes that sharper
still: the remaining positive extension class is not vague. It is a
residual-`Z_2`-odd non-additive mixed bridge with one real amplitude on the
unique odd class `i(S-S^2)`.

There is now also a strongest positive invented route on branch:
the `Z_3` phase-lift mixed bridge family

`K_lambda = d I + r (e^{i lambda delta_src} S + e^{-i lambda delta_src} S^2)`,

with exact weak-only source `delta_src = 2pi/3`. This family is axiom-native
in its inputs, preserves the local two-Higgs admissible data, and turns on the
unique odd slot for any nonzero `lambda`.

But the branch is now sharper than that too. The new `Z_3` character-transfer
theorem shows exact weak-source transfer does **not** leave a continuous
activation law:

- exact source transfer requires a genuine `Z_3` character
- so `lambda` is discrete, not continuous
- on the local continuity strip the only exact branches are `{-1,0,+1}`
- the source-oriented nontrivial branch is `lambda = +1`

So the free-`lambda` ambiguity is gone on that invented family.

And the next theorem is harsher still: the new circulant mass-basis no-go
shows the whole exact `Z_3`-covariant circulant bridge class still fails the
physical standard leptogenesis tensor. In the `Z_3` basis that family is
real-diagonal, and the current Majorana diagonalization inside the doublet
block is only a real orthogonal rotation, so the heavy-neutrino-basis kernel
stays real symmetric and

`Im[(Y^dag Y)_(1j)^2] = 0`

for all `j`.

So the live blocker is now even stricter:

> not a free `lambda`, and not any exact `Z_3`-covariant circulant bridge.
> The denominator now needs a genuinely new non-circulant, right-sensitive,
> CP-carrying bridge that survives as a complex off-diagonal object in the
> heavy-neutrino mass basis.

The new singlet-doublet CP-slot tool sharpens that again into the exact
physical carrier. On the current Majorana stack, the minimal surviving
mass-basis CP source is a `Z_3`-basis singlet-doublet slot family with one
residual-`Z_2`-even amplitude `u` and one residual-`Z_2`-odd amplitude `v`.
After the exact real doublet rotation,

`Im[(K_mass)_{0j}^2] = -2 u v sin(2 phi)`,

and at the exact weak-only source phase `phi = 2 pi / 3` this becomes

`Im[(K_mass)_{0j}^2] = sqrt(3) u v`.

So the live blocker is no longer just “some non-circulant bridge.” It is now:

> derive the microscopic singlet-doublet slot amplitudes `u` and `v` on the
> admitted neutrino lane, or prove the current stack cannot supply them.

The next obvious realization route now closes negatively too. On the simplest
`2↔3`-symmetric canonical two-Higgs sublane

`x = (a,b,b)`, `y = (c,d,d)`, `delta = 2 pi / 3`,

exact source-phase alignment to that singlet-doublet carrier forces `b d = 0`,
while the physical CP tensor on the same sublane is proportional to `b d`.
So that whole simplest symmetric two-Higgs rescue class is exhausted. The live
remaining object is therefore narrower still:

> derive the singlet-doublet slot amplitudes on a more asymmetric /
> right-sensitive realization of the admitted two-Higgs lane.

The next theorem is harsher again. On the **full admitted canonical two-Higgs
lane**

`Y = diag(x_1,x_2,x_3) + diag(y_1,y_2,y_3 e^{i delta}) C`

with the exact source phase `delta = 2 pi / 3`, exact singlet-doublet slot
alignment gives two polynomial conditions whose sum forces

`x_3 y_3 = 0`,

while the physical heavy-neutrino-basis CP tensor on that same lane is
proportional to `x_3 y_3`.

So the exact source-phase aligned branch on the **full admitted canonical
two-Higgs lane** is structurally CP-empty. The live blocker is therefore
sharper than “find a better asymmetric point on the same lane.” It is now:

> derive a genuinely new post-canonical, right-sensitive mixed bridge with the
> physical singlet-doublet slot amplitudes `u` and `v`, or prove the axiom
> surface cannot supply one.

And the support-class ambiguity is now closed too. The new post-canonical
slot-supported support-class theorem shows the spectator diagonal and
doublet-block data are CP-inert on the physical singlet-doublet carrier, so
any future positive bridge can be reduced to the minimal slot-supported family
itself. So the live blocker is now exact in both extension class and support
class:

> derive a post-canonical, right-sensitive, **slot-supported** mixed bridge
> with the two real singlet-doublet slot amplitudes `u` and `v`, or prove the
> axiom surface cannot supply one.

That obstruction is now sharpened again. The new post-canonical positive polar
section theorem shows the raw right-frame note was **not** a total no-section
theorem. On the generic full-rank patch, the exact right orbit already admits
the unique positive intrinsic representative

`Y_+(H) = H^(1/2)`,

and on that representative

`K_+(H) = Y_+(H)^dag Y_+(H) = H`.

So the remaining post-canonical bridge is now read intrinsically from the
Hermitian data through

`K_Z3(H) = U_Z3^dag H U_Z3`,

with intrinsic singlet-doublet slot pair

- `a(H) = (K_Z3(H))_01`
- `b(H) = (K_Z3(H))_02`.

Therefore the honest blocker is no longer “find any right-frame law.” The
generic full-rank right orbit already has one.

But the next theorem kills the obvious closure hope there too. On the exact
residual-`Z_2` aligned Hermitian core

`H_act = [[a,b,b],[b,c,d],[b,d,c]]`,

the positive-section slot pair collapses exactly to one repeated real scalar,

`a_slot(H_act) = b_slot(H_act) = (a+b-c-d)/3`,

so after the real Majorana doublet rotation the physical CP tensor vanishes
exactly. The aligned Hermitian core is intrinsically CP-empty even on the
positive section.

So the live blocker has now moved fully onto the Hermitian-data side. It is:

> derive the active-neutrino Hermitian symmetry-breaking law away from the
> aligned core, equivalently the breaking slots away from
> `d_2 = d_3`, `r_12 = r_31`, `phi = 0`,
> or accept that full zero-import DM does not close on the current stack.

That target is now formula-level precise too. On the active Hermitian grammar

`H = [[d_1,r_12,r_31 e^{-i phi}],[r_12,d_2,r_23],[r_31 e^{i phi},r_23,d_3]]`,

the intrinsic positive-section CP tensor is exactly

- `Im[(K_mass)01^2] = -r_31 (d_2-d_3+r_12-r_31 cos(phi)) sin(phi) / 3`
- `Im[(K_mass)02^2] =  r_31 (2 d_1-d_2-d_3+r_12-2 r_23+r_31 cos(phi)) sin(phi) / 3`

So the remaining last-mile DM object is not “some CP-supporting deformation.”
It is now the exact `H`-side law for:

- the phase slot `phi`
- `B_1 = d_2-d_3+r_12-r_31 cos(phi)`
- `B_2 = 2 d_1-d_2-d_3+r_12-2 r_23+r_31 cos(phi)`.

In the canonical breaking-source coordinates of the active Hermitian package,
that endpoint sharpens again. Writing

- `H = H_core + B(delta,rho,gamma)`
- `H_core = [[A,b,b],[b,c,d],[b,d,c]]`
- `B = [[0,rho,-rho-i gamma],[rho,delta,0],[-rho+i gamma,0,-delta]]`

the intrinsic positive-section DM tensor becomes exactly

- `Im[(K_mass)01^2] = -2 gamma (delta + rho) / 3`
- `Im[(K_mass)02^2] =  2 gamma (A + b - c - d) / 3`.

So the remaining last-mile object is now triplet-level precise:

- `gamma` is the mandatory CP-odd source
- `delta + rho` is the breaking-breaking interference channel
- `A + b - c - d` is the core-breaking interference channel.

The new axiom-boundary theorem sharpens that one more step. The current exact
stack still does **not** derive a positive value law for the triplet itself.
The strongest exact law now available is only the zero-locus / minimal-source
law:

- the breaking triplet is exactly the 3-real complement of the aligned
  residual-`Z_2` Hermitian core
- its zero locus is exactly that aligned core
- the coefficients `(delta,rho,gamma)` are still not axiom-side outputs

So the honest live blocker is now even cleaner:

> derive the positive coefficient law for the breaking triplet, or accept that
> full zero-import DM stays bounded on the current stack.

The local neutrino lane now fixes the exact carrier for that problem too. The
branch is no longer blocked on “finding the right local carrier.” The exact
DM-side Hermitian carrier is already

`B_H,min = (A,B,u,v,delta,rho,gamma)`,

with unified extension

`U_min = (A,B,u,v,delta,rho,gamma,a_sel,e)`

if the selector leg is carried too. So the live question is not carrier
invention. It is the positive value law on that already-fixed carrier,
especially its breaking-triplet leg.

The strong-CP / CKM side now sharpens what does **not** solve that law. The
current strong-CP theorem fixes a weak-only `Z_3` source orientation and keeps
the color commutant blind to the weak phase, while the current CKM tensor-slot
closure remains quark-block specific. On the DM branch itself, holding the
exact source phase

`phi = 2 pi / 3`

fixed still leaves a continuum of distinct triplets `(delta,rho,gamma)` and a
continuum of distinct leptogenesis kernels. So strong-CP closure does not yet
populate `B_H,min`; it only orients the source. The missing object is now a
cross-sector transfer / coefficient law that turns that oriented weak CP
source into the neutrino Hermitian carrier, especially `gamma`.

The new staged triplet theorems now fix the **form** of that missing law more
sharply than before.

First, the source-direction leg is exact. Under character conjugation

`phi -> -phi`,

the active Hermitian law splits as

- `H_odd = gamma T_gamma`
- `H_even = H_core + delta T_delta + rho T_rho`.

So the nontrivial weak `Z_3` character lands uniquely on the CP-odd triplet
slot `gamma`. The missing law is therefore not vague about the odd source
direction anymore.

Second, the response-form leg is exact. Once `gamma` is isolated, the
intrinsic DM tensor factorizes as

- `cp1 = -2 gamma (delta + rho) / 3`
- `cp2 =  2 gamma (A + b - c - d) / 3`.

So the even response sector is exactly the pair

- `E1 = delta + rho`
- `E2 = A + b - c - d`

and the remaining transfer law is one odd source times two even response
channels.

The new weak-triplet transfer-class theorem closes the last structural freedom
in that map. Once the current source-side data are reduced to:

- one selector amplitude slot `a_sel`
- one two-channel weak tensor pair `(tau_E,tau_T)`

and the DM endpoint is reduced to:

- one odd triplet source `gamma`
- two even response channels `E1`, `E2`

any linear transfer law compatible with the exact odd/even split is forced
into block form:

- `gamma = c_odd a_sel`
- `[E1, E2]^T = M_even [tau_E, tau_T]^T`

So the live gap is no longer a generic cross-sector `3 x 3` mystery. The
transfer-class theorem reduces it to one real odd coefficient `c_odd` plus one
real `2 x 2` even response matrix `M_even`.

The new single-axiom coefficient-boundary theorem sharpens that one final step.
Here “axiom” means only the single framework axiom `Cl(3)` on `Z^3`. On that
single-axiom surface plus the current derived atlas rows:

- the odd normalization is now fixed by the new bosonic matching theorem:
  `c_odd = +1` on the source-oriented branch convention
- the new weak even swap-reduction theorem sharpens the even leg to
  `M_even = v_even [1,1]`, equivalently
  `[E1, E2]^T = v_even (tau_E + tau_T)`
- the new even bosonic-normalization theorem then fixes
  `v_even = (sqrt(8/3), sqrt(8)/3)`
- so the transfer coefficients are now fully fixed:
  `gamma = a_sel`,
  `E1 = sqrt(8/3) tau_+`,
  `E2 = (sqrt(8)/3) tau_+`
  with `tau_+ = tau_E + tau_T`

So the branch can no longer honestly say that the current single-axiom stack
is still blocked on transfer-coefficient normalization. That part is now done.
The honest live gap is narrower and harsher: derive the source amplitudes
`a_sel` and `tau_+` on the same single-axiom surface, then rebuild the
leptogenesis kernel around that exact transfer law, or accept that the
denominator remains bounded.

Third, the normalization target is now exact too. At fixed `M_1` and washout,
full closure would require

`epsilon / epsilon_DI = eta_obs / eta_DI = 0.936`,

which is a `3.37x` enhancement over the current reduced kernel. But the exact
source phase is already

`phi = 2 pi / 3`,

so even a phase-only improvement can supply at most

`1 / sin(2 pi / 3) = 1.155x`.

Therefore the missing theorem cannot be mainly a phase-fixing theorem. It has
to normalize the amplitude/response sector itself: `gamma`, `delta + rho`, and
`A + b - c - d`.

---

## What The Branch Really Shows

The selector script now says three important things at once:

1. `y_nu = 1` points to `k_B ~ 3.8`
2. `y_nu = alpha_LM` points to `k_B ~ 5.8`
3. `y_nu = alpha_LM^2` points to `k_B ~ 7.8`

That makes `k_B = 8` the only simple integer-power candidate that overlaps the
observed `eta` band. See
[`scripts/frontier_dm_select_kb.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_select_kb.py:567)
and
[`scripts/frontier_dm_select_kb.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_select_kb.py:820).

The same script also shows the calibrated seesaw Yukawa is:

- `y_0(k_B=7) ~ alpha_LM^1.59`
- `y_0(k_B=8) ~ alpha_LM^2.09`

So `k_B = 8` is not arbitrary numerology. It is the one staircase level that
lands near a simple power-law suppression. See
[`scripts/frontier_dm_select_kb.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_select_kb.py:1114)
and
[`scripts/frontier_dm_select_kb.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_select_kb.py:1179).

There is also now a more concrete bounded mechanism behind that attraction:

- [`scripts/frontier_dm_neutrino_yukawa_candidate.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_neutrino_yukawa_candidate.py)
- [`docs/DM_NEUTRINO_YUKAWA_CASCADE_CANDIDATE_NOTE_2026-04-14.md`](/Users/jonBridger/Toy%20Physics-dm/docs/DM_NEUTRINO_YUKAWA_CASCADE_CANDIDATE_NOTE_2026-04-14.md)

That candidate shows a second-order EWSB cascade naturally lands near the
`k_B = 8` Yukawa scale, which is stronger than the old bare `alpha_LM^2`
guess. It still does **not** upgrade the lane to theorem status.

There is now also an exact geometry companion:

- [`scripts/frontier_dm_neutrino_cascade_geometry.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_neutrino_cascade_geometry.py)
- [`docs/DM_NEUTRINO_CASCADE_GEOMETRY_NOTE_2026-04-14.md`](/Users/jonBridger/Toy%20Physics-dm/docs/DM_NEUTRINO_CASCADE_GEOMETRY_NOTE_2026-04-14.md)

That geometry shows the weak-axis insertion splits `T_1` exactly into one
singlet path and a residual two-state `T_2` path, so the second-order cascade
is now supported by exact operator algebra as well as numerics.

There is now also an exact operator-selection companion:

- [`scripts/frontier_dm_neutrino_operator_selection_obstruction.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_neutrino_operator_selection_obstruction.py)
- [`docs/DM_NEUTRINO_OPERATOR_SELECTION_OBSTRUCTION_NOTE_2026-04-14.md`](/Users/jonBridger/Toy%20Physics-dm/docs/DM_NEUTRINO_OPERATOR_SELECTION_OBSTRUCTION_NOTE_2026-04-14.md)

That runner proves the branch currently carries two inequivalent
framework-native operator surfaces:

- `Xi_5` = exact staggered mass / taste-chirality surface
- `Gamma_1` = weak-axis EWSB insertion

They differ in 3+1 chirality behavior, hop order, and orbit action. So the
missing theorem is not just a prefactor or staircase choice; it is also an
operator-selection problem.

There is now also an exact bridge theorem:

- [`scripts/frontier_dm_neutrino_dirac_bridge_theorem.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_neutrino_dirac_bridge_theorem.py)
- [`docs/DM_NEUTRINO_DIRAC_BRIDGE_THEOREM_NOTE_2026-04-15.md`](/Users/jonBridger/Toy%20Physics-dm/docs/DM_NEUTRINO_DIRAC_BRIDGE_THEOREM_NOTE_2026-04-15.md)

That theorem closes the `Gamma_1` versus `Xi_5` ambiguity on the local
post-EWSB chiral surface:

- the local spatial Higgs family `M(phi) = sum_i phi_i Gamma_i` is exactly
  chiral off-diagonal on `C^16`
- the exact selector picks axis minima
- after EWSB axis selection, the local Dirac surface is uniquely `Gamma_i`
  up to the broken `S_3` choice and sign
- in the branch convention with weak axis 1, the direct post-EWSB operator is
  `Gamma_1`
- `Xi_5` is excluded as a direct neutrino Dirac Yukawa surface because it is
  chiral diagonal, not chiral off-diagonal

So operator selection is no longer the live blocker.

There is now also an exact base-normalization audit:

- [`scripts/frontier_dm_neutrino_base_coupling_theorem.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_neutrino_base_coupling_theorem.py)
- [`docs/DM_NEUTRINO_BASE_NORMALIZATION_AUDIT_2026-04-15.md`](/Users/jonBridger/Toy%20Physics-dm/docs/DM_NEUTRINO_BASE_NORMALIZATION_AUDIT_2026-04-15.md)

That audit proved the current branch did **not** yet fix a unique
unsuppressed neutrino base ratio:

- full `C^16` Frobenius normalization gives `y_nu^(0) / g_weak = 1/sqrt(2)`
- active chiral-subspace normalization gives `y_nu^(0) / g_weak = 1`
- operator norm also gives `1`

So at that stage the direct local bridge normalization still appeared open.

There is now also an exact bosonic-normalization theorem:

- [`scripts/frontier_dm_neutrino_bosonic_normalization_theorem.py`](scripts/frontier_dm_neutrino_bosonic_normalization_theorem.py)
- [`docs/DM_NEUTRINO_BOSONIC_NORMALIZATION_THEOREM_NOTE_2026-04-15.md`](docs/DM_NEUTRINO_BOSONIC_NORMALIZATION_THEOREM_NOTE_2026-04-15.md)

That theorem imports the retained mainline observable-principle machinery and
closes the ambiguity cleanly:

- the raw chiral bridge `Y = P_R Gamma_1 P_L` is nilpotent
- on a scalar local baseline it has zero bosonic `log|det|` response
- the retained scalar Hermitian completion is exactly `Gamma_1 = Y + Y^dag`
- `Gamma_1` carries the nontrivial even bosonic source-response
- therefore the physical normalization surface is the full bosonic
  `Gamma_1` family, not the active bridge by itself

So the branch can now promote the full-space benchmark

`y_nu^(0) / g_weak = 1/sqrt(2)`

and reject the active-space `1` ratio as a non-bosonic comparator rather than
a live physical ambiguity.

There is now also an exact retained local Schur companion:

- `scripts/frontier_dm_neutrino_schur_suppression_theorem.py`
- `docs/DM_NEUTRINO_SCHUR_SUPPRESSION_THEOREM_NOTE_2026-04-15.md`

That theorem closes the local second-order coefficient that used to be the
live blocker:

- the weak-axis selector potential has exact transverse quadratic coefficient
  `32`
- the exact Schur complement of `m I + j Gamma_1` on `T_1` generates
  `j^2 / m`
- with the already-selected base surface `j = g_weak/sqrt(2)`, the retained
  local coefficient is exactly

  `y_nu^eff = g_weak^2 / 64 = 6.66e-3`

- on the present seesaw calibration, this gives `k_eff ~= 8.01`

So the old bounded `k_B = 8` attraction is now an exact retained local
Dirac-lane theorem, not just a candidate mechanism.

There is now also an exact weak-matching obstruction companion:

- [`scripts/frontier_dm_neutrino_weak_matching_obstruction.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_neutrino_weak_matching_obstruction.py)
- [`docs/DM_NEUTRINO_WEAK_MATCHING_OBSTRUCTION_NOTE_2026-04-15.md`](/Users/jonBridger/Toy%20Physics-dm/docs/DM_NEUTRINO_WEAK_MATCHING_OBSTRUCTION_NOTE_2026-04-15.md)

That runner attacks the obvious reuse path directly and shows why the
top-Yukawa protection route does **not** automatically transfer:

- the direct bridge operator is `Gamma_1`, not `G5`
- `Gamma_1` is not central in `Cl(3)`
- generic propagator and gauge-chain factorization fails for `Gamma_1`

So the missing theorem is no longer "maybe the old Ward logic still works."
It does not, at least not without a genuinely new weak-sector identity.
The generic `g_bare` rigidity theorem does not bypass this either, because it
only removes an extra scalar once the same concrete operator family is fixed.
That operator-family matching is exactly what remains open here.

There is now also an exact weak-vector companion:

- [`scripts/frontier_dm_neutrino_weak_vector_theorem.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_neutrino_weak_vector_theorem.py)
- [`docs/DM_NEUTRINO_WEAK_VECTOR_THEOREM_NOTE_2026-04-15.md`](/Users/jonBridger/Toy%20Physics-dm/docs/DM_NEUTRINO_WEAK_VECTOR_THEOREM_NOTE_2026-04-15.md)

That theorem upgrades the direct bridge family itself:

- `Y_i = P_R Gamma_i P_L` is an exact weak vector under the derived `SU(2)`
- it carries the exact spin-1 adjoint Casimir
- it is trace-orthogonal as an operator triplet

So the branch now closes the weak representation content of the direct bridge.
But that theorem also sharpens the remaining gap:

- weak covariance is homogeneous, so `lambda Y_i` satisfies the same vector law
- therefore representation content alone does **not** fix the physical base
  coefficient

So the blocker is no longer "is `Gamma_1` really weak-sector-native?" It is
"what fixes the coefficient of that already-native weak vector?"

---

## Why This Is Still Not Closure

The branch does **not** currently derive a neutrino Yukawa rule.

The theorem-grade `n_link = 2` result that exists on branch is on the gauge
side:

- [`docs/YT_VERTEX_POWER_DERIVATION.md`](/Users/jonBridger/Toy%20Physics-dm/docs/YT_VERTEX_POWER_DERIVATION.md:162)
- [`docs/YT_MATCHING_NATIVE_NOTE.md`](/Users/jonBridger/Toy%20Physics-dm/docs/YT_MATCHING_NATIVE_NOTE.md:243)

Those notes derive the two-link power for the gauge vertex / vacuum
polarization channel. They do **not** prove that the neutrino Dirac Yukawa
inherits the same counting.

The top-Yukawa theorem is also not enough. It is a heaviest-fermion result with
its own trace identity and color normalization, not a universal neutrino-Yukawa
theorem. See
[`docs/YT_FORMAL_THEOREM_NOTE.md`](/Users/jonBridger/Toy%20Physics-dm/docs/YT_FORMAL_THEOREM_NOTE.md:14).

The 4D/right-handed completion is also not enough by itself. The branch's
right-handed-sector note gives chirality and anomaly-fixed RH singlets, but it
does **not** produce a neutrino Yukawa normalization theorem:

- the taste algebra commutes with `gamma_5`, so both chirality sectors carry
  the same taste representations
- the right-handed quantum numbers are fixed by anomaly cancellation, not by a
  new graph-canonical Yukawa derivation

See
[`docs/RIGHT_HANDED_SECTOR_NOTE.md`](/Users/jonBridger/Toy%20Physics-dm/docs/RIGHT_HANDED_SECTOR_NOTE.md:57)
and
[`docs/RIGHT_HANDED_SECTOR_NOTE.md`](/Users/jonBridger/Toy%20Physics-dm/docs/RIGHT_HANDED_SECTOR_NOTE.md:137).

What still stops short of a full theorem is no longer the Dirac suppression
step. That local step is now closed by the Schur theorem.

The new bridge theorem also sharpens the cascade statement:

- `Gamma_1` is the direct local post-EWSB Dirac surface on `C^16`
- but `P_{T_1} Gamma_1 P_{T_1} = 0`
- so the first exact closed action on the generation triplet is still
  second order through `O_0 + T_2`

On the neutrino side, the branch remains explicit that the absolute scale is
not derived from the axiom alone:

- `docs/DM_LEPTOGENESIS_NOTE.md`
- `scripts/frontier_dm_select_kb.py`
- `docs/NEUTRINO_MAJORANA_Z3_NONACTIVATION_THEOREM_NOTE.md`

That is the Nature-bar objection. The retained local Dirac lane now points
sharply to `k_B ~= 8`, but the denominator is still not zero-import because
the Majorana-side activation and texture law are not yet axiom-forced.

---

## Why The CP Phase Does Not Rescue This

The complex-Z_3 phase is not the dominant ambiguity.

On the current leptogenesis runner:

- scanning the phase from `30` to `90` degrees moves `eta` by only a factor of
  about `1.7`
- moving the staircase from `k_B = 7` to `k_B = 8` moves `eta` by about a
  factor of `11`

So the staircase ambiguity dominates the denominator error budget. The phase is
secondary until the Yukawa/staircase selector is fixed. See
[`scripts/frontier_dm_leptogenesis.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_leptogenesis.py:670)
and
[`scripts/frontier_dm_leptogenesis.py`](/Users/jonBridger/Toy%20Physics-dm/scripts/frontier_dm_leptogenesis.py:681).

There is also no unified phase/input surface yet: the complex-neutrino fit uses
`|eps|/B ~ 0.50`, while the current leptogenesis benchmark now uses the exact
minimal-bridge value `eps/B = alpha_LM/2 = 0.0453`. So phase refinement alone
still cannot close the lane.

---

## Clear Blocker

The denominator-side blocker is now:

> derive or rule out an axiom-side activation law for the unique same-
> chirality charge-`2` Majorana source on the anomaly-fixed `nu_R` channel.

The sharpest current version is:

> the branch now fixes the direct Dirac bridge, its physical bosonic
> normalization, and the exact retained local second-order Schur coefficient.
> What it still lacks is the theorem that turns on the unique Majorana source
> and feeds that one amplitude into the three-generation `A/B/epsilon`
> structure without fitted leftovers.

The imported Majorana cluster now sharpens that boundary:

- `docs/NEUTRINO_MAJORANA_CHARGE_TWO_PRIMITIVE_REDUCTION_NOTE.md`
- `docs/NEUTRINO_MAJORANA_UNIQUE_SOURCE_SLOT_NOTE.md`
- `docs/NEUTRINO_MAJORANA_PFAFFIAN_EXTENSION_NOTE.md`
- `docs/NEUTRINO_MAJORANA_LOCAL_PFAFFIAN_UNIQUENESS_NOTE.md`
- `docs/NEUTRINO_MAJORANA_Z3_NONACTIVATION_THEOREM_NOTE.md`
- `docs/NEUTRINO_MAJORANA_OBSERVABLE_PRINCIPLE_OBSTRUCTION_NOTE.md`
- `docs/NEUTRINO_MAJORANA_CURRENT_STACK_EXHAUSTION_NOTE.md`
- `docs/NEUTRINO_MAJORANA_NAMBU_SOURCE_PRINCIPLE_NOTE.md`

Those notes reduce the remaining microscopic object to one charge-`2`
primitive and one local source slot. The new local-Pfaffian uniqueness theorem
also closes the local realization question: if that primitive is admitted as a
local bilinear completion, the Pfaffian lane and local generator `log(mu)` are
forced. The `Z_3` non-activation theorem closes the obvious escape hatch on the
generation side: the retained three-generation / `Z_3` texture can shape
`A/B/epsilon` once a pairing sector is admitted, but it cannot activate that
sector on the current retained normal grammar. The observable-principle
obstruction theorem closes the other obvious escape hatch on `main`: the exact
scalar determinant/source-response toolkit is charge-zero and cannot force the
Majorana slot either.

The new Nambu-source theorem changes the honest blocker shape again.

On the unique local `nu_R` block:

- the current retained stack is still exhausted negatively
- but once one imposes canonical-basis closure of the local quadratic grammar,
  the minimal admissible source family is the Nambu-complete
  `span{J_x, J_y, J_z}`
- so the charge-`2` source direction is no longer missing in principle on that
  local block

So the honest claim is now:

- local Dirac suppression is exact
- the current retained stack alone is exhausted
- the branch now has one exact beyond-retained-stack source principle
- the branch now also has the positive local Nambu-complete bosonic
  observable `W_N = log ||s|| + const` on the admitted source family
- the branch now also has the exact local non-homogeneous comparator
  `Q_2 = ||s||^2` on that same family
- more sharply, the retained normal slice `J_z` gives the exact local
  background-normalized response curve
  `W_rel = (1/2) log(1 + rho^2)`, `Q_rel = rho^2`
- the branch now also has the exact local axis-exchange fixed point
  `rho = 1` on that normalized curve, with
  `W_rel = (1/2) log 2`, `Q_rel = 1`, once the selector is taken to be
  covariant under the exact canonical exchange of the retained normal and
  pairing axes on the admitted local block
- the remaining theorem gap is no longer source-direction existence or local
  finite-point selection
- it is the absolute Majorana scale / staircase embedding of that now-selected
  local point on the already-fixed
  source ray and `Z_3` texture class, together with the remaining
  three-generation amplitude closure
- more sharply, the new self-dual staircase-lift obstruction theorem closes
  the last obvious current-stack escape hatch: the exact local self-dual point
  still collapses to one positive ray, and the current `Z_3` lift remains
  homogeneous under the same rescaling, so the selected local point does not
  itself lift to an absolute staircase anchor
- more sharply, the admitted Pfaffian/Nambu source family itself now carries
  an exact no-stationary-scale theorem: its scale-sensitive bosonic generator
  is only logarithmic in the overall source scale, and its remaining exact
  class invariants are scale-invariant, so no hidden internal endpoint or
  stationary selector remains on the present source class
- the old necessity theorem was correct on the earlier logarithmic observable
  family alone, but that branch is now partly closed positively: the branch
  has both a local non-homogeneous comparator and a local background
  normalization
- the sharper remaining local boundary is now beyond the local normalized
  curve itself: the self-dual point `rho = 1` is exact on the admitted block,
  but joint positive rescaling still leaves it unchanged, so the theorem is
  still scale-relative rather than staircase-absolute
- more sharply still, the new algebraic/spectral bridge obstruction theorem
  closes the next obvious escape hatch: once the selected local self-dual ray
  and the current generation texture class are both homogeneous in the same
  positive scale, the obvious finite bridge class built from block assembly,
  Schur complements, characteristic-polynomial coefficients, eigenvalue gaps,
  singular values, and normalized spectral ratios remains homogeneous or
  scale-free, so it still cannot pick a finite absolute staircase anchor
- more sharply still, the new scalar-datum transplant obstruction theorem
  closes the obvious atlas-reuse escape hatch on the absolute-scale side:
  the current exact scalar atlas datums from the hierarchy/observable side
  are only fixed dimensionless constants, and when transplanted
  multiplicatively into the selected Majorana lane they still leave the
  common staircase scale factored out exactly, so they do not supply the
  missing absolute-scale datum either
- more sharply still, the new source-response matching obstruction theorem
  closes the obvious matching escape hatch too: even if one tries to equate
  the exact local self-dual values `W_rel = (1/2) log 2`, `Q_rel = 1` to the
  current generation-side Pfaffian/log and quadratic pairing observables,
  absolute matches depend on arbitrary representative normalization while
  relative matches fix only ratios to an arbitrary reference scale, so the
  current source-response matching class does not supply the missing
  staircase anchor either
- more sharply still, the new tensor-variational transplant obstruction
  theorem closes the strongest gravity/atlas rescue hatch too: feeding the
  current self-dual Majorana source ray into the exact direct-universal
  tensor/local-closure family still leaves the stationary field linear in the
  source and the stationary action quadratic in the source, so normalized
  stationary profiles are unchanged and no finite positive staircase selector
  emerges on that transplant class either
- more sharply still, the new partition/projective transplant obstruction
  theorem closes the strongest QG/measure rescue hatch too: feeding the
  current self-dual Majorana source ray into the exact universal partition/
  projective/refinement family still leaves the measure-compensated density
  quadratic and monotone in the source scale, while exact Schur/projective
  closure and exact refinement pullback preserve that same scale law, so no
  finite positive staircase selector emerges on that class either
- more sharply still, the new continuum-bridge transplant obstruction theorem
  closes the last obvious inverse-limit/QG loophole too: the current
  continuum bridge is only an interpretation frontier for that same exact
  compatible discrete family, and every finite-stage cylinder density on the
  self-dual Majorana ray remains quadratic and monotone in the same source
  scale, so a bare inverse-limit reinterpretation adds no finite selector
  either

More sharply:

> the branch no longer needs "some genuinely new charge-`2` source principle"
> as a vague blocker. It now has one exact candidate: the local Nambu source
> principle, and it now also has an exact local self-dual point. The live
> blocker is now sharper than "find a staircase-embedding law on the present
> stack": it is to derive a genuinely new non-homogeneous local-to-generation
> bridge or absolute-scale datum that lifts that selected local point beyond
> the current homogeneous `Z_3` texture class and beyond the now-exhausted
> obvious algebraic/spectral bridge class and beyond the now-exhausted
> current scalar-atlas datum class and beyond the now-exhausted current
> source-response matching class and beyond the now-exhausted current
> tensor-variational transplant class and beyond the now-exhausted current
> partition/projective transplant class and beyond the now-exhausted current
> continuum-bridge transplant class, then close the remaining `A/B/epsilon`
> amplitudes without fitted leftovers.
