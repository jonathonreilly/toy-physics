# Koide Higgs-Dressed Self-Energy Coherence Theorem

**Date:** 2026-04-20  
**Lane:** charged-lepton Koide, constructive transport avenue  
**Status:** exact coherence sharpening on top of the omitted-channel
self-energy theorem. This still does not derive Koide from retained framework
data alone, but it localizes the remaining transport object to one coherent
coupling vector rather than merely one self-energy matrix.  
**Runner:** `scripts/frontier_koide_higgs_dressed_self_energy_coherence_theorem.py`

## Question

The omitted-channel self-energy theorem showed that the full local transport
correction is carried by one rank-1 omitted-channel self-energy

```text
Sigma_omit(lambda) = g g† / (lambda - m_0).
```

The natural next question is:

```text
is the physical branch restored by some one arm of this correction, or by its
full coherent rank-1 structure?
```

This note answers that.

## Bottom line

It is a coherence effect.

Decompose the rank-1 omitted-channel matrix `g g†` into:

- the first-arm magnitude piece,
- the second-arm magnitude piece,
- the diagonal-only magnitude sum,
- the off-diagonal-only mixing piece,
- the full coherent rank-1 combination.

Each partial mode still has a unique nearby local Koide branch, but all of
them miss the physical origin on the negative side. Only the full coherent
rank-1 combination restores the local branch to

```text
h_0(lambda_*) = 0
```

and gives the physical slope

```text
alpha ~= 0.959212206....
```

Numerically at `lambda = lambda_*`:

- arm-1 only root: `h_0 ~= -1.58 x 10^(-3)`,
- arm-2 only root: `h_0 ~= -3.39 x 10^(-3)`,
- diagonal-only root: `h_0 ~= -8.03 x 10^(-3)`,
- off-diagonal-only root: `h_0 ~= -6.24 x 10^(-3)`,
- full coherent root: `h_0 = 0`.

So the physical transport correction is not generated by a scalar magnitude
effect. It is generated by the full coherent coupling vector `g`.

## 1. Meaning

This sharpens the live object one more step.

The previous note said:

```text
derive the omitted-channel self-energy term.
```

This note sharpens it to:

```text
derive the coherent omitted-channel coupling vector g
```

because only the full rank-1 coherence of `g g†` restores the physical local
branch.

So the remaining transport problem is no longer just "derive a self-energy."
It is "derive the coherent microscopic coupling vector whose rank-1
self-energy produces the physical branch."

## 2. Honest scope boundary

This note does **not** claim:

- a retained derivation of `g`,
- a retained derivation of `lambda_*`,
- a full retained derivation of Koide `Q = 2/3`.

It does claim a real reduction:

- no partial omitted-channel mode reproduces the physical local branch,
- the full coherent rank-1 combination does,
- the remaining microscopic object is therefore the coherent coupling vector
  itself.

That is narrower than the previous transport target.
