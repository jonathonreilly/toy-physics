# Exact Shell Projector for Star-Supported Strong-Field Sources

**Date:** 2026-04-13  
**Branch:** `codex/review-active`  
**Script:** `scripts/frontier_star_shell_projector.py`  
**Status:** Exact shell-level reduction plus bounded exterior-vacuum consequence

## Purpose

The gravity attack had already narrowed the remaining strong-field problem to
the microscopic-to-macroscopic matching step:

- the exact field admits the flux-fixed decomposition

  `phi = Q G_0 + h`

- the coarse-grained radial harmonic exterior law is already vacuum-close
- the live question is why the exact microscopic field should collapse onto the
  macroscopic exterior law outside the matching band

This note extracts the strongest shell-level matching statement currently
available on the Codex branch.

## Exact shell projector on the star support

Work on `codex/review-active` already uses exact source classes supported on

`S = {0, ±e_x, ±e_y, ±e_z}`.

For each basis source site `s in S`, let `G_s` be the lattice Green field
generated by a unit source at `s` on the same finite Dirichlet box.

The script checks the shell-mean profiles of these seven Green fields on every
available exterior shell and finds:

`<G_s>_shell = <G_0>_shell`

to machine precision for all `s in S`.

By linearity, any exact field sourced on `S` therefore satisfies

`<phi>_shell = Q K_shell`

where:

- `Q` is the total enclosed discrete charge
- `K_shell = <G_0>_shell` is the universal centered shell kernel

So, for the current exact source classes, shell-level coarse-graining is not a
fit to arbitrary source detail. It is fixed exactly by total charge.

## Exact corollary for the actual Codex source families

The script then applies this to the two exact source families already used in
the strong-field program:

1. the exact local `O_h` family
2. the broader exact finite-rank family

For both families:

- the shell averages of the exact field agree with `Q K_shell` to machine
  precision across all tested shells

That means the shell-level matching problem is already solved for the current
source support surface.

## Bounded consequence for the exterior metric

The previous coarse-grained gravity result used a radial harmonic fit

`phi_eff(r) = a / r`

to the shell data outside `R_match`.

This note shows that the fitted coefficient `a` is not being extracted from
arbitrary shell data. It is extracted from the exact universal shell kernel
`Q K_shell`.

At `R_match = 4.5`, the script finds:

### Exact local `O_h` family

- `a(field) = a(Q K_shell)` to machine precision
- direct metric residual: `7.17e-4`
- coarse exterior residual from the charge-fixed shell kernel: `4.56e-6`

### Exact finite-rank family

- `a(field) = a(Q K_shell)` to machine precision
- direct metric residual: `2.79e-2`
- coarse exterior residual from the charge-fixed shell kernel: `1.19e-5`

So the vacuum-close exterior is no longer “fit from the source details.” It is
the bounded radial asymptotic of an exact charge-fixed shell projector.

## What this closes

This closes another real ambiguity in the gravity program:

> for the exact star-supported source classes already on the branch, the
> shell-level coarse-grained exterior field is determined exactly by total
> charge and one universal shell kernel

That is a stronger statement than the earlier fitted exterior-law note.

## What this does not close

This note still does **not** close:

1. the full nonlinear 4D spacetime theorem
2. the near-source sewing rule as a theorem-grade metric statement
3. the final derivation of the continuum radial harmonic law from the universal
   shell kernel without the remaining bounded asymptotic step

## Updated gravity target

After this note, the remaining gravity problem is narrower again:

- shell-level coarse-graining is no longer the blocker for the current
  star-supported exact source classes
- the live blocker is now the metric sewing theorem:
  why and how the exact microscopic field transitions from the star-supported
  lattice source region to the macroscopic isotropic exterior metric

That is a much more specific target than “derive strong gravity.”
