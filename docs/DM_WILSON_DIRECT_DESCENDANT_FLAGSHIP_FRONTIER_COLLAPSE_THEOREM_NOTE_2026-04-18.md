# DM Wilson Direct-Descendant Flagship Frontier Collapse Theorem

**Date:** 2026-04-18  
**Status:** exact flagship-route refinement theorem after the Wilson-parent audit
and the local-Schur-note audit  
**Script:** `scripts/frontier_dm_wilson_direct_descendant_flagship_frontier_collapse_theorem_2026_04_18.py`

## Question

After auditing
[DM_WILSON_PARENT_CORRECTNESS_AUDIT_NOTE_2026-04-18.md](./DM_WILSON_PARENT_CORRECTNESS_AUDIT_NOTE_2026-04-18.md)
and
[DM_WILSON_DIRECT_DESCENDANT_LOCAL_SCHUR_SOURCE_FAMILY_THEOREM_NOTE_2026-04-18.md](./DM_WILSON_DIRECT_DESCENDANT_LOCAL_SCHUR_SOURCE_FAMILY_THEOREM_NOTE_2026-04-18.md),
is Wilson-native support provenance for the charged-lepton support `E_e` still a
separate blocker on the live flagship PMNS-assisted DM route?

Or, on the actual flagship route, has the remaining direct-descendant science
object already collapsed further than the previous
"microscopic value law plus support provenance" wording?

## Bottom line

It has collapsed further on the flagship route.

On the live PMNS-assisted DM route, current `main` already fixes the relevant
lepton supports through the PMNS projector interface:

- the lepton supports `E_nu` and `E_e` are fixed;
- no new support-selection theorem is needed for the flavored transport packet;
- once `dW_e^H` on that fixed `E_e` is known, the downstream chain to the
  selected transport column and near-closing DM value is already algorithmic.

Therefore the live flagship direct-descendant blocker is **not**:

- Wilson-native support provenance for `E_e`.

It is exactly:

- the right-sensitive microscopic value / selector law on
  `L_e = Schur_{E_e}(D_-)`,
- equivalently the retained selector law on
  `dW_e^H = Schur_{E_e}(D_-)`,
- equivalently the intrinsic `2`-real `Z_3` doublet-block point-selection law.

Wilson-native support provenance remains a real open problem only for the
stronger program that seeks a fully Wilson-native descendant theorem. It is not
an extra independent blocker on the current flagship selector gate.

## What is already exact

### 1. The PMNS interface already fixes the support data needed by the flagship route

From
[DM_LEPTOGENESIS_PMNS_PROJECTOR_INTERFACE_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_PMNS_PROJECTOR_INTERFACE_NOTE_2026-04-16.md):

- the lepton supports `E_nu` and `E_e` are fixed;
- the flavored transport packet is readable from the Hermitian pair;
- that packet does not require a new support-selection theorem.

So on the flagship PMNS-assisted route, `E_e` is already available as imported
support data. The flagship route does not wait on a separate Wilson-native
support theorem before it can state its microscopic target.

### 2. Once `dW_e^H` on fixed `E_e` is known, the downstream chain is already algorithmic

From
[DM_LEPTOGENESIS_NE_PROJECTED_SOURCE_LAW_DERIVATION_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_NE_PROJECTED_SOURCE_LAW_DERIVATION_NOTE_2026-04-16.md)
and
[DM_LEPTOGENESIS_NE_CHARGED_SOURCE_RESPONSE_REDUCTION_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_NE_CHARGED_SOURCE_RESPONSE_REDUCTION_NOTE_2026-04-16.md):

- `dW_e^H` is the exact charged-sector Schur pushforward of the microscopic
  charge-`-1` law;
- `dW_e^H` reconstructs `H_e` exactly;
- on `N_e`, `H_e` determines the transport packet;
- the exact transport selector then chooses the physical column
  algorithmically.

So the flagship path only needs the microscopic law on the fixed charged
support. It does not need a second independent support-identification theorem
to keep the downstream chain moving.

### 3. The remaining flagship gate is already identified as the right-sensitive selector law on `dW_e^H`

From
[DM_LEPTOGENESIS_PMNS_MICROSCOPIC_SELECTOR_REDUCTION_THEOREM_NOTE_2026-04-17.md](./DM_LEPTOGENESIS_PMNS_MICROSCOPIC_SELECTOR_REDUCTION_THEOREM_NOTE_2026-04-17.md):

- the remaining open microscopic object is exactly the right-sensitive selector
  law on `dW_e^H = Schur_Ee(D_-)`,
- equivalently the intrinsic `2`-real `Z_3` doublet-block point-selection law.

So the flagship gate is already scoped more sharply than the broader
Wilson-native descendant program.

### 4. The publication authority files already classify the gate that way

The publication matrix, claims table, and full claim ledger all state the same
remaining positive theorem target:

- the retained selector law for the physical source branch / point,
- equivalently the right-sensitive microscopic selector law on
  `dW_e^H = Schur_Ee(D_-)`.

That is already the manuscript-facing statement of the open flagship gate.

### 5. The Wilson-parent audit stays important, but for a stronger upstream program

From
[DM_WILSON_PARENT_CORRECTNESS_AUDIT_NOTE_2026-04-18.md](./DM_WILSON_PARENT_CORRECTNESS_AUDIT_NOTE_2026-04-18.md):

- the current Wilson-parent story should not be treated as fully correct for
  the DM route;
- a Wilson-native map into `I_e / P_e / dW_e^H` is still not proved.

That is a real open problem. But it is the open problem for the stronger
Wilson-native descendant program. It is not an extra independent blocker on the
already-PMNS-assisted flagship route, because that route already fixes the
needed charged support and already knows the downstream reduction once the
microscopic `L_e` law is supplied.

## Theorem 1: the live flagship direct-descendant frontier collapses to the right-sensitive microscopic law on `L_e`

Assume the current flagship PMNS-assisted route, including:

1. the PMNS projector interface fixing the lepton supports `E_nu` and `E_e`;
2. the exact charged-source-response reduction
   `dW_e^H = Schur_{E_e}(D_-)`;
3. the exact projected-source-law derivation and exact transport selector;
4. the microscopic selector reduction theorem.

Then the remaining direct-descendant science object on the live flagship route
is exactly:

- the right-sensitive microscopic value / selector law on
  `L_e = Schur_{E_e}(D_-)`.

Equivalently, the remaining object is exactly:

- the microscopic selector law on `dW_e^H = Schur_Ee(D_-)`,
- equivalently the intrinsic `2`-real `Z_3` doublet-block point-selection law.

Wilson-native support provenance for `E_e` is not an additional independent
flagship blocker.

### Reason

On the flagship route, `E_e` is already fixed by the PMNS interface.
With `E_e` fixed, any microscopic completion that yields the same
`L_e = Schur_{E_e}(D_-)` yields the same Hermitian response law on `E_e`, hence
the same `H_e`, the same `N_e` packet, the same selected transport column, and
the same downstream flavored DM value.

So the remaining flagship ambiguity cannot live in support provenance once the
support is already fixed on the route. The remaining ambiguity lives only in
the right-sensitive microscopic law on the descended charged-support object.

## Corollary 1: the previous "value law plus support provenance" wording was too broad for the flagship route

That broader wording is still acceptable for the stronger Wilson-native
descendant program.

But for the actual live flagship PMNS-assisted route, it is broader than
necessary. The route already imports the needed support data, so the remaining
flagship blocker is narrower:

- the right-sensitive microscopic selector law on `L_e`,
- not a separate support-provenance branch.

## Corollary 2: the next honest positive DM theorem should target `L_e` directly

The next positive theorem should therefore attack one of the following:

- an axiom-native right-sensitive value law for `L_e = Schur_{E_e}(D_-)`;
- an exact right-sensitive selector law directly on `dW_e^H`;
- a theorem-native reduction from the intrinsic `2`-real `Z_3` doublet-block
  law to that `L_e` law.

The next flagship move should not spend its main effort on Wilson-native
support provenance unless we explicitly switch goals from flagship gate closeout
to the stronger Wilson-native descendant program.

## What this closes

- the apparent fork between microscopic value law and support provenance on the
  live flagship route
- the confusion between the stronger Wilson-native descendant agenda and the
  actual manuscript-facing flagship blocker
- the risk of spending the next attack cycle on a fake support-provenance
  branch that the flagship route does not need

## What this does not close

- a Wilson-native descendant theorem from the current parent stack
- an actual right-sensitive microscopic value law for `L_e`
- the retained selector law choosing the physical source branch / point
