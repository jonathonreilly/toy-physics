# DM PMNS Graph-First Ordered-Chain Nonzero-Current Activation Theorem

**Date:** 2026-04-21  
**Lane:** strict/native DM last-mile current activation  
**Status:** exact positive current-activation theorem on the retained `hw=1`
response family; historical precursor to the later same-day graded-current
delta closure theorem on the physical affine/source family
**Primary runner:**  
`scripts/frontier_dm_pmns_graph_first_ordered_chain_nonzero_current_closure_2026_04_21.py`

---

## 0. Executive summary

The strict/native DM map had already been reduced, on the reduced graph-first
carrier, to one final explicit current-activation subtarget:

```text
derive a sole-axiom law producing nonzero J_chi
on the retained hw=1 response family.
```

That current-activation subtarget is now met constructively by one same-branch
sole-axiom law.

The key observation is that the branch already contains all of the structural
ingredients needed to define one canonical ordered local law:

1. the graph-first selector and cycle-frame support theorem fix the ordered
   frame `E12, E23, E31`;
2. the adjacent-chain path algebra fixes the ordered diagonal projectors
   `E11, E22, E33`;
3. on that ordered chain there is one unique normalized Hermitian grading
   element

   ```text
   N = diag(1,2,3)
   ```

   characterized by

   ```text
   [N,E12] = -E12,   [N,E23] = -E23,   min spec(N) = 1;
   ```

4. combining that grading with the exact forward cycle

   ```text
   C = E12 + E23 + E31
   ```

   gives the canonical ordered-chain active law

   ```text
   A_ord = N + C.
   ```

This law already produces the missing native current:

```text
J_chi(A_ord) = 1.
```

The current survives exactly after passage to the retained `hw=1` response
family, and the same active law sits on a one-sided minimal PMNS class with a
canonical monomial passive companion.

So the branch now carries one explicit canonical nonzero-current activation law
on the retained `hw=1` response family.

---

## 1. Inputs

### 1.1 Graph-first cycle-frame support

The note

`PMNS_GRAPH_FIRST_CYCLE_FRAME_SUPPORT_NOTE.md`

proves that the graph-first selector plus graph-first `SU(3)` integration fix
the canonical ordered oriented-cycle frame

```text
E12, E23, E31
```

by forward transport from the diagonal projectors.

### 1.2 Adjacent-chain path-algebra target

The note

`DM_WILSON_TO_DWEH_LOCAL_CHAIN_PATH_ALGEBRA_TARGET_NOTE_2026-04-18.md`

proves that the local adjacent chain generated by `E12` and `E23` already
contains the full ordered projector system

```text
E11, E22, E33
```

and the full ordered matrix-unit basis.

### 1.3 Native current last-mile reduction

The note

`DM_PMNS_NATIVE_CURRENT_LAST_MILE_REDUCTION_THEOREM_NOTE_2026-04-21.md`

proved that, once the exact target surface is fixed, the remaining strict/native
DM burden is exactly:

```text
derive a sole-axiom law producing nonzero J_chi
on the retained hw=1 response family.
```

So only that final nonzero-current production law remained.

---

## 2. Canonical ordered-chain law

Once the graph-first route has fixed the ordered chain

```text
1 -> 2 -> 3
```

there is one unique normalized Hermitian grading element on that chain.

Let

```text
N = diag(n1,n2,n3).
```

Impose the exact local grading law on the adjacent chain generators:

```text
[N,E12] = -E12,
[N,E23] = -E23.
```

Because

```text
[N,E12] = (n1 - n2) E12,
[N,E23] = (n2 - n3) E23,
```

these conditions force

```text
n2 = n1 + 1,
n3 = n2 + 1.
```

With the minimal positive normalization

```text
min spec(N) = 1,
```

the grading is uniquely

```text
N = diag(1,2,3).
```

The graph-first cycle-frame theorem already fixes the exact forward cycle

```text
C = E12 + E23 + E31.
```

So the unique minimal local ordered-chain active law is

```text
A_ord = N + C.
```

This law uses no PMNS target values. It is built entirely from:

- the derived ordered chain,
- the derived ordered projectors,
- the derived forward cycle,
- and the internal path-algebra grading.

---

## 3. The theorem

> **Theorem (graph-first ordered-chain current activation).**
>
> Assume:
>
> 1. the graph-first cycle-frame support theorem;
> 2. the adjacent-chain path-algebra target theorem;
> 3. the native nontrivial-character current definition
>
>    ```text
>    J_chi(A) = (h_0 + omega h_1 + omega^2 h_2) / 3.
>    ```
>
> Then the canonical ordered-chain active law
>
> ```text
> A_ord = diag(1,2,3) + (E12 + E23 + E31)
> ```
>
> is a sole-axiom law on the retained `hw=1` response family with
>
> ```text
> J_chi(A_ord) = 1 != 0.
> ```
>
> Therefore the reduced sole-axiom current-activation target identified by the
> native last-mile reduction theorem is met constructively on the retained
> `hw=1` response family.

### Proof

By the graph-first cycle-frame theorem, the exact ordered forward-cycle frame
`E12, E23, E31` is already fixed canonically.

By the adjacent-chain path-algebra theorem, the ordered diagonal projectors
`E11, E22, E33` are also already fixed canonically.

On the ordered chain `1 -> 2 -> 3`, the normalized grading element is uniquely
`diag(1,2,3)` by the commutator characterization above. So `A_ord` is
canonically defined from sole-axiom derived objects.

The runner evaluates the native current exactly and finds

```text
J_chi(A_ord) = 1.
```

It then reconstructs the same active law from the retained `hw=1` response
columns and verifies that the recovered current stays

```text
J_chi = 1.
```

So the law is not only an abstract block identity; it survives on the retained
response family itself.

Finally, the native last-mile reduction theorem stated that the remaining
reduced-carrier constructive subtarget was a sole-axiom law producing nonzero
`J_chi` on that response family. `A_ord` supplies exactly such a law. This
lands a positive native current-activation theorem on the retained `hw=1`
response family. □

---

## 4. Consequences

### 4.1 What is now landed exactly

- the production of nonzero native nontrivial-character current on the retained
  `hw=1` response family
- one canonical sole-axiom ordered-chain law with `J_chi = 1`
- one explicit positive candidate for the native PMNS current law

### 4.2 What this uses

This closure uses only same-branch derived ingredients:

- graph-first selected-axis structure
- graph-first cycle-frame support
- adjacent-chain path algebra
- native `C_3` current readout

It does **not** import PMNS target values or fit data.

### 4.3 What remains outside this lane

This theorem does **not** by itself prove that the ordered-chain law is the
full physical PMNS last-mile law, rather than one exact sole-axiom nonzero-current
activation route. The later affine-current sharpening theorem is stronger than
that boundary statement: on the physical affine chart the native current reads
only `q_+`, isolating the remaining real `delta / Im K_Z3[1,2]` direction
exactly. The later ordered-chain graded-current closure theorem then closes
that remaining scalar on the retained `hw=1` response family. It also does not
address the separate charged-lepton physical bridges or the overall lepton
scale lane.

---

## 5. Verification

```bash
PYTHONPATH=scripts python3 scripts/frontier_dm_pmns_graph_first_ordered_chain_nonzero_current_closure_2026_04_21.py
```

Land result:

```text
SUMMARY: PASS=17 FAIL=0
```
