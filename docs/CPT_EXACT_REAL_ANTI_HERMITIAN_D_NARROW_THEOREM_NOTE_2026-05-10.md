# CPT Exact — Real Anti-Hermitian D Narrow Theorem

**Date:** 2026-05-10
**Type:** bounded_theorem
**Claim scope:** the standalone algebraic implication that, given a real
anti-Hermitian staggered hopping operator `D` on an even-period cubic
lattice with three discrete operators `(C, P, T)` defined as in the
parent CPT note — `C` the sublattice parity `(-1)^{x_1+x_2+x_3}`,
`P` the spatial inversion `x -> -x mod L`, `T` complex conjugation —
the composite operator `Theta = C P T` satisfies the exact algebraic
identity `Theta D Theta^{-1} = D` (equivalently, on the Hermitian lift
`H = i D`, `[Theta_H, H] = 0` with `Theta_H = P K` the antiunitary
representative). This is purely a statement about discrete-symmetry
algebra on a real anti-Hermitian operator. No "physical Hamiltonian
identification" framing is claimed, no Hermitian-lift bridge is
imported, no SME-coefficient extraction is claimed, no continuum CPT
theorem is invoked.

**Status authority:** independent audit lane only. This source note
does not set or predict an audit outcome; later status is generated by
the audit pipeline after independent review.

**Runner:** [`scripts/audit_companion_cpt_exact_real_anti_hermitian_d_exact_2026_05_10.py`](../scripts/audit_companion_cpt_exact_real_anti_hermitian_d_exact_2026_05_10.py)

**Authority role:** Pattern A narrow rescope of the load-bearing
algebraic core of the parent `cpt_exact_note` row, isolating the real
anti-Hermitian `D` discrete-symmetry algebra from the
Hermitian-Hamiltonian bridge and SME-extraction content of the parent.

## Statement

Let `D` be a real anti-Hermitian operator on a complex Hilbert space
`V = C^N` (i.e. `D^T = D` componentwise real-valued and `D^* = -D`,
where `D^*` denotes complex conjugation of matrix entries; equivalently
`D` has real antisymmetric matrix entries). Let `C, P, T` be three
operators on `V` satisfying:

```text
C real, diagonal, involutory   (C^T = C, C real, C^2 = I)
P real, permutation, involutory (P^T = P^{-1} = P, P^2 = I)
T = K complex conjugation       (T |v> = |v^*>, T^2 = I, T linear-conjugate)
```

and the action identities

```text
C D C = -D        (anti-Hermitian conjugation through C)         (1)
P D P = -D        (anti-Hermitian conjugation through P)         (2)
T D T = D         (D real => complex conjugation acts trivially) (3)
```

Define `Theta := C P T`. Then:

**(C1) Composite invariance.** `Theta D Theta^{-1} = D`.

**(C2) Hermitian-lift commutator.** On the Hermitian lift `H := i D`
with `Theta_H := P K` (the antiunitary representative absorbing the
`C`-factor sign into the `i -> -i` step of complex conjugation), the
algebraic identity `[Theta_H, H] = 0` holds exactly.

**(C3) Antiunitary square is a scalar.** `Theta^2` acts as a scalar
`s I` on `V` with `s = ±1`. The sign `s` depends on the commutation
properties of `C` and `P`: when `[C, P] = 0` on `V`, `s = +1`; when
`{C, P} = 0`, `s = -1`. The sign is not load-bearing for (C1) or (C2)
and is determined separately by the (C, P)-algebra on the framework
instance.

The framework-specific instance — staggered Cl(3) hopping operator
`D = sum_link i eta(x,mu) (delta_{x+mu,y} - delta_{x,y+mu})` on
`Z^3 mod L` with even `L`, `C(x) = (-1)^{x_1+x_2+x_3}`,
`P : x -> -x mod L` — satisfies (1), (2), (3) and therefore inherits
(C1), (C2), (C3). This special case is shown for sanity in the runner
at `L = 4`; the algebra also closes for any other operator satisfying
the abstract premises.

## Proof

Pure algebraic substitution.

**(C1).** Substitute into `Theta D Theta^{-1}`:

```text
Theta D Theta^{-1}
  = (C P T) D (C P T)^{-1}
  = C P T D T^{-1} P^{-1} C^{-1}
  = C P (T D T) P C                              (T^2 = I, P, C involutory)
  = C P D P C                                    (by (3): T D T = D)
  = C (-D) C                                     (by (2): P D P = -D)
  = -C D C
  = -(-D)                                        (by (1): C D C = -D)
  = D.
```

**(C2).** Write `H = i D`. The antiunitary representative for the
combined CPT action on `H` is `Theta_H = P K`, where `K` is complex
conjugation. To see this, note that on the staggered hopping operator
the sublattice parity `C` acts as `-1` on `H` independently of its
action on `D` (because `H = i D` and the `i` carries a `-1` under
complex conjugation that cancels the `C`-sign):

```text
Theta_H H Theta_H^{-1}  =  P K (i D) K^{-1} P^{-1}
                       =  P (-i) (K D K^{-1}) P                (K i K^{-1} = -i)
                       =  -i P (T D T) P                       (K acts as T on D)
                       =  -i P D P                             (by (3))
                       =  -i (-D)                              (by (2))
                       =  i D
                       =  H.
```

Hence `Theta_H H = H Theta_H`, i.e. `[Theta_H, H] = 0`.

**(C3).** `Theta^2 = (C P T)^2 = C P T C P T = C P C P T^2 = C P C P` (the
intermediate step uses that `C` and `P` are real-valued operators, so
`T C = C T` and `T P = P T`, and `T^2 = I`). Hence `Theta^2 = (CP)^2`.
If `[C, P] = 0` then `(CP)^2 = C^2 P^2 = I`, so `s = +1`. If
`{C, P} = 0` (i.e. `CP = -PC`) then `(CP)^2 = C P C P = -C C P P = -I`,
so `s = -1`. In both cases `Theta^2` is a scalar, which is what is
needed for (C1) and (C2) to make sense as a discrete symmetry of `D`.

∎

## Derivable corollaries

**(R1) C-only and P-only break.** From (1) and (2), `C` alone and `P`
alone send `D -> -D`; neither is a symmetry of `D`. The combination
`Theta = C P T` is.

**(R2) Spectrum reality.** From (3), `T D T = D`. Hence the spectrum
of `D` is closed under complex conjugation: if `lambda` is an
eigenvalue with eigenvector `v`, then `-lambda^*` is an eigenvalue
with eigenvector `T v` (using anti-Hermiticity `D^dagger = -D` and
`T linear-conjugate`). For purely imaginary eigenvalues `lambda = i mu`
with `mu real`, this gives `mu -> mu`, consistent with real
anti-Hermitian spectrum.

Both follow algebraically from the premise identities.

## What this claims

- The composite algebraic identity (C1) `Theta D Theta^{-1} = D`,
  **conditional on** the three premise identities (1)-(3) and the
  abstract operator types of `C, P, T`.
- The Hermitian-lift commutator (C2) `[Theta_H, H] = 0` on `H = iD`
  with `Theta_H = P K`, derived by the antiunitary `i -> -i` step.
- The antiunitary square (C3) `Theta^2 = I`.
- Two derivable corollaries.

## What this does NOT claim

- Does **not** derive the premise identities (1)-(3) themselves. (1)
  is the sublattice-parity anticommutation with the staggered link
  variable; (2) is the spatial-inversion anticommutation; (3) is the
  staggered phases being real. Their derivation on the specific
  Cl(3)-staggered framework is a separate algebraic content carried
  by the parent `cpt_exact_note` row and its primary runner
  `scripts/frontier_cpt_exact.py`.
- Does **not** identify `D` with any physical Hamiltonian. `H = iD`
  is treated as a Hermitian lift on the algebraic content; the
  Hermitian-Hamiltonian bridge note is downstream and is **not**
  imported.
- Does **not** claim any SME-coefficient extraction. The CPT-odd
  decomposition `H^{odd}_a = ...` is downstream content and is **not**
  imported here.
- Does **not** invoke the continuum CPT theorem (Jost 1957, Streater-
  Wightman). The lattice algebraic statement is self-contained on the
  premise identities.
- Does **not** claim CP violation from CKM phases. Free-field
  algebra only.

## Relation to the parent CPT exact note

The parent `cpt_exact_note` bundles the algebraic identity above with
several distinct downstream items:

1. The premise identities (1)-(3) themselves on the specific
   staggered Cl(3) framework.
2. The Hermitian-Hamiltonian bridge note's antiunitary `i -> -i`
   handling.
3. SME-coefficient extraction `a_mu = 0` and Frobenius-norm
   `||H^{odd}|| = 0` numerical exhibits on `L = 4, 6, 8`.
4. The pure algebraic substitution (C1)-(C3) on abstract `D, C, P, T`
   satisfying (1)-(3).

This narrow theorem isolates item 4 from items 1-3. The premise
identities (1)-(3) enter as hypotheses; no Hermitian-lift bridge is
imported; no SME extraction is claimed. The algebraic substitution
can be reviewed independently of any framework-specific upstream and
of the bridge note's antiunitary step.

## Open derivation gap

Out of scope of this narrow theorem (downstream items, to be carried
by separate authorities):

- Derivation of premise identities (1)-(3) on the specific Cl(3)
  staggered framework — parent `cpt_exact_note` content.
- Hermitian-Hamiltonian bridge `H = i D` — separate bridge note.
- SME coefficient extraction — separate bridge note.
- Continuum CPT theorem connection — out of lattice scope.
- Extension to interacting theory (gauge fields, Yukawa couplings).

## Cited dependencies

None. This narrow note has zero ledger dependencies because it states
only algebraic substitution on abstract operators `D, C, P, T`
satisfying the three premise identities (1)-(3) plus the abstract
operator-type axioms.

## Forbidden imports check

- No PDG observed values consumed.
- No literature numerical comparators consumed.
- No fitted selectors consumed.
- No admitted unit conventions load-bearing on the claim.
- No same-surface family arguments.

## Validation

Primary runner: [`scripts/audit_companion_cpt_exact_real_anti_hermitian_d_exact_2026_05_10.py`](../scripts/audit_companion_cpt_exact_real_anti_hermitian_d_exact_2026_05_10.py)
verifies, at exact sympy precision:

1. Each of (C1), (C2), (C3) reduces by `sympy.simplify` to the claimed
   identity on a small abstract operator instance and on a
   framework-specific 8-site (`L = 4`) staggered hopping model.
2. The two corollaries follow.
3. The framework-specific staggered Cl(3) operator at `L = 4`
   satisfies premises (1)-(3) and therefore inherits (C1)-(C3).
4. Counterfactual probe: a generic complex (not real) anti-Hermitian
   operator does **not** satisfy (3), breaking the composite
   invariance — confirming `D` real is a load-bearing premise.

## Cross-references

- `cpt_exact_note` — parent broad note that supplies the premise
  identities (1)-(3) on the specific framework and bundles the
  Hermitian-lift + SME-extraction content.
- `physical_hermitian_hamiltonian_and_sme_bridge_note_2026-04-30` —
  separate bridge note carrying the antiunitary step `H = iD`.
- `observable_principle_from_axiom_note` — downstream consumer of the
  "real anti-Hermitian D" structural fact in Move 2 of the
  observable-principle retention chain.
