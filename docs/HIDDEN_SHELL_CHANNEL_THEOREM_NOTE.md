# Exact Hidden-Shell Channel Theorem on the `3+1` Plaquette Surface

**Date:** 2026-04-16  
**Status:** exact constructive local hidden-sector theorem  
**Script:** `scripts/frontier_hidden_shell_channel_theorem.py`

## Question

After the quotient-surface transfer no-go, is the remaining hidden filling
sector completely opaque, or does it reduce to a finite exact local alphabet?

## Exact answer

It reduces to a finite exact local alphabet.

In the first nontrivial quotient-transfer sector at rooted size `n = 5`, every
duplicate quotient class is:

1. one unit `4`-cube boundary move
2. plus one shared exterior `3`-cell

Under the full symmetry group of the unit `4`-cube, that local shell reduces to

- `6` exact unordered shell orbits
- `12` exact ordered shell orbits

So the hidden sector is real, but it is not amorphous.

## Theorem 1: every `n = 5` duplicate class is a one-cube shell

For every same-quotient duplicate pair at `n = 5`:

- the two rooted fillings have size `5`
- their symmetric difference is exactly one unit `4`-cube boundary
- each representative contains exactly `4` of the `8` cube facets
- the two representatives share exactly one exterior `3`-cell outside the
  witness cube

So the entire first hidden quotient sector is generated by a local

`4`-cube boundary + exterior cell

shell.

## Theorem 2: exact unordered shell alphabet

Canonicalize the local shell under the full symmetry group of the unit `4`-cube
and forget which of the two complementary representatives is listed first.

Then the `2848` exact duplicate quotient classes reduce to exactly `6`
unordered shell orbits.

Their exact continuation-channel multiplicities are:

- `(47, 49)`: `512`
- `(49, 50)`: `576`
- `(49, 51)`: `864`
- `(49, 52)`: `576`
- `(51, 53)`: `320`

So the entire `n = 5` hidden quotient sector feeds only `5` distinct scalar
continuation pairs.

The pair `(49, 51)` is realized by two distinct unordered shell orbits, so the
unordered local alphabet has `6` states but only `5` scalar count channels.

## Theorem 3: exact ordered shell alphabet

Now keep track of which complementary representative is occupied.

Then the local shell reduces to exactly `12` ordered orbits under unit `4`-cube
symmetry.

Their next quotient continuation counts are distributed as:

- count `47`: `1` ordered orbit
- count `49`: `5` ordered orbits
- count `50`: `1` ordered orbit
- count `51`: `3` ordered orbits
- count `52`: `1` ordered orbit
- count `53`: `1` ordered orbit

The exact ordered-orbit multiplicities are:

- `576`: `6` orbits
- `512`: `2` orbits
- `320`: `2` orbits
- `288`: `2` orbits

So the first hidden sector already admits a finite ordered state alphabet.

### Stronger form: exact one-step transfer histograms

The ordered alphabet carries more than a scalar continuation count.

For each of the `12` ordered shell orbits, the runner computes the full one-step
quotient `|dV|` histogram. Those histograms are all distinct.

So the first hidden quotient layer already has a finite exact transfer kernel:

> `12` ordered shell states, each with its own exact one-step quotient
> boundary-size histogram.

This is the first positive rooted reduction result on the branch:

> the first hidden quotient layer collapses to a finite local `12`-state
> ordered shell alphabet.

The correct propagated statement is now recorded in
`docs/HIDDEN_TWO_SHELL_PROPAGATION_THEOREM_NOTE.md`:

> that one-shell alphabet does not stay closed as a final Markov state. Its
> one-step rooted image is still local, but it lifts to an exact two-shell
> sector.

## Canonical representatives

The runner prints canonical representatives for all `6` unordered and all `12`
ordered shell orbits.

These are exact normal forms for the first hidden quotient sector.

## Why this matters

This is the first constructive answer to the quotient-transfer no-go.

That no-go said:

> the quotient surface key is the right counting object, but it is not an exact
> rooted transfer state.

This theorem says:

> the missing extra state is not arbitrary. At the first hidden quotient layer
> it collapses to a finite local cube-shell alphabet.

So the next viable enriched rooted program is no longer vague. It has a concrete
candidate state space:

1. quotient surface key
2. local hidden shell channel

What happens beyond `n = 5` is no longer opaque. The next exact note proves the
first propagated hidden layer is an exact two-shell sector rather than an
arbitrary memory term.

## Honest status

This note does **not** derive analytic `P(6)`.

It does something more constructive than the prior no-gos:

1. it isolates the first hidden quotient sector exactly
2. it proves that sector has a finite local channel decomposition
3. it gives explicit canonical representatives for those channels

That is the cleanest constructive bridge yet from “quotienting is necessary”
to “what extra rooted state might actually work.”

## Commands run

```bash
python3 scripts/frontier_hidden_shell_channel_theorem.py
```

Output summary:

- exact `n = 5` duplicate quotient class count: `2848`
- exact unordered shell orbit count: `6`
- exact ordered shell orbit count: `12`
- exact unordered continuation-channel histogram:
  - `(47,49): 512`
  - `(49,50): 576`
  - `(49,51): 864`
  - `(49,52): 576`
  - `(51,53): 320`
