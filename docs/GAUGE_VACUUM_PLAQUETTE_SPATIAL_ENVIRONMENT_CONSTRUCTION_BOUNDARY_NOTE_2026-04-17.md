# Gauge-Vacuum Plaquette Spatial-Environment Construction Boundary

**Date:** 2026-04-17  
**Status:** exact constructive boundary on the plaquette PF lane; after the
transfer realization and the new underdetermination theorem, the earliest
missing exact datum is the explicit one-step spatial kernel `K_6^env` together
with the rim map to `eta_6`  
**Script:** `scripts/frontier_gauge_vacuum_plaquette_spatial_environment_construction_boundary.py`

## Question

If the live plaquette PF gap is now the explicit spatial-environment pair
`S_6^env / eta_6`, what exact object must actually be derived first to
construct that pair?

## Answer

The earliest missing constructive datum is one level lower:

- the explicit orthogonal-slice one-step kernel `K_6^env(U',U)`,
- together with the exact rim boundary map producing `eta_6`.

That is the real next construction target.

The current stack already proves that integrating the unmarked spatial Wilson
environment between adjacent orthogonal slices defines one exact positive
self-adjoint kernel `K_beta^env(U_(k+1),U_k)` and therefore one transfer object
`S_beta^env`. It also proves that the environment coefficients are normalized
boundary amplitudes generated from that transfer object and one positive
conjugation-symmetric rim state `eta_beta`.

So once `K_6^env` and `eta_6` are explicit, the rest follows:

- the class-sector transfer object `S_6^env`,
- the coefficients `rho_(p,q)(6)`,
- and the resulting plaquette Perron/Jacobi data.

But the new spatial-environment underdetermination theorem shows that the
current exact stack still does **not** force that kernel/rim pair uniquely.
Distinct admissible kernel / rim-state pairs can still induce different
boundary data and different plaquette PF data.

## Setup

From the exact spatial-environment transfer theorem already on `main`:

- integrating the Wilson weight between adjacent orthogonal slices defines one
  exact kernel `K_beta^env(U_(k+1), U_k)`,
- that kernel defines one positive self-adjoint transfer object `S_beta^env`,
- and the boundary coefficients are exact amplitudes generated by
  `S_beta^env` and `eta_beta`.

From the exact residual-environment identification theorem:

- once the marked half-slice multiplier and the local Wilson factor are
  stripped, the only remaining open plaquette datum is the environment object.

From the exact spatial-environment transfer underdetermination theorem:

- even after the transfer realization and the Wilson parent/compression
  theorem, distinct admissible positive `S_6^env / eta_6` pairs can still
  induce different `rho_(p,q)(6)` and different plaquette PF data.

## Theorem 1: explicit kernel plus rim state is sufficient to determine the live environment coefficients

Fix the exact one-step kernel `K_6^env` and the exact rim boundary state
`eta_6`.

Then the orthogonal-slice transfer object is explicit, and the environment
coefficients are fixed by the exact boundary-amplitude law:

`rho_(p,q)(6)
 = <chi_(p,q), (S_6^env)^(L_perp-1) eta_6>
   / <chi_(0,0), (S_6^env)^(L_perp-1) eta_6>`.

So the current constructive target is not an abstract family of positive
diagonal operators. It is the explicit kernel/rim pair that generates the
already-identified transfer law.

## Theorem 2: the current exact stack still does not force that kernel/rim pair

The runner exhibits two distinct admissible positive self-adjoint
conjugation-symmetric one-step kernels together with two admissible rim states.

Both lie inside the current exact structural surface.

But they induce different normalized boundary character data and different
plaquette PF data for the same explicit source operator `J` and the same exact
local Wilson factor `D_6^loc`.

So the current exact stack still does **not** determine unique explicit
`K_6^env / eta_6` data.

## Corollary 1: the next derivation target is now exact and constructive

The live plaquette PF target is now narrower than
“construct the full residual environment” and also more primitive than
“identify the whole transfer object `S_6^env`”.

It is exactly:

- derive the one-step orthogonal-slice kernel `K_6^env(U',U)`,
- derive the exact rim boundary map to `eta_6`,
- then compress to the marked class sector.

That is the next constructive seam on this PF branch.

## What this closes

- exact identification of the earliest missing constructive datum on the
  plaquette PF lane
- exact clarification that the open object is now kernel/rim data, not a
  generic residual operator
- exact separation between the transfer realization already proved and the
  explicit kernel construction still missing

## What this does not close

- explicit `K_6^env`
- explicit `eta_6`
- explicit `S_6^env`
- explicit coefficients `rho_(p,q)(6)`
- explicit framework-point plaquette Perron data
- a global sole-axiom PF selector

## Command

```bash
python3 scripts/frontier_gauge_vacuum_plaquette_spatial_environment_construction_boundary.py
```
