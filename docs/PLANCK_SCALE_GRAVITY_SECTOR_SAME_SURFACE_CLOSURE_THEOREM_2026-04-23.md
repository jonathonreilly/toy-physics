# Planck-Scale Gravity-Sector Same-Surface Closure Theorem

**Date:** 2026-04-23
**Status:** final hardening of the GSI / physical-carrier step
**Verifier:** `scripts/frontier_planck_gravity_sector_same_surface_closure_theorem.py`

## Question

Can Gravity-Sector Identification be tightened so it is not read as a free
choice of the Planck carrier?

## Result

Yes, on the accepted physical-gravity review contract.

The direct-universal gravity stack already supplies a canonical local
gravitational boundary/action sector on the same physical `Cl(3)` / `Z^3`
package:

- exact positive-background local boundary-action family on `PL S^3 x R`;
- exact Lorentzian Einstein/Regge stationary action family;
- canonical textbook Einstein-Hilbert-style geometric/action equivalence on the
  chosen smooth realization.

The Planck packet supplies the unique source-free primitive codimension-1
one-step incidence carrier on the same physical boundary surface:

`N_cell = P_A = 1_(|eta| = 1)`.

If the theory has one gravitational boundary/action sector on one physical
boundary surface, then the local primitive representative of that sector must be
that unique source-free codimension-1 carrier. Thus GSI is not a coefficient
choice. It is the same-surface compatibility condition between the already
accepted gravity/action sector and the already forced primitive boundary source.

The remaining possible rejection is no longer:

> maybe the coefficient is being fit.

It is:

> reject the same-surface single-gravity-sector review contract.

That is a rejection of the submitted physical theory surface, not a remaining
algebraic or numerical gap in the Planck derivation.

## Inputs

1. **Physical lattice.** The `Cl(3)` / `Z^3` substrate is physical, not a
   disposable regulator surface.
2. **Canonical gravity sector.** The direct-universal gravity route supplies a
   canonical local gravitational boundary/action sector on the chosen
   `PL S^3 x R` realization.
3. **Same physical boundary surface.** The Planck primitive cell and the local
   gravity/action boundary cell are read on the same physical codimension-1
   boundary section.
4. **Single-sector semantics.** One physical boundary surface does not carry two
   inequivalent source-free local carriers for the same leading gravitational
   area/action density.
5. **Primitive source-free locality.** The microscopic leading boundary
   representative is local, source-free, additive, and codimension-1.

None of these inputs contains `1/4`, `nu = 5/4`, `a = l_P`, or a fitted
multiplier.

## Theorem 1: the gravity route supplies the target sector

The branch is not trying to infer gravity from a bare counting number. The
accepted gravity stack already provides the target physical sector:

`S_grav / k_B = A c_light^3 / (4 G hbar) = A / (4 l_P^2)`.

The relevant question is therefore not whether a primitive count can invent
gravity. The question is which microscopic local source on the same physical
boundary surface represents the already accepted gravitational boundary/action
sector.

## Theorem 2: the Planck packet supplies a unique source-free primitive boundary source

On the time-locked primitive event cell, the exact one-step codimension-1
incidence source is

`P_A = P_t + P_x + P_y + P_z`.

The section-canonical worldtube theorem and the worldtube-to-boundary counting
theorem already force this as the unique time-complete, spatially isotropic,
primitive unit-incidence count on the retained event frame.

Alternative objects are either:

- not primitive one-step incidence counts;
- not source-free;
- not same-surface local boundary carriers;
- quotient carriers that discard retained multiplicity;
- scalar/free-energy or curvature-density object classes rather than primitive
  incidence-source classes.

Thus the primitive local source is unique before any numerical coefficient is
evaluated.

## Theorem 3: same-surface single-sector compatibility forces GSI

Assume a single physical boundary surface with one gravitational boundary/action
sector.

Let `N_grav` be its microscopic source-free local representative at the primitive
cell scale. By primitive source-free locality, `N_grav` must be a local additive
codimension-1 primitive boundary source. By Theorem 2, the only such source on
the retained Planck packet is `P_A`.

Therefore

`N_grav = P_A`.

This is exactly Gravity-Sector Identification in operator form.

## Corollary: Planck normalization

The source-free default state gives

`rho_cell = I_16 / 16`.

Therefore

`c_cell = Tr(rho_cell P_A) = 4/16 = 1/4`.

The standard gravitational area/action normalization gives

`S_grav / k_B = A / (4 l_P^2)`.

The microscopic same-surface density is

`S_cell / k_B = c_cell A / a^2`.

Equating the same physical density:

`c_cell / a^2 = 1 / (4 l_P^2)`.

Substitute `c_cell = 1/4`:

`a^2 = l_P^2`,

so

`a = l_P`.

## What A Reviewer Can Still Reject

A reviewer can still reject one of the review-contract inputs:

1. the physical-lattice reading;
2. the accepted canonical gravity/action stack;
3. same-surface reading of the Planck and gravity boundary cells;
4. single-sector semantics for the gravitational boundary/action density;
5. source-free primitive locality for the microscopic boundary representative.

But those are not hidden coefficient choices. Rejecting any of them changes the
physical theory being reviewed. Inside that theory surface, the Planck
normalization is closed.

## Safe Claim

Use:

> On the accepted physical-gravity review contract, GSI follows from
> same-surface single-sector compatibility: the already accepted gravitational
> boundary/action sector has only one source-free primitive local representative
> on the Planck cell, namely `P_A`. This closes `a = l_P`.

Do not use:

> Bare cell algebra alone proves gravity exists.

Do not use:

> The coefficient was chosen because it gives Planck.
