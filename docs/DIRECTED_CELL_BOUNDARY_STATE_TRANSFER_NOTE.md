# Exact Directed-Cell Boundary-State Transfer Obstruction on the `3+1` Plaquette Surface

**Date:** 2026-04-16  
**Status:** exact obstruction theorem  
**Script:** `scripts/frontier_directed_cell_boundary_state_transfer.py`

## Question

Does the exact local directed-cell theorem automatically give an exact transfer
for the full rooted same-boundary gauge problem?

## Exact answer

No.

A transfer that only grows across **current boundary faces** is not exact.

The full rooted problem already contains:

1. **internal-face-return sectors**
2. a genuine **`k = 3` root-launch sector**

by `n = 3`.

So the exact local directed theorem is real, but by itself it does not close the
global rooted transfer.

## Theorem 1: boundary-shellable growth undercounts the exact rooted problem

Compare two growth rules for connected rooted `3`-chains:

1. **boundary-shellable growth**
   - add a new cell only across a current boundary face
2. **exact connected growth**
   - add a new cell across any plaquette face shared with the current cluster

Then the exact rooted counts through `n = 5` are:

- `n = 1`: `4`
- `n = 2`: `60`
- `n = 3`: `1164`
- `n = 4`: `25000`
- `n = 5`: `566644`

while the boundary-shellable counts are:

- `n = 1`: `4`
- `n = 2`: `60`
- `n = 3`: `1100`
- `n = 4`: `22660`
- `n = 5`: `495520`

So the exact shellable undercount is:

- `n = 3`: `64`
- `n = 4`: `2340`
- `n = 5`: `71124`

Therefore boundary-shellable transfer is not the exact rooted transfer.

## Theorem 2: the first exact multi-launch sector appears already at `n = 3`

Let `k` be the number of occupied `3`-cells incident to the tagged root face
`q`.

Then the exact root-launch sector decomposition is:

- `n = 1`: `{1: 4}`
- `n = 2`: `{1: 60}`
- `n = 3`: `{1: 1160, 3: 4}`
- `n = 4`: `{1: 24852, 3: 148}`
- `n = 5`: `{1: 562352, 3: 4292}`

So the exact single-launch directed sector is **not** the whole rooted problem
even at `n = 3`.

The first `k = 3` rooted sector appears immediately once exact connected growth
is used.

## Corollary: what the local directed theorem does and does not close

The local directed-cell theorem closes the exact finite local boundary problem.

It does **not** imply that the full rooted problem is generated by repeatedly
launching one directed root cell across the current boundary.

Two exact obstructions remain:

1. cells can attach across a face that is temporarily internal
2. the root face itself has a three-launch sector

So a full honest closure has to contain more than a single directed-cell
transfer.

## Why this matters for analytic `P(6)`

This note rules out a tempting but false inference:

> finite exact local directed-cell theorem => finite exact global transfer

That implication is wrong.

So if analytic `P(6)` is ever closed, the closure has to explicitly account for:

- local directed-cell couplings
- root-face odd-parity multi-launch
- internal-face-return sectors
- more than one-shell local boundary-face data

This is the real state of the gauge-vacuum program after the local theorem.

## Honest status

This note does **not** derive analytic `P(6)`.

It closes the exact next obstruction:

1. why a current-boundary transfer is not exact
2. where the first exact missing sectors enter

That is a genuine theory-grade correction to the previous rooted-engine lane.

The next stronger exact no-go is now recorded in
`docs/ONE_SHELL_FACE_STATE_TRANSFER_NO_GO_NOTE.md`, which proves that even the
full multiset of exact one-shell local boundary-face states still does not
determine the next rooted continuation count.

## Commands run

```bash
python3 scripts/frontier_directed_cell_boundary_state_transfer.py
```

Output summary:

- corrected exact rooted counts through `n = 5`
- exact boundary-shellable undercount through `n = 5`
- exact first `k = 3` root-launch sector at `n = 3`
