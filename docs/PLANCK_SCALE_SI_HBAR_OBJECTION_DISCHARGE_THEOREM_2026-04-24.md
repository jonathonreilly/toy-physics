# Planck-Scale SI Hbar Objection Discharge Theorem

**Date:** 2026-04-24
**Status:** discharges the SI-`hbar` objection as a unit-convention non-claim
**Verifier:** `scripts/frontier_planck_si_hbar_objection_discharge_theorem_2026_04_24.py`

## Question

Does the branch fail as a Planck derivation because it does not predict the SI
decimal value of `hbar`?

## Result

No.

The branch must not claim to predict the SI decimal value of `hbar`, because
that is a dimensionful unit-convention statement. The physical target is the
dimensionless relation

`a^2 / l_P^2 = a^2 c_light^3 / (hbar G) = 1`

and the reduced action-count statement

`Phi(I_16) = gamma = 1`.

Those are the invariant statements. The SI number assigned to `hbar` depends on
the chosen action unit. Rescaling the action unit rescales the numerical value
of every action, including `hbar`, while leaving dimensionless ratios
unchanged.

Thus the SI-`hbar` objection is discharged as follows:

1. If the reviewer asks for the SI decimal value of `hbar`, the request is not
   a physical prediction target.
2. If the reviewer asks for a structural action unit, the branch supplies
   `gamma=1` as primitive integral action count.
3. If the reviewer asks for the Planck length relation, the branch supplies
   `a^2/l_P^2 = 1` on the canonical realified gravity response surface with the
   standard gravitational area/action normalization.

## Theorem 1: dimensionful constants are not invariant predictions

Let `U_A` be an action unit and let the numerical value of a dimensionful
action `S` be

`[S]_{U_A} = S / U_A`.

If the unit is rescaled to

`U_A' = lambda U_A`,

then

`[S]_{U_A'} = [S]_{U_A} / lambda`.

So a claimed prediction of the bare decimal number `[hbar]_{U_A}` is not
coordinate-free. It changes when the unit of action is changed.

Only dimensionless ratios such as

`S / hbar`

or

`a^2 c_light^3 / (hbar G)`

are invariant physical targets.

## Theorem 2: the branch closes the reduced action target

The primitive phase trace theorem gives

`Phi(P) = gamma Tr(P)/16`.

The primitive integral action-count theorem gives

`Phi(I_16) = 1`.

Therefore

`gamma = 1`.

This is the structural action-unit result available inside the theory. It is a
reduced action count, not a joule-second decimal.

## Theorem 3: the Planck ratio is the correct hbar-containing statement

The standard gravitational area/action normalization uses

`l_P^2 = hbar G / c_light^3`.

The branch derives the dimensionless microscopic coefficient

`c_cell = 1/4`.

With the standard normalization this gives

`a^2 = l_P^2`,

equivalently

`a^2 c_light^3 / (hbar G) = 1`.

That is the hbar-containing physical statement. It is invariant under unit
rescalings when all dimensionful quantities are transformed consistently.

## Consequence

The reviewer cannot demand both:

1. no unit convention dependence; and
2. a prediction of the SI decimal value of `hbar`.

Those demands conflict. The Nature-grade physical target is a dimensionless
relation. The branch's hbar-related claim is therefore:

> reduced action count closes `gamma=1`, and the Planck relation closes
> `a^2 c_light^3/(hbar G)=1`; the SI decimal value of `hbar` is not a physical
> prediction target.

## Safe Claim

Use:

> The branch derives the reduced action unit `gamma=1` and the dimensionless
> Planck relation `a^2 c_light^3/(hbar G)=1`. It does not predict the SI decimal
> value of `hbar`, because that is a unit-convention number.

Do not use:

> The branch predicts the SI value of `hbar`.

Do not use:

> Failure to predict a dimensionful SI constant is a physical failure of the
> Planck derivation.
