# Planck-Scale Bare Gravity-Sector Uniqueness Attempt

**Date:** 2026-04-24
**Status:** B3 reduction/no-go theorem; not a B3 closure
**Verifier:** `scripts/frontier_planck_bare_gravity_sector_uniqueness_attempt.py`

## Question

Can the retained `Cl(3)` / `Z^3` local event/translation algebra, together with
finite locality, `3+1` time-lock, tensorial covariance, additivity,
second-order continuum consistency, no extra background fields, and
nondegenerate propagation, force the accepted gravitational boundary/action
sector as its unique long-distance geometric action sector?

## Verdict

Not yet.

This attempt proves a useful reduction:

> If the bare event/translation algebra is supplemented by a derived soldered
> metricity / equivalence Ward identity, then the admissible long-distance
> local geometric action sector is unique up to normalization, a cosmological
> volume term, and four-dimensional topological boundary ambiguity. The
> nontrivial propagating part is the accepted Einstein/Regge
> boundary/action sector.

But the retained `Cl(3)` / `Z^3` algebra alone does not yet derive that
soldering/Ward identity. Therefore:

`B3 status: OPEN`.

The exact remaining obstruction is:

> derive that primitive translation defects are represented by one soldered
> metric/coframe variable whose local frame redundancy gives the conserved
> symmetric spin-2 response Ward identity.

Without that criterion, "geometric action sector" is not a derived object class.
It is a name for the missing physical interpretation.

## Retained Bare Data

The retained bare package supplies:

1. a local primitive event algebra with `16` atomic event states;
2. a `Z^3` translation/adjacency algebra;
3. a time-locked `3+1` event frame;
4. finite-range local difference operators generated by lattice shifts;
5. cubical / retained-frame representation channels;
6. additive local densities obtained by summing cell-local functions.

These data are strong kinematics. They determine a local event frame and a
translation surface. They do not by themselves select a metric tensor, a
coframe, a torsion-free connection, or a universal stress-tensor coupling.

## Theorem 1: Bare Locality Gives Many Continuum Symbols

Let an admissible bare action be any finite-range, translation-compatible,
additive quadratic local action on fields valued in a retained representation
channel of the primitive event algebra.

Then its long-distance symbol is a finite polynomial in the lattice momenta:

`K(p) = K_0 + K_1^i p_i + K_2^{ij} p_i p_j + O(p^3)`.

Time-lock supplies the fourth direction and the retained tensor bookkeeping.
Second-order continuum consistency restricts the principal symbol to the
quadratic part. Nondegenerate propagation requires that the kinetic block not
vanish on the physical channel.

But this still leaves multiple inequivalent sectors:

- scalar conformal / breathing modes;
- vector or gauge-like plaquette modes;
- symmetric tensor modes;
- antisymmetric/torsion-like modes;
- topological density changes that affect boundary representatives.

The retained algebra contains these representation channels; it does not
automatically mark the symmetric tensor channel as the gravitational one.

So the requested assumptions do not yet force a unique gravitational action
sector. They force a finite menu of local continuum action symbols.

## Theorem 2: Conditional Metric-Sector Uniqueness

Add the following criterion:

**Soldered metricity / equivalence Ward identity.** Primitive translation
defects are represented in the long-distance limit by one soldered coframe
`e^a_mu` or metric `g_mu nu`; local frame changes are gauge; matter responds to
the geometric variable through one conserved symmetric source; and the geometric
field equation has the corresponding covariant conservation identity.

Equivalently, the response law has the form:

`delta S_matter = (1/2) integral sqrt(|g|) T^{mu nu} delta g_mu nu`,

with:

`nabla_mu T^{mu nu} = 0`

and the geometric Euler tensor obeys:

`nabla_mu E^{mu nu} = 0`.

Now impose:

1. locality;
2. additivity under gluing;
3. tensorial covariance under the derived local frame gauge;
4. no independent background fields;
5. no independent propagating torsion/connection field;
6. second-order continuum field equations;
7. nondegenerate long-distance propagation in `3+1`.

Then the four-dimensional local scalar density has only the following
nontrivial possibilities:

`L = alpha sqrt(|g|) + beta sqrt(|g|) R + dB + topological_4D`.

The volume term carries no spin-2 propagation. The four-dimensional topological
term carries no local bulk propagation. Nondegenerate propagation requires
`beta != 0`.

Therefore the unique propagating metric action sector is the curvature-linear
sector. Additivity and a well-posed glued-cell variational principle force the
matching codimension-1 boundary term. On a PL/cubical realization this is the
same intrinsic sector as the accepted Regge/GHY boundary/action family, up to
overall normalization and topological convention.

This is the positive reduction:

`soldered metricity Ward identity + requested locality/covariance criteria`

implies:

`accepted gravitational boundary/action sector`.

## Why This Does Not Close B3

The conditional proof uses a theorem about metric/coframe geometric actions.
The edge-Clifford kinematic soldering theorem now supplies the flat primitive
edge/Clifford pairing `edge_i <-> Gamma_i` and the induced cell metric
`delta_ij`. That closes the "is there any native tangent frame?" subproblem.
It does not supply the dynamical metric/coframe object class or the equivalence
Ward identity.

If the phrase "geometric action sector" already means "metric/coframe action
with the conserved symmetric spin-2 Ward identity," then the uniqueness theorem
is valid but the B3 load has been moved into that definition.

If the phrase is not given that meaning, the action class is too broad and the
bare algebra admits non-gravitational local tensor/action sectors satisfying
the listed algebraic constraints.

## Explicit Overclaim Test

The following would overclaim:

> The bare event/translation algebra already derives the gravitational sector.

The theorem proved here is weaker and sharper:

> The bare event/translation algebra reduces B3 to one missing primitive: derive
> the soldered metricity / equivalence Ward identity. Once that primitive is
> derived, standard local-action uniqueness forces the accepted
> boundary/action sector.

## Exact Extra Criterion Needed

The next theorem must prove one of the following equivalent forms from the
retained algebra:

1. **Soldering form.** The three spatial translations plus the time-lock
   assemble into a coframe whose local changes are gauge, not physical extra
   fields.
2. **Ward form.** Long-distance translation defects couple to one conserved
   symmetric tensor source, and the induced response operator satisfies the
   covariant conservation identity.
3. **Spin-2 form.** The only nondegenerate long-distance deformation of the
   physical event/translation surface is a massless symmetric rank-2 carrier
   with the self-coupling identity that removes lower-spin gauge artifacts.

Any one of these would make the metric/coframe action class earned rather than
assumed. Then the conditional uniqueness theorem above would close B3.

## Consequence For Axiom-Native Planck

This file does not downgrade the accepted physical-gravity Planck theorem. It
only audits the stronger bare-cell-alone upgrade.

Current status:

- B1 physical-lattice ontology is closed on the retained observable-algebra
  reading.
- B2 source-free primitive state is closed by finite-cell naturality.
- B4 same-surface boundary representative is conditionally closed once gravity
  is derived.
- B3 remains open until the soldered metricity / equivalence Ward identity is
  derived from the retained `Cl(3)` / `Z^3` package.

Thus the honest stronger claim is:

> Bare `Cl(3)` / `Z^3` has reduced the Planck gap to one precise gravitational
> object-class theorem, not yet to a completed bare-axiom derivation of gravity.

## Safe Claim

Use:

> B3 is reduced to a metricity/Ward-identity theorem. If the retained
> `Cl(3)` / `Z^3` algebra derives a soldered metric/coframe response with the
> conserved symmetric spin-2 Ward identity, then locality, additivity,
> covariance, second-order consistency, and nondegenerate propagation force the
> accepted gravitational boundary/action sector.

Do not use:

> Bare `Cl(3)` / `Z^3` already proves the gravitational action sector with no
> additional object-class theorem.
