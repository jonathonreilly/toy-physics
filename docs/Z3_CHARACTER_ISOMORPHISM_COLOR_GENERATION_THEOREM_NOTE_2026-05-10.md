# Z_3 Character Isomorphism — Color SU(3)_c Triplet ↔ Generation S_3 Triplet

**Date:** 2026-05-10

**Status:** proposed_retained narrow positive structural theorem. The
existing retained cross-sector closure
(`docs/CKM_KOIDE_CROSS_SECTOR_Z3_CLOSURE_THEOREM_NOTE_2026-04-25.md`)
establishes `N_gen = N_color = 3` as **integer equality only** — its
boundary statement explicitly says the "shared Z³ provenance motif"
is "explanatory only, not load-bearing." This theorem strengthens
that closure by identifying the **load-bearing structural content**
of the shared Z³ provenance: under the **Z_3 cyclic permutation of
the Z³ lattice axes** (the same Z_3 acting in BOTH sectors via the
staggered-Dirac taste-cube structure), the color fundamental triplet
and the generation triplet decompose as **isomorphic** Z_3
representations.

This is a fundamentally new structural primitive on the current
framework surface: a load-bearing structural bridge between color
and generation sectors, beyond the integer equality `3 = 3`.

**Primary runner:** `scripts/frontier_z3_character_isomorphism_color_generation.py`

**Lane:** 6 — Charged-lepton mass retention (M5-c structural prerequisite)

---

## 1. Theorem statement

**Theorem (Z_3 Character Isomorphism via Z³ Lattice 3-Cycle).**

Let `T_3` denote the cyclic Z_3 subgroup generated by the
**3-cycle permutation of the three independent coordinate axes
of the retained Z³ spatial substrate**. Under the staggered-Dirac
realization (per `CL3_TASTE_GENERATION_THEOREM`), this Z³-axis
3-cycle acts:

- on the **color triplet** `V_color` (the 3-dim color fundamental
  of SU(3)_c) as a 3-cycle permutation of the 3 color labels (which
  correspond to the 3 lattice axes via the staggered-Dirac taste
  cube)
- on the **generation triplet** `V_gen` (the 3-dim hw=1 = A_1 + E
  subrepresentation of S_3 on the taste cube) as a 3-cycle
  permutation of the 3 generation candidates

Under T_3, both representations have **identical character vectors**:

```text
   χ_{V_color}(e)  = 3,   χ_{V_color}(c)  = 0,   χ_{V_color}(c²) = 0     (1.1)
   χ_{V_gen}(e)    = 3,   χ_{V_gen}(c)    = 0,   χ_{V_gen}(c²)   = 0     (1.2)
```

where `c` is the T_3 generator. By the character-orthogonality
theorem, equal character vectors on a finite group imply isomorphic
representations. Therefore:

```text
   V_color  ≅  V_gen     as T_3 representations                          (1.3)
```

Both decompose as the regular representation of T_3:

```text
   V_color  ≅  V_gen  ≅  χ_0  ⊕  χ_ω  ⊕  χ_{ω²}                          (1.4)
```

where `χ_k(c^n) = ω^{kn}` and `ω = e^{2πi/3}`.

## 2. Setup and conventions

### 2.1 The Z³ lattice 3-cycle T_3

The retained framework substrate is the **Z³ spatial lattice**
(axiom AX2 per `MINIMAL_AXIOMS_2026-05-03.md`). The lattice has
three independent coordinate axes `{x, y, z}` corresponding to the
three lattice basis vectors `{e_1, e_2, e_3}` of Z³.

The cyclic group T_3 = ⟨c⟩ ≅ Z/3Z acts on the lattice axes by:

```text
   c: e_1 → e_2 → e_3 → e_1                                              (2.1.1)
```

(a 3-cycle of basis vectors). T_3 ⊂ O(3, Z) (the orthogonal group
of the lattice), and T_3 is also a subgroup of the cubic symmetry
group of the cube (the rotational symmetry that permutes the 3 axes).

### 2.2 How T_3 acts on color labels

Per the staggered-Dirac realization (D1-D7 of the YT chain), the
3 colors of SU(3)_c are identified with the 3 lattice axes via
the taste-cube structure. Specifically:

- The staggered Dirac operator η-phases on Z³ (D2) define a
  taste-doubling `2³ = 8` (D3)
- The 8-dim taste space decomposes under Cl(3) action (D4)
- The SU(3)_c gauge structure emerges from the 3-axis structure
  of Z³ (D7 via residual swap on complementary axes)

Therefore the 3-cycle T_3 of lattice axes INDUCES a 3-cycle on
the 3 color labels. Specifically, if color `c_a` corresponds to
lattice axis `e_a`, then under T_3:

```text
   c: c_1 → c_2 → c_3 → c_1                                              (2.2.1)
```

This is a non-central Z_3 subgroup of SU(3)_c — it permutes color
labels rather than acting by the central phase rotation `ω · I`.

### 2.3 How T_3 acts on generation labels

Per `CL3_TASTE_GENERATION_THEOREM`, the three generation-candidate
states are the Z₃-orbit of hw=1 = A_1 + E on the staggered-Dirac
taste cube. The Z_3 here IS the 3-cycle of lattice axes (the same
T_3).

Therefore T_3 acts as a 3-cycle on the 3 generation labels:

```text
   c: g_1 → g_2 → g_3 → g_1                                              (2.3.1)
```

### 2.4 The common origin

T_3 in both sectors arises from the SAME source: the 3-cycle
permutation of the three Z³ lattice axes. This is the load-bearing
structural meaning of the "shared Z³ provenance motif" that the
existing retained cross-sector closure flags as explanatory-only.

## 3. Character computation

### 3.1 Character of V_color under T_3

The 3-cycle permutation matrix on the color triplet (in the basis
where colors are indexed by lattice axes):

```text
   P_color  =  [[0, 0, 1],
                [1, 0, 0],
                [0, 1, 0]]                                                (3.1.1)
```

Eigenvalues: 1, ω, ω² (cube roots of unity).

Characters (traces of the action):

```text
   χ_{V_color}(e)     = Tr(I_3)       = 3
   χ_{V_color}(c)     = Tr(P_color)   = 0
   χ_{V_color}(c²)    = Tr(P_color²)  = 0
```

### 3.2 Character of V_gen under T_3

The generation 3-cycle permutation matrix on the generation triplet
(in the basis where generations are indexed by Z₃-orbit position):

```text
   P_gen   =  [[0, 0, 1],
               [1, 0, 0],
               [0, 1, 0]]                                                 (3.2.1)
```

Same structure as P_color (3-cycle permutation matrix on 3 elements).

Characters:

```text
   χ_{V_gen}(e)       = Tr(I_3)       = 3
   χ_{V_gen}(c)       = Tr(P_gen)     = 0
   χ_{V_gen}(c²)      = Tr(P_gen²)    = 0
```

### 3.3 Equal characters → isomorphic representations

The character vectors are identical: `(3, 0, 0)`. By the standard
character orthogonality theorem (a basic fact of finite-group
representation theory), this implies the two representations are
isomorphic as T_3 representations.

Both decompose as the **regular representation** of T_3:

```text
   V  ≅  χ_0  ⊕  χ_ω  ⊕  χ_{ω²}                                          (3.3.1)
```

where `χ_k(c^n) = ω^{kn}`.

## 4. What the isomorphism establishes

### 4.1 Structural (not just numerical) cross-sector bridge

The existing retained cross-sector closure
`CKM_KOIDE_CROSS_SECTOR_Z3_CLOSURE_THEOREM_NOTE_2026-04-25` proves
`N_gen = N_color = 3` by direct integer equality. Its boundary
statement explicitly defers the structural content:

> exhibit a **shared Z³ provenance motif** across the color and
> generation surfaces ... This reading is **explanatory only**:
> it is consistent with the retained closure but not load-bearing.

This theorem ELEVATES that motif to load-bearing: the integer
equality `3 = 3` reflects a **representation-theoretic
isomorphism** under the common T_3 = (Z³-axis 3-cycle).

### 4.2 Foundation for cross-sector composite identifications

D9 (`docs/YUKAWA_COLOR_PROJECTION_THEOREM.md`) defines the
composite Higgs as `(1/N_c) Σ_a ψ̄_a ψ_a` summed over color indices.
With the Z_3 isomorphism above, the analogous generation-sum
`(1/N_gen) Σ_g χ̄_g χ_g` on the lepton-generation triplet is the
isomorphic-Z_3-character object on the generation side.

This DOES NOT directly produce a lepton composite Higgs (the
quark-color D9 condensate arises from QCD-strong dynamics, which
has no analog in the lepton-generation sector). But it provides
the algebraic substrate for any future cross-sector identity that
needs a Z_3-isomorphic generation analog of a color operator.

### 4.3 Specifically: opens M5-c route from #912

The combined no-go `CHARGED_LEPTON_Y_TAU_WARD_COMBINED_NO_GO_NOTE_2026-05-10`
listed M5-c (Koide-anchored cross-sector `y_τ ↔ y_t`) as
research-level distant. The Z_3 character isomorphism is one
of the structural primitives that a Koide-anchored cross-sector
y_τ/y_t identity would require — the algebraic step of identifying
the lepton-generation triplet with the quark-color triplet as
Z_3 representations.

With this theorem, M5-c's first prerequisite (structural bridge,
not just integer equality) is in place. Closing M5-c still
requires:

- Koide flagship Q + δ closure (in flight on parallel BAE probe lane)
- A specific Koide-anchored derivation of `y_τ/y_t` using the
  Z_3-isomorphic algebra
- Resolution of the D9-vs-no-D9-prime issue: the composite Higgs
  is quark-side; the lepton side has no analog DYNAMICAL composite

## 5. What this theorem does NOT establish

- A Lane 6 closure or any lepton mass closure.
- A Koide flagship Q + δ closure.
- A `y_τ` Ward identity.
- A "lepton composite Higgs" as a DYNAMICAL field (D9 stays
  quark-only).
- A falsification of `CHARGED_LEPTON_Y_TAU_WARD_COMBINED_NO_GO_NOTE_2026-05-10`
  or `CHARGED_LEPTON_D12_PRIME_MATCHING_NO_GO_NOTE_2026-05-10`.
- A claim that the Z_3 isomorphism alone closes anything physical.

## 6. Falsifiers

The theorem is falsified by any one of:

1. A demonstration that the staggered-Dirac taste-cube identifies
   the 3 colors with anything OTHER than the 3 lattice axes (which
   would break the T_3 induced action on color labels).
2. A demonstration that the generation candidates are NOT the
   Z₃-orbit of hw=1 = A_1 + E (which would contradict
   CL3_TASTE_GENERATION_THEOREM).
3. A computational verification showing the character vectors
   under T_3 differ between V_color and V_gen.

## 7. What this opens up

Future work that this theorem ENABLES:

- **M5-c attempts** can now cite the Z_3 character isomorphism as
  a structural foundation instead of relying on the integer
  equality alone.
- **Koide-anchored cross-sector identities** can use the algebraic
  isomorphism to relate quark-color-summed operators to
  generation-summed operators on the lepton side.
- **Symmetry-breaking analyses** can investigate which T_3-non-invariant
  perturbations might generate the observed mass hierarchies.

These are research-level directions, not immediate closures.

## 8. Cross-references

- Retained cross-sector closure (integer equality):
  `docs/CKM_KOIDE_CROSS_SECTOR_Z3_CLOSURE_THEOREM_NOTE_2026-04-25.md`
- Generation candidate structure:
  `docs/CL3_TASTE_GENERATION_THEOREM.md`
- Staggered-Dirac realization:
  `docs/YT_WARD_IDENTITY_DERIVATION_THEOREM.md` (D1-D7)
- Combined no-go citing M5-c:
  `docs/CHARGED_LEPTON_Y_TAU_WARD_COMBINED_NO_GO_NOTE_2026-05-10.md`
- D17-prime, D16-prime, D12-prime matching no-go:
  PRs #1018, #1026, #1048
- Minimal axioms substrate:
  `docs/MINIMAL_AXIOMS_2026-05-03.md`

## 9. Boundary

This is a NARROW POSITIVE structural theorem on the cross-sector
algebraic relationship between color and generation triplets,
under the natural T_3 (Z³-axis 3-cycle) common to both sectors.

It LIFTS the existing retained cross-sector closure from "integer
equality" to "representation isomorphism" — a real strengthening
of the structural content of the framework on the cross-sector
surface.

It does NOT close any physical observable on its own; it provides
algebraic groundwork that future Lane 6 or PMNS or CKM cross-sector
work can build on.

A class-A runner verifies the character computation
(`scripts/frontier_z3_character_isomorphism_color_generation.py`).
