# Lane 5 `(C1)` A4 Parity-Gate-To-CAR Boundary Note

**Date:** 2026-04-29
**Status:** support / current-surface boundary result on `main`; does
not close `(C1)` and does not promote any theorem or claim.
**Runner:** `scripts/frontier_hubble_lane5_c1_a4_parity_gate_car_boundary.py`
**Lane:** 5 -- Hubble constant derivation, `(C1)` absolute-scale gate

## Purpose

After A1 and A2, the next live `(C1)` route was A4:

```text
Can the primitive parity-gate carrier route supply the missing native
CAR/coframe response on P_A H_cell?
```

The answer is no as a standalone derivation. The parity-gate packet remains a
valid conditional positive theorem: once the primitive rank-four block is
accepted as a two-mode CAR edge carrier, the residual self-dual `Z_2`
half-zone fixes the missing tangent selector and gives the exact `1/4`
Widom coefficient. But the parity gate itself is an even selector. It does
not force the odd Clifford/CAR generators, the metric-compatible coframe
response, or the dimensional action unit on `P_A H_cell`.

Thus A4 closes the direct shortcut:

```text
primitive parity gate + rank(P_A)=4
  => metric-compatible Clifford/CAR coframe response on P_A H_cell.
```

It preserves the conditional support chain:

```text
metric-compatible Clifford/CAR coframe response
  => primitive two-mode CAR carrier
  + primitive parity gate
  => c_Widom = c_cell = 1/4.
```

## Minimal Premises Used

- `P_A H_cell` is the rank-four primitive boundary block with
  `c_cell = Tr((I_16/16) P_A) = 1/4`.
- The primitive parity-gate theorem supplies a residual `Z_2` self-dual
  low-transverse-Laplacian half-zone.
- The primitive-CAR edge theorem and Target 3 Clifford phase bridge are used
  only to preserve the conditional positive chain.
- Standard finite matrix algebra is used to test whether a `Z_2` gate can
  force Clifford/CAR semantics.

No measured value of `G`, `hbar`, `M_Pl`, `H_0`, or any cosmological
observable enters this boundary.

## Result

The parity gate supplies exactly the selector it was designed to supply:

```text
Delta_perp(q) < 1
```

has normalized measure `1/2` under the all-tangent half-period involution.
Inside the two-mode CAR carrier this yields

```text
<N_x> = 2 + 2*(1/2) = 3,
c_Widom = 3/12 = 1/4.
```

However, the gate is not the CAR/coframe response. At the finite active-block
level, a `Z_2` gate is represented by a rank-two projector `Q_+` on
`K = P_A H_cell ~= C^4`, or equivalently by one involution
`Z_gate = 2 Q_+ - I_K`. The algebra generated by this data is only

```text
span{I_K, Q_+},
```

and its commutant remains large. It cannot generate the full
`M_4(C)`/`Cl_4(C)` active edge algebra. The same rank-four block and the same
half-gate data are compatible with non-CAR two-qubit response operators that
fail the Clifford anticommutator law.

Therefore A4 exposes a sharper open import:

```text
an orientation/statistics lift from the even primitive Z_2 parity gate to
odd metric-compatible Clifford/CAR coframe generators on P_A H_cell.
```

## Runner Witness

The runner checks thirteen facts.

1. `P_A` gives a rank-four host and `c_cell = 1/4`.
2. A primitive half-gate has rank `2` on the active block.
3. The gate algebra has dimension `2`, while `Cl_4(C) ~= M_4(C)` needs
   dimension `16`.
4. The gate commutant has dimension `8`, so the response algebra is still
   underdetermined.
5. The `2D` and `3D` APBC finite grids realize the exact self-dual half-zone.
6. If the CAR/coframe response is added, the parity-gate chain gives
   `c_Widom = c_cell = 1/4`.
7. The same gate data allow non-CAR two-qubit response operators with the
   wrong Clifford anticommutators.
8. The gate does not define the odd edge maps; it can commute with one
   response choice while CAR generators cross gate sectors.
9. Adding the parity gate does not break the A2 `(S,kappa)` action-unit
   rescaling degeneracy.

Current output:

```text
TOTAL: PASS=13, FAIL=0
```

## Claim Boundary

This note does **not** demote:

- `AREA_LAW_PRIMITIVE_PARITY_GATE_CARRIER_THEOREM_NOTE_2026-04-25.md`;
- `AREA_LAW_PRIMITIVE_CAR_EDGE_IDENTIFICATION_THEOREM_NOTE_2026-04-25.md`;
- `AREA_LAW_NATIVE_CAR_SEMANTICS_TIGHTENING_NOTE_2026-04-25.md`;
- `PLANCK_TARGET3_CLIFFORD_PHASE_BRIDGE_THEOREM_NOTE_2026-04-25.md`.

Those artifacts remain valid on their stated conditional surfaces. This note
only blocks the stronger A4 shortcut that tries to use the parity gate itself
as the missing derivation of the CAR/coframe response.

Safe wording:

> The primitive parity gate supplies the exact half-zone selector inside the
> primitive-CAR edge carrier. It does not by itself force the active block to
> be a metric-compatible Clifford/CAR coframe module.

Unsafe wording:

> The parity-gate theorem derives native CAR horizon statistics from
> `rank(P_A)=4`.

That stronger statement is precisely what the finite-algebra witness blocks.

## Surviving Routes

- Prove the A1 `P_A` Clifford/CAR module-morphism theorem directly.
- Prove an orientation/statistics lift from the primitive `Z_2` parity gate
  to the odd Clifford coframe generators.
- Prove a non-rescaling-invariant metrology theorem coupling the
  dimensionless lattice action to the primitive boundary/action carrier.
- Add a minimal carrier/metrology axiom and keep `(C1)` conditional rather
  than promoted.
