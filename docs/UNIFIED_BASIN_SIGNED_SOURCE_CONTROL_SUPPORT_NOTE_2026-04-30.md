# Unified Basin Signed-Source Control Support Note

**Date:** 2026-04-30
**Status:** support. Salvage note for the signed-source control observations
**Claim type:** positive_theorem
preserved after the unified-basin wrapper failed audit.

**Audit-conditional perimeter (2026-05-02):**
The current generated audit ledger records this row `audited_conditional` with
`auditor_confidence = high`, `chain_closes = false`, and `claim_type =
positive_theorem`. The audit chain-closure explanation is exact: "The
support note accurately excerpts signed-source control rows from the
archived unified-basin wrapper, but it has no registered runner and
the cited wrapper is an audited-failed source. The numerical control
observation is therefore not independently reproducible from the
active row." This rigorization edit only sharpens the boundary of
the conditional perimeter; nothing here promotes audit status. The
supported content of this note is the **salvage framing**: the
signed-source control observations are preserved here as a narrow
finite excerpt from the archived wrapper, and the note explicitly
declares the parent wrapper as `audited_failed` and outside the
retained surface. The exact numerical rows (zero-source controls
seeds 0/1/2; `+/-` ratios from -1.000 to -0.937; doubled-source
ratios 1.927–1.996) are excerpts, not live recomputations; the
support note has no own runner because it is by design a salvage of
already-printed values from
[`UNIFIED_BASIN_FREEZE_NOTE.md`](../archive_unlanded/unified-basin-signed-source-salvage-2026-04-30/UNIFIED_BASIN_FREEZE_NOTE.md).
A future repair would either (a) attach a live runner that
recomputes the signed-source rows on the retained generator, or (b)
keep the note as bounded salvage citing the archived wrapper; option
(b) is the current shape and the supported perimeter is exactly
that bounded salvage scope.

---

## 0. Provenance

The source wrapper
[`UNIFIED_BASIN_FREEZE_NOTE.md`](../archive_unlanded/unified-basin-signed-source-salvage-2026-04-30/UNIFIED_BASIN_FREEZE_NOTE.md)
is archived under recovery tag
`archive_unlanded/unified-basin-signed-source-salvage-2026-04-30/`.
The audit rejected the wrapper because it combined two different surfaces as
one unified basin: the signed-source/non-label drift rows and a separate
complex-action kernel comparison.

## 1. Surviving observations

The signed-source side remains useful as a narrow finite control observation:

- on the printed drift/restore row, seeds `0`, `1`, and `2` have exact zero
  source controls in the table;
- the neutral `+/-` cancellation column is exact on the printed rows;
- the signed-source ratios are near antisymmetric on the finite sample, with
  `+/-` ratios from `-1.000` to `-0.937`;
- the doubled-source response ratios are near linear on the finite sample,
  with printed values from `1.927` to `1.996`.

## 2. Boundary

This note does not claim a unified basin, geometry-generic transfer, or a
gravity-specific complex-action crossover. The complex-action rows in the
archived wrapper remain separate kernel diagnostics and are not promoted here.
