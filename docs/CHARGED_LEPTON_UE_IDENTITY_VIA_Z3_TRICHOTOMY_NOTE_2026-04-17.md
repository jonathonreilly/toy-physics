# `U_e = I` via `Z_3` Trichotomy (Charged-Lepton Lane)

**Date:** 2026-04-17
**Status:** proposed_retained-grade replacement citation chain for the "`U_e = I` on
`H_hw=1`" step used in the PMNS-as-f(H) closure theorem.
**Script:** `scripts/frontier_charged_lepton_ue_identity_via_z3_trichotomy.py`
**Runner:** `PASS = 40, FAIL = 0`
**Framework convention:** "axiom" means only the single framework axiom
`Cl(3)` on `Z^3`.

## Adversarial finding this note resolves

The PMNS-as-`f(H)` closure theorem
(`PMNS_FROM_DM_NEUTRINO_SOURCE_H_DIAGONALIZATION_CLOSURE_THEOREM_NOTE_2026-04-17.md`)
uses as load-bearing input

> `U_e = I` in the generation axis basis on `H_hw=1`

and cites the [DM Neutrino Dirac Bridge
theorem](./DM_NEUTRINO_DIRAC_BRIDGE_THEOREM_NOTE_2026-04-15.md) for it. That
theorem is retained on the local post-EWSB Dirac surface on `C^16` (picks
`Gamma_1` over `Xi_5`, chiral off-diagonal, diagonal in the axis basis at
the convention weak axis), but the **`C^16` -> `3x3` reduction to an
effective Yukawa is explicitly flagged OPEN** there:

> derive the normalization / suppression theorem for the effective neutrino
> Dirac Yukawa generated by the exact `Gamma_1` bridge on `C^16`.

The closure therefore routes the charged-lepton step `U_e = I` through an
open normalization chain on `C^16`. That is a citation-chain weakness.

This note supplies a **retained** replacement route that uses the `Z_3`
generation-charge selection rule on `H_hw=1` directly, with no `C^16`
normalization step.

## The cleaner route in one line

`H_hw=1` carries conjugate `Z_3` triplets `q_L = (0,+1,-1)`,
`q_R = (0,-1,+1)`. A single Higgs doublet with definite `Z_3` charge `q_H`
forces the charged-lepton Yukawa `Y_e` onto exactly one of three exact
permutation patterns (the **trichotomy**). On the branch `q_H = 0` this
pattern is diagonal, and `|U_e| = I` in the axis basis follows.

No post-EWSB `C^16` reduction. No normalization-theorem hole.

## Retained inputs

1. **Three-generation observable space `H_hw=1`** —
 [`THREE_GENERATION_OBSERVABLE_THEOREM_NOTE.md`](./THREE_GENERATION_OBSERVABLE_THEOREM_NOTE.md).
 The exact retained operator algebra on `H_hw=1` is `M_3(C)` generated by
 lattice translations plus the induced `C_3[111]` corner cycle. The three
 axes `X_1, X_2, X_3` are a priori `S_3`-symmetric.

2. **`Z_3` generation-charge trichotomy** —
 [`NEUTRINO_DIRAC_Z3_SUPPORT_TRICHOTOMY_NOTE.md`](./NEUTRINO_DIRAC_Z3_SUPPORT_TRICHOTOMY_NOTE.md).
 For a single Higgs doublet with definite Z_3 charge `q_H` and conjugate
 Z_3 triplets `q_L = (0,+1,-1)`, `q_R = (0,-1,+1)`, the invariance
 condition
 ```
 q_L(i) + q_H + q_R(j) = 0 mod 3
 ```
 forces the Yukawa support onto one of three permutation patterns:
 - `q_H = 0` -> diagonal
 - `q_H = +1` -> forward cyclic
 - `q_H = -1` -> backward cyclic

 This theorem is stated on the neutrino Dirac lane but the derivation
 depends only on the conjugate Z_3 triplets and the single-Higgs support
 rule. It transports verbatim to the charged-lepton Yukawa `Y_e` (see
 [`LEPTON_SINGLE_HIGGS_PMNS_TRIVIALITY_NOTE.md`](./LEPTON_SINGLE_HIGGS_PMNS_TRIVIALITY_NOTE.md),
 which uses exactly this transport).

3. **One-generation matter closure** —
 [`ONE_GENERATION_MATTER_CLOSURE_NOTE.md`](./ONE_GENERATION_MATTER_CLOSURE_NOTE.md).
 Retained Standard-Model single-generation Yukawa structure.

## The theorem chain

### Step 1 — Conjugate Z_3 triplets on `H_hw=1` (retained)

`H_hw=1 = span{X_1, X_2, X_3}`. Under the retained `C_3[111]` cycle the
left-handed lepton triplet carries `q_L = (0, +1, -1) mod 3` and the
right-handed singlets carry `q_R = (0, -1, +1) mod 3`. The two triplets
are conjugate, i.e. `q_L(i) + q_R(i) = 0 mod 3` for each `i`.

### Step 2 — Trichotomy on `Y_e` (retained, transported from neutrino lane)

For a single Higgs doublet carrying definite `Z_3` charge `q_H`, the
Yukawa term

```
e_R^† Y_e (H^† L_L) + h.c.
```

is `Z_3`-invariant only if `q_L(i) + q_H + q_R(j) = 0 mod 3` for each
nonzero entry `(i, j)` of `Y_e`. Row-by-row and column-by-column this
equation has exactly one solution (because `q_L` is a conjugate triplet),
so the support is one of three permutation patterns exactly as in the
trichotomy theorem.

### Step 3 — Branch condition `q_H = 0` (GAUGE, retained)

On the `q_H = 0` branch the trichotomy forces

```
Y_e = diag(y_1, y_2, y_3).
```

**Status upgraded from CONDITIONAL to GAUGE (retained) by
[`HIGGS_Z3_CHARGE_PMNS_GAUGE_REDUNDANCY_THEOREM_NOTE_2026-04-17`](./HIGGS_Z3_CHARGE_PMNS_GAUGE_REDUNDANCY_THEOREM_NOTE_2026-04-17.md).**
That theorem proves the three `q_H` branches give **identical**
`Y_e Y_e†` on the `L_L`-axis basis, hence identical `U_e = I`, hence
identical `|U_PMNS|` up to the separately-flagged `σ_hier` row
permutation. Concretely, `Y_e[q_H = ±1] = Y_e[q_H = 0] · P_±` with `P_±`
a right-handed (`e_R`) cyclic permutation matrix; since PMNS depends
only on left-handed diagonalizers, the right-handed relabeling is
invisible to PMNS observables.

`q_H` is therefore not a physical conditional on the PMNS prediction —
it is a gauge choice. Setting `q_H = 0` is the canonical representative
in which `Y_e` is diagonal (simplest form); the other two branches are
exactly equivalent physically.

The retained atlas
([`NEUTRINO_MASS_REDUCTION_TO_DIRAC_NOTE.md`](./NEUTRINO_MASS_REDUCTION_TO_DIRAC_NOTE.md)
and the trichotomy note) still does not **derive** `q_H = 0` as a
forced charge assignment — but the gauge-redundancy theorem proves
that no such derivation is needed, because `q_H` is not a physical
parameter. The flag that was previously CONDITIONAL is now discharged
as GAUGE (retained).

### Step 4 — Diagonal `Y_e` implies `|U_e| = I` (elementary)

For diagonal `Y_e = diag(y_1, y_2, y_3)`:

```
Y_e Y_e^† = diag(|y_1|^2, |y_2|^2, |y_3|^2)
```

The left-diagonalizer `U_e` of `Y_e Y_e^†` is the identity in the axis
basis, up to the phases of the diagonal entries (which are physically
absorbed into the charged-lepton fields) and up to ordering (addressed in
Step 5). Entrywise, `|U_e| = I_3`.

### Step 5 — Hierarchy pairing `sigma_hier = (2, 1, 0)` (OBSERVATIONAL)

With `U_e = I` in the axis basis, the PMNS matrix becomes
`U_PMNS = U_ν` up to a row permutation `P_σ` that labels which axis is
`e`, which is `μ`, which is `τ`. The retained `C_3[111]` cycle makes the
three axes `{X_1, X_2, X_3}` `S_3`-symmetric before observation, so there
are six a priori permutations. The PMNS-closure's numerical closure singles out

```
sigma_hier = (2, 1, 0)
```

(electron ↔ largest-`H`-eigenvalue axis, muon ↔ middle, tau ↔ smallest)
as the **unique chamber minimizer** with `χ² < 10⁻⁴` against the PDG
central values `(sin² θ_12, sin² θ_13, sin² θ_23) = (0.307, 0.0218, 0.545)`
after 60 random-start searches. This is a **discrete observational choice**,
not axiom-derived.

Notes on what would upgrade `sigma_hier` to retained:

- A retained axis-to-mass-label theorem (e.g. from the graph-first weak-axis
 structure plus a mass-scale ordering argument). The graph-first selector
 fixes the *weak* axis but does not order lepton masses; neither does the
 `Gamma_1` diagonality condition. **Currently no retained fact pins
 `sigma_hier`.**
- A retained `Z_3`-charge ↔ hierarchy correlation on the diagonal `Y_e`
 entries. The trichotomy is silent on the relative magnitudes of
 `(y_1, y_2, y_3)`; that data is not in the support theorem.

So `sigma_hier` remains observational on the current retained stack. This
is an honest, separate flag from the `q_H = 0` condition.

## Theorem statement

> **Theorem (`Z_3` trichotomy `U_e = I` route, charged-lepton lane).**
> Assume the retained three-generation observable theorem on `H_hw=1`, the
> retained `Z_3` support trichotomy for a single Higgs doublet, and the
> branch condition `q_H = 0` on the charged-lepton Yukawa. Then:
>
> 1. `Y_e = diag(y_1, y_2, y_3)` in the axis basis of `H_hw=1`;
> 2. `|U_e| = I_3` entrywise in that basis;
> 3. `U_PMNS = P_σ · U_ν` where `U_ν` is the neutrino-Hermitian eigenbasis
> unitary and `P_σ` is the discrete hierarchy-labeling permutation.
>
> No step in this chain uses the `C^16` -> `3×3` Dirac normalization
> theorem (which remains open in the DM-neutrino Dirac-bridge note).

## Status table (this route)

| Step | Content | Status |
|------|---------|--------|
| 1 | conjugate Z_3 triplets `q_L, q_R` on `H_hw=1` | RETAINED |
| 2 | trichotomy support rule on `Y_e` | RETAINED |
| 3 | `q_H = 0` branch condition | **GAUGE (retained)** via [`HIGGS_Z3_CHARGE_PMNS_GAUGE_REDUNDANCY_THEOREM_NOTE_2026-04-17`](./HIGGS_Z3_CHARGE_PMNS_GAUGE_REDUNDANCY_THEOREM_NOTE_2026-04-17.md) |
| 4 | diagonal `Y_e` ⇒ `U_e = I` | RETAINED (linear algebra) |
| 5 | hierarchy pairing `σ_hier = (2, 1, 0)` | **OBSERVATIONAL** (discrete) |

Compare to the original PMNS-closure citation chain:

| Step | Content | Status |
|------|---------|--------|
| 1 | `Gamma_1` on `C^16`, chiral off-diagonal | RETAINED |
| 2 | `Xi_5` excluded | RETAINED |
| 3 | `C^16` → `3×3` effective Yukawa | **OPEN** (normalization theorem) |
| 4 | `Gamma_1` diagonal in axis basis of `H_hw=1` | RETAINED |
| 5 | hierarchy pairing `σ_hier = (2, 1, 0)` | **OBSERVATIONAL** |

The replacement route removes the "OPEN" step 3. Under the
gauge-redundancy theorem, `q_H = 0` is now a **GAUGE choice (retained)**
— not a conditional — because the three `q_H` branches give identical
PMNS up to the separately-flagged `σ_hier`. This is a strictly stronger
result than "conditional," since conditional implied a physical branch
dependency; gauge means no physical dependency at all.

## Replacement citation text for the PMNS-closure theorem

Suggested replacement for the PMNS-closure note §2 "The charged-lepton
basis" paragraph:

> **§2. The charged-lepton basis (revised).** On `H_hw=1` the retained
> conjugate Z_3 triplets are `q_L = (0,+1,-1)` and `q_R = (0,-1,+1)` under
> the retained `C_3[111]` generation cycle
> ([`THREE_GENERATION_OBSERVABLE_THEOREM_NOTE`](./THREE_GENERATION_OBSERVABLE_THEOREM_NOTE.md)).
> For a single Higgs doublet with definite Z_3 charge `q_H`, the retained
> trichotomy
> ([`NEUTRINO_DIRAC_Z3_SUPPORT_TRICHOTOMY_NOTE`](./NEUTRINO_DIRAC_Z3_SUPPORT_TRICHOTOMY_NOTE.md);
> applied to `Y_e` via the same conjugate-triplet derivation used in
> [`LEPTON_SINGLE_HIGGS_PMNS_TRIVIALITY_NOTE`](./LEPTON_SINGLE_HIGGS_PMNS_TRIVIALITY_NOTE.md))
> forces the charged-lepton Yukawa into one of three permutation patterns.
> On the canonical `q_H = 0` branch (a conditional input, not
> axiom-derived), `Y_e = diag(y_1, y_2, y_3)` and therefore `|U_e| = I_3`
> in the axis basis. See
> [`CHARGED_LEPTON_UE_IDENTITY_VIA_Z3_TRICHOTOMY_NOTE_2026-04-17`](./CHARGED_LEPTON_UE_IDENTITY_VIA_Z3_TRICHOTOMY_NOTE_2026-04-17.md)
> for the full replacement chain and explicit flagging of (i) the `q_H = 0`
> conditional branch and (ii) the observational hierarchy pairing
> `σ_hier = (2, 1, 0)`.

## Runner-verified content

The runner
[`scripts/frontier_charged_lepton_ue_identity_via_z3_trichotomy.py`](../scripts/frontier_charged_lepton_ue_identity_via_z3_trichotomy.py)
executes **`PASS = 40, FAIL = 0`** across six parts:

- **Part 1 (retained charges).** `q_L`, `q_R` are the conjugate triplets.
- **Part 2 (Y_e trichotomy).** The three Z_3 support patterns for `Y_e`
 are verified, pairwise disjoint, and partition the 3×3 grid.
- **Part 3 (diagonal ⇒ identity).** Four test diagonal `Y_e` (including
 hierarchical and order-1 cases, with random complex phases) all satisfy
 `Y_e Y_e^†` exactly diagonal and `|U_e| = I_3`.
- **Part 4 (status of `q_H = 0`).** Documented as CONDITIONAL per the
 retained atlas; the trichotomy allows `q_H ∈ {0, ±1}`.
- **Part 5 (status of σ_hier).** Documented as OBSERVATIONAL per
 the PMNS-closure 60-seed numerical closure; no retained fact pins it.
- **Part 6 (replacement citation chain).** Route uses no `C^16` → `3×3`
 normalization and lists its conditional/observational inputs explicitly.

## Claim discipline

This note **positively claims**:

- a retained Z_3-trichotomy route from `q_H = 0` to `|U_e| = I` on
 `H_hw=1`, independent of the open `C^16` → `3×3` Dirac-bridge
 normalization;
- explicit flagging of `q_H = 0` as a conditional branch;
- explicit flagging of `σ_hier = (2, 1, 0)` as a discrete observational
 choice;
- a replacement citation chain for the PMNS-closure that removes the
 open-step extrapolation.

This note **does not claim**:

- a retained derivation of `q_H`;
- a retained derivation of `σ_hier`;
- that the open Dirac-bridge normalization is no longer open (it is —
 but the PMNS-as-`f(H)` closure no longer depends on it for `U_e = I`);
- numerical closure of the charged-lepton mass spectrum (separate carrier).

## What this does not change

the PMNS-closure numerical results (pinned `(m_*, δ_*, q_+*)`, `δ_CP`,
Jarlskog, `|U_PMNS|` 3-σ check) are **unchanged**: the replacement route
yields the identical endpoint `|U_e| = I` in the axis basis and the
identical definition of `U_PMNS = P_σ · U_ν`. The replacement only tightens
the citation chain under `|U_e| = I`.

## Position on publication surface

- `U_e = I` step in PMNS-as-`f(H)` closure: **retained-grade citation chain
 available** (this note) in addition to the PMNS-closure citation.
- Open `C^16` → `3×3` Dirac-bridge normalization: **still open**, but no
 longer blocking the charged-lepton `U_e = I` step in the selector closure.
- Non-axiom inputs on the charged-lepton lane: now reduced to a single
 observational flag (`σ_hier` hierarchy-pairing). The previously-listed
 `q_H = 0` conditional is discharged as GAUGE (retained) via the
 gauge-redundancy theorem.

## Command

```bash
python3 scripts/frontier_charged_lepton_ue_identity_via_z3_trichotomy.py
```

Expected: `PASS = 40, FAIL = 0`.
