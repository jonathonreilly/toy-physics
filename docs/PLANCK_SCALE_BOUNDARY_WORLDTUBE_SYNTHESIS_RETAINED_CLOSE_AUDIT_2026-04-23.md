# Planck-Scale Boundary Worldtube Synthesis Retained-Close Audit

**Date:** 2026-04-23  
**Status:** historical synthesis audit, superseded as current status by the direct counting and one-axiom default-datum closure notes  
**Audit runner:** `scripts/frontier_planck_boundary_worldtube_synthesis_retained_close_audit.py`

## Supersession Note

This audit records the direct Planck lane before the later direct
worldtube-to-boundary counting theorem and one-axiom default-datum theorem.
Its "not retained" conclusion is historical for that earlier state.

Use the current canonical status packet instead:

- [PLANCK_SCALE_NATIVE_DERIVATION_THEOREM_PACKET_2026-04-23.md](./PLANCK_SCALE_NATIVE_DERIVATION_THEOREM_PACKET_2026-04-23.md)
- [PLANCK_SCALE_DIRECT_WORLDTUBE_PACKET_COEFFICIENT_CHAIN_2026-04-23.md](./PLANCK_SCALE_DIRECT_WORLDTUBE_PACKET_COEFFICIENT_CHAIN_2026-04-23.md)
- [PLANCK_SCALE_ONE_AXIOM_ACCEPTANCE_HOSTILE_REVIEW_MEMO_2026-04-23.md](./PLANCK_SCALE_ONE_AXIOM_ACCEPTANCE_HOSTILE_REVIEW_MEMO_2026-04-23.md)

## Question

Can the two remaining blockers on the direct Planck lane

- source-free democratic cell state
  `rho_cell = I_16 / 16`,
- and direct cell-counting law
  `c_cell = c_wt = Tr(rho_cell P_A)`,

be collapsed into one stronger theorem, or is the route still only a
conditional/support close candidate?

## Bottom line

Yes, there is a clean **one-shot reduction target**.

The surviving direct route is now equivalent to one stronger theorem candidate:

> **Source-Free Elementary Boundary Event Theorem.**
> On the exact time-locked primitive cell `H_cell = C^16`, the elementary
> codimension-1 gravitational boundary coefficient is the normalized
> source-free event readout of the full section-canonical minimal one-step
> worldtube packet
>
> `P_A = sum_(|eta|=1) P_eta`.

If that theorem were proved on the retained surface, then both remaining
blockers would collapse at once:

1. source-free / no-datum event symmetry on the primitive cell would force the
   tracial state
   `rho_cell = I_16 / 16`;
2. packet-exact physicality would force the counted event to be the full packet
   `P_A`, not a proper quotient;
3. the canonical projector/event readout would then give

   `c_cell = Tr(rho_cell P_A) = 4/16 = 1/4`;

4. and the direct Planck route would close:

   `a^2 = l_P^2`,
   hence
   `a = l_P`.

So the lane **does** admit a single exact reduction.

But the current branch does **not** yet derive that theorem from the retained
accepted stack. The route therefore remains:

- `conditional / support`,
- not retained closure,
- not yet an axiom-native retained derivation of Planck.

## Upstream exact inputs already fixed

The branch-local direct lane has already isolated the following exact inputs:

1. **Exact primitive carrier**

   After time-lock, the minimal local carrier is the four-bit cell

   `eta = (eta_t, eta_x, eta_y, eta_z) in {0,1}^4`,

   so the exact primitive carrier has dimension `16`.

2. **Section-canonical packet**

   The exact minimal one-step worldtube packet is the Hamming-weight-1 packet

   `P_A = sum_(|eta|=1) P_eta`,

   and this coarse packet is already section-canonical on the direct boundary
   stack.

3. **No-proper-quotient packet physicality**

   The branch-local packet/readout work has already shown that the full packet
   `P_A` is the physical coarse carrier and that quotient-only readout is not
   admissible on the current physical-lattice semantics surface.

4. **Canonical projector/event readout on the cell**

   On the commuting primitive cell event algebra generated by the atomic
   projectors `P_eta`, the normalized additive yes/no readout is the trace law

   `mu_rho(P) = Tr(rho P)`.

These are real exact reductions. The direct route is no longer coefficient
fishing.

## The one-shot synthesis theorem candidate

The strongest possible collapse of the remaining blockers is:

### Candidate theorem

> **Source-Free Elementary Boundary Event Theorem.**
> Let `A_cell` be the diagonal event algebra generated by the primitive cell
> projectors `P_eta` on `H_cell = C^16`.
>
> Assume:
>
> 1. the elementary codimension-1 gravitational boundary coefficient is a
>    normalized additive event readout on `A_cell`;
> 2. source-free/no-datum boundary data are carried on the primitive cell
>    itself, with no extra same-cell weighting datum;
> 3. the physical event counted by the elementary boundary cell is the exact
>    full section-canonical one-step worldtube packet `P_A`, not a proper
>    quotient.
>
> Then:
>
> - the unique source-free primitive-cell state is the tracial state
>   `rho_cell = I_16 / 16`,
> - the elementary boundary coefficient is
>   `c_cell = Tr(rho_cell P_A) = 1/4`.

This is the exact theorem shape that would collapse the route in one shot.

## Why this one theorem collapses both blockers

The two surviving blockers were:

1. the branch had not yet promoted
   `rho_cell = I_16 / 16`
   to retained status;
2. the branch had not yet promoted
   `c_cell = c_wt = Tr(rho_cell P_A)`
   to retained status.

The candidate theorem packages them into one physical statement:

- **event grammar on the primitive cell** gives the trace readout;
- **source-free/no-datum primitive-cell physicality** removes any nonuniform
  same-cell weighting;
- **packet-exact physicality** identifies the counted event with `P_A`.

Then the value is immediate:

`c_cell = Tr((I_16 / 16) P_A) = rank(P_A)/16 = 4/16 = 1/4`.

So the synthesis theorem really would collapse both blockers simultaneously.

## What is and is not already earned

### Already earned

- the exact primitive carrier `C^16`;
- the exact packet `P_A`;
- the exact no-proper-quotient/full-packet semantics;
- the exact event/readout law `mu_rho(P) = Tr(rho P)` on the primitive cell
  event algebra.

### Not yet earned

- that source-free/no-datum elementary boundary data must be read on the full
  primitive cell rather than the older scalar Schur/free-energy grammar;
- that source-free/no-datum on that primitive cell already forces the tracial
  state on the retained accepted surface.

That second point is the real unresolved content. On the current branch, the
uniform primitive-cell state is still only earned after the stronger local
flip-invariance promotion on the non-Schur lane.

So the one-shot theorem exists as a reduction target, but its core physical
premise is still new.

## Why the route is still only conditional / support

The current branch has two strong exact partial results:

1. **Projector/event route**

   If the physical elementary boundary readout is a primitive-cell event
   observable, then the exact packet coefficient is quarter.

2. **Non-Schur source-free route**

   If source-free primitive-cell data are invariant under the full local
   bit-flip group, then the unique source-free state is `I_16 / 16`.

But neither of those is yet retained on the currently accepted authority
surface:

- the projector/event route is still a new non-scalar observable grammar
  relative to the older scalar Schur/free-energy grammar;
- the full primitive-cell tracial state is still conditional on the stronger
  flip-invariance promotion.

So the synthesis theorem is not presently a retained theorem. It is the
sharpest exact reduction target.

## Exact retained-close verdict

The honest claim-strength label on this lane is:

- **exact reduction theorem** for the route shape,
- plus overall **conditional / support** status for Planck closure.

More explicitly:

- the route is **not retained closure**;
- the route is **not yet an axiom-native retained derivation of Planck**;
- the route is reduced to **one stronger theorem candidate**:
  the Source-Free Elementary Boundary Event Theorem above.

That is the sharpest honest status.

## What would count as a retained close

To upgrade the Planck lane from conditional/support to retained closure, one of
the following would have to happen:

1. prove the Source-Free Elementary Boundary Event Theorem directly from the
   already-accepted retained framework surface;
2. prove an exact equivalent theorem implying both
   `rho_cell = I_16 / 16`
   and
   `c_cell = Tr(rho_cell P_A)`
   without adding a new physical grammar;
3. explicitly promote the primitive-cell event grammar and source-free tracial
   law into the accepted retained package boundary.

Anything weaker leaves the route conditional/support.

## Safe wording

**Can claim**

- the surviving direct Planck route is reduced to one exact theorem candidate;
- that candidate theorem would collapse both remaining blockers at once;
- the current branch still does not prove it on the retained accepted surface;
- the lane remains conditional/support, not retained.

**Cannot claim**

- that Planck is already derived axiom-natively on this branch;
- that the primitive-cell tracial state is already retained without the
  stronger source-free promotion;
- that the physical elementary boundary coefficient is already proved to be the
  worldtube-packet event weight.

## Changed files

- `docs/PLANCK_SCALE_BOUNDARY_WORLDTUBE_SYNTHESIS_RETAINED_CLOSE_AUDIT_2026-04-23.md`
- `scripts/frontier_planck_boundary_worldtube_synthesis_retained_close_audit.py`
