# DM Wilson-to-`dW_e^H` Local Path-Algebra Minimal Certificate

**Date:** 2026-04-18  
**Status:** exact current-stack theorem packaging the whole structured
Wilson-to-`dW_e^H` route as one minimal local certificate upstream of the DM
selector  
**Script:** `scripts/frontier_dm_wilson_to_dweh_local_path_algebra_minimal_certificate_2026_04_18.py`

## Question

Once the DM Wilson attack has been sharpened from a generic `9`-channel source
family to one local adjacent-chain path-algebra target, can the whole route be
stated in one finite minimal certificate form?

## Bottom line

Yes.

The whole structured Wilson-to-`dW_e^H` route is exactly one minimal local
certificate with two layers:

1. **generator layer**:
   one local unital `*`-monomorphism
   `Phi_chain : A_chain -> End(H_W)`;
2. **descended-response layer**:
   one real-linear Wilson first-variation identity
   `dW_W o Phi_chain = dW_e^H`.

That is already enough, because current `main` has already proved:

- `dW_e^H` reconstructs `H_e` exactly;
- `H_e` fixes the charged packet;
- the exact flavored transport selector then determines the relevant column
  algorithmically.

So the sharpest reviewer-facing DM Wilson target is no longer:

- `9` unrelated Wilson probes.

It is:

- one local adjacent-chain path algebra,
- plus one descended first-variation identity into `dW_e^H`.

## What is already exact

### 1. The local generator target is already fixed

From
[DM_WILSON_TO_DWEH_LOCAL_CHAIN_PATH_ALGEBRA_TARGET_NOTE_2026-04-18.md](./DM_WILSON_TO_DWEH_LOCAL_CHAIN_PATH_ALGEBRA_TARGET_NOTE_2026-04-18.md):

- one adjacent two-edge chain already generates the full `Herm(3)` codomain;
- the generic `9`-channel Wilson Hermitian source-family target is the
  Hermitian shadow of one local path-algebra embedding `Phi_chain`.

### 2. The descended codomain is already the exact compressed DM object

From
[DM_LEPTOGENESIS_NE_PROJECTED_SOURCE_LAW_DERIVATION_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_NE_PROJECTED_SOURCE_LAW_DERIVATION_NOTE_2026-04-16.md)
and
[DM_LEPTOGENESIS_NE_CHARGED_SOURCE_RESPONSE_REDUCTION_NOTE_2026-04-16.md](./DM_LEPTOGENESIS_NE_CHARGED_SOURCE_RESPONSE_REDUCTION_NOTE_2026-04-16.md):

- the live PMNS-assisted DM codomain is exactly `dW_e^H = Schur_Ee(D_-)`;
- once `dW_e^H` is known, the remaining packet and transport chain is already
  downstream algorithmic.

So there is no further source-side object between `dW_e^H` and the flavored DM
column.

## Theorem 1: exact minimal local certificate form of the Wilson-to-`dW_e^H` route

The following are equivalent:

1. theorem-grade structured Wilson realization of the DM last-mile source law;
2. one minimal local certificate consisting of:
   - one local unital `*`-monomorphism
     `Phi_chain : A_chain -> End(H_W)`,
   - and one descended first-variation identity
     `dW_W o Phi_chain = dW_e^H`.

### Proof

`(1) => (2)`.

By the local chain path-algebra target theorem, theorem-grade structured Wilson
realization already sharpens to one local generator object `Phi_chain`.

The same theorem shows the induced Hermitian shadow spans the full `Herm(3)`
codomain. So the only remaining Wilson-to-DM step is the descended
first-variation identity into that exact codomain:

`dW_W o Phi_chain = dW_e^H`.

Therefore theorem-grade structured Wilson realization implies the two-layer
certificate.

`(2) => (1)`.

Assume the two-layer certificate.

The generator layer `Phi_chain` supplies the full local structured Wilson
source side. The descended-response layer identifies its first-variation law
with the exact DM codomain `dW_e^H`.

Then by the current exact DM reduction:

- `dW_e^H` reconstructs `H_e`,
- `H_e` fixes the charged packet,
- the exact flavored selector determines the relevant transport column.

So the two-layer certificate implies theorem-grade structured Wilson
realization of the DM last-mile source law.

Therefore the whole route is exactly equivalent to the displayed minimal local
certificate.

## Corollary 1: the reviewer-facing Wilson/DM checklist is now finite and local

A future positive theorem on this lane may now be judged by the checklist:

- realize `Phi_chain` on one adjacent nearest-neighbor two-edge chain;
- prove `dW_W o Phi_chain = dW_e^H`;
- then read off the selected flavored DM column downstream.

## Corollary 2: there is no honest intermediate source-side target left

Because the Hermitian shadow of `Phi_chain` already spans all `9` real
directions of `Herm(3)`, there is no honest intermediate target between:

- the local generator layer,
- and the descended identity into `dW_e^H`.

So the route is already minimal in target shape.

## What this closes

- exact packaging of the whole structured Wilson-to-`dW_e^H` route as one
  minimal local certificate
- exact replacement of the diffuse `9`-probe picture by a finite local
  generator-plus-response theorem target
- exact statement that no further honest source-side layer remains between
  `Phi_chain` and `dW_e^H`

## What this does not close

- a positive realization of `Phi_chain`
- a positive theorem that `dW_W o Phi_chain = dW_e^H`
- the DM microscopic selector law
- the DM flagship gate

## Why this matters

This note fixes the right success criterion for the next attack.

The remaining Wilson problem is no longer vague. It is exactly:

- realize the local path algebra,
- then prove the descended first-variation identity.
