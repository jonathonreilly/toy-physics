# Exact Bridge-Side Closure on the Star-Supported Finite-Rank Source Class

**Date:** 2026-04-13  
**Script:** `scripts/frontier_star_supported_bridge_class.py`  
**Status:** Exact bridge-side closure across the star-supported finite-rank class on the current bridge surface

## Purpose

The earlier strong-field notes progressively closed the shell source, bridge,
local static-constraint lift, and microscopic Schur boundary action.

What still remained as a plausible objection was:

> perhaps that closure package is still special to the exact local `O_h`
> family and one benchmark finite-rank example

This note closes that source-class objection on the current bridge surface.

## Exact class statement

Take any exact source field on the seven-point star support generated by a
finite-rank positive-semidefinite support operator on the current Dirichlet
box.

Then on the current `R = 4` sewing surface:

1. the exterior projector field `phi_ext = Pi_R^ext phi` gives the exact shell
   source `sigma_R = H_0 phi_ext`, supported on the same finite band
   `3 < r <= 5`
2. the native same-charge bridge
   - `psi = 1 + phi_ext`
   - `chi = 1 - phi_ext = alpha psi`
   gives the exact local static conformal constraint pair
   - `H_0 psi = 2 pi psi^5 rho`
   - `H_0 chi = -2 pi alpha psi^5 (rho + 2S)`
   with
   - `rho = sigma_R / (2 pi psi^5)`
   - `S = 0.5 rho (1/alpha - 1)`
3. the exact shell trace is the stationary point of the same microscopic
   Schur-complement boundary action on `Gamma_R`

So the bridge-side closure package is not special to one symmetric family. It
extends exactly to the full star-supported finite-rank class on the current
bridge surface.

## Numerical sanity across random class members

The script checks several randomly generated star-supported finite-rank source
operators and source vectors. Across all tested seeds it finds:

- shell support remains exactly in `3 < r <= 5`
- both local static conformal constraints hold to machine precision
- the same microscopic Schur boundary action remains stationary at the exact
  shell trace to machine precision

These checks are sanity, not the theorem itself. The theorem follows from the
same lattice identities already used in the closure package. The random sweep
shows the result is not limited to the previously hand-tuned benchmark sources.

## What this closes

This closes one of the last honest gravity gaps:

> on the current bridge surface, exact closure is no longer limited to the
> benchmark `O_h` family; it extends to the full star-supported finite-rank
> source class

So the remaining generality gap is no longer source-class dependence within the
star-supported finite-rank sector.

## What this still does not close

This note still does **not** close:

1. closure beyond the current static conformal bridge
2. support classes broader than the current star-supported finite-rank sector
3. fully general nonlinear GR

## Updated gravity target

After this note, the remaining gravity blocker is sharper again:

- the star-supported finite-rank source-class extension on the current bridge
  surface is no longer open
- the live remaining gap is the extension beyond the current static conformal
  bridge, and then any wider support-class generalization
