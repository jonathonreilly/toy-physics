# Per-Site Hilbert Space Dimension = 2 from Cl(3) Per-Site Uniqueness

**Date:** 2026-05-02
**Type:** positive_theorem
**Claim scope:** the per-site Hilbert space at every lattice site of the
framework's Cl(3) ⊗ Z^3 substrate has complex dimension exactly 2;
for a finite block `Λ`, the tensor-product site space has complex
dimension `2^|Λ|`.
**Status authority:** independent audit lane only. This source note does
not set or predict an audit outcome; later status is generated by the
audit pipeline after independent review.
**Loop:** `positive-only-r4-20260502`
**Cycle:** 1 (Block 1)
**Branch:** `physics-loop/positive-only-r4-block01-per-site-dim-20260502`
**Runner:** `scripts/cl3_per_site_hilbert_dim_two_check.py`
**Log:** `outputs/cl3_per_site_hilbert_dim_two_check_2026-05-02.txt`

## Cited authorities (one hop)

- [`AXIOM_FIRST_CL3_PER_SITE_UNIQUENESS_THEOREM_NOTE_2026-04-29.md`](AXIOM_FIRST_CL3_PER_SITE_UNIQUENESS_THEOREM_NOTE_2026-04-29.md)
  provides the per-site uniqueness theorem used here: the framework's
  faithful irreducible Cl(3) site module has complex dimension exactly 2
  and is represented by the canonical Pauli generators `γ_i = σ_i`, up to
  the equivalence specified in that note.

This is the only load-bearing one-hop dependency.

## Admitted-context inputs

- **A2 — Z^3 substrate.** The framework's spatial substrate; each site
  carries a copy of Cl(3).
- **Site-Hilbert-space identification.** The Hilbert space at each lattice site is the faithful irreducible Cl(3) representation. Standard structural definition.

No physics conventions admitted beyond the cited Cl(3) per-site uniqueness theorem.

## Statement

For every lattice site `x ∈ Z^3` on the framework's Cl(3) ⊗ Z^3 substrate:

**(D1) Site Hilbert space dim.** `dim_C H_x = 2` exactly.

**(D2) Pauli realization.** The Cl(3) generators at site x are realized (up to unitary equivalence) as the standard Pauli matrices `σ_1, σ_2, σ_3`.

**(D3) Total Hilbert dim on finite block.** For a finite block `Λ ⊂ Z^3` with `|Λ|` lattice sites, the total Hilbert space is `⊗_{x ∈ Λ} H_x` with `dim_C = 2^{|Λ|}`.

The representation-theoretic uniqueness input is not reproved here; it is
the cited upstream theorem.

## Proof

### Step 1 — Cite the per-site uniqueness theorem

By [`AXIOM_FIRST_CL3_PER_SITE_UNIQUENESS_THEOREM_NOTE_2026-04-29.md`](AXIOM_FIRST_CL3_PER_SITE_UNIQUENESS_THEOREM_NOTE_2026-04-29.md), the minimal complex spinor irrep of Cl(3) has dimension 2.

### Step 2 — Apply to the framework substrate

The framework's per-site algebra at every `x ∈ Z^3` is Cl(3) (axiom A1 of A_min). The site Hilbert space is the faithful irreducible representation. By Step 1, `dim_C H_x = 2`. ∎

### Step 3 — Pauli realization (D2)

The cited per-site uniqueness theorem also supplies the Pauli-generator
realization on the framework's site module. Hence each site carries that
Pauli-matrix realization. ∎

### Step 4 — Total Hilbert dim (D3)

Tensor product: `⊗_{x ∈ Λ} H_x` has dim `∏_x dim H_x = 2^{|Λ|}`. ∎

## Hypothesis set used

- `axiom_first_cl3_per_site_uniqueness_theorem_note_2026-04-29`.
- A2 (Z^3 substrate, structural).
- Site Hilbert space = faithful irreducible Cl(3) rep (structural).

## Corollaries

C1. **Single two-level site module.** Each Cl(3) site is a two-level
complex module in the same dimension class as a qubit. This is a
dimension statement only.

C2. **Pauli algebra per site.** The site operator algebra is
`M_2(C)` on the cited Pauli realization.

C3. **Total state-space size formula.** A finite cubic block of side `L`
has `|Λ| = L^3` sites and `2^{L^3}`-dimensional tensor-product site space.

## Out of scope

This note does not prove a bosonic-CCR no-go, a many-body dynamics
theorem, a continuum limit, or an efficient simulation claim. Those would
require separate statements and runners.

## Review classification

Positive-theorem proposal. Single one-hop chain. Direct application of
the cited per-site uniqueness theorem to the framework substrate, plus
the tensor-product dimension formula for finite blocks.

```yaml
claim_type_author_hint: positive_theorem
claim_scope: "dim_C H_x = 2 for every lattice site; total dim_C = 2^|Λ| for finite block."
upstream_dependencies:
  - axiom_first_cl3_per_site_uniqueness_theorem_note_2026-04-29
```
