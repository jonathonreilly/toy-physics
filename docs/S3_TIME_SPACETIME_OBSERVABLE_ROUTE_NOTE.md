# Route 2 Observable Pass: `S^3` + Anomaly-Forced Time

**Date:** 2026-04-14  
**Branch:** `codex/review-active`  
**Role:** Route 2 / observable student  
**Purpose:** check whether the retained `S^3` + anomaly-forced-time stack
already supplies an exact spacetime-lift observable or dynamics bridge on
`PL S^3 x R`

## Verdict

**Exact kinematic lift observable exists. Exact dynamics bridge does not.**

The retained stack gives two exact ingredients:

- `S^3` compactification is closed
- anomaly-forced time is exact (`d_t = 1`)

Together they define the clean kinematic background candidate:

    PL S^3 x R

From the route-2 committee perspective, the only exact observable one can
write down from the current retained stack is the background-lift selector:

    O_lift := 1[S^3 closed] * 1[d_t = 1]

On the current atlas, `O_lift = 1`.

That is an exact kinematic predicate. It tells us the route has a clean
spacetime scaffold.

It does **not** yet give a GR field law.

## What the route does and does not supply

### Exact on the current retained stack

- `S^3` topology
- anomaly-forced time
- the combined lift `PL S^3 x R`
- a clean route selector saying the background scaffold is present

### Not yet exact on the current retained stack

- no exact observable that reconstructs curvature or tensor dynamics
- no exact action on `PL S^3 x R` whose Euler-Lagrange equations give GR
- no exact uniqueness theorem saying Einstein dynamics is the only compatible
  local lift

So Route 2 is currently a **kinematic lift with an exact selector observable**,
not a theorem-grade dynamical closure.

## Why this is the sharp blocker

The atlas and route notes are consistent on the negative point:

- topology is not the problem
- clock structure is not the problem
- the missing piece is a dynamics carrier

That means the remaining theorem would have to be one of:

1. an exact action on `PL S^3 x R`
2. an exact spacetime-lift observable that reconstructs metric response
3. a uniqueness theorem forcing Einstein dynamics from the retained lift

Without one of those, the route cannot underpin a GR field law.

## Relation to the atlas

The derivation atlas already exposes the reusable exact tools:

- `S^3` cap uniqueness
- `S^3` boundary-link closure
- anomaly-forced time
- the existing clean `PL S^3 x R` background candidate

But the atlas does not yet contain a row that is both:

- exact on the route-2 background, and
- dynamics-carrying for the GR metric law

That is why this route stays bounded.

## Bottom line

Route 2 does produce an exact background-lift observable:

    O_lift = 1

on the current retained stack.

It does **not** produce an exact GR dynamics bridge.

So the sharp current statement is:

> `S^3` + anomaly-forced time gives an exact kinematic spacetime lift to
> `PL S^3 x R`, but the atlas still lacks an exact observable/action/uniqueness
> theorem that turns that lift into Einstein dynamics.
