# Teleportation Retention-Theorem Attack Note

**Date:** 2026-04-26
**Status:** planning / retention route and side-12 certificate
**Runner:** `scripts/frontier_teleportation_retention_theorem_attack.py`

## Scope

This artifact attacks the remaining blockers after the unconditional-closure
attack:

- derive or retain the variational selector;
- prove the signed sparse scaling floor;
- realize the pulse/detector threshold classes as implementation envelopes.

The result is stronger, but still conditional. The scope remains ordinary
quantum state teleportation only. No matter, mass, charge, energy, object, or
faster-than-light transport is claimed.

## Selector Retention Theorem

The bare one-axiom route is now closed negatively on the audited selector
invariants. The local-Hermitian flow surface admits:

```text
equivalent_pairs = 32
selector_entropy_bits = 5.000000
```

These candidates share the audited teleportation observables, so the original
bare axiom cannot pick one without extra selector content.

The minimal sufficient completion has three clauses:

```text
1. retarded causal-positive orientation;
2. minimal generator/action norm over equivalent Bell-record writers;
3. no-dwell cubic-covariant nearest-neighbor carrier.
```

The remaining decision is therefore explicit: retain this variational
completion as lane-level physics, derive it from a stronger framework theorem,
or do not promote the lane.

## Side-12 Signed Sparse Certificate

The signed sparse branch has now been tested through side `12` at fixed
`G=-1000`.

```text
side=4:  gap=0.0244025,       gap*L^2=0.390440, Bell*=0.999702000000
side=6:  gap=0.0120618,       gap*L^2=0.434225, Bell*=0.999709000000
side=8:  gap=0.00704654,      gap*L^2=0.450979, Bell*=0.999711000000
side=10: gap=0.00459031,      gap*L^2=0.459031, Bell*=0.999711069000
side=12: gap=0.003218725689,  gap*L^2=0.463496, Bell*=0.999711313114
```

The runner uses the sharper finite theorem pressure:

```text
gap(L) >= 0.390 / L^2
Bell*(L) >= 0.999702
```

on the audited sides. The side-12 preparation bounds at `eps=1e-3` are:

```text
T_ad <= 9.417e+05
beta <= 8.056e+03
```

The remaining proof obligation is the all-even-side induction or analytic
operator inequality extending the gap and Bell floors beyond side `12`.

## Local Controller Envelope

The record code has length `8`, minimum Hamming distance `5`, and corrects two
slot flips. For a `1e-6` word-failure target:

```text
slot_threshold = 2.622e-03
area_budget = 0.050937 rad
```

The implementation envelope used here is:

```text
leakage = 1.000e-05
crosstalk = 2.000e-05
implemented_area_bound = 0.023000 rad
implemented_slot_error = 5.589e-04
word_failure_bound = 9.757e-09
```

This is a dimensionless local controller envelope. It is not a fabricated
controller or measured noise spectrum.

## Material Ising-Domain Envelope

The detector class is mapped to a local 3D Ising-domain implementation envelope:

```text
domain_side = 5
spins_per_slot = 125
slots = 8
local_bonds = 2400
J_over_T = 1.000
defect = 2.000e-03
p_spin = 2.006e-03
word_failure_bound = 7.498e-131
log10_overlap_bound = -655.141
arrhenius_wall = 1.929e-22
minimal_odd_domain_side_for_target = 3
```

This is a material envelope in local Ising-domain variables, not a named lab
material or fabricated detector stack.

## Retained Status

The remaining blockers are now:

- selector derivation from the bare axiom is closed negatively on audited
  invariants; the variational completion must be retained, derived by a
  stronger theorem, or rejected;
- signed sparse scaling is supported through side `12`, but still needs an
  all-even-side induction or analytic operator inequality;
- controller/detector thresholds now have local implementation envelopes, but
  still lack fabricated hardware, named material parameters, and measured
  spectra.

The lane remains planning / conditional theory, not unconditional nature-grade
closure.

## Audit dependency repair links

This graph-bookkeeping section records explicit dependency links named by a prior conditional audit so the audit citation graph can track them. It does not promote this note or change the audited claim scope.

- [teleportation_native_record_apparatus_note](TELEPORTATION_NATIVE_RECORD_APPARATUS_NOTE.md)
