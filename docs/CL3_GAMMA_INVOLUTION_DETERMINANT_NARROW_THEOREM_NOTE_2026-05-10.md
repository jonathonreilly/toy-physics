# Cl(3) γ-Involution Determinant Identity — Narrow Theorem

**Date:** 2026-05-10
**Claim type:** positive_theorem
**Status authority:** independent audit lane only. This source note does
not set or predict an audit outcome; later status is generated by the
audit pipeline after independent review.
**Source-note proposal disclaimer:** this note is a source-note proposal;
audit verdict and downstream status are set only by the independent
audit lane.
**Primary runner:** [`scripts/frontier_cl3_gamma_involution_determinant_narrow.py`](../scripts/frontier_cl3_gamma_involution_determinant_narrow.py)

## 1. Claim scope

Let `Cl(3,0)` be the real Clifford algebra on three generators
`γ_1, γ_2, γ_3` satisfying `{γ_i, γ_j} = 2 δ_{ij} · 1`. The sibling
narrow theorem
[`CL3_COMPLEXIFICATION_SPLIT_NARROW_THEOREM_NOTE_2026-05-10.md`](CL3_COMPLEXIFICATION_SPLIT_NARROW_THEOREM_NOTE_2026-05-10.md)
supplies a real-algebra isomorphism `Cl(3,0) ≅ M_2(C)` sending
the Clifford generators to the Pauli matrices. Define on `M_2(C)` the
**γ-involution**

```text
γ : M_2(C) → M_2(C),       γ(M) := σ_2 · M^T · σ_2.                  (def-γ)
```

Then:

- **Symplectic-adjoint identity.** For all `M ∈ M_2(C)`,
  `γ(M)` equals the cofactor transpose of `M`, i.e. `γ(M) = adj(M)`
  (so `M · γ(M) = γ(M) · M = det(M) · I_2`).

- **Gamma-norm determinant identity.** For all `M ∈ M_2(C)`,
  the scalar (grade-0 / identity-coefficient) component of `M · γ(M)`
  is `det(M)`:

  ```text
  ⟨M · γ(M)⟩_0  =  det(M)         (so |M · γ(M)| = det(M) · I_2).
  ```

  Equivalently, the **γ-norm** `|M|_γ := √|⟨M · γ(M)⟩_0|` satisfies

  ```text
  |M|_γ  =  √|det(M)|.
  ```

- **Gamma grade-action identity.** In the
  positive-chirality Pauli realisation
  `(I_2, σ_1, σ_2, σ_3, σ_1σ_2, σ_1σ_3, σ_2σ_3, ω = σ_1σ_2σ_3 = i I_2)`
  of `Cl(3,0) ⊗_R C`, the γ-involution `γ(M) = σ_2 M^T σ_2` acts as
  `+id` on `I_2` and `ω`, and as `-id` on the vector and bivector
  grades: `γ(σ_i) = -σ_i` (`i = 1, 2, 3`) and `γ(σ_iσ_j) = -σ_iσ_j`
  (`i ≠ j`). This grade pattern is the **antiautomorphic transpose**
  of the standard reversion: reversion preserves identity and vector
  grades and flips bivector and pseudoscalar grades, while γ
  preserves identity and pseudoscalar grades and flips vector and
  bivector grades. Either is a valid `M_2(C)` involution; this note
  records the specific γ-involution `γ(M) = σ_2 M^T σ_2`.

- **Full-lattice determinant-readout boundary.**
  The γ-norm determinant identity gives the γ-norm of a **single**
  `Cl(3) ≅ M_2(C)`
  element as the square root of its determinant. It is **NOT** an
  identity for the framework's full staggered-Dirac determinant
  `|det(D + J)|`, which is a determinant of a `2 N_taste L_t L_s³ × 2
  N_taste L_t L_s³` matrix acting on the lattice tensor-product space
  `(Cl(3))^{⊗ sites} ⊗ Z⁴`. The per-determinant geometric-mean readout
  `v(L_t) ∝ |det(D + J)|^{1/(N_taste · L_t)}` admitted by
  `HIERARCHY_HEAT_KERNEL_D4_COMPRESSION_BOUNDED_THEOREM_NOTE_2026-05-10.md`
  §4.3 is **not** derived from the per-element identity. The relation between the
  per-element γ-norm and the lattice determinant readout is recorded
  here only as a boundary statement, not as a closure.

The three identities above are abstract finite-dimensional complex
matrix-algebra statements about `M_2(C) ≅ Cl(3,0)`. The final item is
an explicit boundary disclaimer.

## 2. Why this note exists

The companion bounded theorem
`HIERARCHY_HEAT_KERNEL_D4_COMPRESSION_BOUNDED_THEOREM_NOTE_2026-05-10.md`
§4.3 admits the per-determinant geometric-mean readout
`v(L_t) ∝ |det(D + J)|^{1/(N_taste · L_t)}` as a structural
admission and recasts it via heat-kernel and Stefan-Boltzmann
dimensional-analysis support. A natural question is whether the
admission can be retired by **algebraic** means — specifically, whether
the Cl(3) γ-involution structure forces a `√det`-shaped readout via
the identity `|M|_γ = √|det(M)|`.

This narrow theorem isolates the **abstract `Cl(3)` algebraic content**
of that question: the γ-norm determinant identity is exact, but applies to
per-element γ-norms in `M_2(C)`, NOT to the framework's full lattice
determinant. The conclusion is an explicit boundary disclaimer:
the per-element γ-norm identity does not on its own close the
geometric-mean readout admission. Recording the exact identities plus
the boundary gives the audit lane a single algebraic statement about
the γ-involution that is true on its own terms and correctly
delineates what it does and does not close.

## 3. Cited authorities (one hop)

- [`CL3_COMPLEXIFICATION_SPLIT_NARROW_THEOREM_NOTE_2026-05-10.md`](CL3_COMPLEXIFICATION_SPLIT_NARROW_THEOREM_NOTE_2026-05-10.md)
  — sibling narrow theorem supplying the real-algebra isomorphism
  `Cl(3,0) ≅ M_2(C)` used to land the Pauli realisation. Status
  authority: independent audit lane only; effective status
  pipeline-derived.

No other note's effective status is consumed.

## 4. Admitted-context inputs

- **Standard `2 × 2` complex matrix algebra.** Cofactor transpose
  identity `M · adj(M) = det(M) · I_2`, transpose properties, and
  exact arithmetic on `M_2(C)`. Admitted-context mathematical
  infrastructure on the framework's accepted surface.

No physics conventions admitted. No staggered-Dirac realization, no
Grassmann partition input, no `g_bare`, no lattice-action input.

## 5. Proof

### 5.1 Gamma-involution as symplectic adjoint

Write `M = ((a, b), (c, d)) ∈ M_2(C)`. Direct computation:

```text
σ_2 · M^T · σ_2  =  σ_2 · ((a, c), (b, d)) · σ_2
                 =  ((d, -b), (-c, a))
                 =  adj(M)
```

(where `adj(M)` is the cofactor transpose / classical adjugate, which
for `2 × 2` is `((d, -b), (-c, a))`). The standard cofactor identity

```text
M · adj(M)  =  det(M) · I_2  =  adj(M) · M
```

then gives `M · γ(M) = γ(M) · M = det(M) · I_2`. ∎

### 5.2 Gamma-norm determinant identity

From the cofactor identity, `M · γ(M) = det(M) · I_2`, so the scalar
(identity-coefficient) part is `⟨M · γ(M)⟩_0 = det(M)`. The γ-norm
defined as `|M|_γ := √|⟨M · γ(M)⟩_0|` is therefore

```text
|M|_γ  =  √|det(M)|.
```

For `M = c · I_2` (scalar in the identity grade), `det(M) = c²`, so
`|M|_γ = |c|`. For invertible `M`, `|M|_γ > 0`. ∎

### 5.3 Gamma action by grade on the Pauli realisation

Pauli matrices: `σ_1` and `σ_3` are real and symmetric (`σ_i^T = σ_i`
for `i = 1, 3`). `σ_2` is purely imaginary and anti-symmetric:
`σ_2^T = -σ_2`. Compute `γ(σ_i) = σ_2 σ_i^T σ_2` case by case using
`σ_1 σ_2 = i σ_3`, `σ_2 σ_3 = i σ_1`, `σ_3 σ_1 = i σ_2`,
`σ_i² = I_2`, and `σ_2² = I_2`:

```text
γ(σ_1)  =  σ_2 σ_1 σ_2   (σ_1^T = σ_1)
        =  (σ_2 σ_1) σ_2  =  (-i σ_3) σ_2  =  -i · (σ_3 σ_2)
        =  -i · (-i σ_1)  =  -σ_1.

γ(σ_2)  =  σ_2 σ_2^T σ_2   (σ_2^T = -σ_2)
        =  σ_2 · (-σ_2) · σ_2  =  -σ_2 · (σ_2 σ_2)  =  -σ_2 · I_2
        =  -σ_2.

γ(σ_3)  =  σ_2 σ_3 σ_2   (σ_3^T = σ_3)
        =  (σ_2 σ_3) σ_2  =  (i σ_1) σ_2  =  i · (σ_1 σ_2)
        =  i · (i σ_3)    =  -σ_3.

γ(I_2)  =  σ_2 I_2 σ_2  =  σ_2² = I_2.
```

Hence `γ(σ_i) = -σ_i` for `i = 1, 2, 3` and `γ(I_2) = I_2`. By
linearity over `C`, `γ` acts as `-id` on the vector grade
`Span_C(σ_1, σ_2, σ_3)` and as `+id` on the identity grade. For
bivectors with `i ≠ j`, use the antiautomorphic property of γ
(from the transpose):

```text
γ(σ_i σ_j)  =  γ(σ_j) γ(σ_i)  =  (-σ_j)(-σ_i)  =  σ_j σ_i
            =  -σ_i σ_j           (since {σ_i, σ_j} = 0 for i ≠ j).
```

So `γ` acts as `-id` on the bivector grade as well. On the
pseudoscalar `ω` (in the positive-chirality Pauli realisation,
`ω = σ_1 σ_2 σ_3 = i · I_2`),

```text
γ(σ_1 σ_2 σ_3)  =  γ(σ_3) γ(σ_2) γ(σ_1)
                =  (-σ_3)(-σ_2)(-σ_1)
                =  -σ_3 σ_2 σ_1.
```

Using `σ_3 σ_2 = -i σ_1`, `σ_3 σ_2 σ_1 = (-i σ_1) σ_1 = -i · I_2 = -ω`.
Hence

```text
γ(ω)  =  -(-ω)  =  +ω,
```

so γ preserves the pseudoscalar grade in this realisation. The
matrix-level fact `γ(σ_1 σ_2 σ_3) = +σ_1 σ_2 σ_3` is verified
directly by computing `σ_2 (σ_1 σ_2 σ_3)^T σ_2 = σ_1 σ_2 σ_3`.

In summary, on the 8-element Cl(3) basis
`(I_2, σ_1, σ_2, σ_3, σ_1σ_2, σ_1σ_3, σ_2σ_3, ω)`, γ acts with
signs `(+, -, -, -, -, -, -, +)`, preserving identity and
pseudoscalar grades and flipping vector and bivector grades. This
is the **antiautomorphic transpose involution** restricted to the
Pauli realisation. It differs from reversion (which preserves
identity and vector grades and flips bivector and pseudoscalar
grades) in the grading pattern; both are valid `M_2(C)` involutions
with `|M|_γ² = det(M)` (this norm-identity follows from the
symplectic-adjoint identity and is independent of the per-grade sign
pattern). ∎

### 5.4 Per-element vs full-lattice readout boundary

The γ-norm determinant identity gives the γ-norm of a single
`Cl(3) ≅ M_2(C)`
element. The framework's per-determinant geometric-mean readout

```text
v(L_t)  ∝  |det(D + J)|^{1/(N_taste · L_t)}
```

(admitted by
`HIERARCHY_HEAT_KERNEL_D4_COMPRESSION_BOUNDED_THEOREM_NOTE_2026-05-10.md`
§4.3) extracts a mass-dim-1 scale from a high-dimensional lattice
determinant of `D + J : (C²)^{⊗ N_sites} ⊗ C^{taste} ⊗ C^{spinor}
→ (C²)^{⊗ N_sites} ⊗ C^{taste} ⊗ C^{spinor}`. The exponent
`1/(N_taste · L_t)` is the **reciprocal of the per-spatial-corner
mode count**, not the `1/2` factor produced by the per-element
γ-norm.

Specifically, three structurally distinct factors enter this readout:

1. **mass-dim conversion.** `|det(D + J)|` has mass-dim equal to the
   total dimension of `D` (each eigenvalue has mass-dim 1); to read a
   mass-dim-1 scale, one takes the geometric-mean reciprocal power.
2. **mode count.** The exponent `1/(N_taste · L_t)` is the reciprocal
   of the per-spatial-corner mode count at `L_s = 2`. This count is a
   lattice-block fact, not a Cl(3)-algebra fact.
3. **square-root sub-choice.** Whether to read eigenvalues of `D` or
   eigenvalues of `D†D` (giving a `1/2` factor) is a separate
   convention (heat-kernel note §4.3 discusses both choices and
   notes the framework's bilinear-selector closure uses the
   `D`-eigenvalue convention; cf. eq (10) of that note).

None of (1)-(3) is forced by the per-element γ-norm identity.
The Cl(3) γ-involution structure provides the `√det` identity on
single `Cl(3) ≅ M_2(C)` elements, but the lattice determinant
`|det(D + J)|` is a determinant on a **tensor-product** space, and
its `1/(N_taste · L_t)`-th power is a per-mode geometric-mean readout
governed by lattice-block dimensional analysis, not by per-element
γ-norm.

This is the boundary of the γ-norm identity's applicability. The
boundary records it explicitly so that the per-determinant readout
admission in the heat-kernel note is **not** mis-read as following
from the per-element identity.
∎

## 6. What this note does NOT claim

- Does **not** derive the per-determinant geometric-mean readout
  `v(L_t) ∝ |det(D + J)|^{1/(N_taste · L_t)}` from Cl(3) γ-involution
  algebra. That admission stands as recorded in
  `HIERARCHY_HEAT_KERNEL_D4_COMPRESSION_BOUNDED_THEOREM_NOTE_2026-05-10`
  §4.3 and is **not** retired by this note.
- Does **not** identify the abstract `Cl(3) ≅ M_2(C)` per-element
  γ-norm with any specific physical Cl(3)-element on the framework's
  Z^3 lattice substrate. That identification depends on the
  staggered-Dirac realization gate
  `STAGGERED_DIRAC_REALIZATION_GATE_NOTE_2026-05-03.md` substep 1
  and is out of scope.
- Does **not** assert that γ-involution structure is the unique
  natural choice of involution on `M_2(C)`. Alternative involutions
  (Hermitian conjugate, transpose) give different per-element
  norms; this note records the specific γ-involution
  `γ(M) = σ_2 M^T σ_2` and its determinant identity, not a
  uniqueness theorem.
- Does **not** close the EWSB hierarchy formula
  `v = M_Pl × α_LM^16 × (7/8)^(1/4)` or any of its substeps. The
  separate substeps (`(7/8)` exact algebra, `(1/4)` exponent, `L_t =
  4` selection, `α_LM^16` chain) are handled by their own notes.
- Does **not** make any physical / observational claim, consume any
  PDG value, fitted constant, or admit any lattice convention beyond
  the abstract Cl(3) ≅ M_2(C) algebra.

## 7. Forbidden imports check

- No PDG observed values consumed.
- No literature numerical comparators consumed.
- No fitted selectors consumed.
- No admitted unit conventions load-bearing on the claim.
- No same-surface family arguments.
- No staggered-Dirac realization gate output consumed.
- No `g_bare = 1` derivation output consumed.
- No lattice-action carrier (Wilson plaquette, staggered phases,
  Grassmann partition, Hilbert-space-on-physical-lattice)
  load-bearing.

## 8. Open derivation gap

The step from the per-element γ-norm identity `|M|_γ = √|det(M)|` on
`M_2(C)` to "the framework's per-determinant geometric-mean readout
`v(L_t) ∝ |det(D + J)|^{1/(N_taste · L_t)}` is forced by Cl(3) γ-norm
structure" is **not** closed by this note. The boundary records this
explicitly. The gap is `(1)` the mass-dim conversion from a tensor-
product lattice determinant to a per-mode geometric-mean scale, and
`(2)` the reciprocal-mode-count exponent `1/(N_taste · L_t)`, neither
of which is a per-element `Cl(3)` algebra fact.

Closing this gap (i.e., retiring the per-determinant geometric-mean
readout admission in the heat-kernel note) is the open work of a
future bounded theorem; possible paths include zeta-regularization on
the tensor-product space, structural derivation of `N_taste = 2^D`
from the staggered-Dirac realization gate, or an independent
heat-kernel argument tighter than the current
Stefan-Boltzmann-source-lineage support. None of these is undertaken
here.

## 9. Validation

Primary runner:
[`scripts/frontier_cl3_gamma_involution_determinant_narrow.py`](../scripts/frontier_cl3_gamma_involution_determinant_narrow.py)
verifies via sympy exact symbolic arithmetic on `2 × 2` complex
matrices:

1. **Symplectic-adjoint identity.** For symbolic `M = ((a, b), (c, d))`,
   `σ_2 · M^T · σ_2 = adj(M)` entrywise.
2. **Gamma-norm determinant identity.** For symbolic `M`,
   `M · γ(M) = det(M) · I_2` entrywise and
   `⟨M · γ(M)⟩_0 = det(M)`.
3. **Numerical γ-norm.** For ten random `M ∈ M_2(C)` (rational + i ·
   rational entries), verify `|M|_γ = √|det(M)|` exactly.
4. **Vector-grade action.** `γ(σ_i) = -σ_i` for `i = 1, 2, 3`
   and `γ(I_2) = I_2` exactly.
5. **Bivector-grade action.** `γ(σ_i σ_j) = -σ_i σ_j` for
   `i ≠ j` exactly (`9 - 3 = 6` non-identity off-diagonal pairs).
6. **Pseudoscalar action.** `γ(ω) = +ω` where
   `ω = σ_1 σ_2 σ_3 = i · I_2` in the positive-chirality realisation.
7. **Full-lattice readout boundary.** Construct a `4 × 4` block-diagonal "lattice"
   matrix `D_lat = M_1 ⊕ M_2` with two `M_i ∈ M_2(C)`. Show that
   `|det(D_lat)|^{1/2}` (the "geometric-mean readout at `N_modes = 2`")
   equals `|det(M_1) · det(M_2)|^{1/2}` (a per-element-product fact),
   which is NOT the same as `|M_1|_γ + |M_2|_γ` or any other per-element
   γ-norm combination. This is a numerical witness that per-element
   γ-norm does not produce the per-determinant readout.
8. **γ-involution as composition.** Verify the equivalent
   reversion-and-grading-automorphism decomposition on the Cl(3) basis
   `(I_2, σ_1, σ_2, σ_3, σ_1σ_2, σ_1σ_3, σ_2σ_3, ω)`.

Expected output: `PASS=N FAIL=0` with `N ≥ 30`.

## 10. Cross-references

Load-bearing markdown-link upstream (single one-hop dependency):

- [`CL3_COMPLEXIFICATION_SPLIT_NARROW_THEOREM_NOTE_2026-05-10.md`](CL3_COMPLEXIFICATION_SPLIT_NARROW_THEOREM_NOTE_2026-05-10.md)
  — sibling narrow theorem supplying the real-algebra isomorphism
  `Cl(3,0) ≅ M_2(C)`.

Plain-text (non-load-bearing) reader pointers are written without
markdown links so the citation-graph builder does not parse them as
upstream dependency edges:

- `HIERARCHY_HEAT_KERNEL_D4_COMPRESSION_BOUNDED_THEOREM_NOTE_2026-05-10.md`
  — companion bounded theorem whose §4.3 per-determinant
  geometric-mean readout admission is the motivating question for
  this narrow theorem. The boundary records that this note does **not**
  retire that admission.
- `HIERARCHY_BOSONIC_BILINEAR_SELECTOR_NOTE.md`
  — parent note that adopts the per-determinant readout in the
  bilinear-selector closure (cf. heat-kernel note eq (10)).
- `HIERARCHY_MATSUBARA_DECOMPOSITION_NOTE.md`
  — retained authority on `|det(D + m)|` product structure at `L_s
  = 2`. The boundary does not consume its effective status; reader pointer
  only.
- `CPT_EXACT_NOTE.md`
  — authority on `D` real anti-Hermitian; reader pointer only,
  the boundary does not consume its effective status.
- `CL3_FAITHFUL_IRREP_DIM_TWO_NARROW_THEOREM_NOTE_2026-05-10.md`
  — sibling narrow theorem identifying the unique `Cl(3,0)`
  faithful complex irrep as 2-dim, the setting on which the γ-norm identity
  is computed. Not load-bearing for the abstract `M_2(C)`
  algebraic content.

## 11. Citation-graph note

Load-bearing markdown-link upstream is the sibling narrow theorem
[`CL3_COMPLEXIFICATION_SPLIT_NARROW_THEOREM_NOTE_2026-05-10.md`](CL3_COMPLEXIFICATION_SPLIT_NARROW_THEOREM_NOTE_2026-05-10.md)
only. Cross-references to the heat-kernel note, the bilinear-selector
note, the Matsubara decomposition note, the CPT-exact note, and the
sibling irrep-dim note are plain-text reader pointers (not markdown
links); the theorem and boundary do not consume their effective statuses.
