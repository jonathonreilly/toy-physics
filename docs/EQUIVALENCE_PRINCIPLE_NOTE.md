# Equivalence Principle

**Date:** 2026-04-05
**Status:** proposed_retained positive — m_inertial = m_gravitational to 0.8%

## Result

Uniform field f = g*z gives:
- F ~ g^1.008 (linear in field = inertial mass)
- F ~ M^0.998 (linear in source strength = gravitational mass)
- Combined: m_inertial = m_gravitational to 0.8%

The deflection grows as layer^1.14 (sub-quadratic), which means the
beam doesn't accelerate at constant rate — wave optics modifies the
trajectory. But the total deflection is exactly proportional to the
field strength.

## What this means

The valley-linear action S = L(1-f) couples the field to the propagator
in a way that makes gravitational and inertial mass identical. This is
not an accident — it's structural: the same parameter (f = s/r) controls
both the deflection (gravitational mass) and the response to uniform
force (inertial mass).
