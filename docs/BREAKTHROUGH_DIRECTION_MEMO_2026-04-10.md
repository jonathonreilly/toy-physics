# Breakthrough Direction Memo

**Date:** 2026-04-10

## Strongest Current Result

The strongest defended result is the retained staggered force-first stack:

- force-based staggered card closes `17/17` on the retained 1D/3D operating
  points
- cycle-bearing battery closes `9/9` on three admissible graph families
- scaled sibling keeps that `9/9` battery closed through side `12`
- self-gravity is retained on those same graph families
- two-field wave coupling is retained with hard `5/5`
- a causal-DAG sibling shows the same force/Born/norm/family story survives on
  layered DAGs

That is a real methodological result: the staggered force-first gravity lane is
portable across an admissible graph class and is not just a periodic-cubic
artifact.

## Strongest Negative Result

The strongest negative result is the coin diagnosis:

> the chiral / coined gravity blockers all trace back to the coin mixing period.

Across the retained chiral/Dirac lanes, the same root pathology keeps
reappearing as chromaticity, equivalence trouble, and `N`-oscillation. That is
the cleanest structural no-go in the branch.

The second strongest negative is that the local graph-Poisson source sector is
structurally too smooth relative to the external `1/r` control. That is no
longer a tuning mystery.

## What Would Count As A Breakthrough

An incremental extension is another sibling harness that passes on one more
graph family or one more field law.

A real breakthrough would be:

> one canonical, graph-native, endogenous matter-plus-field dynamics that
> simultaneously closes the retained force battery, family robustness, and
> source-scale closure on both an admissible cycle-bearing graph family and an
> admissible causal-DAG family, without relying on external kernels or
> architecture-specific rescue semantics.

That would move the project from “strong lattice/graph methodology” toward a
credible discrete-physics architecture claim.

## Single Highest-Value Next Experiment

Build one canonical iterative endogenous two-field harness on exactly two
families:

- one retained cycle-bearing family
- one retained causal-DAG family

Use the best current nonlocal/retarded source law as the starting point and
require, in one harness:

1. zero-source reduction
2. source linearity
3. additivity
4. TOWARD force
5. iterative stability
6. exact matter norm
7. family robustness
8. graph-native gauge where cycles exist
9. materially reduced source-scale gap without external-kernel rescaling

If that closes, the project crosses from “clever retained probes” to a real
candidate architecture. If it fails, the branch learns exactly where the
remaining obstruction lives.
