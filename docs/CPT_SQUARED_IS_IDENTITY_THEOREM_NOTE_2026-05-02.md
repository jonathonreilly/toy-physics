# (CPT)² = Identity on the Framework's CPT Structure

**Date:** 2026-05-02
**Type:** positive_theorem
**Claim scope:** the antiunitary CPT operator on the framework's RP-reconstructed H_phys, defined by composing the framework's exact C (sublattice parity), P (spatial inversion), and T (complex conjugation), satisfies the involution identity (CPT)² = I exactly on H_phys with no phase factor.
**Status authority:** independent audit lane only. This source note does
not set or predict an audit outcome; later status is generated by the
audit pipeline after independent review.
**Loop:** `positive-only-r3-20260502`
**Cycle:** 2 (Block 2)
**Branch:** `physics-loop/positive-only-r3-block02-cpt-squared-20260502`
**Runner:** `scripts/cpt_squared_is_identity_check.py`
**Log:** `outputs/cpt_squared_is_identity_check_2026-05-02.txt`

## Cited authorities (one hop)

- [`CPT_EXACT_NOTE.md`](CPT_EXACT_NOTE.md) provides:
  - **C operator:** sublattice parity ε(x) = (-1)^{x_1+x_2+x_3}, real diagonal involutory.
  - **P operator:** spatial inversion x → -x mod L, real involutory permutation.
  - **T operator:** complex conjugation K (since H is real).
  - **(CP)² = I** explicitly.

This is the only load-bearing one-hop dependency.

## Admitted-context inputs

- **Antiunitary operator squared yields unitary.** Standard QM result: T·T·U = T·(T·U) where T antiunitary. (T·U) is antilinear, so T·(T·U) = U is linear. So T² is unitary. Pure linear algebra.
- **Antiunitary commutation rule** with unitary: T · U = U* · T for any unitary U. Pure structural.

No physics conventions admitted beyond what `CPT_EXACT_NOTE.md` provides.

## Statement

Let CPT = C·P·T be the antiunitary CPT operator on the framework's
`H_phys`, with C, P, T as in `CPT_EXACT_NOTE.md`. Then:

**(I1)** (CPT)² = I on H_phys, exactly. No phase factor.

**(I2)** CPT is therefore an involution on H_phys: applying CPT twice returns the original state.

This note does not claim a complex-linear eigenspace decomposition for
the antiunitary CPT operator. Fixed-state questions for the antiunitary
involution are out of scope here.

## Proof

### Step 1 — Decomposition

CPT = (CP) · T where CP is unitary (real, by `CPT_EXACT_NOTE.md`: both C and P are real operators, so CP is real -> its complex conjugate equals itself: (CP)* = CP) and T = K (complex conjugation).

### Step 2 — Compute (CPT)²

```
(CPT)² = (CP·T)(CP·T) = CP·T·CP·T
```

By the antiunitary commutation rule (T·U = U*·T for unitary U):

```
T·CP = (CP)* · T = CP · T   (since CP is real ⇒ (CP)* = CP)
```

So:

```
(CPT)² = CP · (T·CP) · T = CP · CP · T · T = (CP)² · T²
```

### Step 3 — Apply cited identities

By `CPT_EXACT_NOTE.md`:
- (CP)² = I (cited identity, line 77 of source note)
- T = K (complex conjugation), so T² = K² = I (applying conjugation twice on any complex number returns it)

Therefore:

```
(CPT)² = I · I = I
```

establishing (I1). ∎

### Step 4 — Involution statement (I2)

Any operator A satisfying A² = I is an involution, so applying CPT
twice returns the original state. Because CPT is antiunitary, this note
does not infer a complex-linear eigenspace decomposition from (I1). ∎

## Hypothesis set used

- `cpt_exact_note`: provides (CP)² = I and explicit C, P, T constructions.
- Antiunitary commutation rule (basic linear algebra).
- T² = K² = I (basic complex analysis).

## Out of scope

This note does not prove CPT selection rules, Majorana-mass consequences,
a new reality theorem for the Hamiltonian, or a CPT gauge-action
interpretation. Those require separate hypotheses and are not used for
the in-scope involution claim.

## Review classification

Positive-theorem proposal on the cited CPT source surface. Single
one-hop chain. Pure algebraic consequence of (CP)² = I (cited from
`CPT_EXACT_NOTE.md`) plus T² = I.

```yaml
claim_type_author_hint: positive_theorem
claim_scope: "(CPT)² = I exactly on H_phys"
upstream_dependencies:
  - cpt_exact_note
admitted_context_inputs:
  - antiunitary commutation rule T·U = U*·T
  - T² = K² = I
```
