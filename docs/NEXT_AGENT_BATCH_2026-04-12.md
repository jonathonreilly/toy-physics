# Next Agent Batch

**Date:** 2026-04-12  
**Status:** highest-value attack queue after the latest RH, `y_t`, DM, and compactification results  
**Scope:** only the remaining paper-critical theorem gaps

## Priority Order

1. generation physicality
2. `S^3` cap-map uniqueness / compactification
3. DM graph-clock to cosmological relic mapping
4. renormalized top-Yukawa matching `Z_Y(mu) = Z_g(mu)`
5. graph-canonical right-handed singlet-template derivation
6. CKM / Higgs-`Z_3` charge closure
7. gauge-coupling equal-partition theorem

## Why This Order

- generation physicality is now the largest remaining matter-sector hole
- `S^3` compactification is the largest remaining cosmology/topology hole
- DM has a real graph-native freeze-out threshold now, so the missing physical map is sharply exposed
- `y_t` is no longer diffuse: bare matching is closed, renormalized matching is the only missing step
- right-handed matter is now paper-usable in bounded `3D + 4D chirality` form, so full graph-canonical RH closure is no longer above the three items above
- CKM is active on the Mini and is no longer critical path
- `g_2` remains bounded on the equal-partition assumption and should stay below the matter/topology/relic gates

## Worker A: Generation Physicality

**Goal:** close or kill the physical-generation claim at theorem grade.

**Current best boundary:**
- exact orbit algebra `8 = 1 + 1 + 3 + 3`
- no canonical physical-family theorem

**Attack target:**
- derive a graph-canonical matter assignment for the two triplets
- or prove that no such assignment exists on the current surface

**Success bar:**
- not just bounded distinctness
- must produce a canonical physical-family basis or a theorem reducing the claim to an explicit open post-`Z_3`-breaking hierarchy step

**Failure bar:**
- if the obstruction persists, rewrite the paper to treat generations as open

## Worker B: S^3 Compactification

**Goal:** attack the last cap-map uniqueness step.

**Current best boundary:**
- shell theorem is strong
- `S^2` boundary signature is strong
- compactification remains blocked on uniqueness of the cap map

**Attack target:**
- prove the closure map is forced by the graph axioms or spectral determinacy
- or isolate the exact minimal extra topological principle required

**Success bar:**
- a theorem reducing `finite regular graph + shell structure` to a unique closed simply connected `3`-manifold completion

## Worker C: DM Relic Mapping

**Goal:** bridge graph-native freeze-out to the physical relic law.

**Current best boundary:**
- direct lattice Sommerfeld/contact enhancement is real
- graph-native freeze-out threshold exists
- physical map to `x_F = m/T_F` and `Omega_DM / Omega_b` is still imported

**Attack target:**
- derive the map from graph clock / graph dilution to the cosmological freeze-out variables
- or sharply prove the minimal external cosmology inputs that remain unavoidable

**Success bar:**
- remove the current dependence on imported Boltzmann/Friedmann machinery as the decisive freeze-out law

## Worker D: Renormalized y_t Matching

**Goal:** close or isolate `Z_Y(mu) = Z_g(mu)`.

**Current best boundary:**
- exact bare theorem closed:
  - gauged Ward identity
  - projector factor `Tr(P_+)/dim = 1/2`
  - `y_0 = g_0 / sqrt(6)`
- renormalized matching is still open

**Attack target:**
- derive a scheme-level lattice Slavnov-Taylor / Ward identity enforcing the renormalized matching
- or prove the exact obstruction from independent finite rescalings cannot be removed on the current surface

**Success bar:**
- a real renormalized theorem, not another bare normalization rewrite

## Worker E: Graph-Canonical RH Template

**Goal:** derive the right-handed singlet template from the graph, not from `4D` anomaly completion assumptions.

**Current best boundary:**
- `3D` graph/taste gives left-handed gauge algebra, left-handed matter quantum numbers, and antiparticle structure
- `4D` chirality gives RH singlets
- anomaly cancellation then fixes RH hypercharges

**Attack target:**
- derive the actual RH singlet-template structure from the graph/spacetime extension, not just its charges once assumed

**Success bar:**
- remove the need to assume `(1,3) + (1,3) + (1,1) + (1,1)` as an input template

## Worker F: CKM

**Goal:** continue on the Mini only.

**Current best boundary:**
- bounded support for Interpretation B
- Higgs `Z_3` charge step still finite-size dependent and not theorem-grade

**Instruction:**
- do not treat CKM as critical path until the Higgs-charge step is universal or explicitly bounded

## Worker G: Gauge Couplings

**Goal:** attack the equal-partition principle only if higher-priority gates stall.

**Current best boundary:**
- `g_2^2 = 1/(d+1)` remains bounded on an unproved equal-partition assumption

**Instruction:**
- do not spend prime worker budget here while generation, compactification, DM relic mapping, and renormalized `y_t` remain open
