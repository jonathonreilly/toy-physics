# Gauge-Vacuum Plaquette First Symmetric Three-Sample Positive-Cone Order Witness

**Date:** 2026-04-17
**Status:** exact PF-only controlled-truncation witness on the explicit
three-sample seam; beyond the exact radical reconstruction map and the
current-stack constraint boundary, positivity of the retained character data
already forces the first symmetric retained sample triple into one explicit
simplicial cone and yields universal order inequalities
**Type:** positive_theorem
**Runner:** `scripts/frontier_gauge_vacuum_plaquette_first_three_sample_local_wilson_retained_positive_cone_obstruction_2026_04_17.py`

## Question

Is there one honest theorem on the explicit three-sample evaluation seam that
is genuinely stronger than:

- the exact radical reconstruction map, and
- the current-stack statement that no further universal linear collapse is
  available?

## Answer

Yes.

On the first symmetric retained witness sector, once the coefficient triple is
required to come from the positive-type character-measure data already proved on
the Wilson surface, the three named sample vector is not arbitrary.

It lies in one exact simplicial cone generated by the three radical column rays
of the explicit sample matrix.

Equivalently, the exact inverse map gives three necessary-and-sufficient linear
half-space inequalities on the sample vector.

This yields theorem-grade partial control on the seam:

- the first retained three-sample vector is already restricted to an explicit
  positive cone,
- `Z_B - Z_A >= 0` on that cone,
- and after dividing by the trivial-channel coefficient, the retained profile
  obeys
  `Z_A / a_(0,0) <= 1 <= Z_B / a_(0,0)`.

So the first retained `beta = 6` PF seam is now sharper than “evaluate three
numbers.” It is “evaluate three numbers inside a known exact cone with a known
order witness.”

## Setup

From
[GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_CHARACTER_MEASURE_THEOREM_NOTE.md](./GAUGE_VACUUM_PLAQUETTE_SPATIAL_ENVIRONMENT_CHARACTER_MEASURE_THEOREM_NOTE.md):

- the environment class function has an exact `SU(3)` character expansion with
  nonnegative conjugation-symmetric coefficients,
- so on the first symmetric retained witness sector the retained coefficient
  triple satisfies
  `a_(0,0) >= 0`, `a_(1,0) >= 0`, `a_(1,1) >= 0`.

From
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_EXACT_RADICAL_RECONSTRUCTION_MAP_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_EXACT_RADICAL_RECONSTRUCTION_MAP_NOTE_2026-04-17.md):

- the first symmetric retained witness sector is

`v = a_(0,0) chi_(0,0)
   + a_(1,0) (chi_(1,0) + chi_(0,1))
   + a_(1,1) chi_(1,1)`,

- the three named holonomies are

`W_A = W(-13 pi / 16,  5 pi / 8)`,

`W_B = W( -5 pi / 16, -7 pi / 16)`,

`W_C = W(  7 pi / 16,-11 pi / 16)`,

- and the exact radical-form sample matrix is

`F = [[1, a, 0],`
`     [1, b, c],`
`     [1, d, e]]`,

with sign pattern

`a < 0`, `b > 0`, `c > 0`, `d > 0`, `e < 0`.

Numerically,

`a = -2.2961005941905386`,

`b =  0.2613226430950979`,

`c =  1.7859942368881772`,

`d =  2.0797612210984413`,

`e = -1.1711232097915510`.

## Theorem 1: exact positive cone on the first retained three-sample seam

Let

`Z = [Z_A, Z_B, Z_C]^T`

be the first symmetric retained sample vector, where

`Z_i = <K(W_i), v>`.

Then

`Z = F a_vec`,

with

`a_vec = [a_(0,0), a_(1,0), a_(1,1)]^T`.

If `a_(0,0), a_(1,0), a_(1,1) >= 0`, then `Z` lies in the exact simplicial cone

`C = Cone(r_0, r_1, r_2)`,

generated by the three column rays

`r_0 = (1, 1, 1)^T`,

`r_1 = (a, b, d)^T`,

`r_2 = (0, c, e)^T`.

Because `det(F) != 0`, this is equivalent to the three exact half-space
conditions

`F^(-1) Z >= 0`

componentwise.

So positivity of the retained character data already cuts the first explicit
three-sample seam down to one explicit polyhedral cone.

## Corollary 1: exact half-space inequalities on the sample triple

Writing

`F^(-1) Z = [a_(0,0), a_(1,0), a_(1,1)]^T`,

the first retained three-sample vector must satisfy the three inequalities

`0.3719114640 Z_A + 0.2487452986 Z_B + 0.3793432374 Z_C >= 0`,

`-0.2735457399 Z_A + 0.1083337983 Z_B + 0.1652119416 Z_C >= 0`,

`-0.1682131790 Z_A + 0.4047855318 Z_B - 0.2365723528 Z_C >= 0`.

These are not arbitrary fitted bounds. They are the exact half-space faces of
the cone `C`, numerically displayed.

## Theorem 2: exact order witness on the positive cone

On the first symmetric retained positive cone,

`Z_B - Z_A = (b - a) a_(1,0) + c a_(1,1) >= 0`.

So every nonnegative retained coefficient triple satisfies

`Z_B >= Z_A`,

with strict inequality unless

`a_(1,0) = a_(1,1) = 0`.

This gives one theorem-grade partial ordering on the explicit three-sample
seam without evaluating `beta = 6`.

## Corollary 2: normalized retained profile brackets the trivial baseline

If `a_(0,0) > 0` and

`x = a_(1,0) / a_(0,0)`,

`y = a_(1,1) / a_(0,0)`,

then the normalized retained sample profile is

`Z_A / a_(0,0) = 1 + a x`,

`Z_B / a_(0,0) = 1 + b x + c y`,

`Z_C / a_(0,0) = 1 + d x + e y`.

Because `a < 0` and `b, c > 0`,

`Z_A / a_(0,0) <= 1 <= Z_B / a_(0,0)`.

So on the retained positive cone:

- `W_A` is always below the trivial baseline,
- `W_B` is always above the trivial baseline,
- `W_C` remains genuinely mixed because its two nontrivial orbit contributions
  have opposite sign.

## What this closes

- one exact positive-cone description of the first retained three-sample seam
- one exact half-space description of that cone via the already-known inverse
  map
- one exact order witness `Z_B >= Z_A` on the retained positive sector
- one exact normalized retained-profile bracket
  `Z_A / a_(0,0) <= 1 <= Z_B / a_(0,0)`
- one honest controlled-truncation witness that is genuinely stronger than the
  current-stack constraint boundary

## What this does not close

- explicit same-surface `beta = 6` values
  `Z_6^env(W_A), Z_6^env(W_B), Z_6^env(W_C)`
- explicit closed-form class-sector matrix elements of `K_6^env`
- explicit closed-form class-sector matrix elements of `B_6(W)`
- exact control of higher retained sectors
- the global sole-axiom PF selector theorem

## Why this matters

This is the strongest honest theorem now available on the first explicit
three-sample seam without pretending the `beta = 6` integrals are already
evaluated.

The branch can now say something stronger than:

- there is no hidden linear collapse, and
- the matrix is explicit.

It can say:

- the first retained three-sample seam already lives in an exact positive cone,
- that cone already forces one universal sample ordering,
- and any first-sector retained approximation to the live `beta = 6` data must
  respect that geometry.

## Command

```bash
python3 scripts/frontier_gauge_vacuum_plaquette_first_three_sample_local_wilson_retained_positive_cone_obstruction_2026_04_17.py
```
