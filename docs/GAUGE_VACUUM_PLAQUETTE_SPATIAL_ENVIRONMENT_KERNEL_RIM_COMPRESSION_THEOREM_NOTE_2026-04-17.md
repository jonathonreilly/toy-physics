# Gauge-Vacuum Plaquette Spatial-Environment Kernel / Rim Compression Theorem

**Date:** 2026-04-17  
**Status:** exact pre-compression theorem on the plaquette PF lane; once the
orthogonal-slice kernel `K_6^env` and the local rim map are explicit, the
class-sector transfer object `S_6^env`, the boundary states `eta_6`, the
coefficients `rho_(p,q)(6)`, and the downstream plaquette PF data follow
canonically  
**Script:** `scripts/frontier_gauge_vacuum_plaquette_spatial_environment_kernel_rim_compression_theorem.py`

## Question

If the live constructive target is now `K_6^env` plus the rim map to `eta_6`,
does the current exact stack already justify that as a canonical theorem-level
compression statement, rather than only as a good informal summary?

## Answer

Yes.

The current exact stack already proves the right pre-compression structure.

From the spatial-environment transfer theorem:

- integrating the unmarked Wilson weight between adjacent orthogonal slices
  defines one exact kernel `K_beta^env(U_(k+1), U_k)`,
- and the environment boundary class function is exactly a boundary amplitude
  generated from that kernel and one rim-induced boundary state `eta_beta(W)`.

From the local/environment factorization theorem:

- after trivial-channel normalization, the non-marked mixed-link factors are
  rep-independent scalars on the marked source sector,
- so the only nontrivial local marked data sit on the rim adjacent to the
  marked plaquette.

Therefore the live environment data are canonically generated by two exact
pre-compression objects:

1. the orthogonal-slice kernel `K_beta^env`,
2. one local rim map `B_beta` whose image gives the boundary states
   `eta_beta(W)`.

Compressing those objects to the marked class sector determines:

- the transfer object `S_beta^env`,
- the boundary states `eta_beta`,
- the coefficients `rho_(p,q)(beta)`.

So the remaining constructive gap is exactly explicit `K_6^env / B_6`, not any
new post-compression PF formalism.

## Setup

From the exact spatial-environment transfer theorem already on `main`:

- one orthogonal-slice kernel `K_beta^env(U_(k+1), U_k)` is defined by
  integrating the Wilson weight between adjacent unmarked spatial slices,
- the environment boundary class function is an exact boundary amplitude of the
  resulting transfer law,
- and `eta_beta(W)` is induced by the local rim coupling of the marked
  plaquette holonomy to the adjacent unmarked slice.

From the exact local/environment factorization theorem:

- non-marked mixed-link factors collapse to trivial-channel scalars on the
  marked source sector after normalization,
- while the local marked-link contribution is already explicit.

So the remaining nontrivial local marked data are exactly rim data.

## Theorem 1: one-slab kernel plus rim map canonically determine the class-sector transfer pair

Let `H_slice` be the orthogonal-slice Hilbert space of the unmarked environment
and let `P_cls` be the canonical compression to the marked class-function
sector.

Let `K_beta^env` be the exact one-slab orthogonal-slice kernel and let
`B_beta(W)` be the local rim map whose image is the induced boundary state on
one edge slice.

Then the exact class-sector transfer/boundary data are the canonical
compressions

`S_beta^env = P_cls K_beta^env P_cls`,

`eta_beta(W) = P_cls B_beta(W)`.

Therefore the environment boundary coefficients are exact compressed
boundary-amplitude data:

`z_(p,q)^env(beta)
 = <chi_(p,q), (S_beta^env)^(L_perp-1) eta_beta(e)>`.

Equivalently,

`rho_(p,q)(beta)
 = z_(p,q)^env(beta) / z_(0,0)^env(beta)`.

So once `K_6^env` and `B_6` are explicit, the class-sector transfer object and
its boundary coefficients are explicit automatically.

## Corollary 1: downstream plaquette PF data then follow canonically

The exact factorized plaquette source-sector law already has the form

`T_src(6) = exp(3 J) D_6^loc R_6^env exp(3 J)`.

After the exact spatial-environment transfer realization,
`R_6^env` is equivalent to the boundary data generated by the compressed
kernel/rim pair.

So once `K_6^env` and `B_6` are explicit, the remaining plaquette PF data are
no longer new structural unknowns. They are simply the Perron/Jacobi data of
the now-explicit factorized source-sector operator.

## What this closes

- exact theorem-level justification that `K_6^env` plus a local rim map is the
  canonical pre-compression environment object
- exact clarification that `S_6^env`, `eta_6`, and `rho_(p,q)(6)` are
  compressed descendants of that pre-compression object
- exact clarification that the remaining gap is explicit pre-compression
  construction, not new post-compression PF formalism

## What this does not close

- explicit formula for `K_6^env`
- explicit formula for the rim map `B_6`
- explicit `S_6^env`
- explicit `rho_(p,q)(6)`
- explicit framework-point plaquette Perron data
- a global sole-axiom PF selector

## Why this matters

This is the cleanest next theorem seam because it turns the live plaquette PF
gap into a true construction problem.

The repo no longer needs to ask vaguely for “the environment PF data.”
It now knows the exact object to derive next:

- the one-slab orthogonal kernel `K_6^env`,
- the local rim map `B_6`.

Everything downstream is canonical once those exist.

## Command

```bash
python3 scripts/frontier_gauge_vacuum_plaquette_spatial_environment_kernel_rim_compression_theorem.py
```
