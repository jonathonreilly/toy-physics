# Koide `dW_e^H` Cyclic Compression Note

**Date:** 2026-04-18
**Status:** support - structural or confirmatory support note
Koide lane
**Runner:** `scripts/frontier_koide_dweh_cyclic_compression.py`

## Question

The exact charged projected Hermitian source law on the current DM / leptons
stack is
```
dW_e^H(X) = Re Tr(X H_e)
```
on `Herm(3)`, equivalently the exact `9`-real Hermitian response package for
the charged block `H_e`.

That is the right generic codomain for the full charged-lepton packet. But the
Koide lane does not ask for a generic `Herm(3)` target. It asks for the cyclic
`3`-real descendant already isolated in the April 18 Koide notes.

So what is the exact positive compression from the full `dW_e^H` law to the
Koide-relevant part?

## Bottom line

There is an exact canonical compression:
```
H_e  ->  H_cyc := P_cyc(H_e),
```
where
```
P_cyc(X) = (1/3) sum_{k=0}^2 C^k X C^{-k}
```
is the exact cyclic average on `Herm(3)`.

The image is exactly
```
span_R{B0, B1, B2},
```
with
```
B0 = I,
B1 = C + C^2,
B2 = i(C - C^2).
```

So the generic `9`-real charged Hermitian law compresses canonically to the
three cyclic response channels
```
r0 = dW_e^H(B0),
r1 = dW_e^H(B1),
r2 = dW_e^H(B2).
```

Those three numbers reconstruct the Koide-relevant descendant exactly:
```
H_cyc = (r0/3) B0 + (r1/6) B1 + (r2/6) B2.
```

## Why this matters

This is the clean positive bridge between:

- the exact DM-side charged Hermitian source law `dW_e^H`, and
- the exact Koide-side cyclic `3`-response law.

It means the Koide lane does **not** need a generic new `9`-channel closure
first. It needs the exact microscopic law for the three cyclic sums selected
by `P_cyc`.

That is a much smaller target.

## Exact basis-level compression

Use the standard Hermitian basis generated by the adjacent-chain algebra:

- diagonal slots
  `D1 = E11`, `D2 = E22`, `D3 = E33`
- symmetric off-diagonal slots
  `X12`, `X23`, `X13`
- antisymmetric off-diagonal slots
  `Y12`, `Y23`, `Y13`

Then the cyclic projector acts exactly as:
```
P_cyc(D1) = P_cyc(D2) = P_cyc(D3) = B0 / 3,
P_cyc(X12) = P_cyc(X23) = P_cyc(X13) = B1 / 3,
P_cyc(Y12) = P_cyc(Y23) =  B2 / 3,
P_cyc(Y13) = -B2 / 3.
```

So the `9` basis directions collapse to only `3` cyclic channels.

## Exact generic formula

Write a generic Hermitian target as
```
H_e =
  d1 D1 + d2 D2 + d3 D3
+ x12 X12 + x23 X23 + x13 X13
+ y12 Y12 + y23 Y23 + y13 Y13.
```

Then
```
P_cyc(H_e)
  = ((d1 + d2 + d3) / 3) B0
  + ((x12 + x23 + x13) / 3) B1
  + ((y12 + y23 - y13) / 3) B2.
```

So Koide only sees three exact linear combinations of the full Hermitian law:

- total diagonal sum
  `d_sum = d1 + d2 + d3`
- cyclic symmetric sum
  `x_sum = x12 + x23 + x13`
- signed cyclic antisymmetric sum
  `y_sum = y12 + y23 - y13`

## Exact response formulas

The three cyclic responses are therefore:
```
r0 = Re Tr(B0 H_e) = d1 + d2 + d3,
r1 = Re Tr(B1 H_e) = 2 (x12 + x23 + x13),
r2 = Re Tr(B2 H_e) = 2 (y12 + y23 - y13).
```

Equivalently:
```
H_cyc = (r0/3) B0 + (r1/6) B1 + (r2/6) B2.
```

So the Koide lane only depends on the cyclic compression of `dW_e^H`, not on
the whole generic packet.

## Relation to the physical-lattice caution

This theorem is not a claim that bare `hw=1` is the whole physical story.

It is a statement about the charged Hermitian source law *after* the current
charged-sector reduction has produced `H_e`. The full-lattice / taste-cube
question remains live upstream. But once the effective charged block is in
hand, the Koide-relevant part is exactly its cyclic compression.

That is compatible with the April 18 full-lattice descent theorem: a larger
physical carrier may matter upstream, but the Koide target downstream is still
the same cyclic `3`-response bundle.

## Observed witness

The observed charged-lepton amplitude operator built from the PDG `sqrt(m)`
spectrum is already cyclic, so this compression does not throw away any of the
actual Koide target. It lands exactly on the observed amplitude operator up to
numerical precision.

So the compression is not just an abstract convenience. It is the exact sector
where the observed target already lives.

## Consequence

The positive next science target is now sharper:

1. derive the microscopic source law for the three cyclic responses
   `(r0, r1, r2)`;
2. derive the positive selector principle on that compressed bundle;
3. then attach the final charged-lepton readout.

The full `9`-real `dW_e^H` law is still the right generic charged-packet
target. But for Koide specifically, the exact positive compression to three
channels is now already done.

## What this does not yet close

This note does **not** yet derive:

- the microscopic Wilson / lattice law producing `(r0, r1, r2)`,
- the selector principle forcing the Koide circle,
- the final retained charged-lepton readout primitive.

But it removes one major ambiguity about target size.

## Bottom line

The exact charged Hermitian source law `dW_e^H` compresses canonically to the
three-response cyclic Koide bundle. So the live positive target is no longer a
generic `9`-channel closure first. It is the microscopic law for those three
cyclic sums.
