# DM Denominator Blocker Note

**Date:** 2026-04-14  
**Branch:** `codex/dm-main-derived`

---

## Status

**HISTORICAL PRE-SCHUR BLOCKER**

This note predates the retained local Dirac-lane closure and is now a
historical summary only. The current authority surface for the denominator is:

- `docs/DM_ETA_ROUTE_AUDIT_2026-04-14.md`
- `docs/DM_NEUTRINO_YUKAWA_BLOCKER_NOTE_2026-04-14.md`
- `docs/NEUTRINO_MAJORANA_Z3_NONACTIVATION_THEOREM_NOTE.md`

After the `g_bare` rigidity pass, the next honest blocker for the **full**
ratio

`R = Omega_DM / Omega_b`

is the **baryon denominator**, i.e. the origin of

`eta = n_B / n_gamma`.

This note states the blocker sharply:

- BBN is **not** the problem
- the freeze-out numerator is **not** the problem
- the remaining full-closure problem is still `eta`

---

## What Is Already Closed Enough

1. **The numerator / freeze-out side**
   - the direct-observable `sigma_v` route exists
   - the Coulomb shape has a lattice-native route
   - the Boltzmann / thermodynamic bridge is materially stronger than before

2. **The conversion `eta -> Omega_b`**
   - this is counting, not nuclear physics
   - `BBN_FROM_FRAMEWORK_NOTE.md` makes that boundary explicit
   - the BBN objection is therefore retired at the authority level

So the remaining denominator problem is not "do more BBN."

---

## The Real Blocker

The real blocker is:

> derive `eta` from the framework, or stop claiming that the specific number
> `R = 5.48` is a zero-import theorem.

The branch’s own strongest denominator reads already say this:

- [DM_CLOSURE_ASSESSMENT.md](/Users/jonBridger/Toy%20Physics-dm/docs/DM_CLOSURE_ASSESSMENT.md:44)
- [DM_CONSOLIDATED_STATUS.md](/Users/jonBridger/Toy%20Physics-dm/docs/DM_CONSOLIDATED_STATUS.md:177)
- [DM_ELEGANT_BRAINSTORM.md](/Users/jonBridger/Toy%20Physics-dm/docs/DM_ELEGANT_BRAINSTORM.md:431)

The cleanest harsh read is:

1. the function `R(eta)` is derivable
2. the specific numerical value `R = 5.48` still needs either:
   - observed `eta`, or
   - a closed baryogenesis / asymmetry theorem

---

## What Does Not Save The Denominator

1. **BBN does not save it**
   - `eta -> Omega_b` is already just counting
   - that moves the blocker back one step, to `eta`

2. **The ratio does not cancel `eta`**
   - the branch already has the negative algebra on this point
   - `R` genuinely depends on `eta` through the denominator

3. **ADM is not ready**
   - the branch’s own brainstorm says the naive charge-ratio route gives the
     wrong number
   - it is not current closure authority

4. **Leptogenesis is still bounded support, not closure**
   - the retained local Dirac lane is now much tighter and selects the
     `k_B ~= 8` scale on the present seesaw calibration
   - but the remaining Majorana / `Z_3` activation law is still not derived

5. **Current EW baryogenesis is still bounded**
   - transport remains bounded
   - the detonation / regime mismatch remains unresolved on the full lane

---

## Honest Publication Surface

With the current branch, the strongest honest claims are:

1. **`Omega_DM` / freeze-out numerator is derived much more strongly than
   before**

2. **`R(eta)` is derivable as a theorem surface**
   - given the observed baryon asymmetry, the framework fixes the
     dark-to-baryon ratio structurally

3. **The full numerical statement `R = 5.48` is not yet a zero-import theorem**
   - unless `eta` is derived rather than observed

That is the branch-safe denominator boundary.

---

## Next Hard Problem

If the `g_bare` rigidity theorem is accepted, the next serious task is:

1. either close `eta` from a framework baryogenesis / asymmetry mechanism
2. or narrow the paper claim explicitly to an `R(eta)` theorem

The harsh denominator-side narrowing is now clearer than before:

- the phase ambiguity is secondary
- the staircase step is dominant
- the live theorem gap is now the Majorana / `Z_3` activation law

For a Nature-bar universal-theory claim, option 1 is the real target.
Option 2 is the honest fallback.
