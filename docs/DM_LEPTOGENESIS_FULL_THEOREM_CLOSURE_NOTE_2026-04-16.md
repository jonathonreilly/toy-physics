# DM Leptogenesis Full Theorem Closure

**Date:** 2026-04-16  
**Authority runner:** `scripts/frontier_dm_leptogenesis_full_axiom_closure.py`  
**Framework convention:** "axiom" means only `Cl(3)` on `Z^3`.

## Status

**FULL THEOREM CLOSURE**

The refreshed branch no longer has a transport-side non-axiom boundary.

What is now theorem-native on the branch:

- exact source package
  - `gamma = 1/2`
  - `E1 = sqrt(8/3)`
  - `E2 = sqrt(8)/3`
- exact transfer coefficients
- exact projection law
  - physical denominator `= K00 = 2`
- exact equilibrium conversion factors
  - `d_N = 0.003901498367656259`
  - `s/n_gamma = 7.039433661546651`
- exact radiation expansion law
  - `H_rad(T) = sqrt(4*pi^3*g_*/45) * T^2 / M_Pl`
- exact normalized transport branch
  - `E_H(z) = 1`
- exact direct transport solve

## Exact theorem-native result

On that exact branch:

- `epsilon_1 / epsilon_DI = 0.9276209209197268`
- `kappa_axiom = 0.004829545290766509`
- `eta / eta_obs = 0.188785929502`

## Meaning

The branch is now transport-complete from `Cl(3)` on `Z^3` alone.

So the old final exact boundary on `H_rad(T)` is gone. The remaining issue is
no longer missing derivation; it is numerical disagreement:

- the theorem-native leptogenesis result undershoots observation by a factor
  `~ 5.297`

That is the honest end state of the exact DM denominator lane on the refreshed
branch.
