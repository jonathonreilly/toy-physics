# Neutrino Majorana Pure-Retained `mu` Impossibility

**Date:** 2026-04-16  
**Script:** `scripts/frontier_neutrino_majorana_pure_retained_mu_impossibility.py`

## Question
On the pure-retained sole-axiom Majorana lane, can the current retained
grammar, observable, `Z_3`, or toolkit data force `mu > 0`?

## Answer
No.

The retained Majorana stack now closes by exact contraposition.

- the pure-retained microscopic lane stays charge-zero and scalar on `nu_R`
- if Majorana reopens at all, the missing object is exactly `mu J2`
- retained observable and retained `Z_3` closures do not activate that slot
- the current retained toolkit contains no already-realized charge-`2`
  primitive on the doubled `nu_R` line

Therefore `mu > 0` leaves the pure-retained lane, and equivalently:

`mu = 0` on the pure-retained sole-axiom Majorana bank.

## Consequence

This is the capstone negative closeout for the retained Majorana lane.

Any positive Majorana reopening now requires:

- a genuinely new charge-`2` primitive, or
- a genuinely new source law that activates the canonical `J2` slot,

and either way it lies beyond pure retention.

## Verification

```bash
python3 scripts/frontier_neutrino_majorana_pure_retained_mu_impossibility.py
```
