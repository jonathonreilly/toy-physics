# Physics Critic Tracker

**Purpose:** adversarial review inbox for the strongest skeptical critiques of
the current science claims, retained results, control stack, portability
surface, and experimental framing.

## Open Critiques

| Date | Target Claim / Lane | Critique | Evidence / Files | What Would Falsify Or Resolve It | Status |
| --- | --- | --- | --- | --- | --- |
| 2026-04-12 | `c9d90df` overclaim / interpretation: Poisson uniqueness | The strongest overclaim in the promoted surface is that the Poisson field equation is "unique" rather than merely preferred on this bounded sweep. The note upgrades a single `16^3` Dirichlet-lattice scan over 21 sampled operators into a general uniqueness statement, while the same note still concedes finite-size bias and possible conditioning artifacts. Skeptical read: this surface supports a bounded operator-preference result, not a theorem-level uniqueness claim. | Note overclaim: [`docs/POISSON_EXHAUSTIVE_UNIQUENESS_NOTE.md`](POISSON_EXHAUSTIVE_UNIQUENESS_NOTE.md) lines 5-6, 23-27, 42-44, 48-51.<br>Runner scope: [`scripts/frontier_poisson_exhaustive_uniqueness.py`](../scripts/frontier_poisson_exhaustive_uniqueness.py) lines 764-838 and 898-943.<br>Commit: `c9d90df` | Re-run the same protocol on larger `N`, add at least one orthogonal local-operator family beyond `(-nabla^2)^alpha`, and publish conditioning/regularization diagnostics showing the `alpha = 1` crossing and NN-only convergence survive outside the hand-picked family. | open |
| 2026-04-12 | `c9d90df` weakest null gate: gravitational entanglement controls | The weakest control stack in the promotion is the entanglement note's null logic. The shipped script computes nonzero `I(A:B)` only through the RPA helper, but both advertised nulls force that branch to zero by construction (`mode != "cross"` passes `G=0` into the MI routine), so "PASS" here does not actually falsify an alternative entanglement mechanism. Skeptical read: the null gates confirm the implementation wiring, not the physical claim. | Note claim: [`docs/GRAVITATIONAL_ENTANGLEMENT_NOTE.md`](GRAVITATIONAL_ENTANGLEMENT_NOTE.md) lines 13-20 and 40-42.<br>Construction-level nulls: [`scripts/frontier_gravitational_entanglement.py`](../scripts/frontier_gravitational_entanglement.py) lines 267-331, 389, and 434-473.<br>Commit: `c9d90df` | Show a retest where `I(A:B)` is extracted from the evolved joint state rather than injected via the RPA cross block, and the same nulls still close when the RPA shortcut is disabled. | open |
| 2026-04-12 | `c9d90df` portability hole: `d = 3` selection | The promoted `d = 3` story is not yet portable across harness families. The script measures gravity with a propagation proxy through analytic `d`-dimensional potentials, then imports separate orbit/atom stability theory for the upper bound. Skeptical read: this is a stitched argument tied to ordered Dirichlet cubes and a 2D/analytic measurement proxy, not a numerically demonstrated family-independent dimension-selection mechanism. | Method and bounded claim: [`docs/DIMENSION_SELECTION_NOTE.md`](DIMENSION_SELECTION_NOTE.md) lines 10-23 and 55-75.<br>Script framing: [`scripts/frontier_dimension_selection.py`](../scripts/frontier_dimension_selection.py) lines 5-20 and 142-220.<br>Commit: `c9d90df` | Close this with a full `d`-dimensional retest on an independent graph/boundary family, or with a single dynamical mechanism that reproduces both the lower and upper bounds without relying on the current proxy plus imported external stability arguments. | open |
| 2026-04-12 | `c9d90df` code / harness loophole: non-local divergence in uniqueness sweep | The strongest harness loophole is that the "non-local operators diverge" discriminator is not run under matched solver conditions. The NN baseline uses `max_iter=25, mixing=0.3`, while the non-local cases switch to `max_iter=40, mixing=0.1`; that makes the claimed divergence gap partly confounded by solver tuning rather than isolating operator structure. Skeptical read: until this is rerun under a common convergence protocol, the discriminator can be a numerical-stability artifact. | Baseline vs non-local settings: [`scripts/frontier_poisson_exhaustive_uniqueness.py`](../scripts/frontier_poisson_exhaustive_uniqueness.py) lines 901-929 and summary lines 934-943.<br>Promoted interpretation: [`docs/POISSON_EXHAUSTIVE_UNIQUENESS_NOTE.md`](POISSON_EXHAUSTIVE_UNIQUENESS_NOTE.md) lines 29-31 and 50-51.<br>Commit: `c9d90df` | Re-run NN and non-local operators with the same continuation/mixing/iteration policy, or provide a solver-robustness sweep showing the divergence verdict is invariant to optimizer settings and regularization choices. | open |
| 2026-04-12 | `c9d90df` experiment-facing skepticism: entanglement as unique/measurable discriminator | The sharpest experiment-facing gap is that the promoted entanglement note treats `I(A:B)` as a gravitational discriminator without showing either uniqueness against non-gravitational couplings or an observable measurement protocol. The script computes MI from an RPA/correlation-matrix proxy, not from an experimentally specified estimator, and the note does not include a matched non-gravity comparator that could fake the same signal. Skeptical read: this is not yet a unique or measurable discriminator, only an internal model observable. | Experimental framing: [`docs/GRAVITATIONAL_ENTANGLEMENT_NOTE.md`](GRAVITATIONAL_ENTANGLEMENT_NOTE.md) lines 34-42.<br>Proxy observable construction: [`scripts/frontier_gravitational_entanglement.py`](../scripts/frontier_gravitational_entanglement.py) lines 267-331 and 499-596.<br>Commit: `c9d90df` | Resolve this with either a matched non-gravitational control showing the same MI cannot be reproduced at comparable strength, or a concrete measurement pipeline that reconstructs the claimed MI from accessible observables under noise and finite sampling. | open |

## Resolved Critiques

| Date | Target Claim / Lane | Resolution | Evidence / Retest | Status |
| --- | --- | --- | --- | --- |
| 2026-04-11 | `60fbcad` harness loophole: the strict staggered `17`-card was not self-contained at the reviewed historical commit | Closed on retained `main`. The current repo-local runner no longer imports from `.claude/worktrees/sleepy-cerf`; it is self-contained, and the retained rerun reproduced the frozen strict-card surface on the repository tree alone. This resolves the specific reproducibility objection for the retained strict card, while leaving the separate semantic/control critiques of the force-based card untouched. | Current self-contained runner: [`scripts/frontier_staggered_17card.py`](../scripts/frontier_staggered_17card.py).<br>Retained note updated with the rerun status: [`docs/STAGGERED_FERMION_CARD_2026-04-10.md`](STAGGERED_FERMION_CARD_2026-04-10.md).<br>Reproduced scores on `main`: 1D `17/17`; 3D `n=9/11/13` all `17/17`, with `n=11,13` keeping the documented `4/6` family gate because `N_sites > 1000`. | resolved |

## Critic Rules

- Be maximally skeptical, but specific.
- Prefer concrete holes over general pessimism.
- Separate:
  - code / harness bugs
  - control failures
  - portability overclaims
  - interpretation overreach
  - experiment-readiness gaps
- Every critique must point to exact notes, scripts, logs, or commits.
- Every critique must include what evidence would resolve it.
- If a critique is answered, move it to `Resolved Critiques` with the retest or
  new artifact that closed it.
