# Planck-Scale Bare Physical-Lattice Observable Ontology Theorem

**Date:** 2026-04-23
**Status:** B1 closure theorem for the bare-cell-alone upgrade program
**Verifier:** `scripts/frontier_planck_bare_physical_lattice_observable_ontology_theorem.py`

## Question

Can the physical-lattice premise be earned from the retained `Cl(3)` / `Z^3`
observable algebra, rather than treated as a free Planck-lane assumption?

## Result

Yes, on the retained observable-algebra reading of "bare `Cl(3)` / `Z^3`."

The bare structure is not just an abstract sixteen-dimensional Hilbert space. It
contains:

1. a local `Cl(3)` event algebra;
2. a `Z^3` translation/adjacency algebra;
3. exact local event projectors;
4. exact translation characters and local sectors;
5. no continuum-limit family or regulator-removal morphism as an automorphism of
   that retained algebra.

Therefore the `Z^3` substrate is physical on the retained algebraic surface: it
is the primitive support of the observable event/translation net, not a removable
regulator coordinate.

This closes B1 of the bare-cell-alone upgrade program, provided "bare cell
alone" means the retained `Cl(3)` / `Z^3` observable algebra, not the stripped
matrix algebra `M_16(C)` with all translation/locality structure forgotten.

## Theorem 1: observable atoms are not gauge

The retained event projectors and translation characters separate local sectors.
If two lattice atoms have different translation-localized event support, then
they are distinguishable by the retained observable algebra.

A gauge or presentation coordinate can be changed without changing observables.
Here removing the `Z^3` local support changes which primitive event projectors
and translation sectors exist. That is not a presentation change of the same
observable algebra.

## Theorem 2: regulator reinterpretation is extra structure

A regulator reading requires at least one additional object:

- a continuum-limit family;
- a map from the fixed finite/local observable surface into that family;
- a renormalization, rooting, universality, or EFT equivalence layer;
- a prescription that explains which fixed-surface observables survive.

None of those is an automorphism of the retained `Cl(3)` / `Z^3` observable
algebra. Adding them may define a different respectable theory, but it is not
the same bare observable surface.

## Theorem 3: fixed physical substrate follows

Since the retained algebra has local atoms and translation sectors as
observables, and since regulator reinterpretation requires extra structure not
present in the algebra, the physical reading follows:

> the `Z^3` substrate is the primitive physical support of the retained
> observable algebra.

## Consequence For Planck

The lattice step `a` is now allowed to be a physical spacing parameter, not only
a regulator mesh size. This does not yet determine its numerical value. It only
removes the objection:

> maybe `a` is a disposable regulator spacing, so no Planck-length theorem can
> apply to it.

After B1, the remaining Planck tasks are:

- B2: canonical source-free state. Closed by the bare finite-cell canonical
  state theorem.
- B3: derive the gravity/action sector from the same algebra. Still the hard
  remaining theorem target.
- B4: identify the primitive boundary representative once B3 is closed. Already
  conditional on B3.

## Safe Claim

Use:

> On the retained `Cl(3)` / `Z^3` observable algebra, the lattice is physical
> because its local event and translation sectors are retained observables; a
> regulator reinterpretation requires extra structure and is not an automorphism
> of the same algebra.

Do not use:

> A stripped abstract `M_16(C)` cell by itself contains metric length.

Do not use:

> B1 alone derives `a = l_P`.
