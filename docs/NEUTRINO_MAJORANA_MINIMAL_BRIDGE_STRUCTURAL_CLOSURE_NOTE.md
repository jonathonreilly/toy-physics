# Majorana Minimal-Bridge Structural Closure

**Date:** 2026-04-16  
**Status:** exact positive structural-closure theorem on the minimal
post-retained Majorana bridge  
**Atlas front door:** canonical toolkit on `main` at
`docs/publication/ci3_z3/DERIVATION_ATLAS.md`  
**Script:** `scripts/frontier_neutrino_majorana_minimal_bridge_structural_closure.py`

## Question

After the pure-retained Majorana lane closes negatively at

`mu = 0`,

does the branch already contain a coherent **post-retained** Majorana bridge
that closes the structural lane positively, or is Majorana itself still the
open pacing item?

## Bottom line

Yes. On the minimal admitted post-retained bridge, the Majorana structural
lane is already closed.

The exact chain now reads:

1. the missing local charge-`2` direction is forced into the admissible source
   grammar by the Nambu source principle
2. the genuinely new one-generation source increment is rephasing-equivalent
   to the unique pure-pairing ray

   `mu J_x`, with `mu >= 0`

3. the minimal finite endpoint-exchange bridge fixes the doublet anchor

   `k_B = 8`

4. the exact weak-axis `1 + 2` adjacency fixes the singlet placement

   `k_A = 7`

5. the minimal symmetric residual-sharing lift fixes the doublet split law

   `eps/B = alpha_LM / 2`

So the live Majorana question is no longer:

- does the branch realize a post-retained Majorana source class?
- where does that class sit on the finite staircase?
- is the doublet split still a fitted leftover?

Those are closed on the minimal bridge.

What remains open has moved **downstream** of the Majorana structural lane:

- full PMNS / solar / flavor closure
- the exact CP-asymmetry kernel and its bookkeeping-consistent washout closure

## Inputs

This capstone compresses the following exact branch surfaces:

- [NEUTRINO_MAJORANA_PURE_RETAINED_MU_IMPOSSIBILITY_NOTE.md](./NEUTRINO_MAJORANA_PURE_RETAINED_MU_IMPOSSIBILITY_NOTE.md)
- [NEUTRINO_MAJORANA_NAMBU_SOURCE_PRINCIPLE_NOTE.md](./NEUTRINO_MAJORANA_NAMBU_SOURCE_PRINCIPLE_NOTE.md)
- [NEUTRINO_MAJORANA_SOURCE_RAY_THEOREM_NOTE.md](./NEUTRINO_MAJORANA_SOURCE_RAY_THEOREM_NOTE.md)
- [NEUTRINO_MAJORANA_ENDPOINT_EXCHANGE_MIDPOINT_THEOREM_NOTE.md](./NEUTRINO_MAJORANA_ENDPOINT_EXCHANGE_MIDPOINT_THEOREM_NOTE.md)
- [NEUTRINO_MAJORANA_ADJACENT_SINGLET_PLACEMENT_THEOREM_NOTE.md](./NEUTRINO_MAJORANA_ADJACENT_SINGLET_PLACEMENT_THEOREM_NOTE.md)
- [NEUTRINO_MAJORANA_RESIDUAL_SHARING_SPLIT_THEOREM_NOTE.md](./NEUTRINO_MAJORANA_RESIDUAL_SHARING_SPLIT_THEOREM_NOTE.md)
- [DM_NEUTRINO_ATMOSPHERIC_SCALE_THEOREM_NOTE_2026-04-15.md](./DM_NEUTRINO_ATMOSPHERIC_SCALE_THEOREM_NOTE_2026-04-15.md)

Those notes already prove, in order:

1. the pure-retained bank cannot activate the Majorana amplitude
2. the smallest admitted post-retained local source grammar is Nambu-complete
3. the genuinely new local source direction collapses to one rephasing class
4. the finite staircase anchor is the unique midpoint `k_B = 8`
5. the exact weak-axis geometry places the singlet at `k_A = 7`
6. the residual split on that bridge is `eps/B = alpha_LM/2`
7. on the current diagonal benchmark, the atmospheric scale is no longer a fit

So the remaining honest question is no longer whether the Majorana lane itself
still lacks a structural theorem.

## Exact theorem

### 1. Pure-retained Majorana closes negatively

On the current pure-retained bank, the remaining Majorana object is exactly the
one real amplitude

`mu`,

and the retained bank forces

`mu = 0`.

So the pure-retained lane is done, but negatively.

### 2. The minimal admitted post-retained source grammar is Nambu-complete

On the unique local `nu_R` block, the charge-zero normal direction and the
unique charge-`2` pairing slot form one exact pseudospin `su(2)` quadratic
algebra.

If the local quadratic source grammar is required to be closed under
CAR-preserving canonical basis changes, then the retained slice `span{J_z}` is
not closed, while the minimal closed family is

`span{J_x, J_y, J_z}`.

So the missing charge-`2` direction is forced on the post-retained lane.

### 3. The genuinely new one-generation source direction is one ray

Inside that admitted local family, the retained normal direction already sits
on `J_z`, so the genuinely new increment lies in the transverse pairing plane
`span{J_x, J_y}`.

Exact local `nu_R` rephasing acts as `SO(2)` on that plane, so every nonzero
new increment is equivalent to the canonical pure-pairing ray

`mu J_x`, with `mu >= 0`.

So the one-generation source-direction question is closed.

### 4. The finite staircase anchor is fixed

On the exact `16`-step hierarchy register, the minimal endpoint-exchange lift
of the local axis exchange is the unique involution

`k -> 16-k`.

Its unique fixed point is

`k_B = 8`.

So the absolute doublet anchor is closed on the minimal finite bridge.

### 5. The singlet placement and doublet split are fixed

The exact weak-axis `1 + 2` return geometry singles out one UV-directed
channel through `O_0` and one residual rank-`2` doublet channel through `T_2`.

Preserving that adjacency on the midpoint-anchored bridge places the singlet
exactly one step above the doublet:

`k_A = 7`.

At the same time, the local self-dual point `rho = 1`, the one-step suppression
`B/A = alpha_LM`, and the exact symmetric rank-`2` residual sharing force

`eps/B = alpha_LM / 2`.

So the three-generation Majorana placement/split law is closed on the minimal
bridge.

### 6. The downstream atmospheric benchmark is already compatible

Once this Majorana bridge is combined with the exact retained Dirac coefficient
`y_nu^eff = g_weak^2 / 64`, the current diagonal benchmark predicts

`m_3 = 5.058e-2 eV`

and

`Dm^2_31 = 2.539e-3 eV^2`.

So the branch no longer needs a fitted atmospheric scale on that benchmark.

## The theorem-level statement

**Theorem (Majorana minimal-bridge structural closure on the current branch).**
Assume:

1. the pure-retained Majorana lane closes negatively at `mu = 0`
2. the post-retained local source grammar is required to be canonically closed
3. the genuinely new local source increment is taken modulo the exact local
   `nu_R` rephasing
4. the local axis exchange is lifted to the exact finite `16`-step hierarchy
   register by the minimal endpoint-exchange bridge
5. the exact weak-axis `1 + 2` split is lifted by the minimal adjacent and
   symmetric residual-sharing bridge

Then the post-retained Majorana structural lane is closed with exact data

`mu J_x`, `k_B = 8`, `k_A = 7`, `eps/B = alpha_LM/2`.

Equivalently: Majorana is no longer the open structural pacing item on this
branch. The remaining open work has moved downstream to PMNS/flavor and the
exact CP-kernel closure.

## What this closes

This closes the Majorana structural lane on the minimal post-retained bridge:

- the local source class is admitted and directionally fixed
- the absolute doublet anchor is fixed
- the singlet placement is fixed
- the doublet split law is fixed

So the branch no longer needs to treat the post-retained Majorana program as an
open collection of source/staircase/texture ambiguities.

## What this does not close

This note does **not** prove:

- the pure-retained lane already closes positively
- full PMNS or solar closure
- full flavor-matrix closure
- a fully settled exact leptogenesis CP kernel and washout package
- full positive neutrino closure from one unextended axiom-only bank

In particular, this note packages the Majorana structural lane only. It does
not promote the downstream CP/leptogenesis tail to closed status.

## Safe wording

**Can claim**

- the pure-retained Majorana lane is closed negative at `mu = 0`
- the minimal post-retained Majorana bridge is structurally closed
- the exact bridge data are `mu J_x`, `k_B = 8`, `k_A = 7`,
  `eps/B = alpha_LM/2`
- Majorana is no longer the structural pacing item on this branch

**Cannot claim**

- that the pure-retained bank already gives a positive Majorana amplitude
- that full neutrino closure is complete
- that the downstream CP/leptogenesis tail is already packaged without further
  work

## Command

```bash
python3 scripts/frontier_neutrino_majorana_minimal_bridge_structural_closure.py
```
