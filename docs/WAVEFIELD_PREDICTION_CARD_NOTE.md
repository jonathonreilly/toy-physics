# Wavefield Prediction Card Note

**Date:** 2026-04-05  
**Status:** retained exact-lattice wavefield discriminator, stated as a clean
prediction card

## Purpose

This note compresses the retained exact-lattice wavefield lane into the
smallest useful outsider-facing prediction card.

The question is not whether the lane exists.
It does.

The question is what the cleanest promoted observable is, what the null is,
what parameter dependence is actually retained, and what still remains
exploratory.

## Retained basis

The strongest exact-lattice wavefield evidence is in:

- [`docs/SOURCE_RESOLVED_WAVEFIELD_ESCALATION_NOTE.md`](/Users/jonreilly/Projects/Physics/docs/SOURCE_RESOLVED_WAVEFIELD_ESCALATION_NOTE.md)
- [`docs/RETARDED_FIELD_CAUSALITY_PROBE_NOTE.md`](/Users/jonreilly/Projects/Physics/docs/RETARDED_FIELD_CAUSALITY_PROBE_NOTE.md)
- [`docs/RETARDED_FIELD_DELAY_PROXY_NOTE.md`](/Users/jonreilly/Projects/Physics/docs/RETARDED_FIELD_DELAY_PROXY_NOTE.md)

The key retained exact-lattice result is:

- exact zero-source reduction survives
- the weak-field sign stays `TOWARD`
- the mass-scaling class stays near-linear
- the detector-line phase-ramp observable is coherent and much stronger than
  the earlier compact pocket

## Clean prediction card

### Promoted observable

Use the detector-line phase ramp, not just a single centroid shift.

The promoted readout is:

- phase-ramp slope across the detector line
- phase-ramp span across the same line
- optionally, a quadrature-style phase lag relative to the same-site control

The strongest promoted observable is the detector-line phase ramp because it
is more structural than a single phase-lag number.

### Null

The standard null is the same exact-lattice baseline with the wavefield turned
off or removed:

- zero source
- same-site-memory / instantaneous control
- no stable detector-line ramp after calibration

The null expectation is:

- `phase slope ≈ 0`
- `phase span ≈ 0`
- `Y ≈ 0` if the readout is phrased as lock-in quadrature

### Retained parameter dependence

The exact-lattice wavefield lane retains the following:

- exact zero-source reduction
- `TOWARD` sign on the frozen larger exact family
- `F~M ≈ 1`
- a coherent phase ramp with `R^2 ≈ 0.96`
- a large separation from the same-site-memory control

On the frozen larger exact family, the wavefield lane also shows:

- a much larger detector-line response than same-site memory
- phase-ramp slopes that remain negative and substantial across the tested
  source strengths
- a consistent exact-lattice wave-like signature rather than a one-point
  coincidence

### What remains exploratory

Do **not** overstate these parts yet:

- generated-geometry transfer
- continuum theorem / asymptotic proof
- absolute experimental amplitude
- any claim that the detector-line ramp is already a full causal-field theory

The retained claim is narrower:

- the exact-lattice wavefield lane gives a clean phase-sensitive discriminator
- it survives zero-source reduction
- it keeps the weak-field sign and mass-scaling class
- it is stronger than the compact pocket and is now the best exact-lattice
  wavefield card

## Best external phrasing

If this is written for an outsider, the safest phrasing is:

"On the retained exact-lattice family, the promoted observable is a
detector-line phase ramp. The null is the same-site / instantaneous control,
which should have no stable ramp after calibration. The retained wavefield
lane keeps zero-source reduction, TOWARD sign, and near-linear mass scaling
while producing a coherent ramp with `R^2` near `0.96`."

## Final verdict

**clean prediction card**
