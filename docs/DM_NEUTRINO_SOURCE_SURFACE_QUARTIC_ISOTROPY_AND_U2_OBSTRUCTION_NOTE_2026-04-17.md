# Quartic-Isotropy Identity + U(2)-Invariance Obstruction — Frobenius Uniqueness + Full-W at Physically-Forced m + Quartic Extension

**Date:** 2026-04-17
**Status:** **three new obstruction theorems** + strictly narrowed gap; one conditional closure candidate gated on an unretained axiom
**Script:** `scripts/frontier_dm_neutrino_source_surface_quartic_isotropy_and_u2_obstruction.py`
**Framework convention:** "axiom" means only the single framework axiom `Cl(3)` on `Z^3`.

## Scope and discipline

The selector Z_3 doublet-block selector gate has two surviving sub-obstructions
on the integration branch:

- **(P1) parity-mixing functional-selection ambiguity** (F1 vs F2 vs F3
 disagree)
- **(P2) m-dependence in the cubic variational obstruction** of the full observable-principle generator
 `W[J_act] = log|det(D + J_act)| − log|det(D)|` at scalar baseline

This note pursues the three attack lines specified in the quartic-isotropy+U(2) obstruction
mission and records a definite result on each.

## Setup (retained theorem-grade, no new axioms)

- **Active-affine chart on the source-oriented sheet** (retained):
 `H(m, delta, q_+) = H_base + m T_m + delta T_delta + q_+ T_q`,
 with the exact `T_delta, T_q, T_m` from
 [DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_AFFINE_POINT_SELECTION_BOUNDARY_NOTE_2026-04-16](./DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_AFFINE_POINT_SELECTION_BOUNDARY_NOTE_2026-04-16.md).
- **Chamber** (retained): `q_+ >= sqrt(8/3) − delta`, from
 [DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_HALF_PLANE_THEOREM_NOTE_2026-04-16](./DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_HALF_PLANE_THEOREM_NOTE_2026-04-16.md).
- **Schur baseline** (retained): `D = m I_3` forced on `H_hw=1`, from
 [DM_NEUTRINO_SOURCE_SURFACE_SCHUR_SCALAR_BASELINE_THEOREM_NOTE_2026-04-17](./DM_NEUTRINO_SOURCE_SURFACE_SCHUR_SCALAR_BASELINE_THEOREM_NOTE_2026-04-17.md).
- **Observable-principle generator** (retained):
 `W[J] = log|det(D+J)| − log|det D|`, from
 [OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md](./OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md).
- **Z_3 doublet-block closed forms** (retained):
 `K11 = −q + 2sqrt(2)/9 − 1/(2 sqrt(3))`,
 `K22 = −q + 2sqrt(2)/9 + 1/(2 sqrt(3))`,
 `K12 = (m − 4sqrt(2)/9) + i (sqrt(3) delta − 4 sqrt(2)/3)`, from
 [DM_NEUTRINO_SOURCE_SURFACE_Z3_DOUBLET_BLOCK_POINT_SELECTION_THEOREM_NOTE_2026-04-16](./DM_NEUTRINO_SOURCE_SURFACE_Z3_DOUBLET_BLOCK_POINT_SELECTION_THEOREM_NOTE_2026-04-16.md).
- **Z_3-circulant norm form** (retained): on the active sheet with the Schur
 baseline,
 `det(m I + J_act) = m^3 − 3 m |w|^2 + 2 Re(w^3)`,
 with `w = q_+ + i delta`.

## Line 1 — Frobenius uniqueness from PD + coercivity + gauge + quadratic

**Hypothesis.** F1 = `||K_doublet||_F^2` is the unique axiom-native quadratic
functional on the active `(delta, q_+)` plane satisfying:
(i) positive-definite, (ii) coercive, (iii) invariant under a retained
symmetry on the doublet block, (iv) quadratic in `(delta, q_+)`.

### Theorem (Line-1 two-parameter family)

The space of U(2)-invariant quadratic functionals of a 2×2 Hermitian matrix
`K` is generated by `(Tr K)^2` and `det K`. Hence every U(2)-invariant
quadratic functional on `K_doublet` has the form
```
F_{A,B}[K] = A (Tr K)^2 + B det K.
```
Its Hessian in `(delta, q_+)` on the active sheet at fixed `m` (with the
closed forms above) is
```
Hess F_{A,B} = diag( -6 B, 8 A + 2 B ).
```
`F_{A,B}` is positive-definite iff `B < 0` AND `A > -B/4 > 0`.

**Corollary.** Positive-definiteness + coercivity (on a 2D real domain
this adds nothing beyond PD) + U(2)-invariance + quadratic leave a
**two-parameter cone** `{(A, B) : B < 0, A > -B/4}`. The Frobenius
invariant `||K||_F^2 = (Tr K)^2 − 2 det K` corresponds to `(A, B) = (1, -2)`
and is one point in this cone. **F1 is not uniquely pinned by the four
stated properties.**

### Line-1 obstruction (chamber-boundary minimizers disagree)

Different `(A, B)` in the PD cone produce DIFFERENT chamber-boundary
minimizers `delta_*`. The runner computes five concrete examples:

```
(A, B) = (+1.00, -2.00) delta_* = 1.146177 <-- F1 (Frobenius)
(A, B) = (+1.00, -1.00) delta_* ≈ 1.204
(A, B) = (+2.00, -3.00) delta_* ≈ 1.190
(A, B) = (+0.50, -1.00) delta_* ≈ 1.244
(A, B) = (+3.00, -0.50) delta_* ≈ 1.299
```

All five are PD, coercive, U(2)-invariant, and quadratic — yet they select
five different points along the chamber boundary. Hence the four-axiom class
is strictly insufficient to pin F1.

### Conditional closure candidate (NOT retained)

There is a stronger gauge axiom that WOULD uniquely pin F1:
**U(2)_L × U(2)_R independent unitary invariance** on the non-Hermitian
doublet block. Under `K → U_L K U_R` (with `U_L, U_R ∈ U(2)` independent),
`(Tr K)^2` is NOT invariant (Tr K changes), but `||K||_F^2 = Tr(K K†)` IS
invariant. The runner verifies this numerically (20/20 random `U_L, U_R`
samples: Tr K changes, `||K||_F^2` preserved).

**Caveat.** U(2)_L × U(2)_R independent invariance is the bifundamental
Yukawa-matrix gauge. Whether it is retrievable from the Cl(3)/Z^3 axiom
on the observable Hermitian operator `H` is **not currently retained in
the atlas**. The retained generation algebra `<P_1, P_2, P_3, C_3[111]>`
acts as the LEFT generator on `H_hw=1`; independent RIGHT action of U(2)
on the doublet block is not part of the retained structure. So the
conditional closure is flagged as a CONDITIONAL GATE that cannot be
promoted without a sole-axiom derivation of `U(2)_R` on the doublet
block.

**Claim label for Line 1.** *Obstruction + conditional closure candidate.*
**Not a theorem-grade closure of the parity-mixing functional-selection
ambiguity.**

## Line 2 — Full-W boundary extremum at physically-forced m

**Hypothesis.** Fix `m` at a physically-forced value (not joint
optimization), extremize `W[J_act; D = m I_3]` over `(delta, q_+)` only.
If one `m` gives a unique chamber-interior or chamber-boundary extremum at
a ledger candidate point, Line 2 closes the selector gate.

### Theorem (Line-2 fixed-m m-dependence)

For every tested physically-forced `m` in the set
```
{ 4 sqrt(2)/9 (F1), sqrt(2), 2 sqrt(2)/3, sqrt(6)/2, 2 sqrt(6)/3 (CP1),
 1, 2, 5, 1/sqrt(3) },
```
the chamber-boundary extremum of `W(m; delta, sqrt(8/3) − delta)` in
`delta` is:

- **m-dependent** (the argmax `delta` varies non-trivially across the
 candidate list, std > 0.1);
- **NOT at any of the five ledger candidates** (Schur-Q sqrt(6)/3, F1
 sqrt(6)/2 − sqrt(2)/18 (F1), (a) 0.9644, (b) 1.2679, (c)
 0.8000), to tolerance 1e-3;
- **chamber-interior joint minima land on the det=0 singular boundary**,
 confirming the cubic variational obstruction (B) at fixed m in a stronger form (not only joint
 (m, delta) stationarity, but also fixed-m interior optimization).

**Illustrative table from the runner** (boundary argmax δ, 8 candidate m
values):
```
 m   m value argmax δ argmin δ
 m = 4sqrt(2)/9 (F1) 0.628539 1.272862 0.367656
 m = sqrt(2)  1.414214 1.530898 0.080079
 m = 2sqrt(6)/3 (CP1) 1.632993 1.632993 0.000000
 m = 5   5.000000 0.628944 -1.232410
 ...
```

At `m = 4 sqrt(2)/9` (F1's own m-value), the boundary argmax is
**δ ≈ 1.273**, strictly different from F1's own `δ_F1 ≈ 1.146`. So even
choosing the F1 m does not promote F1 to a full-W extremum.

**Corollary.** Fixing `m` at a physically-forced value does not close the
the cubic variational m-dependence obstruction. The cubic variational obstruction ruled out the joint `(m, delta)`
stationarity; this note obstructs the fixed-`m` restriction for every
physically-natural `m` candidate tested.

**Claim label for Line 2.** *New obstruction theorem.* **Not a theorem-grade
closure of the cubic variational m-dependence.**

## Line 3 — Quartic extension of the observable principle

**Hypothesis.** The 4th-order Taylor term of `W` at scalar baseline, built
from `Tr((D^{-1} J_act)^4)`, might carry additional parity-mixing content
beyond cubic-variational-obstructed cubic, possibly producing a unique chamber
extremum when combined with the quadratic and cubic.

### Theorem (Line-3 quartic-isotropy)

On the active pair `(T_delta, T_q)`:
```
Tr(J_act^2) = 6 (delta^2 + q_+^2)   [isotropic]
Tr(J_act^3) = 6 (q_+^3 − 3 delta^2 q_+) = 6 Re(w^3) [parity-mixing]
Tr(J_act^4) = 18 (delta^2 + q_+^2)^2   [isotropic]
```

**Corollary.** `Tr(J_act^4) = (1/2) [Tr(J_act^2)]^2` is **exactly the square
of the quadratic**, hence isotropic in `(delta, q_+)`. The quartic term of
the observable-principle Taylor expansion at scalar baseline adds NO new
parity-mixing information beyond the (cubic-variational-obstructed) cubic.

Explicit expansion of `W` at `D = m I_3`:
```
W(m, delta, q_+) = −(1/2 m^2) Tr(J^2) + (1/3 m^3) Tr(J^3) − (1/4 m^4) Tr(J^4) + O(J^5)
   = − 3 r^2 / m^2 + 2 Re(w^3) / m^3 − 9 r^4 / (2 m^4) + O(r^5)
```
with `r^2 = delta^2 + q_+^2`. Only the cubic is parity-mixing; the
quadratic and quartic are purely isotropic in `r^2`.

**Higher even orders.** The runner verifies (sanity check) that `Tr(J^6)`
is NOT delta↔q_+ swap invariant, so the sextic DOES carry parity-mixing
content. But the sextic and beyond are NOT "the next natural invariant"
for axiom-native closure — they are also cubic-variational-obstructed in their
cubic content because they inherit the same m-dependence structure of
`log|det|` expanded order by order.

**Claim label for Line 3.** *New obstruction theorem.* The
parity-mixing content of the observable-principle Taylor expansion at
scalar baseline is concentrated at cubic order (cubic-variational-obstructed);
quartic is isotropic; higher orders carry parity-mixing but also carry
m-dependence.

## Verdict

**Summary line-by-line:**

| Line | Target | Result |
|------|--------|--------|
| 1 | F1 uniqueness from PD + coercivity + gauge + quadratic | **Obstruction** + conditional closure candidate (U(2)_L × U(2)_R independent gauge axiom, unretained) |
| 2 | Full-W at physically-forced m | **Obstruction** (fixed-m m-dependence) |
| 3 | Quartic extension of observable principle | **Obstruction** (quartic is isotropic, square of quadratic) |

**Classification:** **CASE 3 — three new obstruction theorems + narrower gap.**

**Closure status of the selector gate remains OPEN.**

## What this note strictly NEWLY CLOSES

1. **Parity-mixing quadratic ambiguity class is characterized as a 2-parameter
 family.** Prior to this note, the parity-mixing ambiguity was "three
 natural invariants F1, F2, F3 disagree." This note shows the full
 U(2)-invariant quadratic cone is a 2-parameter family, not just three
 discrete choices. The ambiguity is a genuine continuum, and PD +
 coercivity + U(2) gauge do not collapse it.
2. **Fixed-m m-dependence is a distinct obstruction from the cubic variational obstruction (B).**
 the cubic variational obstruction ruled out the joint `(m, delta)` stationarity; this note
 obstructs the natural weaker problem of fixed-m single-variable
 extremization. No physically-forced `m` succeeds.
3. **Quartic-isotropy theorem.** `Tr(J_act^4) = 18 r^4` is exactly
 `(1/2) Tr(J_act^2)^2`. This is a new structural identity pinning the
 quartic term of the observable principle at scalar baseline.
4. **Conditional closure gate identified for Line 1.** `U(2)_L × U(2)_R`
 independent unitary invariance would uniquely pin F1 among quadratic
 invariants. Future work can attempt to derive this axiom from
 `Cl(3)/Z^3`; a failed attempt would further narrow the gap.

## What this note does NOT do

- does not close the selector gate
- does not promote F1 (or any candidate) to the theorem-grade selector
 point
- does not rule out that a higher-order trace invariant or a non-trace
 invariant (e.g. Pfaffian, Cartan-Killing form pulled back through a
 retained representation) closes the gap
- does not promote `U(2)_L × U(2)_R` gauge invariance on the doublet block
 to axiom-native status

## Atlas inputs used

All retained / theorem-grade on the integration branch:

- [OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md](./OBSERVABLE_PRINCIPLE_FROM_AXIOM_NOTE.md)
- [DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_AFFINE_POINT_SELECTION_BOUNDARY_NOTE_2026-04-16.md](./DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_AFFINE_POINT_SELECTION_BOUNDARY_NOTE_2026-04-16.md)
- [DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_HALF_PLANE_THEOREM_NOTE_2026-04-16.md](./DM_NEUTRINO_SOURCE_SURFACE_ACTIVE_HALF_PLANE_THEOREM_NOTE_2026-04-16.md)
- [DM_NEUTRINO_SOURCE_SURFACE_Z3_DOUBLET_BLOCK_POINT_SELECTION_THEOREM_NOTE_2026-04-16.md](./DM_NEUTRINO_SOURCE_SURFACE_Z3_DOUBLET_BLOCK_POINT_SELECTION_THEOREM_NOTE_2026-04-16.md)
- [DM_NEUTRINO_SOURCE_SURFACE_SCHUR_SCALAR_BASELINE_THEOREM_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_SCHUR_SCALAR_BASELINE_THEOREM_NOTE_2026-04-17.md)
- [DM_NEUTRINO_SOURCE_SURFACE_INFO_GEOMETRIC_SELECTION_OBSTRUCTION_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_INFO_GEOMETRIC_SELECTION_OBSTRUCTION_NOTE_2026-04-17.md)
- [DM_NEUTRINO_SOURCE_SURFACE_CUBIC_VARIATIONAL_OBSTRUCTION_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_CUBIC_VARIATIONAL_OBSTRUCTION_NOTE_2026-04-17.md)
- [DM_NEUTRINO_SOURCE_SURFACE_Z3_PARITY_SPLIT_THEOREM_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_Z3_PARITY_SPLIT_THEOREM_NOTE_2026-04-17.md)
- [DM_NEUTRINO_SOURCE_SURFACE_PARITY_MIXING_SELECTION_OBSTRUCTION_NOTE_2026-04-17.md](./DM_NEUTRINO_SOURCE_SURFACE_PARITY_MIXING_SELECTION_OBSTRUCTION_NOTE_2026-04-17.md)

No new axioms are introduced.

## Narrowed-gap statement

**Before this note (after Paths A, B, C + parity-mixing note):**
```
(G-Var) still requires selection axiom (info-geometric leading-quadratic)
(G-Non-Var) parity-mixing sub-route produces F1 candidate
  but F1 vs F2 vs F3 disagreement is an ambiguity
  (three discrete choices)
cubic variational obstruction: m-dependent boundary extremum / joint CPs singular
Quartic  untested
```

**After this note:**
```
(G-Var) unchanged
(G-Non-Var) parity-mixing quadratic ambiguity is a 2-parameter
  U(2)-invariant PD cone, NOT just {F1, F2, F3}; F1 not
  uniquely pinned by PD + coercivity + gauge + quadratic;
  CONDITIONAL gate: U(2)_L x U(2)_R bifundamental
  invariance, NOT currently retained
cubic variational obstruction: fixed-m restriction also m-dependent and never lands at
  a ledger candidate (new obstruction)
Quartic Tr(J^4) at scalar baseline is (1/2) Tr(J^2)^2 — isotropic
  (NEW theorem); no new parity-mixing at quartic order
```

## Position on publication surface

Appropriate placement:

- atlas obstruction row in
 [DERIVATION_ATLAS.md](./publication/ci3_z3/DERIVATION_ATLAS.md)
 under the DM neutrino source-surface family, sibling to Paths A, B, C
 and the parity-mixing selector law
- **do NOT** use for any publication-grade positive quantitative claim
- **do NOT** promote the Line-1 conditional closure without a sole-axiom
 derivation of `U(2)_L × U(2)_R` bifundamental invariance

## Command

```bash
PYTHONPATH=scripts python3 scripts/frontier_dm_neutrino_source_surface_quartic_isotropy_and_u2_obstruction.py
```

Current expected: `PASS = 18, FAIL = 0`.

## What this file must never say

- that selector is closed
- that the DM flagship gate is closed
- that F1 is the uniquely axiom-forced parity-mixing functional
- that `U(2)_L × U(2)_R` bifundamental invariance is axiom-native retained
- that the quartic gives a new parity-mixing selector (it does NOT; it is
 isotropic)
- that any physically-forced `m` gives a theorem-grade selector via full-W
 (none of the tested values do)

If any future revision of this note tightens those boundaries, it must
cite a new source on the live retained/promoted surface. Until then, the
safe read is: **three new obstruction theorems + narrower gap; selector remains
OPEN**.
