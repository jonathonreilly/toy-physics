# Clifford Volume-Element Chirality Forces Even Total Dimension Narrow Theorem

**Date:** 2026-05-10
**Type:** bounded_theorem
**Claim scope:** the standalone Clifford-algebra implication that, given
generators `gamma_1, ..., gamma_n` of `Cl(p, q)` with `n = p + q`,
a single algebra element `gamma_5` satisfying

```text
(C1)  gamma_5^2 = +I,
(C2)  { gamma_5, gamma_mu } = 0  for all  mu = 1, ..., n,
```

**exists** iff `n` is even; equivalently, no element of `Cl(p, q)` can
anticommute with every generator when `n` is odd. As a consequence,
with `d_s = 3` spatial dimensions, the time-direction count `d_t` must
be **odd**: `d_t in { 1, 3, 5, ... }`. This is purely a statement about
finite-rank Clifford algebra structure; no anomaly content, no
single-clock evolution input, no ABJ admission, no left-handed content,
no gauge group, and no `d_t > 1` exclusion enter. The exclusion of
`d_t > 1` to fix `d_t = 1` is **out of scope** of this narrow theorem and
remains delegated to the parent.
**Status authority:** independent audit lane only. This source note does
not set or predict an audit outcome; later status is generated by the
audit pipeline after independent review.
**Runner:** [`scripts/audit_companion_clifford_volume_chirality_even_dimension_exact.py`](./../scripts/audit_companion_clifford_volume_chirality_even_dimension_exact.py)
**Authority role:** Pattern A narrow rescope of the load-bearing
Clifford-classification Step 3 inside the parent
[`ANOMALY_FORCES_TIME_THEOREM.md`](ANOMALY_FORCES_TIME_THEOREM.md).
This narrow rescope carries the pure Clifford-algebra core only; it
does **not** carry Steps 1-2 (anomaly cancellation requires RH
completion) or Step 4 (single-clock excludes `d_t > 1`) of the parent
theorem.

## Statement

Let `Cl(p, q)` be the real Clifford algebra with generators
`gamma_1, ..., gamma_n` (`n = p + q`) satisfying the canonical
anticommutation relations

```text
{ gamma_mu, gamma_nu }  =  2 eta_{mu nu} I,
```

where `eta = diag(+1, ..., +1, -1, ..., -1)` has signature `(p, q)`. Let
`omega := gamma_1 gamma_2 ... gamma_n` be the volume element. Then:

1. (**Volume-element (anti)commutation rule.**) For every generator
   `gamma_mu`,

   ```text
   omega gamma_mu  =  (-1)^{n-1} gamma_mu omega.                  (V)
   ```

2. (**Even/odd dichotomy.**)
   - If `n` is even, `omega` **anticommutes** with every generator.
     Suitable scalar rescaling (phase) yields a candidate `gamma_5 = c
     omega` with `gamma_5^2 = +I` (when `c` is chosen as the unique
     positive real or unit complex scalar making `(c omega)^2 = +I`),
     and `{ gamma_5, gamma_mu } = 0` for every `mu`.
   - If `n` is odd, `omega` **commutes** with every generator (i.e.
     `omega` is central in `Cl(p, q)`), so no scalar multiple of `omega`
     can anticommute with even one generator. Moreover **no algebra
     element at all** can anticommute with every generator
     simultaneously when `n` is odd.

3. (**Chirality existence theorem.**) An element `gamma_5 in Cl(p, q)`
   satisfying both (C1) `gamma_5^2 = +I` and (C2) `{ gamma_5, gamma_mu }
   = 0` for all `mu` exists if and only if `n = p + q` is **even**.

4. (**Application to `d_s = 3` spatial dimensions.**) Setting
   `n = d_s + d_t = 3 + d_t`, chirality requires `d_t` to be **odd**:

   ```text
   d_t in { 1, 3, 5, 7, ... }.                                    (★)
   ```

## Proof

Pure finite-rank Clifford algebra.

### Step 1. Establish the (V) parity rule.

We need to move `gamma_mu` past `omega = gamma_1 ... gamma_n` to the
right. Pushing `gamma_mu` past one factor `gamma_nu` produces a sign:

- if `nu != mu`, then `gamma_mu gamma_nu = -gamma_nu gamma_mu`
  (anticommute);
- if `nu = mu`, then `gamma_mu gamma_mu = eta_{mu mu} I`, scalar.

So in moving `gamma_mu` left-to-right through `omega`:

- `gamma_mu` encounters `gamma_1, ..., gamma_{mu-1}` first (each different
  from `gamma_mu`), accumulating `mu - 1` sign flips;
- then it encounters its own factor `gamma_mu` (no sign flip; produces
  a scalar absorbed below);
- then it encounters `gamma_{mu+1}, ..., gamma_n` (each different),
  accumulating `n - mu` more sign flips.

Total signs: `(mu - 1) + (n - mu) = n - 1`. Therefore

```text
omega gamma_mu  =  (-1)^{n-1} gamma_mu omega.    (V)
```

The reasoning is independent of `eta_{mu mu}` because the scalar factor
absorbed during the encounter with the same-index generator commutes
with everything.

### Step 2. Even-dimension half: `n` even ⟹ `omega` anticommutes with every `gamma_mu`.

When `n` is even, `(-1)^{n-1} = -1`. So (V) becomes
`omega gamma_mu = -gamma_mu omega`, i.e. `{ omega, gamma_mu } = 0`. The
volume element anticommutes with every generator.

Now `omega^2 = c I` for some real scalar `c` depending on `(p, q)` (the
square of the volume element is always a real scalar in `Cl(p, q)`).

- If `c > 0`, set `gamma_5 = c^{-1/2} omega`; then
  `gamma_5^2 = +I` and `{ gamma_5, gamma_mu } = 0`.
- If `c < 0`, set `gamma_5 = (-c)^{-1/2} (i omega)` (complexified
  rescale); then `gamma_5^2 = +I` and `{ gamma_5, gamma_mu } = 0`
  (`i` commutes with everything so does not affect anticommutation
  with `gamma_mu`).

Either way, a chirality operator exists. ∎ even

### Step 3. Odd-dimension half: `n` odd ⟹ no algebra element anticommutes with every generator.

When `n` is odd, `(-1)^{n-1} = +1`. So (V) becomes
`omega gamma_mu = +gamma_mu omega`, i.e. `omega` commutes with every
generator. Since the generators generate the whole algebra, `omega` lies
in the **center** of `Cl(p, q)`. No scalar multiple of a central element
can anticommute with even one non-central element (a non-central
generator), so no rescaling of `omega` works.

To extend to **arbitrary** algebra elements (not just rescalings of
`omega`), use the following standard structure result: when `n` is odd,
the center `Z(Cl(p, q))` is two-dimensional, spanned by `{ I, omega }`,
and any algebra element decomposes as `x = a + b omega + (off-center
piece)`. The off-center piece is itself a sum of products of fewer than
`n` generators; for any such product `pi = gamma_{i_1} ... gamma_{i_k}`
with `0 < k < n`, the (V)-type identity gives

```text
pi gamma_mu  =  (-1)^{k - delta} gamma_mu pi,
```

where `delta = 1` if `mu in {i_1, ..., i_k}` and `delta = 0` otherwise.
In particular, if we pick `gamma_mu` with index **not** among
`{i_1, ..., i_k}` (which always exists when `k < n`), then
`pi gamma_mu = (-1)^k gamma_mu pi`. For `pi` to anticommute with all
generators we need `pi gamma_mu = -gamma_mu pi` for every choice of
`mu`. Picking `mu` outside `{i_1, ..., i_k}` requires `k` odd; picking
`mu` inside requires `k - 1` odd, hence `k` even. These two conditions
cannot both hold (since `k < n` and `k` exists in `{0, ..., n-1}`), so
no off-center monomial anticommutes with every generator.

Hence no linear combination, including any algebra element of the form
`x = a I + b omega + sum c_pi pi`, can anticommute with every
`gamma_mu`: the central part `a I + b omega` commutes with everything
(it is central), and each off-center monomial fails to anticommute with
at least one generator, so the anticommutator
`{ x, gamma_mu }` cannot vanish simultaneously for every `mu`. ∎ odd

### Step 4. Combine.

Steps 2 and 3 together prove: an algebra element `gamma_5` satisfying
(C1) and (C2) exists if and only if `n = p + q` is even. ∎

### Step 5. Apply at `d_s = 3`.

Set `n = 3 + d_t`. Chirality existence requires `n` even, i.e.
`d_t` **odd**. Hence `d_t in { 1, 3, 5, ... }`. (★)

QED.

## Derivable corollaries

- **Volume-element centrality at odd `n`.** The volume element `omega`
  is central in `Cl(p, q)` if and only if `n` is odd.
- **Chirality grading at even `n`.** When `n` is even, `gamma_5` (as
  constructed in Step 2) is an involution that splits the spinor module
  `S` into `gamma_5`-eigenspaces of eigenvalues `±1`; these are the
  retained Weyl spinors. The exact construction of `S = S_+ \oplus S_-`
  follows by elementary projection.

Both corollaries are pure Clifford-algebra consequences of (V).

## What this claims

- Identity (V): `omega gamma_mu = (-1)^{n-1} gamma_mu omega` for any
  finite-rank `Cl(p, q)`.
- The even/odd dichotomy on `omega`'s (anti)commutation with the
  generators.
- The existence of `gamma_5` satisfying (C1) and (C2) iff `n` is even.
- At `d_s = 3`, `d_t` must be odd: `d_t in { 1, 3, 5, ... }`. (★)

## What this does NOT claim

- Does **not** claim `d_t = 1` (the exclusion of `d_t > 1` is a separate
  load-bearing step that the parent
  [`ANOMALY_FORCES_TIME_THEOREM.md`](ANOMALY_FORCES_TIME_THEOREM.md)
  delegates to
  [`AXIOM_FIRST_SINGLE_CLOCK_CODIMENSION1_EVOLUTION_THEOREM_NOTE_2026-05-03.md`](AXIOM_FIRST_SINGLE_CLOCK_CODIMENSION1_EVOLUTION_THEOREM_NOTE_2026-05-03.md)).
- Does **not** consume the parent's Step 1-2 (left-handed content has
  ABJ anomaly traces; RH SU(2)-singlet completion required for
  cancellation). Steps 1-2 of the parent are out of scope.
- Does **not** consume the parent's admission (i) (the ABJ
  anomaly-to-inconsistency implication on the lattice), which remains
  the parent's open external admission.
- Does **not** consume the gauge group `SU(2) x SU(3) x U(1)` or the
  left-handed content `(2, 3)_{+1/3} + (2, 1)_{-1}`.
- Does **not** identify the volume-element chirality with any physical
  staggered-sublattice realization (e.g. `epsilon(x)` of
  [`CPT_EXACT_NOTE.md`](CPT_EXACT_NOTE.md)); that identification is a
  separate downstream realization step.
- Does **not** claim positive-theorem promotion for the parent. The
  parent retains its bounded_theorem (B-class) status with open
  admission (i).

## Relation to the parent anomaly-forces-time theorem

The parent [`ANOMALY_FORCES_TIME_THEOREM.md`](ANOMALY_FORCES_TIME_THEOREM.md)
bundles five logically separable pieces:

1. Step 1: the left-handed content `(2, 3)_{+1/3} + (2, 1)_{-1}` carries
   nonzero anomaly traces. (Out of scope here.)
2. Step 2: anomaly cancellation requires an SU(2)-singlet right-handed
   completion. (Out of scope; routed to
   [`NATIVE_GAUGE_CLOSURE_NOTE.md`](NATIVE_GAUGE_CLOSURE_NOTE.md).)
3. Step 3: chirality requires `n = d_s + d_t` even. (**Carried here.**)
4. Step 4: single-clock codimension-1 evolution excludes `d_t > 1`.
   (Out of scope; routed to the single-clock theorem note.)
5. Admission (i): ABJ anomaly-to-inconsistency on the lattice (open
   external admission). (Out of scope; remains the parent's open
   admission.)

This narrow rescope keeps **only Step 3** as a standalone Clifford-algebra
identity. The other parent pieces remain on the parent surface; this
narrow does not narrow them.

## Honest open items

The narrow rescope leaves the following parent admissions intact:

- The parent's admission (i) (ABJ anomaly-to-inconsistency on the
  lattice) is **not closed** by this narrow. PR 402 was closed
  without merge; no successor companion note for admission (i) lands
  on `main`. The narrow scope here deliberately decouples from
  admission (i) by carrying only the Clifford-classification step.
- The `d_t > 1` exclusion (parent Step 4) is **not closed** by this
  narrow; it remains delegated to the parent's single-clock
  codimension-1 evolution citation.

These are documented as out-of-scope; the narrow rescope makes no
promotion claim about them.

## Declared dependencies (graph-visible)

| Authority | Audit-lane status (2026-05-10) | Role |
|---|---|---|
| [`ANOMALY_FORCES_TIME_THEOREM.md`](ANOMALY_FORCES_TIME_THEOREM.md) | `unaudited` | parent bundled note this narrow rescope carves out Step 3 from |

No other note is load-bearing for the present Clifford-algebra
identity. The references to `Cl(p, q)` structure are textbook (cf.
Lawson & Michelsohn, *Spin Geometry*, ch. I), not load-bearing
internal authorities.

## Forbidden imports check

- No PDG observed values consumed.
- No literature numerical comparators consumed.
- No fitted selectors consumed.
- No admitted unit conventions load-bearing on the claim.
- No same-surface family arguments.
- No `d_t = 1` claim; `d_t` is only restricted to odd values.

## Validation

Primary runner: [`scripts/audit_companion_clifford_volume_chirality_even_dimension_exact.py`](./../scripts/audit_companion_clifford_volume_chirality_even_dimension_exact.py)
verifies, at exact rational precision via sympy:

1. The (V) parity rule for any small explicit example via
   anticommutator counting on a representative `Cl(p, q)` matrix
   realization for `n = 2, 3, 4, 5, 6`.
2. Even-`n` case: `n in {2, 4, 6}`: omega anticommutes with every
   generator, and a candidate `gamma_5 = c omega` satisfying (C1)+(C2)
   is exhibited as an explicit matrix at each `n`.
3. Odd-`n` case: `n in {3, 5}`: omega is central, no scalar multiple
   of omega anticommutes with any generator, and an exhaustive
   monomial scan over basis elements of `Cl(p, q)` confirms no
   algebra element anticommutes with all generators.
4. Application: at `d_s = 3`, the chirality-allowed `d_t` is exactly
   `{1, 3, 5, 7}` in the tested range, consistent with `d_t` odd.
5. Counterfactual: for `d_t = 2` (so `n = 5`), an explicit
   anticommutator scan shows no `gamma_5` exists.

## Cross-references

- [`ANOMALY_FORCES_TIME_THEOREM.md`](ANOMALY_FORCES_TIME_THEOREM.md) —
  parent bundled note this narrow rescope carves out Step 3 from.
- [`CPT_EXACT_NOTE.md`](CPT_EXACT_NOTE.md) — physical staggered-lattice
  realization of the volume-element chirality `epsilon(x)`; **not**
  load-bearing on the abstract Clifford-classification identity
  carried by this narrow.
- Lawson & Michelsohn, *Spin Geometry*, ch. I (textbook reference for
  the (V) parity rule and Clifford-algebra structure).
