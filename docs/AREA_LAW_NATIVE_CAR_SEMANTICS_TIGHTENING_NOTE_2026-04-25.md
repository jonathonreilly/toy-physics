# Area-Law Native CAR Semantics Tightening Note

**Date:** 2026-04-25
**Status:** support / conditional Target 2 residual-semantics closure;
2026-04-30 carrier identification audited_renaming
**Runner:** `scripts/frontier_area_law_native_car_semantics_tightening.py`

## Purpose

The primitive-CAR bridge theorem closes the `1/4` coefficient inside minimal
local complex-CAR edge semantics. The remaining review question was whether
those semantics are native `Cl(3)/Z^3` horizon physics or an added carrier
axiom.

This note tightened that question. The exact residual premise was not a vague
philosophical choice. It was one concrete statistics statement:

> The active primitive boundary response is generated by a local irreducible
> Clifford-Majorana edge algebra.

The 2026-04-30 theorem
[PLANCK_PRIMITIVE_CLIFFORD_MAJORANA_EDGE_DERIVATION_THEOREM_NOTE_2026-04-30.md](./PLANCK_PRIMITIVE_CLIFFORD_MAJORANA_EDGE_DERIVATION_THEOREM_NOTE_2026-04-30.md)
was audited as `audited_renaming`: it constructs the valid complex-CAR
two-mode carrier, but it does not force the substrate action to preserve
`P_A H_cell` and induce that carrier. The residual premise therefore remains
open as a carrier premise.

## The tightening theorem

Let `K = P_A H_cell` be the rank-four primitive active boundary block:

```text
dim K = rank(P_A) = 4.
```

Assume the local boundary response on `K` is generated by four Hermitian odd
operators

```text
gamma_1, gamma_2, gamma_3, gamma_4
```

obeying the Clifford-Majorana relations

```text
{gamma_i, gamma_j} = 2 delta_ij I_K,
```

and generating the full local active matrix algebra `End(K)`.

Then the complex combinations

```text
c_1 = (gamma_1 + i gamma_2)/2,
c_2 = (gamma_3 + i gamma_4)/2
```

obey the canonical anticommutation relations

```text
{c_a, c_b} = 0,
{c_a, c_b^dagger} = delta_ab I_K.
```

Therefore `K` is, up to unitary equivalence,

```text
K ~= F(C^2),
```

the two-mode complex CAR Fock space used in the primitive-CAR edge theorem.
Because complex `Cl_4` is the full matrix algebra `M_4(C)`, this realization
is irreducible and unique up to a change of Majorana basis.

## Why this is native enough to promote conditionally

The retained framework already uses Clifford/staggered-fermion structure as
its microscopic lattice response language. On that surface, "local irreducible
Majorana edge response on the rank-four primitive packet" is not a fitted
entropy axiom. It is the minimal Clifford-statistics lift of the same active
boundary block:

```text
rank(P_A)=4  ->  irreducible complex Cl_4 block  ->  F(C^2).
```

Combining this note with
`AREA_LAW_PRIMITIVE_CAR_EDGE_IDENTIFICATION_THEOREM_NOTE_2026-04-25.md`
and
`AREA_LAW_PRIMITIVE_PARITY_GATE_CARRIER_THEOREM_NOTE_2026-04-25.md`
gives the positive chain:

```text
P_A rank 4
  + local irreducible Clifford-Majorana edge response
  -> F(C^2)
  -> normal + self-dual tangent Laplacian channel
  -> <N_x> = 3
  -> c_Widom = 1/4.
```

## What rank alone cannot do

The same four-dimensional Hilbert space also admits non-CAR interpretations:

- a single ququart;
- a two-qubit spin system with commuting tensor factors;
- a classical four-atom counting space.

Those structures have the same dimension and the same trace fraction
`4/16`, but they do not by themselves supply fermionic anticommutation,
Majorana parity, or the Widom free-fermion asymptotic theorem. Thus rank,
locality, and additivity alone cannot force the positive Target 2 carrier.

This is the honest residual boundary:

```text
Target 2 closes positively iff the primitive horizon edge is accepted as an
irreducible Clifford-Majorana/CAR edge response.
```

## Consequence for Target 3

Target 3 remains separate from a bare Hilbert-flow route: derive one
irreducible information/action or phase unit from the accepted Hilbert
reduction. The Target 2 work gave Target 3 a sharper object to derive:

```text
derive the Clifford-Majorana edge-statistics principle from the one-axiom
information/action bridge.
```

The Target 3 Clifford phase bridge supplied a conditional positive route:
metric-compatible primitive coframe response on `P_A H_cell` implies that
edge-statistics principle. The 2026-04-30 primitive Clifford-Majorana theorem
was audited as not deriving that coframe response from retained substrate
content. The residual statement therefore remains an independent
edge-statistics/coframe-response premise.

The Target 3 boundary theorem now records the current status in
`PLANCK_TARGET3_PHASE_UNIT_EDGE_STATISTICS_BOUNDARY_NOTE_2026-04-25.md`: the
one-axiom Hilbert-flow bridge supplies a dimensionless `U(1)` phase unit, but
does not by itself fix the dimensional action scale or force CAR rather than
two-qubit/ququart semantics on the same rank-four block.

The conditional bridge is recorded in
`PLANCK_TARGET3_CLIFFORD_PHASE_BRIDGE_THEOREM_NOTE_2026-04-25.md`: assuming
the native metric-compatible Clifford coframe response gives `Cl_4(C)` on
`P_A H_cell`, and the rank-four irreducible module is exactly the two-mode CAR
edge carrier. Thus the native-CAR premise is now a precise coframe-response
target rather than an unstructured selector, while the Hilbert-only warning
remains valid if that coframe structure is not supplied.

The proposed derivation of that target is recorded in
`PLANCK_PRIMITIVE_CLIFFORD_MAJORANA_EDGE_DERIVATION_THEOREM_NOTE_2026-04-30.md`:
the construction verifies the primitive `Cl_4(C)` / `F(C^2)` carrier, but audit
found that the substrate-to-`P_A` forcing step is still missing.

## Package wording

Safe wording:

> The exact `1/4` entanglement carrier is derived conditionally on the
> primitive rank-four boundary block carrying the irreducible
> Clifford-Majorana / `Cl_4(C)` edge response. The 2026-04-30 construction
> verifies that carrier algebra, but audit found that rank four plus the cited
> substrate content still does not force it.

Unsafe wording:

> The finite rank-four primitive trace by itself proves fermionic CAR horizon
> physics.

## Verification

Run:

```bash
python3 scripts/frontier_area_law_native_car_semantics_tightening.py
```

The runner checks explicit two-mode CAR matrices, the Majorana/Clifford
relations, full `M_4(C)` generation, fermion parity, the equivalence between
four Majoranas and two complex CAR modes, and the rank-alone underdetermination
by a commuting two-qubit comparison.

Current output:

```text
SUMMARY: PASS=23  FAIL=0
```
