# Gauge-Vacuum Plaquette First-Sector Minimal-Bulk Completion `3d+1` `rho1` Least-Distortion Selector Theorem

## Statement

The remaining retained-line multiplicity on the selected Wilson branch is
closed on the **full bounded exact-solve set** by one geometric canonicity law:
choose the member of the exact `rho1/rho2` orientation doublet with least
projector-Frobenius distortion to the `rho1` reference slice, breaking any tie
by the smallest loss of the preferred `(1,0)` boundary anchor.

This selects one canonical retained real `3d` slice on the selected ambient,
and the resulting compressed local block lands on the live DM target.

The theorem is therefore exact on the solved set, while the deeper question of
why this least-distortion selector should be physically forced is left to the
separate limits note.

## Authority

- Runner:
  `scripts/frontier_gauge_vacuum_plaquette_first_sector_minimal_bulk_completion_3plus1_line_rho1_least_distortion_selector_theorem_2026_04_20.py`
