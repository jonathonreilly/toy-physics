# Planck-Scale Primitive Phase Trace Reduction Theorem

**Date:** 2026-04-24
**Status:** hbar/action-phase reduction theorem; not an SI hbar prediction
**Verifier:** `scripts/frontier_planck_primitive_phase_trace_reduction_theorem.py`

## Question

Can the hbar lane derive the action-phase target

`q_* = 1/16`

rather than merely naming it?

## Result

It can derive the trace shape, but one scalar remains.

On the full primitive event cell

`H_cell = span{|eta> : eta in {0,1}^4}`,

any source-free primitive action-phase functional that is:

1. finitely additive on orthogonal primitive event projectors;
2. positive on positive event projectors;
3. natural under the event-algebra automorphisms that preserve no extra
   prepared event label;
4. supported on the retained `C^16` primitive event algebra;

must have the form

`Phi(P) = gamma Tr(P) / 16`,

where

`gamma := Phi(I_16)`

is the total reduced action phase assigned to one full primitive event cell.

Therefore every primitive event atom has phase share

`Phi(P_eta) = gamma / 16`.

The algebraic trace denominator `1/16` is now derived. The exact physical value
`q_atom = 1/16` is not derived until the remaining hbar/action-unit scalar is
closed:

`gamma = 1`.

Equivalently:

> exact conventional Planck on the minimal cubical defect is equivalent to the
> statement that one complete source-free primitive `C^16` event cell carries
> one reduced action unit.

This is real hbar progress, but it is not yet a numerical prediction of
`hbar`.

## Theorem 1: source-free phase functionals are normalized traces up to scale

Let `A_cell` be the finite primitive event algebra generated by the sixteen
atomic projectors

`P_eta = |eta><eta|`,

with `eta in {0,1}^4`.

Let `Phi` be a reduced action-phase functional on projectors of `A_cell`.
Assume:

1. `Phi(P + Q) = Phi(P) + Phi(Q)` when `PQ = 0`;
2. `Phi(P) >= 0`;
3. no atomic event is preferred by source-free data;
4. the retained automorphism group acts transitively on the sixteen atoms.

Transitivity gives one common atom value

`Phi(P_eta) = x`.

Finite additivity gives

`Phi(I_16) = sum_eta Phi(P_eta) = 16 x`.

So

`x = Phi(I_16) / 16 = gamma / 16`.

For a projector of rank `r`,

`Phi(P) = gamma r / 16`.

Thus

`Phi(P) = gamma Tr(P) / 16`.

No event-shape freedom remains. Only the total reduced action phase `gamma`
remains.

## Theorem 2: the Planck target is the gamma-one case

The elementary action-phase reduction gives

`a^2 / l_P^2 = 8 pi q_* / eps_*`.

On the minimal cubical defect,

`eps_* = pi/2`,

so

`a^2 / l_P^2 = 16 q_*`.

If the elementary action phase is the minimal primitive event atom,

`q_* = gamma / 16`,

then

`a^2 / l_P^2 = gamma`.

Therefore exact conventional Planck,

`a^2 / l_P^2 = 1`,

is equivalent to

`gamma = 1`.

## Theorem 3: the information/action constant is fixed up to the same scalar

The time-locked axis carrier has exact information

`I_* = log 4 = 2 bits`.

The primitive phase atom is

`q_* = gamma / 16`.

Therefore the phase-per-information constant is

`kappa_info = q_* / I_* = gamma / 32 per bit`.

Exact `kappa_info = 1/32 per bit` is again equivalent to

`gamma = 1`.

So the information/action lane and the action-phase lane now have the same
remaining scalar obstruction.

## Hbar Interpretation

The dimensionless phase is

`q = S / hbar`.

This theorem derives the source-free primitive-cell distribution of `q`:

`q(P_eta) = gamma / 16`.

It does not derive the SI value of `hbar`.

If `S_cell` is the physical action of one complete primitive `C^16` event cell,
then

`gamma = S_cell / hbar`.

Thus

`gamma = 1`

means

`S_cell = hbar`.

That is exactly the remaining structural action-unit theorem. Without an
independent derivation of `S_cell` or of `gamma = 1`, solving for `hbar` is only
a unit-map rewrite.

## What This Closes

This closes the numerical-shape part of the hbar lane:

- the structural `16` is the full primitive `C^16` event-cell count;
- source-free finite additivity and automorphism naturality force each
  primitive event to carry exactly one sixteenth of the total reduced action
  phase;
- the observed `1/32 = 1/(2 * 16)` relation is the same trace result divided by
  the two-bit locked axis carrier.

## What Remains

The only remaining hbar/action-unit scalar is:

`gamma = Phi(I_16) = 1`.

A reviewer can now reject the hbar upgrade only by denying at least one of:

1. action phase belongs to the source-free primitive event functional object
   class;
2. the elementary action phase is the minimal primitive event atom;
3. the complete primitive `C^16` event cell carries one reduced action unit.

Those are the exact remaining objections. The branch should not claim a full
native hbar prediction until the third statement is derived or independently
anchored.

## Safe Claim

Use:

> The hbar lane now derives the primitive action-phase trace shape:
> `q_atom = gamma/16` and `kappa_info = gamma/32` per bit. Exact
> conventional Planck corresponds to the single remaining scalar `gamma = 1`,
> i.e. one complete primitive `C^16` event cell carries one reduced action
> unit.

Do not use:

> The branch predicts the SI value of `hbar`.

Do not use:

> The branch derives `gamma = 1` from bare `Cl(3)` / `Z^3` alone.
