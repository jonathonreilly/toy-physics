# Gauge-Vacuum Plaquette First Propagated Retained Triple Minimality and Current-Bank No-Go

**Date:** 2026-04-17  
**Status:** exact science-only sharpening of the finite plaquette `beta = 6`
target; on the current exact bank the propagated retained three-sample triple
is already the minimal honest positive finite target, and the strongest sharper
negative statement is that even its normalized value is still not determined  
**Script:** `scripts/frontier_gauge_vacuum_plaquette_first_propagated_retained_triple_minimality_current_bank_nogo_2026_04_17.py`

## Question

After the first propagated retained triple target theorem, can the live finite
plaquette target be sharpened further on the current bank?

More precisely, is there now either:

- a stronger exact positive target theorem below the propagated retained
  three-sample triple,
- or a sharper exact no-go than the existing evaluator-route underdetermination
  statement?

## Answer

Yes, but the sharpening is mostly negative.

On the current exact bank:

1. the propagated retained three-sample triple is already the **minimal**
   honest positive finite target;
2. the only exact positive sharpening below it is the equivalent first retained
   propagated coefficient triple obtained by the already-fixed radical inverse
   map;
3. the strongest sharper current-bank negative statement is that even the
   normalized propagated retained triple is still not determined;
4. the exact cone, truncation-envelope, and `Tau`-wedge theorems add real
   constraint geometry, but they still do not evaluate the triple;
5. no finite sample-packet extension can by itself close the full beta-side
   vector.

So the branch can now say the sharper thing:

- the next positive plaquette theorem target is still exactly
  `(Z_6^env(W_A), Z_6^env(W_B), Z_6^env(W_C))`,
- equivalently the first retained propagated coefficient triple,
- and on the current bank there is no smaller exact evaluative target and no
  closed exact value theorem below it.

## Setup

From
[GAUGE_VACUUM_PLAQUETTE_FIRST_PROPAGATED_RETAINED_TRIPLE_TARGET_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_PROPAGATED_RETAINED_TRIPLE_TARGET_NOTE_2026-04-17.md):

- the first honest next evaluative target is the propagated retained
  three-sample triple,
- equivalently the first retained propagated coefficient triple of `v_6`.

From
[GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_ENVIRONMENT_EVALUATOR_ROUTE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_THREE_SAMPLE_ENVIRONMENT_EVALUATOR_ROUTE_NOTE_2026-04-17.md):

- the actual route is already

  `v_6 -> E_3 -> (Z_A, Z_B, Z_C)`,

- and the current exact stack still does **not** determine even the normalized
  three-sample triple.

From
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_EXACT_RADICAL_RECONSTRUCTION_MAP_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_EXACT_RADICAL_RECONSTRUCTION_MAP_NOTE_2026-04-17.md)
and
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_CURRENT_STACK_CONSTRAINT_BOUNDARY_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_CURRENT_STACK_CONSTRAINT_BOUNDARY_NOTE_2026-04-17.md):

- the first symmetric three-sample matrix is explicit and invertible,
- there is no further universal linear collapse below the three named same-surface
  values,
- and the current exact stack adds no symmetry or source-observable relation
  that collapses the burden further.

From
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_POSITIVE_CONE_ORDER_WITNESS_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_POSITIVE_CONE_ORDER_WITNESS_NOTE_2026-04-17.md),
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_CHARACTER_TRUNCATION_ENVELOPE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_CHARACTER_TRUNCATION_ENVELOPE_NOTE_2026-04-17.md),
and
[GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_CONTROLLED_RETAINED_COEFFICIENT_WEDGE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FIRST_SYMMETRIC_THREE_SAMPLE_TAU_CONTROLLED_RETAINED_COEFFICIENT_WEDGE_NOTE_2026-04-17.md):

- the current bank already constrains the finite target by one exact positive
  cone,
- one exact retained/tail decomposition,
- and one exact `Tau`-controlled outer wedge.

From
[GAUGE_VACUUM_PLAQUETTE_FINITE_SAMPLE_PACKET_NONCLOSURE_NOTE_2026-04-17.md](./GAUGE_VACUUM_PLAQUETTE_FINITE_SAMPLE_PACKET_NONCLOSURE_NOTE_2026-04-17.md):

- no finite sample packet can by itself determine the full beta-side vector
  `v_6`.

## Theorem 1: the propagated retained three-sample triple is already the minimal positive finite target on the current bank

Let

`Z = (Z_6^env(W_A), Z_6^env(W_B), Z_6^env(W_C))`.

Then on the first retained witness sector:

1. `Z` is the exact propagated output of the common beta-side vector `v_6`
   under the fixed three-row sample operator `E_3`;
2. the restriction of `E_3` is the explicit radical matrix `F`;
3. `det(F) != 0`;
4. the current exact stack adds no symmetry or holonomy-resolved source law
   collapsing `Z` to fewer than three independent scalar values.

Therefore there is no smaller exact positive finite evaluative target currently
supported below `Z`.

The only exact positive sharpening is the equivalent target

`(a_(0,0), a_(1,0), a_(1,1))`,

obtained from `Z` by the already-fixed radical inverse map.

## Corollary 1: the first retained propagated coefficient triple is only an equivalent reformulation

Because `F` is invertible, the first retained propagated coefficient triple is
equivalent to the propagated retained three-sample triple.

So it is a legitimate reformulation of the target, but not a smaller
independent theorem target.

## Theorem 2: the strongest sharper current-bank negative statement lives directly at the normalized propagated triple

Let

`Z_hat = Z / z_(0,0)^env(6)`.

Then the current exact stack still does **not** determine `Z_hat` uniquely.

More precisely:

1. the evaluator-route theorem already exhibits distinct admissible positive
   conjugation-symmetric `beta = 6` witnesses yielding different normalized
   three-sample triples;
2. the positive-cone, retained-envelope, and `Tau`-wedge theorems constrain
   the admissible region for `Z_hat`, but do not collapse it to a point;
3. therefore the current bank does not support an exact value theorem for the
   propagated retained triple, normalized or unnormalized.

So the stronger current-bank no-go is not merely that the full beta-side vector
is open. It is that even the actual minimal finite target is still
underdetermined at the normalized level.

## Corollary 2: finite-sample extensions do not change the full beta-side nonclosure

Even if one enlarges the retained target by adjoining finitely many more marked
holonomy samples, the finite-sample nonclosure theorem still prevents finite
sampling alone from determining the full beta-side vector `v_6`.

So the first propagated retained triple is the correct finite target for the
next positive theorem attempt, but it is not a finite route to full beta-side
closure by itself.

## What this closes

- exact clarification that the propagated retained three-sample triple is
  already the minimal positive finite plaquette target on the current bank
- exact clarification that the first retained propagated coefficient triple is
  only an equivalent reformulation of that target
- exact sharper no-go that even the normalized propagated retained triple is
  still not determined on the current bank
- exact clarification that the existing cone, truncation, and `Tau`-wedge
  theorems are constraint geometry, not evaluation theorems
- exact clarification that finite sample extension does not change full
  beta-side nonclosure

## What this does not close

- explicit values `Z_6^env(W_A)`, `Z_6^env(W_B)`, `Z_6^env(W_C)`
- explicit values of the first retained propagated coefficient triple
- explicit class-sector matrix elements of `S_6^env`
- explicit class-sector matrix elements of `eta_6(W)`
- the full plaquette `beta = 6` operator closure
- the global sole-axiom PF selector theorem

## Why this matters

This is the sharpest honest answer now available on the finite plaquette seam.

The branch no longer says only:

- the next target is finite.

It can now say:

- that finite target is already minimal on the present bank,
- the only smaller-looking reformulation is equivalent rather than stronger,
- and the strongest current-bank negative result already lives directly at that
  minimal finite target.

So the next positive move remains exactly what the branch was driving toward:

- evaluate `Z_6^env(W_A)`,
- evaluate `Z_6^env(W_B)`,
- evaluate `Z_6^env(W_C)`,
- then apply the exact radical inverse map.

## Command

```bash
python3 scripts/frontier_gauge_vacuum_plaquette_first_propagated_retained_triple_minimality_current_bank_nogo_2026_04_17.py
```
