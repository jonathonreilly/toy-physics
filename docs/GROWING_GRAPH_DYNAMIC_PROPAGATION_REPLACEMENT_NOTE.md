# Growing Graph Dynamic Propagation Replacement Note

**Date:** 2026-04-05  
**Status:** bounded replacement / dynamic propagator outside audit-ratified tier

## One-line read

The reported variable-width per-layer propagator is not yet a retained claim
here. The honest graph-growth / cosmology-proxy observable is the narrower
frontier-delay growth proxy on the existing growing-graph family.

## Why this note exists

The dynamic packet lane is only worth promoting if the layer-to-layer update
actually carries amplitude forward in a stable way.

Until that is demonstrated, the graph-growth story should stay at analog /
proxy level:

- frontier-delay growth
- node-count growth
- mean / RMS radius growth
- static no-growth control

## Primary retained proxy

The retained replacement observable is the frontier-delay proxy frozen in:

- [`scripts/growing_graph_frontier_expansion.py`](/Users/jonreilly/Projects/Physics/scripts/growing_graph_frontier_expansion.py)
- [`docs/GROWING_GRAPH_FRONTIER_EXPANSION_PROXY_NOTE.md`](/Users/jonreilly/Projects/Physics/docs/GROWING_GRAPH_FRONTIER_EXPANSION_PROXY_NOTE.md)

Frozen replay from that proxy:

- step 0 frontier delay: `3.000`
- step 20 frontier delay: `22.000`
- frontier-delay slope: `+0.9325` hops/step
- static control frontier delay: `3.000`

## What this replaces

This replacement note deliberately does **not** claim:

- a variable-width wavepacket
- a cosmological metric
- de Sitter expansion
- any amplitude-carry-forward theorem

It also does not claim that the broader adaptive / coevolving geometry lane is
positive. That lane remains a bounded no-go unless a stronger control law
lands.

## Safe conclusion

- the growing graph expands in a proxy sense
- the static control stays flat
- the frontier-delay observable is the honest retained growth result
- the dynamic propagator lane is not yet review-safe enough to promote

## If revisited

If a true layer-to-layer amplitude-carry-forward propagator is rebuilt later,
it should be checked against the frontier-delay proxy, not against a cosmology
headline.

The first question must be whether the per-layer state actually survives the
update without collapsing the width observable to zero or producing a geometry
artifact.
