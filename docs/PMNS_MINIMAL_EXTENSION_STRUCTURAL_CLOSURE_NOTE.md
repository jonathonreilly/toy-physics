# PMNS Minimal-Extension Structural Closure

**Date:** 2026-04-16  
**Status:** exact positive structural-closure theorem on the minimal
post-retained PMNS extension  
**Atlas front door:** canonical toolkit on `main` at
`docs/publication/ci3_z3/DERIVATION_ATLAS.md`  
**Script:** `scripts/frontier_pmns_minimal_extension_structural_closure.py`

## Question

After the pure-retained PMNS lane closes negatively at

`sigma = 0`, hence `J_chi = 0`,

does the branch already contain a coherent **post-retained** PMNS extension
that closes the structural lane positively, or is PMNS itself still the open
structural pacing item?

## Bottom line

Yes. On the minimal admitted post-retained PMNS extension, the PMNS structural
lane is already closed.

The exact chain now reads:

1. the pure-retained bank sharpens the PMNS blocker to the native cycle mean
   `sigma` and proves `sigma = 0`
2. the remaining microscopic hole is exactly the legitimacy of a nontrivial
   active response pack on the existing graph-fixed `hw=1` triplet
3. if the microscopic active response is required to realize the exact forward
   projected-cycle transport on that ordered basis, then the unique admissible
   nonfree response kernel is

   `K_fwd = C^2`

4. the exact active-response law then forces

   `A_fwd = (1 + 1/lambda_act) I - (1/lambda_act) C`

5. that block is already the exact `C_3`-covariant closure point, so

   `sigma = J_chi = -1/lambda_act`

So the live PMNS question is no longer:

- can the branch realize a nontrivial PMNS response class at all?
- which reduced-cycle point should be chosen once such a class is admitted?

Those are closed on the minimal extension.

What remains open is upstream of that positive extension:

- derive the transport-realizing response principle from the pure-retained
  sole-axiom bank itself, or
- accept that PMNS is extension-dependent on this branch

## Inputs

This capstone compresses the following exact branch surfaces:

- [PMNS_SIGMA_ZERO_NOGO_NOTE.md](./PMNS_SIGMA_ZERO_NOGO_NOTE.md)
- [PMNS_ACTIVE_RESPONSE_PACK_AXIOM_DERIVATION_NOTE.md](./PMNS_ACTIVE_RESPONSE_PACK_AXIOM_DERIVATION_NOTE.md)
- [PMNS_PROJECTED_CYCLE_RESPONSE_SOURCE_PRINCIPLE_NOTE.md](./PMNS_PROJECTED_CYCLE_RESPONSE_SOURCE_PRINCIPLE_NOTE.md)
- [PMNS_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md](./PMNS_HW1_SOURCE_TRANSFER_BOUNDARY_NOTE.md)
- [PMNS_ORIENTED_CYCLE_CHANNEL_VALUE_LAW_NOTE.md](./PMNS_ORIENTED_CYCLE_CHANNEL_VALUE_LAW_NOTE.md)

Those notes already prove, in order:

1. the pure-retained bank cannot lift `sigma` away from zero
2. the exact missing theorem is the microscopic legitimacy of a nonfree active
   response pack on the existing `hw=1` carrier
3. the smallest exact positive reopening principle is transport-realizing
   microscopic response on the graph-fixed triplet
4. once such a lower-level pack is supplied, downstream PMNS closure already
   works exactly
5. the oriented-cycle readout on the reduced carrier is already exact

So the remaining honest question is no longer whether PMNS lacks a structural
post-retained theorem.

## Exact theorem

### 1. Pure-retained PMNS closes negatively

On the current pure-retained bank, the remaining PMNS object is exactly the
cycle/transport mean

`sigma`,

with the nontrivial `C_3` current satisfying `J_chi = sigma` on the covariant
slice.

The retained bank forces

`sigma = 0`,

so it also forces

`J_chi = 0`.

So the pure-retained PMNS lane is done, but negatively.

### 2. The exact missing microscopic object is one nontrivial response pack

The current bank already fixes:

- the graph-fixed ordered forward cycle on the `hw=1` triplet
- the exact active-response law
- the downstream reduced-cycle readout

What it does **not** fix is the microscopic legitimacy of a nonfree response
pack on that carrier.

So the PMNS post-retained hole is not a value-selection problem anymore. It is
the response-pack legitimacy problem.

### 3. The minimal admitted extension forces one response kernel

On the ordered `hw=1` triplet, the exact projected forward cycle is

`C`.

If the microscopic active response is required to realize that exact forward
transport on the ordered response basis, then among the cycle kernels

`{I, C, C^2}`

the unique admissible nonfree forward-support choice is

`K_fwd = C^2`.

So the response-kernel ambiguity is closed on the minimal extension.

### 4. The response law forces one exact PMNS block

Applying the exact active-response law to `K_fwd` gives

`A_fwd = I + (I - K_fwd^{-1}) / lambda_act`

equivalently

`A_fwd = (1 + 1/lambda_act) I - (1/lambda_act) C`.

That block already lies on the exact projected-cycle family `A(a,b) = aI + bC`
with

`b = -1/lambda_act`.

### 5. The forced block is already the covariant PMNS closure point

On the exact projected-cycle family, the covariant readout satisfies

`sigma = J_chi = b`.

Therefore for the forced block `A_fwd`,

`sigma = J_chi = -1/lambda_act`

exactly.

With the retained passive free pack unchanged, the one-sided PMNS lane closes
exactly downstream.

## The theorem-level statement

**Theorem (PMNS minimal-extension structural closure on the current branch).**
Assume:

1. the pure-retained PMNS lane closes negatively at `sigma = 0`
2. the exact graph-fixed ordered forward cycle on the `hw=1` triplet
3. the exact active-response law
4. the minimal post-retained extension principle that the microscopic active
   response must realize the exact forward projected-cycle transport on that
   ordered basis

Then the post-retained PMNS structural lane is closed with exact data

`K_fwd = C^2`,
`A_fwd = (1 + 1/lambda_act) I - (1/lambda_act) C`,
`sigma = J_chi = -1/lambda_act`.

Equivalently: PMNS is no longer the open structural pacing item on this
branch. The remaining open work is whether this extension principle can be
derived from the pure-retained sole-axiom bank itself.

## Downstream Interface

For the separate CP/leptogenesis branch, the clean handoff is **not** the raw
scalar `J_chi` by itself.

The refreshed DM/leptogenesis reduction already says the PMNS-assisted lane is
consumed through the charged-lepton projected Hermitian source law

`dW_e^H`

or, one level upstream, the full microscopic charge-preserving operator

`D`.

So the connection should be read as:

- this PMNS capstone closes the minimal positive PMNS structure on the neutrino
  branch
- the CP/leptogenesis branch should consume the PMNS side through
  `dW_e^H -> H_e -> |U_e|^2^T`, not by trying to ingest the standalone scalar
  `J_chi`

`J_chi` remains the neutrino-side compression of the PMNS last mile, while
`dW_e^H` / `H_e` is the actual downstream flavored interface.

## What this closes

This closes the PMNS structural lane on the minimal post-retained extension:

- the active response class is admitted
- the nonfree response kernel is unique
- the reduced-cycle closure point is unique
- the nontrivial PMNS current is fixed exactly on that extension

So the branch no longer needs to treat the post-retained PMNS program as an
open collection of carrier/value-selection ambiguities.

## What this does not close

This note does **not** prove:

- that the pure-retained sole-axiom bank already implies the extension
  principle
- a positive pure-retained PMNS derivation
- full neutrino closure from one unextended bank
- the downstream CP/leptogenesis tail

In particular, this note packages the PMNS structural lane only. It does not
claim that the full positive neutrino program is finished.

## Safe wording

**Can claim**

- the pure-retained PMNS lane is closed negative at `sigma = 0`, hence
  `J_chi = 0`
- the minimal post-retained PMNS extension is structurally closed
- the exact extension data are
  `K_fwd = C^2`,
  `A_fwd = (1 + 1/lambda_act)I - (1/lambda_act)C`,
  `sigma = J_chi = -1/lambda_act`
- the clean CP/leptogenesis handoff is through `dW_e^H` / `H_e`, not raw
  `J_chi`

**Cannot claim**

- that the pure-retained bank already gives a positive PMNS current
- that PMNS is axiom-derived without the extension principle
- that full neutrino closure is complete

## Command

```bash
python3 scripts/frontier_pmns_minimal_extension_structural_closure.py
```
