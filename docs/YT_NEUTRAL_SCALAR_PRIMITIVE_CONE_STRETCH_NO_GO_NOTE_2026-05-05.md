# YT Neutral Scalar Primitive-Cone Stretch No-Go

Status: exact negative boundary.

This block is cycle 5 of the PR230 non-chunk physics loop.  After the W/Z
source-coordinate transport shortcut closed negatively, the next hard residual
was the neutral-rank route: can the current source/neutral PR230 surface force
the primitive-cone irreducibility needed by the conditional Perron rank-one
support theorem?

The runner answers no.  The minimal allowed premise set is only the current
same-source scalar-source surface, source-created neutral pole support as
source-side evidence, the conditional Perron theorem shape, and current no-go
memory.  It does not import external target values, unit-operator or Ward
readout authority, lattice gauge-normalization shortcuts, unit-overlap
shortcuts, or source-only rows as
Schur/W/Z/`O_H` rows.

The obstruction is algebraic and non-data: a reducible neutral completion can
preserve all source-only `C_ss` rows while leaving a finite orthogonal neutral
direction outside the primitive cone.  The current branch has no same-surface
certificate proving a nonnegative strongly connected neutral transfer matrix
with a positive primitive power, isolated pole, positive source/canonical-Higgs
overlaps, and orthogonal-null control.

Runner:

```bash
python3 scripts/frontier_yt_neutral_scalar_primitive_cone_stretch_no_go.py
# SUMMARY: PASS=12 FAIL=0
```

Artifacts:

- `scripts/frontier_yt_neutral_scalar_primitive_cone_stretch_no_go.py`
- `outputs/yt_neutral_scalar_primitive_cone_stretch_no_go_2026-05-05.json`

Claim boundary:

- no retained or `proposed_retained` top-Yukawa closure;
- no chunk MC packaging or rerun;
- no synthetic O_H, W/Z, Schur, or matched response rows;
- no external-target, unit-overlap, Ward-readout, unit-operator,
  gauge-normalization, or bare-gauge-coupling shortcut.

Next action: supply a strict same-surface neutral primitive-cone certificate,
or pivot to a future-row route: `O_H/C_sH/C_HH` pole rows, same-source W/Z rows
with identities, Schur `A/B/C` kernel rows, or a scalar-LSZ
moment/threshold/FV certificate.
