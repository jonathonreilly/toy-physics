# Koide pointed-origin lattice-axiom derivation theorem

**Date:** 2026-04-24
**Runner:** `scripts/frontier_koide_pointed_origin_lattice_axiom_derivation.py`
**Status:** positive derivation of the pointed source/boundary origin law

## Theorem

The retained finite-lattice source formalism is a **pointed** source/boundary
functor, not an unpointed affine grammar.

The point is supplied by the already-retained Grassmann source axiom:

```text
D[J] = D + J
W[J] = log |det(D+J)| - log |det D|.
```

The subtraction of the undeformed determinant is a source-free basepoint.  A
translated source origin is not the same physical source-free readout; it is a
different baseline operator.

This single pointed-origin law gives:

```text
Q:
  source origin K = 0
  -> Y = I_2
  -> K_TL = 0
  -> Q = 2/3.

delta:
  source-free endpoint object = retained real nontrivial Z3 primitive
  -> no natural rank-one CP1 endpoint selector
  -> spectator = 0.

  determinant/Pfaffian endpoint functor is unital
  -> identity endpoint has phase 0
  -> c = 0.

  eta_APS = 2/9
  -> delta = 2/9.
```

## Q Origin

On the reduced two-slot carrier, the exact pure-block law is:

```text
W(k,0) = log(1+k)
W(0,k) = log(1+k).
```

If the source origin is translated by `b`, the pure-block law becomes:

```text
W_b(k) = log(1+b+k) - log(1+b).
```

Its derivative at the source-free point is:

```text
dW_b/dk |_{k=0} = 1/(1+b).
```

The retained pure-block normalization requires this derivative to be `1`.
Therefore:

```text
b = 0.
```

So the zero-source readout is not an optional convention.  It is the unique
readout preserving the retained lattice source baseline and pure-block
normalization.

## Brannen Endpoint

The source-free boundary object is the real nontrivial `Z3` primitive.  A
physical endpoint functor on that object must be natural under automorphisms
of the retained primitive.

On the real primitive, the automorphism generated by the complex structure is:

```text
J = [[0,-1],[1,0]].
```

A trace-one symmetric endpoint mark commuting with `J` is forced to:

```text
I/2.
```

This is scalar and not a rank-one idempotent.  Therefore a rank-one `CP1`
endpoint line is not a source-free natural endpoint object.  It may be used as
a coordinate for the relative phase, but it is not an independent physical
selector unless extra boundary data are added.

Review sentence: rank-one `CP1` endpoint line is not a source-free natural endpoint object in the retained real-primitive boundary category.

Thus the physical source-free Brannen endpoint descends to the whole real
nontrivial primitive, and the spectator channel is zero.

## Endpoint Unit

The determinant/Pfaffian endpoint readout is unital:

```text
det(I) = 1
phase(det I) = 0.
```

For an affine endpoint coordinate:

```text
R_c(phi) = phi + c,
```

the identity endpoint condition is:

```text
R_c(0) = 0.
```

Therefore:

```text
c = 0.
```

An exact source-free boundary counterterm would map the identity boundary away
from the determinant unit.  It is forbidden by unitality, not by fitting the
target value.

## No Target-Value Cancellation

The theorem excludes target-value cancellation before evaluating the open
endpoint.  A nonzero spectator channel can always be hidden by a compensating
endpoint shift, for example:

```text
delta_open = eta_APS cos(alpha)^2 + eta_APS sin(alpha)^2 = eta_APS.
```

That reproduces the target value while retaining a spectator.  The pointed
lattice-origin derivation forbids the two pieces independently:

```text
CP1 selector absence / descent -> spectator = 0
determinant endpoint unitality -> c = 0.
```

So `delta = eta_APS` follows after both origin laws are derived.  It is not
obtained by adjusting `c` to compensate a selected-line spectator.

## Relationship To The Exhaustion Theorem

`KOIDE_POINTED_ORIGIN_EXHAUSTION_THEOREM` proves that origin-free retained data
cannot select the closing representative and that the pointed-origin law is
necessary within the residual atlas.

This theorem supplies that missing law from the already-retained pointed
lattice source functor.

## Closeout

Expected runner flags:

```text
KOIDE_POINTED_ORIGIN_LATTICE_AXIOM_DERIVATION=TRUE
RETAINED_POINTED_SOURCE_BOUNDARY_ORIGIN_LAW_DERIVED=TRUE
KOIDE_Q_CLOSED_BY_POINTED_SOURCE_ORIGIN=TRUE
KOIDE_BRANNEN_CP1_SELECTOR_ABSENCE_DERIVED=TRUE
KOIDE_ENDPOINT_UNIT_BASEPOINT_DERIVED=TRUE
KOIDE_DELTA_CLOSED_BY_POINTED_BOUNDARY_ORIGIN=TRUE
KOIDE_DIMENSIONLESS_LANE_POINTED_ORIGIN_CLOSURE=TRUE
BOUNDARY=overall_lepton_scale_v0_not_addressed
```

## Falsifiers

- A retained theorem allowing a translated source origin while preserving the
  same undeformed `D` and the pure-block source law `log(1+k)`.
- A retained source-free boundary functor whose endpoint object is a marked
  rank-one `CP1` line rather than the real primitive.
- A retained determinant/Pfaffian endpoint readout whose identity object has
  nonzero phase.
- A retained physical boundary counterterm that shifts the open endpoint while
  remaining compatible with unital determinant gluing.
- A proof that the CP1 selected-line object is retained as physical endpoint
  data, not merely as a phase coordinate.

## Verification

Run:

```bash
python3 scripts/frontier_koide_pointed_origin_lattice_axiom_derivation.py
python3 scripts/frontier_koide_pointed_origin_lattice_axiom_nature_review.py
python3 scripts/frontier_koide_dimensionless_lane_pointed_origin_closure_packet.py
python3 scripts/frontier_koide_lane_regression.py
```
