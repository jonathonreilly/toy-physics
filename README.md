# Discrete Event-Network Toy Model

This workspace contains a small runnable prototype for a discrete event-network model.

`Octopus physics` is a lightweight project nickname; the claims in this README are about the event-network toy model itself.

## How To Read This README

- `Model Axioms`, `Primitives`, and `Toy Rules` describe the conceptual scaffold and the assumptions currently carried by the model.
- `What the current script implements` describes the mechanics that are actually present in the repository.
- `What the current script demonstrates` is narrower: it lists behaviors that the runnable toy model currently produces or pressure-tests.
- `What is still cheating` lists the assumptions that remain unresolved or only partially justified.
- [AUTOPILOT_WORKLOG.md](/Users/jonreilly/Projects/Physics/AUTOPILOT_WORKLOG.md) is the tracked loop-by-loop status and next-step ledger for the autopilot thread.

## Model Axioms

This toy model is built around a compact event-network ontology:

- Reality is an evolving network of events and influence relations.
- Stable objects are persistent self-maintaining patterns in that network.
- Space is inferred from influence neighborhoods and signal delays.
- Duration is local update count along a pattern's history.
- The arrow of time is the direction of increasing durable record formation.
- Free systems follow the locally simplest allowed continuation.
- Inertia is undisturbed natural continuation.
- Gravity is natural continuation in a distorted continuation structure.
- Measurement is the formation of durable records that separate previously combinable alternatives.
- Consciousness is part of the wider ontology as a high-order record-integrating self-model, but it is not simulated here.

## Primitives

The code models the following toy primitives:

- `Event e`: a local change.
- `Link e -> e'`: an allowed influence from one event to another.
- `delta(e,e')`: delay on that influence.
- `k(e,e')`: compatibility weight for that influence.
- `History h`: an ordered chain of linked events.
- `W(h)`: the total weight of a history.
- `R`: a durable record state.
- `S`: a stable self-maintaining pattern in the event network.
- `tau_S(h)`: the internal update count of pattern `S` along history `h`.

## Toy Rules

- Reality evolves by extending compatible histories.
- Free evolution follows the locally best-compatible histories.
- Geometry is inferred from the delay and coupling structure, not assumed first.
- Duration is local update count, not a universal background.
- Dense stable patterns modify local delays, compatibilities, and update rates.
- Alternatives without durable records can combine.
- Alternatives tagged by different durable records cannot combine.
- Conscious systems are part of the wider framework as record-integrating predictive processes, but this prototype only models durable records, not consciousness itself.

## Conceptual Motivation

These points explain how the current toy model is motivated. They are not all direct outputs of the script:

- The ontology starts from events, links, delays, and durable records rather than particles in a pre-given container.
- The local `retained update` term `sqrt(dt^2 - dx^2)` is motivated by the boost-like symmetry pressure test, while the `spent delay` action adds the extra accounting assumption that still has to be justified.
- Gravity-like behavior is motivated by treating persistent self-maintaining patterns as sources of local delay/load rather than by inserting a force law.
- Measurement is motivated as durable record formation, with interference lost when alternatives are separated into different record sectors.

Run it with:

```bash
python3 /Users/jonreilly/Projects/Physics/toy_event_physics.py
```

For a single corrected-base verification pass, run:

```bash
python3 /Users/jonreilly/Projects/Physics/scripts/base_confidence_check.py
```

Add `--full` if you also want the slower helper-backed overlap, route-map, and mechanism-split reruns.

What the current script implements:

- Extracts a classical-looking limit from the same shared local rule by following stationary-action geodesics on the async graph.
- Derives the local delay field from an emergent persistent pattern whose late-time occupancy sources graph-load under neighbor averaging, with the initial disturbance and self-maintenance rule jointly selected by searching interior one-node seeds against a compact rule family.
- Infers causal order from positive local delays on a graph, then orients propagation by that inferred causal order instead of a global step counter.
- Runs a small robustness sweep across rectangular, tapered, and skewed graphs, with hard or wrapped vertical boundaries and compact or extended local rule families.
- Models `measurement` as durable record formation in a two-slit-style path network.
- Uses one shared local edge rule for the causal-shell graph, the asynchronous continuation model, and the slit graph.
- Makes the local rule explicit through a small set of postulates and a derived `LocalRule` object.
- Uses bounded-memory summary passes for the deterministic, pairwise cross-weight, high-end weight-ladder, tracked-palette random, limited-rediscovery random, and geometry-randomized perturbation ensembles so the selector-free perturbation diagnostics can run without materializing every case row at once.
- Reuses cached generated-geometry prediction contexts within a single run so repeated graph-side predictor diagnostics do not rebuild the same procedural, graph-local morph, and whole-shape frontier rows from scratch.
- Trains cross-dataset and generated-geometry predictor diagnostics on the same retained-weight mode rows by default, so leaving `mode_retained_weight` unset no longer mixes all mode weights into a nominally single-weight benchmark.
- Selects structural predictor candidates from mode-only subset frontiers before reporting roughness, procedural, or generated-geometry scores, so those later scores are no longer reused as model-selection data.
- Pressure-tests two of the biggest cheats:
  - why `positive weights only` are too weak for interference
  - why the `square rule` is less arbitrary once reversible linear mixing is required
  - which local scalar remains stable under boost-like frame mixing

What the current script demonstrates:

- The same local rule can be used to extract stationary-action geodesics while still inferring causal order from local delays.
- With a proper-time-style action derived from the same delay field, those geodesics bend inward in a gravity-like way instead of merely routing around a slow region.
- The delay field itself now emerges from a local graph-relaxation rule: the code searches over interior one-node seeds and simple update rules, grows each candidate into an orbit, and lets the most localized stable non-boundary component source the decaying load profile.
- A local-delay graph can recover an inferred causal order without assigning all nodes the same global step number.
- The robustness sweep is now stronger than before: a minimal reduced compact family survives all six tested scenarios, and the extended family also survives all six.
- The compact-family repair is informative rather than ad hoc: the hard-topology winners under the full compact family are `skew-hard -> S[2,3]/B[3]` and `skew-wrap -> S[3,4]/B[1,3]`, which first repairs the compact family to `{3}, {1,3}, {2,3}, {3,4}`.
- Exhaustive minimization inside that repaired family shows one motif is still redundant, so the actual surviving compact subset is just `{1,3}, {2,3}, {3,4}`.
- One-by-one ablation of that three-option subset then shows all three remaining motifs are indispensable in the current sweep: removing `{1,3}` breaks `skew-wrap`, removing `{2,3}` breaks `taper-wrap`, and removing `{3,4}` causes the broadest collapse.
- A deeper mechanism ablation sharpens the story further: removing the delay field collapses the effect, and weakening field relaxation breaks the hardest wrapped skew case, so the field side looks genuinely load-bearing.
- The honest weak spot is now clearer too: replacing the proper-time-style action with plain coordinate delay weakens the sweep, but a bare link-length action still passes the current robustness metric, which means the action principle is not yet uniquely selected by the present tests.
- A final action-response discriminator closes most of that gap: once the benchmark also requires the distorted geodesic to move relative to the free geodesic, the spent-delay action with relaxed field propagation is the only named mechanism that both survives all six scenarios and keeps positive wrapped-case path response.
- A broader retained-weight family sweep then sharpens the remaining ambiguity: the low-weight half of the family fails the combined benchmark, while only the high-weight end survives, with the strongest wrapped-case response at the full spent-delay point `w = 1.0`.
- A cross-pack benchmark widens that test further across larger and mirrored scenario packs. It still favors the high-retained-update end, but it does not yet collapse the family all the way to a single exact coefficient.
- A final high-end proper-time consistency benchmark then zooms in on `w = 0.75..1.00` and checks every off-center boundary target across all packs. In the current grid, `w = 1.0` is the only retained weight that keeps the action gain larger than the extra coordinate delay everywhere, which is the first benchmark here to single out the exact spent-delay point within a tested family.
- The next decomposition makes that crossing more explicit: for a fixed distorted path, the consistency margin is linear in `w`, with a calculable critical weight `w* = 2 * delay_penalty / retained_total`.
- Under the current always-compare selector, the hardest case is now `mirror:rect-hard-large`, and the active winner stays `S[3,4]/B[1,3]` across the scanned `w = 0.75..1.00` range. So the present sign change is no longer a late branch switch; it is a single-branch crossing with the worst target moving as `w` changes.
- A rule-selection diagnostic shows why that branch is preferred: the fallback motif `S[2,3]/B[3,4]` keeps a slightly larger `center gap` at the low end, but the rescue motif `S[3,4]/B[1,3]` carries a much larger `arrival span`, so the current quality key `center gap + arrival span` already favors rescue from the start and favors it even more by `w = 1.0`.
- A frozen-branch comparison then removes another layer of heuristic freedom: the fallback branch becomes proper-time-consistent earlier, but the rescue branch keeps the stronger delay-distortion signature. Both branches still `survive`, so the selector is choosing between two viable motifs rather than between a working and failing one.
- The geometric diagnostic sharpens that point further: both frozen branches keep the same source row on the current hardest case, but they trade off final action contrast against boundary-delay distortion differently. So the selector is not reacting to a source jump; it is reacting to the current robustness quality.
- The focus-metric comparison makes the ambiguity explicit: on the current hardest case, pure geometric focus still favors the fallback branch throughout, a stiffness-style action-per-separation ratio favors the rescue branch throughout, and raw action-gap flips from fallback at low `w` to rescue at high `w`.
- A selector-policy ablation shows what survives when the old gate is removed. The current always-compare selector and the legacy gated selector differ in `20/78` compact benchmark pack-weight cases, concentrated in `base:taper-wrap`, `large:taper-wrap-large`, `large:skew-wrap-large`, and `mirror:rect-hard-large`; all of those changed cases still `survive`, so selector policy is steering branch choice more than pass/fail status.
- A new selector-free frontier layer now keeps nondominated motifs alive instead of collapsing immediately to one winner. It is diagnostic-only: the baseline selector still uses the current `always_compare` quality key, but the script now also reports `robustness`, `proper-time`, `geometry`, and `mixed` frontiers across both families and the benchmark packs.
- The cached frontier pass now runs end-to-end across all `156` family-pack-weight cases in the current benchmark grid. In the present run, the baseline-selected motif lies on the `robustness`, `proper-time`, `geometry`, and `mixed` frontiers in `52/156`, `22/156`, `35/156`, and `114/156` cases respectively, so no single frontier view reproduces the current selector.
- On the current hardest compact case, `mirror:rect-hard-large`, the selected rescue motif `S[3,4]/B[1,3]` sits on the `robustness` and `mixed` frontiers across the scanned retained-weight range, while the fallback motif `S[2,3]/B[3,4]` still remains nondominated on a non-identical view. That is the cleanest selector-free version of the current result: different metric views keep different motifs alive even when the baseline picks only one.
- The frontier reporting is now split three ways in the script: a case-level overview, a motif-frequency aggregate across both families, and a hard-case trace for the fallback/rescue motifs. In the current compact sweep, `S[2,3]/B[3,4]` appears on the `mixed` frontier in all `78` compact cases and on the `proper-time` frontier in `46`, while `S[3,4]/B[1,3]` is the current selected motif in `67/78` compact cases. That makes it possible to talk about selector-invariant motifs separately from selector-dependent winners.
- A first deterministic topology-perturbation ensemble now nudges each benchmark graph in two small ways at `w = 1.0` and tracks the unperturbed frontier palette through those perturbations. This is still diagnostic-only, but it is the first direct test of whether the current motif/frontier claims survive graph-level changes rather than only metric changes.
- Under that perturbation ensemble, the mixed-frontier story is much more stable than the selected-rule story. In the current run, `mixed`-frontier overlap survives in `26/26` perturbed cases for both `compact` and `extended`, while robustness overlap survives in `20/26` for both families. By contrast, the selected motif is retained in only `12/26` compact and `11/26` extended perturbed cases.
- The perturbation results are therefore interesting in a very specific way: some selector-free motif families look topology-robust, but the exact baseline winner is still quite topology-sensitive. That is a more honest and more useful result than simply reporting whether one selected motif keeps winning.
- A cross-weight perturbation comparison then asks whether that topology-robust story also survives a smaller retained-weight shift from `w = 1.0` down to `w = 0.95`. In the current run, the perturbed selected winner is actually stable across those two weights in `26/26` cases for both families, and `mixed`-frontier overlap also stays `26/26` for both families.
- The more fragile pieces are not the perturbed winner across the two high-end weights; they are the stronger overlap claims back to the unperturbed baseline. The base selected motif remains frontier-alive at both weights in `19/26` compact and `20/26` extended cases, while robustness overlap survives at both weights in `20/26` compact and `18/26` extended cases.
- So the current picture is sharper now: the selector-free mixed-frontier family looks robust to both topology nudges and a small retained-weight change, while the exact relationship back to the unperturbed baseline winner remains the part that breaks first.
- A high-end weight ladder now pushes that check one notch farther across `w = 0.90, 0.95, 1.00`. In the current run, the perturbed selected winner stays the same across the full ladder in `25/26` cases for both families, while `mixed`-frontier overlap survives in `26/26` cases for both families.
- The stronger selector-free overlaps are stricter on the full ladder: robustness overlap survives in `16/26` cases for both families, while proper-time and geometry overlap survive in `12/26` compact and `10/26` extended cases. So the mixed frontier still looks like the most stable selector-free object in the current model, but the tighter overlap notions remain much more local.
- A slightly wider seeded-random perturbation ensemble now pushes on the same question from the opposite direction: not hand-picked nudges, but three random removable/addable node choices drawn from each benchmark graph. In the current run, the same split still mostly survives that test.
- Under the widened random ensemble, `mixed`-frontier overlap survives in `38/39` compact and `39/39` extended cases, while robustness overlap survives in `30/39` for both families. By contrast, the exact unperturbed winner is retained in only `12/39` compact and `8/39` extended random perturbation cases.
- That is still the strongest selector-free result so far, but it is a little more honest than the earlier two-variant picture: once we stop insisting on one exact winning motif, the mixed-frontier family still looks substantially more stable than the exact winner under deterministic nudges, a small high-end weight shift, and a broader seeded-random graph ensemble, even though the wider random pass now exposes one compact-family mixed-frontier miss.
- A first limited-rediscovery random ensemble then removes one more cheat: instead of forcing perturbed graphs to stay inside the unperturbed frontier palette, it lets each random perturbation reintroduce a small number of locally rediscovered motifs. In the current `limit = 2` run, that repairs the last random `fragile` cases, but it does not strengthen the selector-free overlap story uniformly.
- Under limited rediscovery, all `39/39` random cases now `survive` in both families, and the mixed-frontier overlap remains `38/39` for `compact` and `39/39` for `extended`. But the stronger overlap claims get weaker: robustness overlap drops to `29/39` compact and `26/39` extended, and base selected-motif frontier survival drops to `32/39` compact and `29/39` extended.
- That tradeoff is useful rather than disappointing. It says some of the earlier random robustness was genuinely helped by palette inheritance, but the mixed frontier still remains the strongest selector-free object even after we relax that inheritance.
- A rediscovery-limit sweep over `0, 1, 2, 3` makes that tradeoff more precise. One extra rediscovered motif is already enough to repair the last random `fragile` cases in both families, and increasing the limit beyond `1` does not improve selected-rule retention, base selected-motif survival, or mixed-frontier overlap.
- In `compact`, the sweep goes `survives 38 -> 39`, `mixed 38 -> 38`, and `robustness 30 -> 29` from limit `0` to `3`. In `extended`, it goes `survives 38 -> 39`, `mixed 39 -> 39`, and `robustness 30 -> 26`. So the mixed frontier saturates early, while the stricter overlap notions erode as more local novelty is allowed back in.
- That is a strong cheat-removal clue: the mixed frontier looks like a more structural object than the exact inherited frontier identity, but the stronger overlap claims are still partly propped up by palette restriction.
- A geometry-randomized benchmark ensemble then pushes on the graph-family cheat more directly. Instead of only punching or shifting nodes inside the existing packs, it jitters the entire column profile of each graph into two deterministic whole-shape variants and then applies the minimal rediscovery limit `1`.
- Under that whole-shape geometry jitter, `compact` still `survives` in `25/26` cases and keeps `mixed`-frontier overlap in `25/26`, while `extended` still `survives` in `26/26` and keeps `mixed` overlap in `26/26`. By contrast, exact selected-winner retention drops to `9/26` in `compact` and `6/26` in `extended`, with robustness overlap at `17/26` and `16/26`.
- That is one of the strongest results in the repo now: even when we stop testing only local node edits and start deforming the whole benchmark shape, the mixed frontier still looks much more stable than the exact winner.
- A procedural graph-generator ensemble then goes one step farther: instead of deforming the original contour, it regenerates each graph from scratch inside the same bounding box using a seeded smooth column-profile rule, again with rediscovery limit `1`.
- Under that procedural generator, both families still `survive` in `25/26` cases and keep `mixed`-frontier overlap in `24/26` cases. Exact selected-winner retention drops to `7/26` in `compact` and `6/26` in `extended`, with robustness overlap at `18/26` and `17/26`.
- That is a harder and more honest result than the earlier pack-local tests: the mixed frontier is no longer merely surviving local edits to hand-authored shapes, but it is also not perfectly stable once the graph family is regenerated from a small independent generator.
- A focused diagnostic on the lone compact procedural `survive` miss narrows that failure further. The miss still keeps mixed-frontier overlap, and its `center_gap` is actually larger than its nearby surviving sibling. What collapses is `arrival_span`, from `1.773` in the surviving sibling to `0.335` in the miss.
- Geometrically, that miss is the one case with a cross-midline, high-variation centerline: `center_range 3.5` and `center_total_variation 8.0`, versus `2.0` and `4.0` in the surviving sibling. So the current evidence points to a specific sensitivity: some deformations preserve local focusing while flattening far-boundary delay distortion enough to miss the present `survives` threshold.
- A direct contour-sensitivity sweep then turns that clue into a mechanism test. Holding the span profile fixed and interpolating only the centerline deformation keeps mixed-frontier overlap intact across the whole sweep. The `survives -> mixed` transition happens only at the highest deformation point, where `arrival_span` falls below the current `0.5` cutoff (`0.680` at `alpha = 0.75` versus `0.335` at `alpha = 1.00`), while `center_gap` remains large (`0.849 -> 0.772`).
- That is the strongest current evidence for what the mixed frontier is sensitive to: not generic contour roughness, but specifically deformations that preserve local focusing while suppressing far-boundary delay spread.
- A final benchmark then tests whether the current axis-aligned `survives/mixed` threshold is the right observable at all. It compares the current `box-min` rule with smooth combined observables (`harmonic`, `geometric`, `arithmetic`) on the core robustness sweep, the geometry-randomized ensemble, the procedural generator, and the contour miss.
- All four observables preserve the current strong cases: `12/12` core survives, `51/51` geometry-randomized survives, and `50/50` procedural survives. The difference is in what happens to the current threshold misses.
- `box-min` leaves the contour miss as `mixed` and promotes none of the procedural misses. `arithmetic` and `geometric` rescue the contour miss but also promote the geometry miss. `harmonic` is the most conservative smoother: it rescues the contour/procedural miss (`survives`, score `1.23`) but leaves the separate geometry miss as `mixed`.
- That makes `harmonic` the strongest tested candidate for a combined focusing observable so far. It smooths the threshold exactly where the model now looks overly sharp, without simply promoting every non-surviving case.
- A follow-up diagnostic then runs that `harmonic` observable through the existing selector/frontier machinery without changing the candidate pool. This pass is still diagnostic-only: it relabels the already-evaluated frontier candidates instead of changing the baseline selector or rerunning the search.
- On the current `156`-case frontier sweep, that harmonic relabeling changes the selected winner in `105/156` cases while keeping the harmonic-selected rule at `survives` in all `156/156` cases. The baseline-selected rule still survives under harmonic in `154/156` cases.
- Under harmonic, the selected rule lands on the `robustness` and `mixed` frontiers in `78/78` cases for both `compact` and `extended`, while the old baseline-selected motif remains on the harmonic `mixed` frontier in only `58/78` compact and `56/78` extended cases.
- That is a stronger threshold-cheat result than the original contour benchmark alone: the sharp `box-min` status rule is not just patching a local miss, it is materially steering the selector/frontier picture even when the underlying candidate set is held fixed.
- A second diagnostic then removes the bucketization step itself: it keeps the harmonic candidate relabeling, but replaces the discrete `survives/mixed/fragile` rank with the continuous harmonic score as the leading selector/frontier axis.
- Relative to the bucketed harmonic pass, that continuous-score rerank changes the selected winner in only `31/156` cases and keeps both the continuous winner and the old bucketed harmonic winner at `survives` in all `156/156` cases.
- But it almost eliminates frontier disagreement. Under continuous harmonic scoring, the selected rule sits on all four frontier views in `78/78` cases for both `compact` and `extended`, while the old bucketed harmonic winner remains on the continuous proper-time/geometry frontiers in only `72/78` and `74/78` compact cases, and `65/78` and `67/78` extended cases.
- That makes the bucketization cheat more precise: the discrete rank is carrying much of the current frontier disagreement, even though it changes the selected winner much less than the earlier `box-min -> harmonic` observable change.
- A third diagnostic then removes the hand-named frontier vocabulary itself. It derives a covariance-based axis set from the harmonic-continuous candidate pool using `center_gap`, `arrival_span`, `min_margin`, `geometric_focus_gap`, and the harmonic focus score, then builds selector-free frontiers on `pc1`, `pc12`, and `pc123`.
- The leading derived axis loads positively on `center_gap`, `arrival_span`, and focus score (`0.469`, `0.456`, `0.568`) and negatively on `min_margin` and `geometric_focus_gap` (`-0.381`, `-0.323`), so the covariance story is not just reproducing one of the earlier named views verbatim.
- Under that derived basis, the current selected rule stays on the full `pc123` frontier in `153/156` cases, but only on `pc1` in `122/156` and on `pc12` in `129/156`. So the same core motif family still survives the full derived frontier, but much of the apparent low-dimensional agreement disappears once the view vocabulary is learned from the candidate cloud instead of named by hand.
- The top derived-axis motif in both families is still `S[2,3]/B[3,4]`, appearing on the `pc123` frontier in `55/78` compact and `52/78` extended cases. That is the strongest current selector-invariant result without the original frontier labels.
- A fourth diagnostic then varies the metric basis itself instead of only the frontier vocabulary. It compares the full five-metric covariance basis against smaller alternatives such as `focus3 = {center_gap, arrival_span, focus_score}` and `action3 = {arrival_span, min_margin, focus_score}` on the same harmonic-continuous candidate pool.
- The result is stronger than a simple “smaller basis gets worse” story. `focus3`, `no_margin`, and `action3` all keep the selected rule on the full `pc123` frontier in `156/156` cases, while the weakest tested basis, `no_focus`, still keeps it in `144/156`.
- Even more interestingly, every tested basis still has nonempty `pc123` overlap with the full five-metric basis in all `156/156` cases. So the full derived frontier is not isolated to one exact metric bundle.
- But the detailed motif ranking is still basis-dependent. In `compact`, the top `pc123` motif stays `S[2,3]/B[3,4]` for `4/5` tested bases; in `extended`, the same motif is top for `3/5`. Bases like `no_focus` and `focus3` can flip the leading motif family even when the broad `pc123` overlap remains intact.
- That makes the current state more precise: the full derived frontier looks structurally real in the toy model, but the fine-grained ordering of motifs is still sensitive to which raw metrics we feed into the covariance analysis.
- A fifth diagnostic then bootstraps the basis choice itself instead of fixing a small palette by hand. It now combines `16` subset bases, `12` seeded linear-random projection bases, and `9` mild nonlinear bases (`37` total basis views) on the same harmonic-continuous candidate pool.
- The nonlinear family is itself broadened beyond a few fixed transforms: it now includes three full-transform views (`signed_sqrt`, `signed_log1p`, `softsign`) plus small random projections of each transformed metric space. That makes the basis generator meaningfully wider than the earlier subset-plus-linear ensemble.
- The full-basis reference still keeps the selected rule on `pc123` in `153/156` cases, and `35/37` sampled bases keep full case-by-case `pc123` overlap with that reference. The weakest current basis is the nonlinear random view `softsign:rand01`, which drops to `145/156` overlap and `71/156` selected-on-`pc123`. So the full derived frontier is still robust, but not perfectly invariant once the generator is broadened.
- The bootstrap stability map is more informative than the smaller basis ablation because it separates ubiquity from dominance. In this run, `S[2,3]/B[3,4]` is the most ensemble-stable `pc123` motif in both families:
  - `compact`: present in `37/37` bases, `2270` basis-case frontiers, `25` top-basis wins
  - `extended`: present in `37/37` bases, `2124` basis-case frontiers, `26` top-basis wins
- With the broadened nonlinear ensemble, the same motif remains fully ubiquitous: in both families it appears in all `16/16` subset bases, all `12/12` linear-random bases, and all `9/9` nonlinear bases.
- But it is still not the only ubiquitous motif. In the current run, `17` motif-family pairs appear on `pc123` in every sampled basis, so the bootstrap result is not “one motif survives and everything else disappears.” The stronger claim is narrower: some motifs are basis-ubiquitous, and `S[2,3]/B[3,4]` currently dominates that ubiquitous set by aggregate coverage and top-basis wins.
- That is the strongest selector-invariant stability map in the repo so far. It no longer asks which motif wins under one chosen basis, or even one basis family, but which motifs keep reappearing on the full derived frontier across subset, linear-random, and nonlinear basis ensembles.
- A sixth diagnostic then stops fixing nonlinear transform intensity by hand too. It sweeps strengths `0.00, 0.25, 0.50, 0.75, 1.00` across the same three transform classes, again with both direct and projected variants, and asks when the transformed family first degrades beyond its own strength-zero baseline.
- On the current grid, none of the direct transformed bases break their identity-strength baselines at all. The first projected transform-induced break appears at strength `0.25` for both `signed_sqrt` and `softsign`, while `signed_log1p` does not break until `1.00`.
- The weakest transformed view is still `softsign:s1.00:rand01`, with `145/156` overlap and `71/156` selected-on-`pc123`. Even under that harsher sweep, the dominant motif `S[2,3]/B[3,4]` remains present in all `45/45` transform-family views in both `compact` and `extended`.
- That makes the nonlinear story more precise: the fragile part is not direct transformation by itself, but the combination of stronger nonlinear distortion with projection. The dominant motif survives that whole family anyway.
- A seventh diagnostic removes another remaining hand-choice inside that projection story. Instead of only keeping two seeded projected variants per mode/strength, it bootstraps `8` deterministic projections for every nonlinear mode/strength pair and asks which motifs still survive the broader projected family.
- This is a stronger test because it widens the projected subspace sampler without touching the underlying candidate pool. If the earlier nonlinear result had been mostly a tiny-palette artifact, it should break here first.
- In the current run, the dominant motif `S[2,3]/B[3,4]` remains the most projection-stable motif in both families across the full projected bootstrap, appearing in all `120/120` projected bases in both `compact` and `extended`.
- The twist is that the weakest projected cell is no longer a high-strength nonlinear corner. It already appears at `signed_log1p` strength `0.00`, which means projection choice itself is now the first real instability once that palette is broadened.
- That narrows the remaining cheat again: the model is no longer leaning on one specific two-projection palette, but the random projection generator itself is now visibly load-bearing.
- An eighth diagnostic tests that new weak spot directly by changing the projection generator, not the toy model. It compares the current raw random projections against row-normalized and orthonormalized variants on the same mode/strength grid.
- This is the right next step because the previous bootstrap showed that projection choice itself now breaks overlap earlier than transform intensity does. If normalization fixes most of that, then the main instability is in the projection generator rather than in the motif structure.
- In the current run, no projection generator cleanly wins on every metric. `row_norm` gives the best averages, `raw` keeps the best worst-case minima, and `orthonormal` improves top-basis dominance while actually worsening the weakest cells.
- That means the projection-induced weakness is not just a trivial scaling artifact. The generator matters, but changing it does not by itself remove the instability.
- A ninth diagnostic then pushes one layer deeper by changing the projection dimension itself. It compares orthonormal projected bases of dimensions `2, 3, 4, 5` on the same transformed metric family.
- That is a direct test of whether the remaining instability is mostly generator noise or metric-space compression. If the higher-dimensional projections restore overlap, then at least part of the weakness is coming from forcing the derived basis through too small a subspace.
- The current result is sharp. Dimension `2` is clearly the weakest (`107/156` minimum overlap, `33/156` minimum selected-on-`pc123`), dimension `3` is much healthier, and dimensions `4` and `5` nearly restore the full reference. Dimension `4` is the strongest by the current worst-case criterion, with `156/156` minimum overlap and `130/156` minimum selected-on-`pc123`.
- That makes the remaining projection story much clearer: a real part of the instability is compression-driven. Once the projected basis is allowed enough room, the same dominant motif `S[2,3]/B[3,4]` remains fully ubiquitous and the overlap almost completely recovers.
- A tenth diagnostic then reruns the selector-free stability map directly on that stronger `4D orthonormal` family instead of treating `3D` as the default projection bottleneck. This is the cleanest version of the question “what survives once the compression cheat is relaxed?”
- In the current run, all `60/60` bases in the `4D orthonormal` family keep nonempty `pc123` overlap with the full reference in all `156` cases. Selected-on-`pc123` is still not perfectly invariant, but it ranges much higher, from `130/156` to `156/156`.
- The dominant motif `S[2,3]/B[3,4]` remains the leading selector-free motif in both families across that whole `4D` ensemble, while `18` motif-family pairs are now fully ubiquitous across all `60` bases.
- That is a more meaningful selector-free result than the earlier `3D` story. Once the projection bottleneck is widened, the remaining disagreement looks more like motif ranking within a robust family than like wholesale loss of overlap.
- An eleventh diagnostic then switches from basis coverage to case-core intersection on that same `4D orthonormal` family. For each case, it intersects the `pc123` motifs across all `60` bases and asks which motifs are actually unavoidable rather than just frequently present.
- That is the stricter structural question. A motif can be ubiquitous across bases overall and still fail to be inevitable in any specific case; the case-core analysis separates those two ideas.
- In the current run, the case core is nonempty in `84/156` cases, and the currently selected rule lies inside that core in `81/156`. But exact basis-indifference is still absent: `0/156` cases have `core == union`, and the largest case core has size `4`.
- The strongest unavoidable motifs are now narrower than the broad basis-wide winners. In `compact`, `S[2,3]/B[3,4]` has `21` core hits out of `78` union hits. In `extended`, the strongest unavoidable motif is actually `S[3,4]/B[1,3]`, with `9` core hits out of `78` union hits.
- That is a useful sharpening. The selector-free family is robust, but truly unavoidable motifs are rarer than basis ubiquity alone suggests, and the `extended` case-core story is not identical to the family-wide coverage story.
- A twelfth diagnostic then turns those case-core counts into a mechanism map using the existing column-profile geometry metrics. It groups the `4D orthonormal` cases into `empty`, `single-selected`, `single-other`, `multi-selected`, and `multi-other` core regimes and compares their contour roughness and topology features.
- That is the right next question because the raw case-core counts alone do not say why some cases have an unavoidable motif and others do not. The mechanism map asks whether empty cores, single-motif cores, and multi-motif cores occupy distinct geometric regimes in the current graph family.
- In the current run, the split is surprisingly crisp. `72` cases have empty cores, and they are skew-heavy (`37/72`) with the same `37/72` midline-crossing count, plus higher average center-variation (`1.03`) and span-range (`1.47`) than the clean single-core regime.
- `74` cases land in the `single-selected` regime, and those are mostly rect+taper shapes (`52/74`) with lower average center-variation (`0.59`). The `multi-selected` regime is small (`7` cases) and mostly taper-shaped (`6/7`).
- That makes the current mechanism story much more concrete: empty cores are not random failures. They are concentrated in rougher, skewed, crossing profiles, while single-motif cores live in smoother rect/taper geometries.
- A thirteenth diagnostic then checks that mechanism continuously rather than only across named scenarios. It builds a small procedural family from the old contour-sensitivity path, holds the span profile fixed, and varies only the centerline roughness.
- That is the strongest follow-up to the mechanism map, because it asks whether the empty/single/multi core split really follows roughness as a continuous control parameter instead of just piggybacking on the benchmark scenario labels.
- The resulting response is not monotone in the interpolation parameter itself, because the realized roughness metrics dip in the middle and rise again. But it is strongly tied to the actual centerline roughness at fixed span profile.
- In `compact`, the low-roughness mid-sweep states (center-variation `2.0`) are the clean ones: all `6/6` weights land selected-in-core there, while the rougher states (center-variation `5.0`, `6.0`, and `8.0`) collapse back to all-empty cores.
- In `extended`, the same family shows a richer pattern: low roughness gives `6/6` multi-selected cores, mid-high roughness (`5.0`) collapses to `6/6` empty cores, and the roughest endpoint (`8.0`) partially re-enters the single-selected regime (`5/6`).
- That is a stronger mechanism result than the named-scenario map alone. The case-core split clearly tracks realized centerline roughness at fixed span profile, even though the response is family-dependent and can be re-entrant.
- A fourteenth diagnostic then removes the last big path-choice cheat in that roughness story. Instead of following one interpolation path, it probes a small basis of independent centerline modes at fixed span profile: `tilt`, `bowl`, `step`, and `zigzag`.
- That is the right next test because a single interpolation path can still hide a shape-specific artifact. The mode basis asks whether the roughness/core link survives across qualitatively different deformations or only along one chosen family.
- The result is more interesting than a simple “more roughness means more failure” rule. The broad roughness link survives, but the mode symmetry matters a lot.
- In `compact`, the smoother `tilt` mode stays mostly single-selected through amplitude `1.0` (`5/6` selected-in-core), and `step` even recovers to `6/6` selected-in-core at amplitude `1.5`. By contrast, `zigzag` is already fully destructive at amplitude `0.5`, with `6/6` empty cores.
- In `extended`, `bowl` at amplitude `1.5` gives `6/6` multi-selected cores, while `zigzag` collapses to `6/6` empty cores at amplitude `0.5` and then re-enters as `6/6` selected-in-core at amplitude `1.5`.
- So the roughness/core link is broader than one interpolation path, but it is not reducible to roughness magnitude alone. The symmetry class of the deformation is now clearly part of the mechanism.
- A fifteenth diagnostic then distills that mode result into a small invariant test. It groups the same fixed-span mode sweep by a simple shape signature: `smooth-monotone`, `step-like`, `curved`, `oscillatory`, plus whether the centerline crosses the midline.
- This is the direct answer to whether those invariants explain the behavior better than raw roughness alone. In the current run, they do.
- At the same roughness in `compact` (`center-variation = 2.0`), the outcomes split sharply by invariant class:
  - `curved/no-cross`: `1/6` selected-in-core
  - `smooth-monotone/cross`: `5/6`
  - `step-like/cross`: `3/6`
- At the same roughness in `extended` (`center-variation = 6.0`), the split is even sharper:
  - `curved/cross`: `6/6` selected-in-core
  - `oscillatory/no-cross`: `0/12`
- Across the current mode sweep there are `9` roughness groups where identical center-variation still splits into different case-core outcomes by invariant class.
- That is the strongest current mechanism statement in the repo: roughness magnitude matters, but monotone vs oscillatory and crossing vs non-crossing explain the case-core behavior better than roughness alone, without yet collapsing it to one single invariant rule.
- A sixteenth diagnostic then turns that into a predictive test rather than another descriptive split. It trains tiny decision trees on the raw sweep features and evaluates them with leave-one-mode-out generalization.
- That is the right next step because it asks whether the learned invariants actually predict the empty/single/multi regime on held-out modes better than roughness alone, instead of only sorting already-observed cases after the fact.
- In `compact`, roughness-only is still the best of the original named trees at `0.46` leave-one-mode-out accuracy, ahead of `mixed` at `0.44` and `invariant-only` at `0.43`.
- In `extended`, roughness-only and `mixed` tie at `0.59`, both ahead of `invariant-only` at `0.51`.
- So the present invariant bundle is explanatory but not yet the best predictive compression.
- A seventeenth diagnostic then removes the last hand-picked-feature cheat in that predictive test. Instead of privileging the current invariant bundle, it exhaustively searches all raw 1-, 2-, and 3-feature subsets from the same mode sweep.
- That search changes the story in a useful way. In `compact`, the best subset is `turning_points + crosses_midline` at `0.56`, which beats roughness-only. In `extended`, several simple summaries tie at `0.59`, including `center_range`, `center_variation`, and `max_step_fraction`.
- So roughness is not uniquely load-bearing in the predictive sweep. The cleaner current claim is that the original invariant bundle was incomplete: smaller raw subsets can beat or match roughness once the feature search is no longer hand-picked.
- An eighteenth diagnostic then asks whether those new best subsets are actually stable across held-out modes or only look good in the aggregate sweep.
- In `compact`, there is no repeat fold winner at all: each held-out mode picks a different 2-feature subset.
- In `extended`, the fold winners narrow to a 2-way split: `center_range` wins `2/4` held-out modes and `center_variation` wins the other `2/4`.
- So the predictive lift now looks real, but it still comes from a small family of simple geometry summaries rather than one locked minimal law.
- A nineteenth diagnostic then asks the harder transfer question again on the corrected same-weight path: train on the same retained-weight mode sweep, then test unchanged on the roughness sweep and the independently generated procedural family at `w = 1.0`.
- That post-fix story is different from the earlier one. In `compact`, a roughness-centered tree on `center_variation` is now the clear transfer winner at `0.87 / 0.73` mean/worst accuracy, with a mixed tree tied on transfer but not on mode CV. In `extended`, there is no comparably strong winner: `center_variation` reaches only `0.47 / 0.33`, while `turning_points` can improve roughness to `0.67` only by collapsing procedural transfer to `0.23`.
- So the current out-of-family claim is narrower and more honest: `compact` still has a strong roughness-centered transfer signal, but `extended` only supports a small tradeoff family rather than one stable winner.
- A twentieth diagnostic then turns that into a selector-free predictor map by scoring every raw `1..3` feature subset by both mode-sweep CV and out-of-family transfer, then keeping only the nondominated subsets.
- In `compact`, both the balanced and transfer fronts still contain a `center_variation` family, while higher-CV subsets such as `max_step_fraction` stay on the balanced frontier only.
- In `extended`, the balanced frontier and transfer frontier now split more clearly: `center_range + turning_points` and close relatives anchor the balanced side, while `center_variation`-based subsets anchor the transfer side.
- So the current predictor-level claim is no longer “roughness wins everywhere.” It is: `compact` keeps a roughness-centered stable core, while `extended` lives on a tradeoff between balanced-fit and transfer-oriented summaries.
- A twenty-first diagnostic then removes another modeling choice: fixed tree depth. On the corrected same-weight path, the depth story is still unstable, but in a different pattern than before.
- In `compact`, the compressed frontier shifts from `4` behaviors at depth `1` to `3` at depth `2` and `3` at depth `3`, with roughness staying on both frontiers at all depths. The top transfer subset changes from `max_step_fraction` at depth `1` to `center_variation` at depths `2` and `3`.
- In `extended`, the split is sharper: depth `1` favors a balanced `turning_points` story with no roughness transfer row, while depths `2` and `3` move to roughness-on-transfer and top transfer subsets `center_range + turning_points` and `center_variation + center_range`.
- So the corrected depth claim is: the predictor fronts are still strongly depth-sensitive, especially in `extended`, but the stable part is now “roughness remains transfer-relevant at moderate depth,” not the older count-based story.
- A twenty-second diagnostic then changes predictor family instead of tree depth. It keeps the structurally relevant feature subsets fixed and compares depth-2 trees to tiny ordinal score models on the corrected benchmark path.
- In `compact`, the tree side now remains strongest overall: `center_variation` as a depth-2 tree still reaches `0.87 / 0.73`, while the best ordinal rows cluster at `0.83 / 0.65`.
- In `extended`, the best mean/worst transfer among the tested families comes from a depth-2 tree on `center_variation + center_range + turning_points`, at `0.52 / 0.50`, while the top ordinal rows sit at `0.49 / 0.33`.
- So the corrected predictor-family story is more specific: trees still carry the best overall transfer on this benchmark, and the ordinal family is no longer the main driver of the transfer claim.
- A twenty-third diagnostic then loosens the ordinal model itself by varying the ordinal internals across `minmax/equal`, `zscore/equal`, and `minmax/spread`.
- In `compact`, several subsets and all three ordinal variants now tie at the same `0.83 / 0.65` transfer level.
- In `extended`, the best ordinal rows also bunch tightly, with `center_variation`-centered subsets topping out at `0.49 / 0.33`.
- So the current ordinal claim is weaker and cleaner: the internals matter less than before, and the ordinal family looks like a small plateau rather than one privileged refinement path.
- A twenty-fourth diagnostic then pushes on the graph-side cheat again with the multi-style generated-geometry ensemble.
- On the corrected benchmark path, `compact` now prefers a simple roughness-centered tree again: `center_variation` as a depth-2 tree reaches `0.58 / 0.56`, while the best ordinal alternatives sit at `0.56 / 0.51`.
- In `extended`, the top generated-geometry row is now an ordinal `center_variation` score at `0.63 / 0.62`, with nearby `center_variation`-centered tree and ordinal rows close behind.
- So the current graph-side read is narrower than the earlier “many-centerline-family winners” story. The generated ensemble still does not produce one universal summary, but after the fix it is much more centered on `center_variation` than on `crosses_midline` or span-only alternatives.
- A twenty-fifth diagnostic then widens the predictor vocabulary itself on that same generated ensemble by adding local-shape features such as `boundary_fraction`, `pocket_fraction`, `boundary_roughness`, and `deep_pocket_fraction`.
- In `compact`, the strongest rows now use `boundary_roughness` together with `center_variation`, reaching `0.63 / 0.62`.
- In `extended`, the strongest rows use `max_step_fraction` together with `deep_pocket_fraction`, reaching `0.68 / 0.67`, while `max_step_fraction` alone remains close at `0.67 / 0.64`.
- So the corrected feature-expansion claim is asymmetric in a different way than before: `compact` still looks mostly centerline/roughness-driven, while `extended` now shows a real gain from local-shape features, especially deep-pocket structure.
- A twenty-seventh diagnostic then derives a tiny local-shape basis automatically from occupied-node neighborhood degrees, instead of hand-picking pocket-like summaries in advance.
- That learned basis now changes the earlier pocket story substantially. In `compact`, a degree basis feature (`degree_5_fraction`) already ties `pocket_fraction` at `0.58`, and the best `pocket + basis` combination nudges up to `0.59`.
- In `extended`, the best small learned basis (`degree_1_fraction`) already beats `pocket_fraction` strongly, at `0.60 / 0.59` versus `0.25 / 0.20`.
- So the current claim is no longer that `pocket_fraction` is uniquely special. On the corrected generated-geometry benchmark, even the tiny learned degree basis can match or beat it, especially in `extended`.
- A twenty-eighth diagnostic then asks the sharper residual question on the stronger generated-geometry ensemble: as the learned neighborhood basis grows, does `pocket_fraction` still add anything once the automatic basis is allowed to catch up?
- In the residual/bridge diagnostics below, `Nw/Mf` means the first scanned parity window (`N`) together with the actual winning feature count (`M`).
- In `compact`, the degree-only basis still trails the pocket row through the `3..6` scan windows, then first reaches parity in the size-`7` window.
- In `extended`, the learned basis already exceeds the pocket row in the size-`3` window and keeps a large margin through size `6`; only the larger `pocket + basis` combinations catch back up near size `7`.
- So the corrected local-shape claim is again asymmetric, but differently so: `pocket_fraction` still helps compact small bases, while extended is already dominated by the learned basis very early.
- A twenty-ninth diagnostic then widens the automatic basis itself to richer local motifs such as pocket-adjacent wall counts, high-degree-neighbor fractions, and simple 2-hop summaries, still without being handed `pocket_fraction` directly.
- On the corrected generated ensemble, the full rich basis now reaches parity in the size-`3` window in both families, but the winning feature split is asymmetric: `compact` still lands on `motif_high_degree_neighbor_fraction`, while `extended` can already land on pocket-adjacent alternatives.
- So the current rich-basis claim is not “one mechanism for both families.” It is: the richer automatic vocabulary collapses the residual quickly, but `compact` and `extended` can reach that collapse through different motif paths.
- A thirtieth diagnostic then ablates those richer motifs through the corrected residual benchmark.
- Removing `motif_high_degree_neighbor_fraction` is still the real compact-side hit: `compact` retreats to the size-`7` parity window, and simple share/count/max replacements do not restore the original fast collapse.
- `Extended` behaves differently on the corrected path. It often keeps the size-`3` parity window even when the high-degree feature is removed, typically by switching to `motif_deep_pocket_adjacent_fraction`.
- So the mechanism story is now split by family: the compact side still looks genuinely tied to the high-degree-neighbor motif, while the extended side can route through pocket-adjacent proxies on the same corrected benchmark.
- A thirty-first diagnostic then sweeps the high-degree threshold itself with `ge_5`, `ge_6`, `ge_7`, and `ge_8`.
- In `compact`, the immediate threshold story survives the fix: `ge_5`, `ge_6`, and `ge_7` all recover the size-`3` parity window, while `ge_8` fails.
- In `extended`, `ge_7` reproduces the high-degree winner on both the default corrected benchmark and the broader multi-style rerun, but `ge_5` and `ge_6` often fall back to deep-pocket proxies instead.
- So the compact mechanism still points to an immediate near-hub threshold around `6..7`, but the extended side is no longer well described by one thresholded feature alone.
- A thirty-second diagnostic then removes the hard-threshold cheat in the opposite direction by replacing the high-degree motif with smooth hub-exposure scores.
- Those soft scores still fail cleanly on the compact side and never beat the thresholded families on the broader rerun.
- So the compact-side mechanism still looks more threshold-like than a smooth hub-exposure proxy.
- A thirty-third diagnostic then tests more graph-native hub notions such as second-hop reach and mixed degree+reach leverage.
- On the corrected default benchmark, `reach-14` can recover the compact parity window, but that does not survive the broader multi-style hub comparison: on the broader rerun, `reach-14` and `reach-24` both fall back to pocket-based compact winners in the size-`8` and size-`7` windows.
- The broader rerun also rejects the leverage family cleanly: none of the leverage variants recover the compact result there, and the bundle does not restore it either.
- So the strongest current mechanism claim is more specific and more honest than the old one: on the broader corrected ensemble, compact still prefers an immediate thresholded near-hub signal (`full-rich`, `ge_6`, `ge_7`, and `share6`/bundle survive), while smoother or more global hub notions do not. Extended remains more permissive and often shifts to deep-pocket or pocket-adjacent proxy motifs instead of insisting on the same high-degree coordinate.
- The corrected mechanism split is now summarized as explicit route maps instead of narrative-only claims.

Compact route map:

| Label | Role | Scope | Canonical expression | Evidence |
| --- | --- | --- | --- | --- |
| `6+ threshold core` | `primary` | `shared` | `motif_high_degree_neighbor_ge_6_fraction ~= motif_high_degree_neighbor_share6_fraction` | `threshold_core_overlap_analysis`, `compact_sparse_bridge_benchmark` |
| `7+ sufficient subroute` | `atomic-standalone` | `shared` | `motif_high_degree_neighbor_ge_7_fraction` | `threshold_core_overlap_analysis`, `compact_sparse_bridge_benchmark` |
| `sparse near-miss residue` | `sparse-residue` | `family-specific` | default `fallback-base` closest row, typically degree-profile-like | `sparse_fallback_residual_trace_benchmark` |

Extended route map:

| Label | Role | Scope | Canonical expression | Evidence |
| --- | --- | --- | --- | --- |
| `hub` | `primary` | `shared` | `motif_high_degree_neighbor_fraction` | `extended_proxy_route_benchmark(full-rich)` |
| `low-degree backup envelope` | `atomic-standalone` | `family-specific` | `motif_low_degree_neighbor_fraction` | `extended_proxy_route_benchmark`, `extended_atomic_route_overlap_benchmark` |
| `pocket nested subroute` | `atomic-standalone` | `family-specific` | `motif_pocket_adjacent_fraction` | `extended_proxy_route_benchmark`, `extended_atomic_route_overlap_benchmark` |
| `deep-pocket nested subroute` | `atomic-standalone` | `family-specific` | `motif_deep_pocket_adjacent_fraction` | `extended_proxy_route_benchmark`, `extended_atomic_route_overlap_benchmark` |
| `low-degree/pocket coexistence` | `coexistence-only` | `family-specific` | mixed pocket-plus-low-degree winner | `extended_proxy_route_benchmark` |
| `sparse-structure fallback` | `family-composite` | `family-specific` | `degree_8_fraction + motif_mean_neighbor_degree + motif_two_hop_open_fraction` on the base fallback route | `degree_profile_fallback_benchmark` |

- The finished overlap gate tightens that `extended` map one notch further: the atomic backups are nested rather than independent peers. In the corrected default and broader ensembles, `deep-pocket` sits entirely inside `pocket`, and `pocket` sits almost entirely inside `low-degree`, so the honest picture is a nested backup chain plus a coexistence-only layer and a final sparse-structure residue.
- The family-level claim after the benchmark-path fixes is therefore narrower and stronger: `compact` still carries the cleaner near-hub threshold story, while `extended` keeps a more permissive hierarchy of nested atomic backups, coexistence-only mixed rows, and a final sparse-structure residue.
- A one-notch wider generated-family rerun now pushes on that corrected split again, using `9/5` geometry/procedural variants with the same three procedural styles. At the support level, the compact threshold core survives unchanged: `ge_6` and `share6 > 0` still match exactly wherever active, and `ge_7` is still a strict subset route.
- The widened parity-level result is more selective in exactly the interesting way. `full-rich` and `ge_7` remain `shared-same`, but `ge_6` drops to `shared-proxy` (`compact` still lands on `highge6`, while `extended` already prefers `deep-pocket`), and `share6` plus the full exposure bundle become `extended-only`.
- So the widened-family evidence now says something sharper than the older broader rerun alone: the compact near-hub threshold story survives one notch wider at the support level, but the parity-level fast-anchor story is stricter. `ge_7` remains a clean shared fast anchor, while `ge_6/share6` no longer stay interchangeable once the wider generated family is asked to pick actual size-`3` winners.
- The widened `extended` route story, by contrast, stays very stable. On that `9/5` rerun, removing `high-degree` still first routes to `deep-pocket`, removing the whole pocket family still falls back to `low-degree`, and removing `low-degree` after that still lands on the same sparse-structure residue. The atomic overlap structure also survives unchanged: `deep-pocket` stays nested inside `pocket`, and `pocket` stays nested inside `low-degree`.
- A continuing generated-family ladder then pushed the same check out through `11/6`, `13/7`, `15/8`, `17/9`, `19/10`, and `21/11` (`ultra`, `mega`, `giga`, `tera`, `peta`, `exa`). The support-level compact result stayed unchanged at every rung, and the parity-level split stayed strikingly stable too: `full-rich` and `ge_7` remained `shared-same`, `ge_6` remained `shared-proxy`, and `share6` plus the exposure bundle remained `extended-only`.
- So the stronger current claim is now: the clean shared fast anchor is not the broad `6+` family, but the stricter `7+` subroute. The `ge_6/share6` support story still survives, but once the generated family is widened far enough to force actual size-`3` winners, `extended` keeps preferring the deep-pocket route there while `compact` stays on the near-hub threshold, all the way through the current `exa` rung.
- A new node-level shell analysis on that full canonical ladder then explains why the split survives. The `ge_6`-but-not-`ge_7` shell is tiny, only about `2%` of nodes, but it is also consistently rougher than the `ge_7` core: boundary deficit stays around `0.59` versus `0.19`, and mean neighbor degree around `0.61..0.62` versus `0.84`.
- That tiny shell is also where the `extended` fallback motifs concentrate. Across `default` through `exa`, the `ge_6`-only shell carries about `1.8..2.3x` as much `deep-pocket` adjacency as the `ge_7` core, about `2.4..2.7x` as much pocket adjacency, and about `2.3..2.7x` as much low-degree exposure.
- The right read is enrichment, not identity. The shell does **not** contain most deep-pocket nodes; it only carries about `4..5%` of all deep-pocket-adjacent nodes because the `ge_7` core is so much larger. But it is a stable rough boundary band where exactly the local signals that feed the `extended` deep-pocket/low-degree backups are overrepresented. So the `ge_6 -> deep-pocket` defection is now best understood as a shell-enrichment effect, not as a disjoint support split.
- A follow-up case-level probe on the corrected `default` and `broader` ensembles then makes that mechanism more specific. The generated graphs where the single-feature `deep-pocket` model is right and the single-feature `ge_6` model is wrong do **not** show the biggest deep-pocket shell spike. Instead, they show a larger shell (`4..5%` of nodes instead of the background `2%`) and much stronger shell low-degree and pocket enrichment: shell-minus-core low-degree exposure jumps to about `+0.41..+0.55`, and shell-minus-core pocket exposure to about `+0.10..+0.21`.
- So the best current case-level read is: the `ge_6 -> deep-pocket` defection is being driven less by a pure deep-pocket shell and more by a rough low-degree / pocket shell where the `deep-pocket` proxy wins on the graph-level regime prediction. In other words, the broad shell-enrichment story is real, but the case-trigger looks more like low-degree/pocket pressure than a simple monotone deep-pocket spike.
- A direct offender map then shows that this is not one single repeated shape. The dominant `deep-pocket-only` offender family is `base:taper-wrap`, whose shell has a strong low-degree gap (`+0.53`) and a moderate pocket gap (`+0.17`) but actually a negative deep-pocket gap (`-0.13`). A smaller secondary family is `base:skew-wrap`, especially under procedural `local-morph` / `mode-mix`, where the shell is rougher and more mixed. So the defection mechanism is now best read as a small offender family headed by `taper-wrap` low-degree shells, not one universal deep-pocket motif.
- A controlled fixed-span mode sweep around `base:taper-wrap` then shows that simple centerline deformations are not enough to recreate that offender. Across `bowl`, `step`, `tilt`, and `zigzag` mode families, there are `0` `deep-pocket-only` cases even though the shell low-degree gap can still become large (`+0.72..+0.95`) and the pocket gap can reach `+0.40`. So a strong low-degree shell by itself is not sufficient to trigger the `ge_6 -> deep-pocket` defection.
- A direct interpolation from `base:taper-wrap` to the actual recurring geometry offenders makes that sharper. Along both `geometry-c` and `geometry-e`, the defection stays off through the interior of the path and only appears at the true offender endpoint (`alpha = 1.00`). `geometry-c` passes through two `ge_6-only` states before flipping, while `geometry-e` passes through two `neither` states, but neither target produces any `deep-pocket-only` interior point. The shell signal at the endpoint is also almost purely low-degree (`+0.84..+0.89`) with effectively zero deep-pocket and pocket gap. So the `taper-wrap` offender looks like a sharper mixed endpoint deformation, not a smooth monotone turn-on of a generic cavity shell.
- An endpoint-jump audit then shows exactly what that final flip is. In both offender targets, the last non-defect state is the `alpha = 0.75` interpolation row, and the `0.75 -> 1.00` jump changes only `4..5` columns and only `4..5` added/removed nodes. Every changed column is a pure `+1` vertical translation with zero span change, concentrated in the upper central band (`x = 1..5` for `geometry-c`, `x = 1,2,3,6` for `geometry-e`). That jump simultaneously flips `crosses_midline` from `False` to `True`, pushes the shell boundary-deficit gap from about `+0.15/-0.22` to `+0.40/+0.27`, and pushes the shell low-degree gap from `-0.15` to about `+0.84..+0.89`, while pocket and deep-pocket shell gaps stay exactly `0.00`. So the `taper-wrap` defection is now best read as a tiny asymmetric lattice jump that creates a low-degree boundary shell, not as the appearance of a new pocket/deep-pocket structure at the endpoint.
- A transplant test then shows that this tiny band jump is locally sufficient much earlier on the same interpolation path. Applying the full endpoint band to the earlier `geometry-c` path already produces `deep-pocket-only` by `alpha = 0.25`, and applying the `geometry-e` band produces `deep-pocket-only` even from the base `alpha = 0.00` state. So the endpoint jump is not just a descriptive difference between the last two rows; it is an active trigger inside the offender path.
- But the same transplant is not a universal hammer. Leave-one-out ablations on the endpoint band often kill the defection (`geometry-c` fails when `x = 1,3,5` are omitted; `geometry-e` fails when `x = 1,2,3,6` are omitted), so the band is not fully redundant. And when the same jump bands are applied across the broader `taper-wrap` mode family, the result is only partly portable: the `geometry-c` jump produces `deep-pocket-only` only on `bowl` states (`3/9` amplitudes), while the `geometry-e` jump produces only `1/9` `deep-pocket-only` case in each mode family. So the mechanism is now: a tiny upper-band lattice jump is locally sufficient in the right `taper-wrap` context, but it still depends on the surrounding shape class rather than acting as a context-free defect switch.
- A final context map makes that “right context” much more explicit. The jump-sensitive states are consistently the smooth, non-crossing ones with the upper band already sitting high and centered. For the `geometry-e` jump, the only `deep-pocket-only` states are the completely flat mode baselines (`center variation = 0`, mirror asymmetry `= 0`, no midline crossing) across all four mode families. For the `geometry-c` jump, the only `deep-pocket-only` states are low-variation `bowl` profiles (`center variation = 2.0`, mirror asymmetry about `0.71`, again no midline crossing). By contrast, the jump-resistant `neither` states are rougher (`center variation ~ 6.9..7.1`), more asymmetric (`~1.10..1.13`), and usually already crossing the midline (`13/17` and `13/19` cases). So the missing context variable is now pretty concrete: the same upper-band jump only acts as a defect trigger when it lands on a relatively smooth, symmetric, non-crossing pre-jump profile.
- A tiny rule search makes that context split even cleaner. For the `geometry-e` jump, several single-feature rules isolate the sensitive states perfectly: `signature = flat`, `center variation = 0`, or equivalently `boundary_roughness <= 0.24` each pick out exactly the four `deep-pocket-only` cases and nothing else. For the `geometry-c` jump, the exact separator is slightly richer: `boundary_roughness <= 0.26` together with a modest asymmetry floor (`mirror_center_asymmetry >= 0.37`) isolates all three `deep-pocket-only` cases with zero false positives. So the endpoint trigger is no longer just qualitative. The current best compressed rule is: low roughness is necessary in both paths, but `geometry-c` also needs a little surviving asymmetry, while `geometry-e` flips only from the perfectly flat baseline.
- A direct generalization check against the secondary `skew-wrap` offender family then shows this is not one universal wrapped-defect rule. The `skew-wrap` `deep-pocket-only` cases satisfy **neither** taper rule: they are rougher (`boundary_roughness ~ 0.28..0.30`), more asymmetric, and already crossing. In fact they collapse to a different tiny rule entirely: on both the corrected default and broader ensembles, `boundary_roughness >= 0.28` alone isolates all `skew-wrap` `deep-pocket-only` cases with zero false positives. So the current honest split is: `taper-wrap` is a low-roughness jump-trigger class, while `skew-wrap` is a roughness-floor class.
- A coverage pass over the full currently observed `deep-pocket-only` offender set then shows that these trigger classes already explain most of what we are seeing, but not in a perfectly scenario-pure way. On both corrected default and broader ensembles, all `skew-wrap` defectors fall into the `roughness-floor` class. `Taper-wrap` still contributes the `jump-asymmetric` geometry defectors we isolated earlier, but it also has a smaller `roughness-floor` exception under `local-morph`. So the best current map is: the observed `extended` defections are mostly covered by two classes, `jump-asymmetric` and `roughness-floor`, with `skew-wrap` living entirely in the latter and `taper-wrap` straddling both.
- A direct gap-signature pass then resolves that remaining `taper-wrap` exception into its own small subtype. The repeated `base:taper-wrap:local-morph-a` defector is **not** just a weak `skew-wrap`: it has a strong positive pocket gap (`+0.52`) but negative deep and low-degree gaps (`-0.38/-0.14`). That is qualitatively different from the `jump-asymmetric` taper geometry defectors (`0.00/0.00/+0.84..+0.89`) and also different from the `skew-wrap` roughness-floor cases, which split between a mild all-positive shell (`+0.12/+0.16/+0.30`) and a rough oscillatory negative-shell case (`-0.35/-0.48/-0.33`). So the current best trigger map is now three-part rather than two-part: `jump-asymmetric`, `roughness-floor`, and a small `pocket-wrap` exception carried by `taper-wrap:local-morph-a`.
- A direct wrapped-scenario subtype scan then checks whether that `pocket-wrap` line really broadens once we stop looking only at the originally named offenders. It does not. Across every wrapped scenario in the corrected `default` and `broader` generated ensembles, the only cases with the current pocket signature (`pocket > 0`, `deep <= 0`, `low <= 0`) are still the repeated `base:taper-wrap:local-morph-a` rows. `Base:skew-wrap` contributes no such cases at all, and even within `base:taper-wrap` the two geometry defectors stay cleanly `jump-asymmetric` rather than pocket-driven. So the honest current read is narrower than “broader pocket subtype”: `pocket-wrap` is still a real repeated trigger, but only as a tiny `local-morph` subcase inside `taper-wrap`.
- A direct `local-morph` ladder then checks whether that narrowness is just an artifact of shallow procedural sampling. It is not. Widening `base:taper-wrap` and `base:skew-wrap` out to `16` deterministic `local-morph` variants still leaves exactly one `pocket-wrap` defector: `base:taper-wrap:local-morph-a`. Additional deeper `taper-wrap` `local-morph` variants do produce more pocket-positive rows, but they stay `ge_6-only` or `neither` rather than flipping to `deep-pocket-only`; `skew-wrap` never produces a pocket-signature defector at all. So the current best read is even tighter: the `pocket-wrap` path is not just rare across wrapped scenarios, it is also rare inside the `local-morph` generator family itself, and pocket-positive shell structure alone is not sufficient to recreate it.
- A final direct comparison against those pocket-positive `taper-wrap` `local-morph` near-misses then shows what the missing ingredient is. The repeated `local-morph-a` defector is not the row with the most positive pocket gap; it is the row with the **most negative deep gap**. Across the full `taper-wrap` `local-morph` ladder, the one-feature rule `deep_gap <= -0.38` isolates all `4/4` copies of the `pocket-wrap` defector with `0` false positives. So the best current mechanism read is: this path is not “positive pocket shell implies deep-pocket defection.” It is a much narrower anti-deep pocket route: strong pocket signal paired with unusually negative deep-gap structure.
- A nearest-neighbor comparison inside the same `taper-wrap` `local-morph` ladder makes that stricter. The closest crossing pocket-signature near-miss is still **not** a defector: it has an even larger pocket gap (`+0.55`), still crosses the midline, and differs from the true defector by only `10` nodes across `3` columns, yet it stays `neither` because its deep gap is much less negative (`-0.17` instead of `-0.38`). The closest same-roughness same-asymmetry near-miss also keeps the pocket signature but stays `ge_6-only` when it does not cross. So crossing does matter, but it is not sufficient; the load-bearing separator inside `local-morph` is still the unusually negative deep-gap structure.
- An exact column-by-column comparison then shows that this is not a one-bit crossing toggle either. Relative to the closest crossing pocket-signature near-miss, the true `pocket-wrap` defector differs only on columns `x = 2,3,4`, and each change mixes a small center shift (`±0.5..1.5`) with a one-step span change. Relative to the closest same-roughness same-asymmetry non-crossing near-miss, the defector differs on columns `x = 1,4,5`, again by small mixed center/span moves rather than one isolated wrap jump. So the current best local read is: the `pocket-wrap` path is a tiny mixed three-column rearrangement that drives the deep gap strongly negative; it is not reducible to “crossing on” or to a single-column pocket pinch.
- A direct transplant test then shows that even that exact three-column rearrangement is **not** locally sufficient on its nearest near-misses. Pasting the full `pocket-wrap` column delta onto either the closest crossing pocket-signature near-miss or the closest same-roughness same-asymmetry non-crossing near-miss keeps the result at `neither`, not `deep-pocket-only`. In both cases the transplanted graph still has a strong pocket signature (`+0.91/-0.14`) and, in one branch, still crosses the midline, but the deep gap collapses back to `0.00` instead of staying strongly negative. So the final current read is: the `pocket-wrap` route is not just a tiny local delta; it is a context-dependent anti-deep pocket trigger whose decisive condition is lost when the same three-column move is transplanted onto nearby rows.
- A suppressor-node probe then closes the loop on that context dependence. The minimal prune result already showed that removing the two extra non-deep nodes `(1,0)` and `(4,0)` from the failed full transplants restores the exact `pocket-wrap` defector. Looking at the local motif changes makes that sharper: removing `(1,0)` alone recreates one left-hand eight-node deep-pocket support cluster but still leaves the graph at `neither`; removing `(4,0)` alone recreates the mirrored right-hand eight-node cluster and also stays `neither`. Only removing **both** restores the full `16` deep-core support set and flips the graph back to `deep-pocket-only`. So those two added nodes are not generic noise. They are paired suppressors, each killing one half of the deep-pocket support structure that the `pocket-wrap` route needs.
- A final cell-level audit makes that even more literal. In the true `pocket-wrap` defector, the deep-pocket candidate cells are exactly `(1,0)` and `(4,0)`. In the failed full transplants, those deep-pocket cells disappear entirely and only the shallower pocket cell `(4,3)` remains. Dropping `(1,0)` restores deep cell `(1,0)` only; dropping `(4,0)` restores deep cell `(4,0)` only; dropping both restores the exact target cell set `(1,0),(4,0),(4,3)` and the exact target outcome. So the suppressor nodes are not merely nearby blockers. They are literally filling the two mirrored deep-pocket cells that the `pocket-wrap` route requires.
- A transplant-context decomposition then makes that lost condition concrete. The failing full transplants do not just reduce the deep-gap contrast; they eliminate deep-pocket shell/core support entirely (`deep shell/core = 0/0` in both transplant branches), while keeping a strong pocket shell signature (`shell/core pocket = 1.00/0.09`) and essentially the same threshold split (`ge6/ge7 = 0.02/0.98`) as the true defector. So the current best mechanism read tightens to: this route requires preserving nonzero deep-pocket adjacency inside the threshold shell/core partition; once deep adjacency collapses to zero globally, the pocket-heavy transplant still stays `neither`.
- A direct minimal-prune rescue then shows the failed full transplants are not missing target structure; they contain extra context. Both near-miss full transplants have zero missing target nodes, and adding donor nodes cannot restore the defect. But removing exactly two shared extra nodes (`(1,0)` and `(4,0)`) from either failed full transplant immediately restores the same `deep-pocket-only` signature as the true `pocket-wrap` defector (`deep/pocket/low = -0.38/+0.52/-0.14`) with the same core-deep support (`deep shell/core = 0/16`). So the tighter local mechanism is not “add deep support back.” It is “avoid a tiny pair of non-deep extra nodes that suppresses the anti-deep pocket route.”
- A direct suppressor-coverage integrity pass then checks whether that pair result is just a nearest-neighbor artifact. It is not. Across every available pocket-signature `local-morph` near-miss (`2/2` rows), the full transplant still lands at `neither` with the same collapsed full-transplant gaps (`+0.00/+0.91/-0.14`), dropping either suppressor alone still fails (`0/2` rescues), and dropping both suppressors always restores the exact target signature (`2/2` rescues and `2/2` exact gap matches). So the paired-suppressor mechanism now holds over the whole currently observed pocket-signature near-miss set, not just one hand-picked comparison.
- A widened-ladder rerun then checks whether that integrity claim survives deeper deterministic `local-morph` enumeration. It does. Over `variant_limit = 16, 24, 32, 40`, the pocket-signature near-miss set grows from `2` to `3` rows, but the suppressor behavior stays exact at every rung: full transplants remain `neither`, single-node drops never rescue, and dropping both suppressors always restores `dpadj-only` with exact target-gap match (`pair_rescue = target_match = rows`, `single_rescue = 0` throughout). So the paired-suppressor read now survives one notch deeper than the original local set and is not tied to the first two near-miss examples.
- A reverse-direction suppressor injection then closes the causal loop on the target itself. Starting from the true `pocket-wrap` defector, adding either suppressor node alone (`(1,0)` or `(4,0)`) already kills the defection (`dpadj-only -> neither`) while preserving only one deep cell; adding both recreates the exact full-transplant collapse (`+0.00/+0.91/-0.14`) with no deep cells left. So the pair is not only sufficient to block in transplant context; each node is individually sufficient to break the target route, and the pair jointly reproduces the complete collapse signature.
- A specificity pass then checks whether that injection effect is a generic `dpadj-only` kill switch on the same `taper-wrap` `local-morph` ladder (`variant_limit = 40`). It is not. Among `7` `dpadj-only` rows, only `2` are pocket-signature cases and only `1` of those (`local-morph-a`, the canonical pocket-wrap defector) is suppressor-sensitive; the other pocket-signature case (`local-morph-y`) is fully suppressor-resistant. Most non-pocket-signature rows are also resistant (`4/5`), with one separate non-pocket-sensitive exception (`local-morph-v`). So the suppressor mechanism now looks context-specific: strongly causal for the canonical pocket-wrap route, but not a universal destroyer of all nearby `dpadj-only` states.
- A final overlap check then explains that split in one line. The suppressor pair kills exactly the rows where `(1,0)` and `(4,0)` are active **deep-pocket** cells in the baseline state (`2/2` kills), and it never kills rows where those coordinates are absent from the deep set (`0/5`). So coordinate occupancy alone is not the mechanism: the pair is load-bearing only when it directly overwrites the currently active deep-pocket support cells.
- A deeper specificity ladder then refines that rule one notch. At `variant_limit = 48`, `56`, and `64`, one new pair-kill row appears where only one suppressor coordinate overlaps the active deep set (`local-morph-\\x8e`, deep-overlap count `= 1`), so the strict “both deep cells” rule is no longer complete (`tp/fp/fn = 2/0/1`). But the broader overlap rule remains exact on the tested ladder (`40,48,56,64`): pair-kill occurs iff deep-overlap count is nonzero (`tp/fp/fn = 3/0/0` at `64`). So the cleaner current mechanism statement is: suppressors act when they overwrite active deep support, and full two-cell overlap is sufficient but not necessary.
- A focused pair-kill diagnostic then makes that overlap rule coordinate-exact. At both `variant_limit = 56` and `64`, the same `3` pair-kill rows appear, and in all `3/3` cases the kill set matches the overlapping deep-support coordinates exactly. `local-morph-a` and `local-morph-v` each require both overlapping coordinates to flip, while the partial-overlap row `local-morph-\\x8e` flips only when the single overlapping coordinate `(4,0)` is added. So the deeper non-pocket rows are not a separate side channel; they are the same active-deep-support overwrite mechanism expressed on different baseline support sets.
- A direct overlap-context rule search then sharpens the difference between the pocket-signature and non-pocket branches of that same mechanism. On the overlap-positive sets at `variant_limit = 64`, `72`, `80`, `96`, `112`, and `128`, membership stays unchanged (still exactly `local-morph-a`, `local-morph-v`, and `local-morph-\\x8e`) and exact one-feature separators remain available in shell/profile space: `boundary_roughness <= 0.288`, `center_total_variation <= 2.500`, and `shell_pocket_fraction >= 0.812` each isolate the canonical `local-morph-a` branch with `0` false positives and `0` false negatives. So up through `128`, the clean read is: `local-morph-a` is the low-roughness, low-total-variation, shell-pocket-saturated tip of the same overwrite-trigger family, while `local-morph-v` and `local-morph-\\x8e` are rougher or more internally varied contexts of that same mechanism.
- The first real breakpoint arrives at `variant_limit = 144`. Two new overlap-positive non-pocket rows appear (`local-morph-\\xe7`, `local-morph-\\xe9`), so overlap-positive membership expands from `3` to `5`. At that point, `boundary_roughness <= 0.288`, `pocket_fraction <= 0.081`, and `shell_boundary_deficit_mean <= 0.391` still isolate the pocket-signature branch exactly, but the earlier `center_total_variation <= 2.500` and `shell_pocket_fraction >= 0.812` separators are no longer exact.
- A second pocket-signature row then appears at `variant_limit = 160` (`local-morph-\\xf6`). That broadens the pocket-signature branch without changing the underlying trigger: pair-kill is still coordinate-exact deep-support overwrite, but the pocket-signature family is now at least two-row wide.
- The next two rungs then show that this is not just a one-off transient. At `variant_limit = 176`, one more non-pocket overlap-positive row appears (`local-morph-\\u0103`), taking the broadened family to `7` rows total, and `variant_limit = 192` reproduces that exact same `7`-row set with the same exact-rule table. So the current best read is that the broadened family has stabilized at least through `192`, not that the `144/160` changes were isolated blips.
- Across that stabilized `160/176/192` band, the robust exact one-feature separators are now `boundary_roughness <= 0.288` and `pocket_fraction <= 0.081`, both of which continue to isolate the full pocket-signature subset (`2/2` true positives, `0/0` errors). The earlier total-variation and shell-pocket-share cuts no longer do.
- A direct row compare of the new `local-morph-\\xf6` branch shows why. It remains an anti-deep pocket state (`deep_gap = -0.38`) with positive pocket gap (`+0.55`), but it is even more compact than canonical `local-morph-a`: lower roughness (`0.244`), lower total variation (`1.00`), no midline crossing, span `2`, and only one overlapping suppressor/deep coordinate `(4,0)`. So the current best read is: the pocket-signature branch is no longer a single canonical row, but it is still the low-roughness, low-pocket-fraction edge of the same overwrite-trigger family.
- A direct row-level compare of `local-morph-a`, `local-morph-v`, and `local-morph-\\x8e` clarifies what is different about those non-pocket pair-kill rows. They are not just “`local-morph-a` minus pocket signal”: `local-morph-v` starts with positive deep and low-degree gaps (`+0.09/+0.24`) plus an extra surviving deep cell `(2,-1)`, while `local-morph-\\x8e` starts with negative pocket gap (`-0.07`) and a different surviving deep cell `(2,2)` plus a wider span profile (`span=4`). In shell/core terms they also shift context (`local-morph-v` has much higher shell deep/low fractions; `local-morph-\\x8e` has lower shell pocket share). So the strongest current read is two-part: the kill trigger is still coordinate-exact deep-support overwrite, but whether a row presents as pocket-signature or non-pocket is set by broader shell/profile context, not only by a one-bit pocket-gap toggle.
- A focused `variant_limit = 192` compare of all five non-pocket overlap-positive rows (`local-morph-v`, `local-morph-\\x8e`, `local-morph-\\xe7`, `local-morph-\\xe9`, `local-morph-\\u0103`) shows the newly added rows are not one single subtype. `local-morph-\\xe7` and `local-morph-\\xe9` are a matched non-crossing branch (`crosses_midline = n`) that flips only when `(1,0)` is injected (`add1 -> ge6-only`, `add4 -> dpadj-only`), while `local-morph-\\u0103` sits with the crossing branch (`crosses_midline = Y`) and instead flips on `(4,0)` injection (`add4 -> ge6-only`, `add1 -> dpadj-only`). So the broadened non-pocket family now cleanly splits into at least two suppressor-response subtypes rather than one coherent new branch.
- A direct subtype-rule extraction pass then turns that split into exact small predicates and checks stability one rung deeper. On both `variant_limit = 192` and `208`, non-pocket overlap-positive membership is unchanged (the same `5` rows) and the subtype table is identical. The add1-sensitive branch (`local-morph-\\xe7`, `local-morph-\\xe9`) is exactly isolated by any of: `crosses_midline = n`, `center_total_variation <= 2.500`, or `core_low_degree_fraction >= 0.285` (`0` FP, `0` FN). The remaining crossing branch (`local-morph-v`, `local-morph-\\x8e`, `local-morph-\\u0103`) then splits exactly by overlap multiplicity: `deep_overlap_count = 2` isolates the single both-sensitive row (`local-morph-v`), and `crosses_midline = Y and deep_overlap_count = 1` isolates the add4-sensitive pair (`local-morph-\\x8e`, `local-morph-\\u0103`). So through `208`, the non-pocket side is no longer just “multiple subtypes exist”; it now has a compact exact-rule map that stayed unchanged at the next rung.
- Completing the blocked `variant_limit = 224` rung then reveals the next breakpoint. Non-pocket overlap-positive membership expands from `5` to `6` rows with a new add4-sensitive crossing row (`local-morph-\\u0133`), while subtype count remains `3`. The add1-sensitive branch stays exactly isolated by `crosses_midline = n` (`2/2`, `0` FP, `0` FN), but the crossing-side exact map broadens from a two-row to a three-row add4-sensitive branch (`local-morph-\\x8e`, `local-morph-\\u0103`, `local-morph-\\u0133`). So the mechanism read updates from “stable through 208” to “first post-208 non-pocket expansion at 224, without changing the core add1-sensitive separator.”
- Running one rung deeper at `variant_limit = 240` then shows the `224` change was not transient. The new `local-morph-\\u0133` row persists and two additional add4-sensitive crossing rows appear (`local-morph-\\u014c`, `local-morph-\\u014f`), expanding non-pocket overlap-positive membership from `6` to `8` while subtype count remains `3`. The add1-sensitive separator is still exact via `crosses_midline = n` (`2/2`, `0` FP, `0` FN), but several equivalent two-term add1 rules rotate to shell-deep variants (for example `boundary_roughness >= 0.297 and shell_deep_fraction <= 0.367`). So the strongest current read is: post-`208` growth continues through `240` on the add4-sensitive crossing branch, while the core add1-sensitive split remains structurally stable.
- A direct `variant_limit = 256` rung then checks whether that `240` growth continues immediately or pauses. It pauses: non-pocket overlap-positive membership remains exactly the same `8` rows as `240`, subtype count stays `3`, and the add1-sensitive separator remains exact via `crosses_midline = n` (`2/2`, `0` FP, `0` FN). So the strongest current read updates to: the post-`208` add4-sensitive crossing expansion occurred at `224/240`, then stabilized through at least `256` without further membership growth.
- A direct `variant_limit = 272` rung then checks whether the `256` plateau is stable for one more step. It is: non-pocket overlap-positive membership is still the same `8` rows, subtype count remains `3`, and the add1-sensitive separator remains exact via `crosses_midline = n` (`2/2`, `0` FP, `0` FN), with no rule-table changes versus `256`/`240` beyond timestamps and elapsed runtime. That stability then survives five more direct rungs at `288`, `304`, `320`, `336`, and `352`, each of which reproduces the same `8` rows, the same `3` subtypes, and the same exact add1-sensitive rule table.
- The next direct rungs at `368`, `384`, `400`, `416`, and `432` then mark the first change after that long plateau. A new add1-sensitive non-crossing row appears (`local-morph-\u01cd`), so non-pocket overlap-positive membership rises from `8` to `9` while subtype count remains `3`, and the entire `368..432` band reproduces the same `9` rows and the same exact-rule table. The strongest one-term separator still survives intact, but now with `3` true positives instead of `2`: `crosses_midline = n` continues to isolate the full add1-sensitive branch exactly (`0` FP, `0` FN).
- The next two rungs at `448` and `464` then break that band again. First, a new add4-sensitive non-crossing row appears (`local-morph-\u0218`), pushing non-pocket overlap-positive membership from `9` to `10` while subtype count still stays `3`. That already breaks the old one-term add1 separator story: `crosses_midline = n` is no longer exact once a non-crossing add4-sensitive row exists, and the add1-sensitive branch now needs low-degree-gated 2-term rules such as `pocket_fraction <= 0.128 and core_low_degree_fraction >= 0.275`.
- The next two rungs at `480` and `496` then create the first genuinely new subtype regime in this thread. Two more rows appear (`local-morph-\u0232`, `local-morph-\u0236`), non-pocket overlap-positive membership rises again from `11` to `13`, and subtype count increases from `3` to `4`. The new fourth subtype is `pair-only-sensitive`, represented by `local-morph-\u0236`, which stays `dpadj-only` under both add1 and add4.
- A direct `variant_limit = 512` rung then shows that this four-subtype regime is real, but not yet stable in membership. One more add1-sensitive row appears (`local-morph-\u025d`), so non-pocket overlap-positive membership rises again from `13` to `14` while subtype count remains `4`. The exact add1-sensitive rule family also shifts again: the clean exact cuts remain 2-term and low-degree-gated, but now the strongest exact rules are `core_low_degree_fraction >= 0.275 and mean_center >= -0.321` or `span_range >= 2.500 and core_low_degree_fraction >= 0.275`, not the earlier pocket-fraction variant.
- The next ten rungs at `528`, `544`, `560`, `576`, `592`, `608`, `624`, `640`, `656`, and `672` then stabilize that new regime one notch higher. A second `pair-only-sensitive` row appears (`local-morph-\u026e`), pushing non-pocket overlap-positive membership from `14` to `15`, and the full `528..672` band reproduces the same `15` rows, the same `4` subtypes, and the same exact-rule table. In that stabilized band, the add1-sensitive family is still captured exactly by the single 2-term low-degree rule `span_range >= 2.500 and core_low_degree_fraction >= 0.275`, while the pair-only-sensitive subtype is now represented by two rows (`local-morph-\u0236`, `local-morph-\u026e`).
- The next four rungs at `688`, `704`, `720`, and `736` then break that stable band in a clean, repeated way. A new add1-sensitive row appears (`local-morph-\u0310`), pushing non-pocket overlap-positive membership from `15` to `16` while subtype count stays `4`, and the full `688..736` band reproduces the same `16` rows and the same exact-rule table. That new row has `cross=Y` and response pattern `neither/dpadj-only`, so the old exact add1-sensitive separator drops out of the exact-rule table entirely, while the both-sensitive subtype remains exactly isolated by `deep_overlap_count >= 1.500`.
- The next two rungs at `752` and `768` then expand that new band again. `752` adds a second new add1-sensitive row (`local-morph-\u034b`), pushing non-pocket overlap-positive membership from `16` to `17`, and `768` adds a new add4-sensitive row (`local-morph-\u035b`), pushing it again from `17` to `18` while subtype count still stays `4`.
- The next five rungs at `784`, `800`, `816`, `832`, and `848` split into two phases. `784` adds two new rows at once (`local-morph-\u036c`, `local-morph-\u036f`), raising non-pocket overlap-positive membership from `18` to `20` and expanding the `pair-only-sensitive` subtype from `2` to `3` rows. `800` adds two more rows (`local-morph-\u0372`, `local-morph-\u037a`), pushing the count again from `20` to `22` and broadening the both-sensitive exact rule family from `1` true positive to `2`. `816` adds `local-morph-\u0386`, pushing the count from `22` to `23`. `832` then adds two more rows (`local-morph-\u0399`, `local-morph-\u03a0`), pushing non-pocket overlap-positive membership from `23` to `25` while subtype count stays `4` and the both-sensitive exact-rule table remains unchanged. `848` then exactly reproduces the `832` subtype membership and exact-rule table (same `25` rows, same `4` subtypes, same both-sensitive rules).
- The next two rungs at `864` and `880` confirm that this `25`-row frontier is a real stable band: both exactly reproduce the same subtype membership and the same exact-rule table as `832/848`.
- The next three rungs at `896`, `912`, and `928` then create and extend the next confirmed hold. `896` adds `local-morph-\u03d4` and `local-morph-\u03d8`, pushing non-pocket overlap-positive membership from `25` to `27`, and both `912` and `928` exactly reproduce that same `27`-row frontier. Subtype count still remains `4`, but `pair-only-sensitive` expands again to `4` rows, while the both-sensitive exact-rule family remains unchanged and still isolates `2` rows via `deep_overlap_count >= 1.500`. So the strongest current read is now: the post-`208` add4-sensitive crossing expansion plateaued from `240` through `352`, a `9`-row add1-growth band then held from `368` through `432`, `528..672` became the first stable checkpoint of the new four-subtype regime, `688..736` formed the next stable band with the `local-morph-\u0310` expansion, `752..816` was a live accelerating growth phase, `832..880` became the next stable `25`-row band, and `896..928` now marks the next confirmed `27`-row hold beyond that.
- A direct `variant_limit = 944` rung then breaks that hold by exactly one row while keeping the same subtype regime. Non-pocket overlap-positive membership rises from `27` to `28` with one new `pair-only-sensitive` row (`local-morph-\u040f`), subtype count remains `4`, and the both-sensitive exact-rule family is unchanged and still isolates `2` rows via `deep_overlap_count >= 1.500`. So the current frontier read is: `896..928` was a real short hold, and `944` starts the next growth phase by extending the `pair-only-sensitive` branch.
- A direct `variant_limit = 960` rung then checks whether `944` was the start of sustained immediate growth or a one-step bump. It is a one-step bump so far: `960` exactly reproduces the `944` non-pocket subtype membership and exact-rule table (`28` rows, `4` subtypes, unchanged both-sensitive rules anchored by `deep_overlap_count >= 1.500`), including the same new `local-morph-\u040f` row and no further additions. So the current frontier read tightens to: `944` was a one-row expansion, and `944..960` is now the next confirmed short hold.
- A direct `variant_limit = 976` rung then breaks that short hold with a two-row expansion while keeping the same subtype regime. Non-pocket overlap-positive membership rises from `28` to `30`, subtype count remains `4`, and the both-sensitive exact-rule family remains unchanged (still isolating `2` rows via `deep_overlap_count >= 1.500`). The two new rows are `local-morph-\u0428` (add1-sensitive, `ge6-only/dpadj-only`) and `local-morph-\u0430` (add4-sensitive, `dpadj-only/ge6-only`). So the strongest current frontier read is now: `944..960` was a short hold, and growth resumes at `976` without a subtype-map change.
- A direct `variant_limit = 992` rung then checks whether the `976` two-row expansion continues immediately or settles into a hold. It settles: `992` exactly reproduces the `976` non-pocket subtype membership and exact-rule table (`30` rows, `4` subtypes, unchanged both-sensitive rules anchored by `deep_overlap_count >= 1.500`), including the same `local-morph-\u0428` and `local-morph-\u0430` additions with no further new rows. So the current frontier read tightens to: `976` was a two-row expansion, and `976..992` is now the next confirmed short hold.
- A direct `variant_limit = 1008` rung then checks whether that `976..992` hold breaks on the next step. It does not: `1008` exactly reproduces the same non-pocket subtype membership and exact-rule table as `992/976` (`30` rows, `4` subtypes, unchanged both-sensitive anchor `deep_overlap_count >= 1.500`, `tp=2`) with no new rows. So the frontier read tightens again to: `976` was a two-row expansion, and `976..1008` is now the confirmed short hold.
- A direct `variant_limit = 1024` rung then checks whether that hold breaks one step deeper. It still does not: `1024` exactly reproduces the same non-pocket subtype membership and exact-rule table as `1008/992` (`30` rows, `4` subtypes, unchanged both-sensitive anchor `deep_overlap_count >= 1.500`, `tp=2`) with no new rows. So the frontier read tightens again to: `976` was a two-row expansion, and `976..1024` is now the confirmed short hold.
- A direct `variant_limit = 1040` rung then breaks that hold by one row without changing the subtype map or both-sensitive anchor. Non-pocket overlap-positive membership rises from `30` to `31` with one new add1-sensitive crossing row (`local-morph-\u0461`, `neither/dpadj-only`), subtype count remains `4`, and the both-sensitive exact-rule family is unchanged (`deep_overlap_count >= 1.500`, `tp=2`). So the frontier now reads: `976..1024` was a real short hold, and growth resumes at `1040` by extending the add1-sensitive branch.
- A direct `variant_limit = 1056` rung then extends that same branch by one more row while preserving the subtype map and both-sensitive anchor. Non-pocket overlap-positive membership rises from `31` to `32` with one new `pair-only-sensitive` row (`local-morph-\u047f`, `dpadj-only/dpadj-only`, `cross=Y`), subtype count remains `4`, and the both-sensitive exact-rule family is unchanged (`deep_overlap_count >= 1.500`, `tp=2`). So the frontier now reads: `976..1024` was the last short hold, `1040` resumed growth, and `1056` continues that same growth phase.
- A direct `variant_limit = 1072` rung then checks whether that `1056` growth continues immediately. It does not: `1072` exactly reproduces the `1056` non-pocket subtype membership and exact-rule table (`32` rows, `4` subtypes, unchanged both-sensitive anchor `deep_overlap_count >= 1.500`, `tp=2`) with no new rows. So the frontier now tightens to: `1040/1056` was a two-step growth phase, and `1056..1072` is the next confirmed short hold.
- A direct `variant_limit = 1088` rung then breaks that short hold by one row while keeping the subtype map and both-sensitive exact-rule anchor unchanged. Non-pocket overlap-positive membership rises from `32` to `33` with one new add4-sensitive crossing row (`local-morph-\u0492`, `dpadj-only/ge6-only`, `cross=Y`), subtype count remains `4`, and the both-sensitive exact-rule family is still anchored by `deep_overlap_count >= 1.500` (`tp=2`). So the frontier now reads: `1056..1072` was a short hold, and growth resumes at `1088` via a new add4-sensitive expansion.
- A direct `variant_limit = 1104` rung then continues that same growth phase by one additional row while preserving the subtype map and both-sensitive exact-rule anchor unchanged. Non-pocket overlap-positive membership rises from `33` to `34` with one new add1-sensitive non-crossing row (`local-morph-\u04ab`, `ge6-only/dpadj-only`, `cross=n`), subtype count remains `4`, and the both-sensitive exact-rule family is still anchored by `deep_overlap_count >= 1.500` (`tp=2`). So the frontier now reads: `1056..1072` was a short hold, `1088` resumed growth, and `1104` extends that growth by adding a new add1-sensitive branch row.
- A new latent-structure pass then reframes what those late rungs mean. Across representative four-subtype checkpoints (`512`, `672`, `912`, `1104`), row count grows much faster than coarse signature count: `14 -> 15 -> 27 -> 34` rows, but only `8 -> 9 -> 13 -> 14` coarse signatures when rows are collapsed onto `{crosses_midline, span_range, core_low_degree_fraction, pocket_fraction, deep_overlap_count, boundary_roughness}`. The late growth is mostly support-filling rather than ontology growth: at `912`, `7/12` new rows land on already-seen signatures, and at `1104`, `6/7` do. So the current best read is “stable taxonomy, expanding membership,” not “new mechanism family every rung.”
- That same pass also shows the present six-feature coarse map is still only a shadow of the deeper rule, not the rule itself. At `1104`, the best two-axis partition on that coarse feature set reaches only `22/34` exact subtype assignments, and the best depth-2 small tree reaches only `23/34`. The best pair is `core_low_degree_fraction` plus `deep_overlap_count`; the best small tree adds `span_range`; but none of them collapse the four-subtype map exactly. So the finite core is real, but it is not yet an exact low-dimensional law on the current coarse observables. The right next step is richer latent-axis or collision analysis inside those shared coarse signatures, while the ladder itself can be demoted to a sparse sentinel.
- A direct sparse-sentinel rung at `variant_limit = 1168` then confirms this thread is still in a live growth phase. Non-pocket overlap-positive membership rises from `34` to `36` while subtype count remains `4`, adding two new rows (`local-morph-\u04cc` add4-sensitive, `local-morph-\u04cd` add1-sensitive), and the both-sensitive exact-rule anchor remains unchanged (`deep_overlap_count >= 1.500`, `tp=2`). So the frontier read updates to: `1056..1072` was a short hold, `1088` resumed growth, `1104` continued it, and `1168` extends it by two additional rows without changing the subtype map.
- The completed latent continuation on `512,672,912,1168` tightens that read without changing its direction. At `1168`, coarse signatures rise only from `13` to `14` while rows rise from `27` to `36`, and `8/9` new rows at `1168` land on already-seen coarse signatures, so late growth is still predominantly support-filling within an already-stable four-subtype map. The best coarse two-axis partition remains inexact but improves slightly in absolute coverage (`23/36` vs `22/34` at `1104`), and the best small tree likewise remains inexact while improving to `24/36` (`23/34` at `1104`). So the current best read is unchanged in kind: the coarse six-feature map captures a finite core but still does not collapse the subtype law exactly.
- A bounded collision-summary pass over that completed `1168` latent log then isolates where the six-bit coarse map is failing. Only `4/14` coarse signatures are subtype-colliding, but those buckets carry `20/36` rows; and `18/20` of those collision rows are specifically add1-vs-add4 ambiguity (with only `2/20` add1-vs-pair-only). So the missing separator signal is now tightly localized: it is concentrated inside add1/add4 mixed buckets that already agree on all six coarse bits, consistent with the current best-order axes (`core_low_degree_fraction`, `deep_overlap_count`, then `span_range`) and pointing next work toward finer low-degree geometry inside those mixed signatures.
- A targeted mixed-bucket rule search then sharpens that collision story one more level. At `1168`, there are three add1-vs-add4 mixed coarse signatures. Two of them already split exactly on finer observables without needing a broader latent model: the large crossing mixed bucket (`cross=Y|span=3+|low=L|pocket=H|overlap=1|rough=H`) is separated exactly by a single extra low-degree-shell cut (`shell_low_degree_fraction >= 0.817`), and the small non-crossing low-pocket bucket (`cross=n|span<3|low=L|pocket=L|overlap=1|rough=L`) is separated exactly by a small `boundary_roughness + mean_center` rule. The remaining unresolved bucket is therefore much narrower than “the whole mixed frontier”: it is the `cross=n|span=3+|low=L|pocket=H|overlap=1|rough=H` bucket, which still has no exact 1-2 term separator on the current observable family. So the next best move is no longer “keep laddering” or even “analyze all mixed buckets”; it is to search for one more latent axis inside that single unresolved coarse bucket.
- A follow-up residual decomposition over the completed `1168` collision-axes output narrows that target one more notch. Inside the unresolved coarse bucket (`cross=n|span=3+|low=L|pocket=H|overlap=1|rough=H`), the `pair-only-sensitive` row peels off exactly via `core_low_degree_fraction >= 0.269` (`1/4` TP, `0` FP, `0` FN, `4/4` accuracy). So the unresolved separator problem is now strictly the 3-row add1-vs-add4 remainder in that bucket, not the full 4-row mixed bucket.
- A direct residual-bucket rule pass then closes that last gap on the current observable family. On the remaining 3-row add1-vs-add4 residual inside `cross=n|span=3+|low=L|pocket=H|overlap=1|rough=H`, the add1-sensitive side is already separated exactly by a single finer low-degree cut: `core_low_degree_fraction >= 0.208` (`2/2` TP, `0` FP, `0` FN, `3/3` accuracy). Concretely, the two add1 rows are `local-morph-\u0428` and `local-morph-\u04ab`, while the lone add4 row is `local-morph-\u04cc`; and the add4 row is simply the low-core-low-degree outlier in that bucket. So at `1168`, the three add1-vs-add4 mixed coarse buckets are no longer “partly unresolved”: all three are exactly split by the current finer observable family, with the missing signal concentrated in low-degree-shell/core geometry rather than in a new mechanism type.
- A bounded sparse-sentinel rerun at `variant_limit = 1232` (hard-capped at `2400s`, completed in `1156.3s`) then advances the same thread beyond that `1168` closeout. Non-pocket subtype membership grows from `36` to `40` rows while subtype count stays `4`, adding `local-morph-\u04f2` (add4-sensitive), `local-morph-\u0522` (pair-only-sensitive), `local-morph-\u0523` (pair-only-sensitive), and `local-morph-\u0528` (both-sensitive). The both-sensitive anchor `deep_overlap_count >= 1.500` remains exact and now lifts from `tp=2` to `tp=3`. So the frontier read updates to: subtype law is still stable in kind, but the active sparse-sentinel ladder remains in a live growth phase at `1232` rather than a new hold.
- A direct sparse-sentinel rung at `variant_limit = 1296` (hard-capped at `2400s`, completed in `1184.6s`) then continues that same live-growth regime with one additional row and no subtype-map break. Non-pocket subtype membership grows from `40` to `41` rows while subtype count remains `4`, with the lone new row `local-morph-\u0544` entering as `pair-only-sensitive` (`dpadj-only/dpadj-only`, `cross=Y`). The both-sensitive anchor `deep_overlap_count >= 1.500` remains exact with the same `tp=3` table as `1232`.
- A direct sparse-sentinel rung at `variant_limit = 1360` (hard-capped at `2400s`, completed in `1238.9s`) then extends the same regime by two more rows without subtype-map drift. Non-pocket subtype membership grows from `41` to `43` rows while subtype count stays `4`; the new rows are `local-morph-\u0580` and `local-morph-\u0594`, both entering as `pair-only-sensitive` (`dpadj-only/dpadj-only`, `cross=Y`). The exact-rule table is unchanged versus `1296` and `1232`, with the both-sensitive anchor still `deep_overlap_count >= 1.500` (`tp=3`). So the strongest current frontier read remains: stable four-subtype law with live sparse-sentinel growth, now extended through `1360`.
- A direct sparse-sentinel rung at `variant_limit = 1424` (completed in `1316.8s`) then checks whether the `1360` growth continues immediately. It does not: `1424` exactly reproduces the `1360` non-pocket subtype membership and exact-rule table (`43` rows, `4` subtypes, unchanged both-sensitive anchor `deep_overlap_count >= 1.500`, `tp=3`) with no new rows. So the strongest current frontier read tightens to: `1232/1296/1360` was an active growth phase, and `1360..1424` is now the next confirmed short hold.
- A direct sparse-sentinel rung at `variant_limit = 1488` (completed in `1447.2s`) then breaks that short hold by exactly one row without changing the subtype map or exact-rule table. Non-pocket subtype membership rises from `43` to `44`, subtype count remains `4`, and the both-sensitive anchor is still exact via `deep_overlap_count >= 1.500` (`tp=3`). The lone new row is `local-morph-\u0614`, which enters as `add4-sensitive` (`dpadj-only/neither`, `cross=n`). So the current frontier read becomes: `1360..1424` was a real short hold, and `1488` starts the next growth phase, but still strictly inside the existing four-subtype law.
- A direct sparse-sentinel rung at `variant_limit = 1552` (completed in `1450.1s`) then checks whether that one-row `1488` bump continues immediately. It does not: `1552` exactly reproduces the `1488` non-pocket subtype membership and exact-rule table (`44` rows, `4` subtypes, unchanged both-sensitive anchor `deep_overlap_count >= 1.500`, `tp=3`) with no new rows. So the current frontier read tightens again: `1488` was a one-row add4-sensitive expansion, and `1488..1552` is now the next confirmed short hold.
- A direct sparse-sentinel rung at `variant_limit = 1680` (completed in `1528.6s`) then breaks that `1488..1552` hold by exactly one row while keeping the subtype count unchanged. Non-pocket subtype membership rises from `44` to `45` with one new `pair-only-sensitive` row (`local-morph-\u0690`, `dpadj-only/dpadj-only`, `cross=n`), and subtype count remains `4`. The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=3`), but the two-term both-sensitive rule list rotates one entry: the older `boundary_fraction >= 0.942 and mean_center <= -0.036` rule drops out and is replaced by `crosses_midline = Y and deep_overlap_count >= 1.500`. So the strongest current frontier read is: stable four-subtype law with resumed sparse-sentinel growth at `1680`, including a small exact-rule-table pivot without a subtype-map break.
- A direct sparse-sentinel rung at `variant_limit = 1808` (completed in `1643.1s`) then extends that same growth phase by six rows with no subtype-map drift and no exact-rule drift. Non-pocket subtype membership rises from `45` to `51` while subtype count remains `4`; the new rows are `local-morph-\u0702` (add4-sensitive), `local-morph-\u070a` (pair-only-sensitive), and `local-morph-\u070b`, `local-morph-\u0723`, `local-morph-\u0733`, `local-morph-\u073d` (all add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=3`), and the two-term rule table is unchanged versus `1680`. So the strongest current frontier read is: the four-subtype law remains structurally stable while sparse-sentinel membership growth is still active through `1808`.
- A direct sparse-sentinel rung at `variant_limit = 1936` (completed in `1737.5s`) then continues that same growth phase by exactly one row with no subtype-map drift and no exact-rule drift. Non-pocket subtype membership rises from `51` to `52` while subtype count remains `4`; the lone new row is `local-morph-\u0795` (`add4-sensitive`, `dpadj-only/neither`, `cross=Y`). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=3`), and the two-term rule table remains unchanged versus `1808` and `1680`. So the strongest current frontier read is: the four-subtype law remains structurally stable while sparse-sentinel membership growth is still active through `1936`.
- A direct sparse-sentinel rung at `variant_limit = 2064` (completed in `1848.3s`) then checks whether that `1936` growth continues immediately. It does not: `2064` exactly reproduces the `1936` non-pocket subtype membership and exact-rule table (`52` rows, `4` subtypes, unchanged both-sensitive one-term anchor `deep_overlap_count >= 1.500`, `tp=3`, and unchanged both-sensitive two-term rule list) with no new rows. So the frontier read tightens to: `1936` was a one-row expansion, and `1936..2064` is now the next confirmed short hold.
- A direct sparse-sentinel rung at `variant_limit = 2192` (completed in `1985.1s`) then breaks that short hold by six rows while preserving the same four-subtype map. Non-pocket subtype membership rises from `52` to `58`; the new rows are `local-morph-\u0874` (pair-only-sensitive), `local-morph-\u088d` (both-sensitive), `local-morph-\u088f` (add4-sensitive), `local-morph-\u0892` (add1-sensitive), `local-morph-\u089b` (add1-sensitive), and `local-morph-\u08ae` (add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`) but now lifts from `tp=3` to `tp=4`, and the both-sensitive two-term family broadens slightly (`boundary_roughness >= 0.305` replacing `>= 0.311`, and `center_total_variation >= 1.500` replacing `>= 2.500`). So the frontier read updates to: `1936..2064` was a short hold, and growth resumes at `2192` without subtype-map drift.
- A direct sparse-sentinel rung at `variant_limit = 2320` (completed in `2092.1s`) then continues that growth phase by seven rows while preserving the same four-subtype map. Non-pocket subtype membership rises from `58` to `65`; the new rows are `local-morph-\u08fb` (pair-only-sensitive), `local-morph-\u091b` (add1-sensitive), `local-morph-\u0924` (add1-sensitive), `local-morph-\u0935` (pair-only-sensitive), `local-morph-\u0937` (add1-sensitive), `local-morph-\u0939` (both-sensitive), and `local-morph-\u096d` (add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`) and lifts again from `tp=4` to `tp=5`, while the both-sensitive two-term family broadens (`boundary_roughness >= 0.299` replacing `>= 0.305`) and rotates one predicate from `crosses_midline = Y and deep_overlap_count >= 1.500` to `deep_overlap_count >= 1.500 and boundary_fraction <= 0.988`. So the frontier read updates to: `2192` was not a one-step bump, and growth continues at `2320` without subtype-map drift.
- A direct sparse-sentinel rung at `variant_limit = 2448` (completed in `2199.4s`) then extends that same growth phase by two rows while preserving the same four-subtype map and exact both-sensitive rule family. Non-pocket subtype membership rises from `65` to `67`; the new rows are `local-morph-\u09a3` (pair-only-sensitive) and `local-morph-\u09c4` (add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the both-sensitive two-term table is unchanged versus `2320`. So the frontier read updates to: growth is still active at `2448` without subtype-map drift.
- A direct sparse-sentinel rung at `variant_limit = 2576` (completed in `2325.8s`) then continues that same growth phase by two rows while preserving the same four-subtype map and exact both-sensitive rule family. Non-pocket subtype membership rises from `67` to `69`; the new rows are `local-morph-\u0a45` (pair-only-sensitive) and `local-morph-\u0a6e` (add4-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the both-sensitive two-term table remains unchanged versus `2448`/`2320`. So the frontier read updates to: growth is still active at `2576` with no subtype-map or both-sensitive-rule drift.
- A direct sparse-sentinel rung at `variant_limit = 2704` (completed in `2457.7s`) then continues that same growth phase by two rows while preserving the same four-subtype map and exact both-sensitive rule family. Non-pocket subtype membership rises from `69` to `71`; the new rows are `local-morph-\u0ada` (add4-sensitive) and `local-morph-\u0aea` (add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the both-sensitive two-term table remains unchanged versus `2576`/`2448`/`2320`. So the frontier read updates to: growth is still active at `2704` with no subtype-map or both-sensitive-rule drift.
- A direct sparse-sentinel rung at `variant_limit = 2832` (completed in `2559.0s`) then extends that same growth phase by one row while preserving the same four-subtype map and exact both-sensitive rule family. Non-pocket subtype membership rises from `71` to `72`; the lone new row is `local-morph-\u0b23` (add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the both-sensitive two-term table remains unchanged versus `2704`/`2576`/`2448`/`2320`. So the frontier read updates to: growth remains active at `2832` with no subtype-map or both-sensitive-rule drift.
- A direct sparse-sentinel rung at `variant_limit = 2960` (completed in `2669.1s`) then extends that same growth phase by one row while preserving the same four-subtype map and exact both-sensitive rule family. Non-pocket subtype membership rises from `72` to `73`; the lone new row is `local-morph-\u0be5` (add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the both-sensitive two-term table remains unchanged versus `2832`/`2704`/`2576`/`2448`/`2320`. So the frontier read updates to: growth remains active at `2960` with no subtype-map or both-sensitive-rule drift.
- A direct sparse-sentinel rung at `variant_limit = 3088` (completed in `2771.0s`) then extends that same growth phase by two rows while preserving the same four-subtype map and exact both-sensitive rule family. Non-pocket subtype membership rises from `73` to `75`; the new rows are `local-morph-\u0c17` (add1-sensitive) and `local-morph-\u0c29` (add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the both-sensitive two-term table remains unchanged versus `2960`/`2832`/`2704`/`2576`/`2448`/`2320`. So the frontier read updates to: growth remains active at `3088` with no subtype-map or both-sensitive-rule drift.
- A direct sparse-sentinel rung at `variant_limit = 3216` (completed in `2884.7s`) then extends that same growth phase by three rows while preserving the same four-subtype map and exact both-sensitive rule family. Non-pocket subtype membership rises from `75` to `78`; the new rows are `local-morph-\u0caa` (add1-sensitive), `local-morph-\u0cb3` (add4-sensitive), and `local-morph-\u0cc9` (add1-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the both-sensitive two-term table remains unchanged versus `3088`/`2960`/`2832`/`2704`/`2576`/`2448`/`2320`. So the frontier read updates to: growth remains active at `3216` with no subtype-map or both-sensitive-rule drift.
- A direct sparse-sentinel rung at `variant_limit = 3344` (completed in `3004.7s`) then extends that same growth phase by six rows while preserving the same four-subtype map and exact both-sensitive rule family. Non-pocket subtype membership rises from `78` to `84`; the new rows are `local-morph-\u0cf7` (add1-sensitive), `local-morph-\u0d07` (add1-sensitive), `local-morph-\u0d12` (add4-sensitive), `local-morph-\u0d31` (add4-sensitive), `local-morph-\u0d3b` (add4-sensitive), and `local-morph-\u0d40` (pair-only-sensitive). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the both-sensitive two-term table remains unchanged versus `3216`/`3088`/`2960`/`2832`/`2704`/`2576`/`2448`/`2320`. So the frontier read updates to: growth remains active through `3344` with no subtype-map or both-sensitive-rule drift.
- A direct sparse-sentinel rung at `variant_limit = 3984` (completed in `3551.8s`) then continues that active growth phase while preserving subtype count (`4`) but rotating the both-sensitive exact family. Non-pocket subtype membership rises from `97` at `3856` to `101`, with new rows `local-morph-\u0fa1` (pair-only-sensitive), `local-morph-\u0fb2` (add1-sensitive), `local-morph-\u0fbe` (add1-sensitive), and `local-morph-\u0fd9` (add1-sensitive). The old one-term both-sensitive anchor `deep_overlap_count >= 1.500` is no longer sufficient by itself, but the best exact both-sensitive family remains small and stable in structure: 2-term rules anchored on `deep_overlap_count >= 1.500` plus a second boundary/core feature. So the current read becomes: the subtype law is still fixed, but the both-sensitive exact family has entered a new rotated 2-term regime by `3984`.
- A direct sparse-sentinel rung at `variant_limit = 4112` (completed in `3661.3s`) then confirms that rotation while extending the frontier again. Non-pocket subtype membership rises from `101` to `106`; subtype count remains `4`; and the one-term both-sensitive anchor stays absent. The exact both-sensitive family remains 2-term only, still anchored on `deep_overlap_count >= 1.500`, with representative exact rules such as `boundary_roughness >= 0.299 and deep_overlap_count >= 1.500`, `deep_overlap_count >= 1.500 and mean_center <= -0.036`, and `deep_overlap_count >= 1.500 and core_deep_fraction <= 0.433`. So `4112` is not a one-rung wobble: it preserves the rotated both-sensitive family while membership growth continues.
- A direct sparse-sentinel rung at `variant_limit = 4240` (completed in `3766.0s`) then confirms the same story one step deeper. Non-pocket subtype membership rises again from `106` to `109` while subtype count stays `4`. The both-sensitive exact family is still 2-term only and still anchored on `deep_overlap_count >= 1.500`; four of the five best exact rules are unchanged versus `4112`, and the only movement is a mild relaxation in one supporting threshold (`core_boundary_deficit_mean >= 0.274` instead of `>= 0.290`). So the strongest current frontier read is: active membership growth continues through `4240`, subtype count remains fixed, and the post-`3984` rotated both-sensitive rule family is now stable enough to treat as a real regime rather than a transient blip.
- A wider sparse-sentinel rung at `variant_limit = 4480` (completed in `3989.5s`) then confirms that the rotated regime is still the right reading. Non-pocket subtype membership rises again from `109` to `115` while subtype count stays fixed at `4`. The both-sensitive one-term anchor is still absent, and the best exact both-sensitive family remains 2-term only with the same qualitative structure as at `4240`: `deep_overlap_count >= 1.500` plus a second boundary/core observable. Four of the five best exact rules are unchanged even at `4480`, while the fifth shifts only mildly from `core_deep_fraction <= 0.433` to `<= 0.443`; the true-positive support of that exact family also grows from `6` to `7`. So the strongest current frontier read is now: active membership growth continues through `4480`, subtype count remains fixed, and the post-`3984` rotated both-sensitive family is stable enough to treat as a genuine higher-frontier regime.
- A direct sparse-sentinel rung at `variant_limit = 4992` (completed in `4399.0s`) then extends that same higher-frontier regime without subtype-map drift. Non-pocket subtype membership rises from `115` to `127` while subtype count remains `4`. The exact both-sensitive family stays 2-term and anchored on `deep_overlap_count >= 1.500`, with four of the five best rules unchanged versus `4480`; the only movement is a mild tightening of one supporting shell term (`shell_boundary_deficit_mean >= 0.358` instead of `>= 0.352`). So the strongest current frontier read updates to: active membership growth continues through `4992`, and the post-`3984` rotated both-sensitive family remains structurally stable at this wider rung.
- A bounded mixed-bucket-axis follow-up at `variant_limit = 5504` (completed in `5865.8s`) then checks whether add1-vs-add4 mixed coarse signatures remain unresolved at the next wider rung. In this run there is exactly one mixed add1/add4 bucket and it is still exactly separable (`7/7`, `2` terms, `tp/fp/fn = 4/0/0`) with a boundary-fraction threshold (`boundary_fraction <= 0.895`) plus a second boundary-threshold term. So the strongest mixed-bucket read updates to: the previously hard mixed-bucket collision does not persist as an exactness blocker at this wider frontier.
- A direct residual-bucket follow-up at `variant_limit = 5504` for `cross=n|span=3+|low=L|pocket=H|overlap=1|rough=H` (completed in `5760.2s`) then tests whether the remaining latent-collision bucket is now exactly separable on the current observable family. It is not: the residual table has `13` rows, and the best add1-sensitive separators reach only `12/13` (best `tp/fp/fn` either `8/0/1` or `9/1/0`) with no exact 1-3 term rule. So the strongest current read is: the mixed-bucket thread is narrowed and mostly closed at `5504`, but one residual add1-vs-add4 coarse bucket remains non-exact on the present feature set.
- A bounded latent-axis continuation on that same frozen `5504` residual table then checks whether simple feature augmentation can close the remaining split without rerunning the full frontier sweep. Using engineered axes from the parsed `13` residual rows (absolute gaps, shell/core shares, and low-order interactions), the best add4-sensitive rule improves to `12/13` with `3` terms (`tp/fp/fn = 3/0/1`) but still no exact 1-3 term separator appears for either class. So the strongest read remains unchanged but tighter: this residual `cross=n|span=3+|low=L|pocket=H|overlap=1|rough=H` bucket is not closed by straightforward thresholdable latent-axis augmentation on the current observables.
- A bounded row-level continuation on that same frozen `5504` residual table then isolates the single add4 miss and tests one extra deterministic axis clause on top of the best add4-sensitive latent-axis rule. The added clause (`low_core + low_shell <= 1e-6 and mean_center <= 0.0`) captures only the missed row (`local-morph-\u088f`) and yields an exact add4-side closure on this table when combined disjunctively with the base latent rule (`tp/fp/fn = 4/0/0`). So the strongest residual-thread read now updates to: the unresolved `cross=n|span=3+|low=L|pocket=H|overlap=1|rough=H` collision at `5504` can be closed on the frozen rows by a compact two-clause disjunctive rule, even though no exact single-conjunction 1-3 term separator was found.
- A bounded same-thread continuation then mirrors that disjunctive closure check on the add1 side of the same frozen `5504` residual table. Starting from the prior zero-FP add1 base clause (`abs_pocket_gap >= 0.058 and mean_center >= -0.035`, `7/9` TP), one extra deterministic clause (`center_total_variation >= 1.5 and abs_low_gap in [0.145, 0.23] and mean_center <= -0.14`) captures exactly the two remaining add1 misses (`local-morph-\u0c17`, `local-morph-\u155a`) without admitting any add4 rows, yielding an exact add1-side closure (`tp/fp/fn = 9/0/0`). So the strongest residual-thread read now tightens further: at `5504`, this previously non-exact coarse residual bucket is exactly separable on the frozen table by compact two-clause disjunctive closures on both add4 and add1 sides.
- A direct sparse-sentinel rung at `variant_limit = 3472` (completed in `3069.9s`) then checks whether that `3344` growth continues immediately. It does not: `3472` exactly reproduces the full `3344` non-pocket subtype membership and exact-rule table (`84` rows, `4` subtypes, unchanged both-sensitive one-term anchor `deep_overlap_count >= 1.500`, `tp=5`, and unchanged both-sensitive two-term family) with no new rows. So the strongest frontier read tightens to: `3344` was a six-row expansion, and `3344..3472` is now the next confirmed short hold.
- A direct sparse-sentinel rung at `variant_limit = 3600` (completed in `3192.3s`) then breaks that short hold by one row while preserving the same four-subtype law and both-sensitive exact-rule family. Non-pocket subtype membership rises from `84` to `85`; the lone new row is `local-morph-\u0e40` (`add1-sensitive`, `neither/dpadj-only`, `cross=Y`). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the full both-sensitive two-term family remains unchanged versus `3472`/`3344`. So the strongest frontier read updates to: `3344..3472` was a real short hold, and `3600` resumes growth without subtype-map drift.
- A direct sparse-sentinel rung at `variant_limit = 3728` (completed in `3313.0s`) then continues growth by four rows while preserving the same four-subtype law and both-sensitive exact-rule family. Non-pocket subtype membership rises from `85` to `89`; the new rows are `local-morph-\u0e81` (`add1-sensitive`), `local-morph-\u0e86` (`add1-sensitive`), `local-morph-\u0ee7` (`add1-sensitive`), and `local-morph-\u0ee8` (`pair-only-sensitive`). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the full both-sensitive two-term family remains unchanged versus `3600`/`3472`. So the strongest frontier read updates to: growth remains active through `3728` without subtype-map drift.
- A direct sparse-sentinel rung at `variant_limit = 3856` (completed in `3427.2s`) then extends that same growth phase by eight rows while preserving the same four-subtype law but rotating the both-sensitive two-term exact family. Non-pocket subtype membership rises from `89` to `97`; the new rows are `local-morph-\u0f11` (`pair-only-sensitive`), `local-morph-\u0f20` (`add4-sensitive`), `local-morph-\u0f37` (`add1-sensitive`), `local-morph-\u0f38` (`add4-sensitive`), `local-morph-\u0f54` (`add1-sensitive`), `local-morph-\u0f57` (`add1-sensitive`), `local-morph-\u0f65` (`pair-only-sensitive`), and `local-morph-\u0f6d` (`add1-sensitive`). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), while the two-term family pivots from the old boundary-fraction/center-variation set to a core/shell-deficit and center-location set still anchored on `deep_overlap_count >= 1.500`. So the strongest frontier read updates to: growth remains active through `3856`, with a first high-frontier two-term rule-family rotation and no subtype-count break.
- A direct sparse-sentinel rung at `variant_limit = 3984` (completed in `3551.8s`) then continues the same growth phase by four rows while preserving the same four-subtype law and both-sensitive exact-rule family from `3856`. Non-pocket subtype membership rises from `97` to `101`; the new rows are `local-morph-\u0fa1` (`pair-only-sensitive`), `local-morph-\u0fb2` (`add1-sensitive`), `local-morph-\u0fbe` (`add1-sensitive`), and `local-morph-\u0fd9` (`add1-sensitive`). The both-sensitive one-term anchor remains exact (`deep_overlap_count >= 1.500`, `tp=5`), and the rotated two-term family from `3856` remains unchanged. So the strongest frontier read updates to: growth remains active through `3984` with stable subtype count and no further high-frontier two-term-family drift.
- A direct `variant_limit = 288` rung then checks whether that plateau persists at the next deeper step. It does: non-pocket overlap-positive membership remains the same `8` rows, subtype count remains `3`, and the add1-sensitive separator remains exact via `crosses_midline = n` (`2/2`, `0` FP, `0` FN), with no subtype-membership or exact-rule-table changes versus `272`/`256` beyond run metadata. So the strongest current read is now: the post-`208` add4-sensitive crossing expansion occurred at `224/240`, then has remained stable through at least `288`.
- A direct default-vs-broader ensemble probe then tightens that result further. Widening the generated graph family from `5/3` to `7/4` geometry/procedural variants does not open this sparse-structure route up to `compact` at all: across all six fallback route sets, `compact` stays at `0/6` parity hits in both ensembles, while `extended` stays at `6/6`, with the same sparse-structure tier in every case and `4/6` fast (`size 3`) rows in both ensembles.
- A residual trace by basis size then shows that this `compact` miss is usually a near miss, not an arbitrary collapse. On most sparse-fallback route sets, the best `compact` row stabilizes around `-0.03/-0.01` mean/worst gap on the default ensemble and `-0.04/-0.07` on the broader one, usually through a degree-profile-like feature such as `degree_5_fraction`. Only when the full degree basis is removed does the miss widen materially, to about `-0.12/-0.13` on the default ensemble and `-0.10/-0.10` on the broader one. So the family split is not a giant cliff, but it is also not closed by widening the graph family alone.
- A final bridge test sharpens that corrected picture. On the canonical sparse route, adding back `motif_high_degree_neighbor_fraction` restores `compact` immediately in the size-`3` parity window in both ensembles. Adding back `motif_pocket_adjacent_fraction` or the whole pocket family only restores `compact` weakly in the size-`6` window, while `motif_deep_pocket_adjacent_fraction` and `motif_low_degree_neighbor_fraction` do not restore `compact` at all. So the `compact` side is near the sparse route, but the clean bridge still comes from the near-hub threshold family rather than from the cavity or low-degree escape routes.
- A follow-up non-hub bridge sweep makes that dependence harder to soften. No tiny non-high-degree combination closes `compact` on the sparse route: `pocket + deep-pocket` still only reaches the size-`6` window, `pocket + low-degree` and the full `pocket + deep-pocket + low-degree` triple only reach size `7`, and the remaining non-hub pairs do not restore parity at all.
- A corrected threshold-proxy rerun then completely overturns the earlier exact-coordinate reading. On the sparse route, `ge_6`, `ge_7`, and `share6` all restore `compact` immediately in the size-`3` parity window in both ensembles, and the full threshold bundle also restores parity by collapsing back to `share6`. What does **not** bridge are `count6`, `share7`, and `count7`, either alone or in any combination that excludes `ge_6`, `ge_7`, and `share6`.
- A final tiny-combination search over all `1..3` predicate subsets makes that boundary explicit: in both the default and broader ensembles, exactly `34/42` subsets restore `compact`, and they are precisely the subsets that contain at least one of `ge_6`, `ge_7`, or `share6`. The remaining `8/42` subsets, built only from `count6`, `share7`, and `count7`, never restore parity and only improve the compact gap to about `-0.02/-0.02`. So the corrected compact-side bridge is not an irreducible single feature after all. It is a small threshold-family core.
- A final node-level overlap analysis then sharpens that core one notch further. Across both ensembles, `share6 > 0` is exactly the same support condition as `ge_6`, while `ge_7` is a strict subset of that support that differs on only about `2%` of nodes. So the corrected bridge is really a two-level support story: a broad `6+`-neighbor condition that can be encoded as either `ge_6` or `share6 > 0`, plus a slightly stricter `7+` variant that still suffices.
- A follow-up scaling check then shows why the `count` variants still fail even though their support is the same. Across both ensembles, `count6 > 0` and `count7 > 0` match the same active nodes as `ge_6` and `ge_7` exactly, so the split is not about which nodes are touched. The difference is normalization: the active shell lives mostly on submaximal-degree nodes (mean active degree about `6.4`, with about `55%` of active nodes below degree `8`), so the fixed `/8` count encoding dilutes the same support by roughly `1.34..1.37x` relative to local-share normalization. The compact threshold core is therefore best read as a support condition plus a scaling choice: binary `ge` and local-share encodings preserve the shell signal, while fixed-count encodings weaken it.
- The corrected overlap-plus-model picture also makes the bridge family itself narrower than it first looked. `ge_6` and `share6` are not behaving like two independent compact routes: they have identical support across both ensembles and essentially the same single-feature compact benchmark score (`0.63/0.59` vs `0.63/0.59` on the default ensemble, `0.61/0.60` vs `0.62/0.62` on the broader one). `ge_7`, by contrast, is a strict subset route: it still restores the sparse bridge, but it covers about `2%` fewer nodes and its standalone compact benchmark score is much weaker (`0.40..0.41`). So the compact threshold core is best read as one broad `6+` route, expressible either as binary support (`ge_6`) or local-share scaling (`share6`), plus a stricter `7+` sufficient subroute rather than a third equally strong branch.
- With that repair in place, `extended` still produces the larger average boundary-delay span, while the reduced `compact` family keeps a slightly larger average center gap.
- The earlier `skew-wrap` miss is now understood as a legacy reduced-family coverage bug, not as a deep compact-vs-extended ontology split.
- The failure-mode story is still useful history: before the repair, skewed cases mostly failed by producing empty or fragmented candidate patterns, not by hitting the boundary filter, which pointed more toward pattern formation than boundary selection.
- Cross-slit interference disappears when histories are separated by durable record sectors, without any appeal to consciousness, while single-slit diffraction remains inside each sector.
- A Hadamard-style reversible mixer preserves the 2-norm but not 1- or 4-norm totals, which is a useful reason the Born-style square survives the pressure test better than arbitrary power rules.
- Under boost-like frame mixing, `sqrt(dt^2 - dx^2)` is the only tested local scalar that remains invariant; the remaining action assumption is the choice to treat `dt - sqrt(dt^2 - dx^2)` as spent delay.
- A fresh log-backed frontier-compression pass over the completed non-pocket logs at `1232`, `3344`, `4992`, and `5504` makes the current phase explicit. Row count keeps climbing (`40 -> 84 -> 127 -> 138`), but coarse signature count nearly saturates (`17 -> 23 -> 25 -> 25`), and after `3344` almost every newly appearing row reuses an already-seen coarse signature (`37/44`, `41/43`, and `11/11` reused-signature new rows). So the late frontier is mostly support-filling inside an already-stable family map. At the same time, the visible frontier observables are still not the hidden exact law: on the `5504` logs, the best two-axis compression reaches only `66/138`, and the best depth-2 tree only `67/138`, both anchored first on `deep_overlap_count` and then only weakly split by roughness, pocket load, or span. The current best read is therefore: stable four-family taxonomy, open membership boundary, and the next real science is latent compression plus translation into coarser physical language, not more dense ladder chasing.
- In that coarser physical language, `both-sensitive` is already the cleanest family: it is the high-overlap loaded branch. The other three families mostly live in the same low-overlap boundary regime and share very similar roughness, pocket-load, and span medians at `5504`, which is exactly why row-count growth keeps filling old signatures while the add1/add4/pair-only split still needs finer boundary clauses. So the current hidden-driver problem is no longer “find a fifth basin”; it is “find the smallest extra boundary variables that separate the three low-overlap families.”
- A direct physical-family-map pass on the completed `5504` table makes that translation tighter. The loaded branch remains compact and exact under a 2-term coarse clause (`boundary_roughness >= 0.267 and deep_overlap_count >= 1.500`, `tp/fp/fn = 7/0/0`). But the three low-overlap families still do not admit compact exact pairwise splits on current coarse observables: best pairwise rows stay partial (`59/96`, `60/96`, `56/92`, `58/92`, `45/74`, `47/74`). So the active closure target remains unchanged and specific: keep the loaded branch fixed, and search for the smallest extra boundary variables that separate add1/add4/pair-only inside the low-overlap regime.
- A richer frozen-row low-overlap pass at `5504` then checks whether the missing separation is just an artifact of the coarse visible axes. Reconstructing the completed `5504` low-overlap rows and adding shell/core gaps, boundary-deficit variables, deep/pocket gaps, and mean-center observables still does **not** produce compact exact 1-3 term pairwise closures. The best low-overlap rows improve, but stay partial (`71/96` for add4-vs-add1, `70/92` for add1-vs-pair-only, `61/74` for add4-vs-pair-only). The medians do show real directional differences: `add1` carries the strongest low-degree and boundary-gap pressure, `pair-only` carries the strongest pocket gap, and `add4` sits between them. So the current hidden-driver problem tightens again: the visible shell/core boundary variables are informative but still insufficient, and the next likely missing axes are profile/asymmetry variables rather than more overlap or coarse shell load.
- A frozen-row profile/asymmetry follow-up at the same `5504` low-overlap table then tests that next hypothesis directly. Those variables are informative too, but still not sufficient on their own: the best 1-3 term pairwise profile rules remain partial (`65/96`, `64/92`, `48/74`). They do expose a different gradient inside the basin, though: `pair-only` has visibly lower mean profile asymmetry, while `add4` carries the strongest half-center imbalance and profile slope. So the low-overlap basin is not featureless, but its internal split is not closed by profile observables alone.
- A final combined pass then merges the strongest visible boundary variables and the strongest profile/asymmetry variables into one bounded 1-3 term search. That still does **not** yield compact exact pairwise closures inside the low-overlap basin: the best rows are effectively unchanged from the richer boundary pass (`71/96`, `70/92`, `61/74`). So the current best hidden-driver read is sharper again: the loaded family is compactly solved, but the low-overlap three-family basin is not the shadow of a tiny single-conjunction rule family on the present observable set. The next likely move is residual or bucketwise decomposition inside that basin, or else a genuinely more local/topological observable family.
- A direct low-overlap residual-bucket decomposition on the frozen `5504` table now makes that next move concrete for the strongest mixed pair (`add1` vs `add4`). The best 2-feature visible signature split (`core_boundary_deficit_mean <= 0.222` plus `boundary_fraction <= 0.826`) closes only two tiny pure add1 buckets (`7` and `2` rows) and leaves one dominant mixed bucket (`87` rows: `48` add1, `39` add4). Inside that dominant bucket, local compact rules are still partial (`63/87` and `64/87`, no exact 1-3 term closure). So the unresolved boundary is now sharply localized: the remaining collision is not spread across the basin, it is concentrated in one large low-overlap core that likely needs more local/topological observables rather than more global threshold recombination.
- A bucket-local motif follow-up on that same dominant `87`-row add1-vs-add4 core then confirms that genuinely local node-motif structure adds signal, but still does not close the collision. Boundary-local motif rates separate the classes a bit better on the add1 side (`69/87`, `tp/fp/fn = 35/5/13`) than the earlier mixed-bucket baseline, with `add1` showing slightly stronger boundary deep-pocket and low-degree-neighbor rates. But the closure is still non-exact, and the add4-side best row remains only `65/87`.
- A final bucket-local combined pass then merges the strongest local motif observables with the strongest visible basin observables inside that same `87`-row core. That still does **not** yield compact exact closure: add1 stays at `69/87`, and add4 improves only slightly to `66/87`. So the current best hidden-driver read is now quite sharp: the remaining low-overlap add1-vs-add4 collision is not just missing one more threshold on the current summary features. The next likely missing class is more explicit support-cell or candidate-topology structure, not further recombination of the existing boundary/profile/motif aggregates.
- A direct bucket-local support-topology pass then tests exactly that missing class on the same frozen `87`-row core, adding explicit candidate-cell counts, candidate-neighborhood overlap, and mirrored candidate suppression variables. Those observables are informative but still not sufficient for compact exact closure: add1 reaches `66/87` and add4 reaches `63/87` (both non-exact). The envelope split is physically suggestive, though: add4 carries noticeably higher mirrored-candidate-occupied rates than add1 (`pmocc~ 0.750 vs 0.600`, `dmocc~ 1.000 vs 0.667`), while add1 keeps slightly higher boundary-neighbor overlap. So the collision now appears tied less to coarse candidate load itself and more to finer candidate placement/identity structure inside the mixed core.
- A bounded two-clause closure pass then checks whether that same frozen `87`-row core is at least a tiny union of sub-basins on the observables already in hand. Joining the bucket-local visible, motif, and support-topology families and searching bounded 1-3 term clauses over the top `24` single predicates still does **not** produce an exact add1/add4 closure. The best add1-side row remains only `66/87`, and the best add4-side row only `62/87`; both are dominated by `boundary_deep_fraction` with one extra clause, and no exact zero-FP two-clause union appears. So the current best hidden-driver read tightens again: the hard low-overlap core is not just waiting for a small disjunctive rewrite on the current observables. The next likely missing class is explicit candidate identity or lobe-placement structure inside that mixed bucket.
- A direct candidate-identity follow-up on that same frozen `87`-row core then confirms that finer placement structure is real signal, not just bookkeeping. Side-resolved candidate-cell identity bits and mirror-occupancy events improve the add1-side split to `68/87`, and the best add4-side row becomes a zero-FP identity cut (`16/0/23`) via `event_present_count <= 1.500`. The dominant events are asymmetric deep/pocket cell placements like `deep_cell:dx1:dy4` and `deep_cell:dx4:dy3`, with add1 carrying more left-leaning `dx1` occupancy and add4 more right-leaning `dx4` occupancy. So the missing separator is now clearly tied to candidate placement structure, not just aggregate candidate load.
- A final bounded two-clause closure pass then folds those candidate-identity bits back into the joined local observable family. That still does **not** produce an exact add1/add4 closure on the frozen `87`-row core. The best add1-side row improves to `70/87` via `boundary_deep_fraction >= 0.408 and event_present_count >= 1.500`, while the best add4-side row remains the zero-FP identity cut `event_present_count <= 1.500` (`16/0/23`). So the current best hidden-driver read is now sharper again: candidate identity is genuinely informative and improves the low-overlap split, but even with bounded disjunction the hard core still does not collapse into a tiny rule family. The next likely missing class is explicit lobe/topology structure over the candidate support graph, not just more scalar recombination.
- A bounded identity-plus-lobe continuation then tests exactly that next hypothesis on the same frozen `87`-row core by joining candidate-identity events with the new lobe/component bridge variables. That still does **not** produce an exact add1/add4 closure. The best add1-side row only nudges to `69/87` via `deep_left_fraction >= 0.292 and event_present_count >= 1.500 and pocket_left_component_count <= 3.500`, while the add4-side best row remains the same zero-FP identity anchor `event_present_count <= 1.500` (`16/0/23`). So the updated read is sharper again: lobe topology contributes only weak refinements on top of candidate identity, and the remaining separator likely lives in more explicit candidate-support subgraph structure or event-topology interactions, not in compact scalar summaries of the lobe geometry.
- A direct candidate-support subgraph pass then sharpens that claim further. Explicit bridge-support-node events and support-subgraph edge structure are now the strongest low-overlap family we have tested on the frozen `87`-row core. The best add1-side row rises to `82/87` (`48/5/0`) via `bridge_node_dx0_dy3 >= 0.500 and bridge_node_dx0_dy4 >= 0.500 and bridge_support_edge_density <= 0.506`, while the best add4-side row reaches `75/87` (`27/0/12`) via `bridge_node_dx0_dy3 <= 0.500`. So the current missing separator is no longer “some generic topology feature”; it is specifically tied to a small set of bridge-support-node placements.
- A bridge-event translation pass then shows that those raw support-node coordinates compress into a much cleaner coarse language. The dominant signal is a `center bridge spine`: on the frozen bucket, `add1` rows have the `dx0:dy3/dy4` central bridge pair essentially all the time (`center_pair~1.000`), while `add4` rows usually do not (`center_pair~0.154`). The rest of the bridge mass already sits overwhelmingly on the right lobe for both families, so the real separator is not generic left/right bridge balance; it is the presence or absence of that central bridge spine.
- A final translated closure pass confirms that this coarse bridge language retains almost all of the raw support-subgraph strength while being much easier to interpret. The best add1-side translated rule is `bridge_center_spine_pair >= 0.500 and identity_deep_left_fraction >= 0.292`, giving `82/87` (`48/5/0`), and the best add4-side translated rule is simply `bridge_center_spine_pair <= 0.500`, giving `81/87` (`33/0/6`). An exception dump then makes the residual structure explicit: there are no add1 misses under the add1-side translated rule, and the remaining failures are a tiny island of `add4` rows that already carry the full central bridge spine. So the next real science target is no longer the whole mixed bucket; it is that small add4 island living *inside* the central-spine regime.
- A direct candidate-lobe/support-graph topology pass then tests that missing class on the same frozen `87`-row add1-vs-add4 core. As a standalone observable family, it is clearly non-closing and weaker than the prior candidate-identity split: the best add1-side row is only `56/87` (`23/6/25`), and the best add4-side row is `62/87` (`32/18/7`). Bridge-style means (`pocket~0.183`, `deep~0.233`, `support~0.407`) still show structured variation, but they do not yield a compact separator by themselves. So the updated read is: lobe topology alone is not the missing law; the likely next high-signal move is a bounded closure pass that explicitly combines candidate-identity bits with these lobe-bridge topology variables.
- A direct candidate-support subgraph pass then executes that follow-up explicitly on the same frozen `87`-row add1-vs-add4 core, adding per-event bridge-participation bits plus bridge-subgraph/lobe interaction summaries. This family is informative but still non-closing: the best add1-side row reaches `82/87` (`48/5/0`) via a 3-term bridge-event + bridge-density clause, while the add4-side best row is `75/87` (`27/0/12`) via `bridge_node_dx0_dy3 <= 0.500`, and no exact compact split appears. The physical split is still structured (`bridge contact mean ~0.407`, pocket-only support ~`0.108`, deep-only ~`0.000`), but the unresolved boundary remains. So the updated read is sharper: bridge-event/subgraph structure carries substantial signal and strongly improves the add1 side, yet the hard core still does not collapse to a tiny exact family; the next likely gain is a bounded closure that joins these bridge-event features with the existing identity+lobe variables.
- A bounded identity+lobe+support joined-closure continuation then runs that exact follow-up on the same frozen `87`-row core, combining the strongest identity bits, lobe topology variables, and support-subgraph bridge events in one compact 1-3 term closure search. It still does **not** improve the frontier: best add1-side remains `82/87` (`48/5/0`) and best add4-side remains `75/87` (`27/0/12`), with both best rows still anchored by the same `bridge_node_dx0_dy3` / `bridge_node_dx0_dy4` bridge-event thresholds from the support pass. So the read tightens again: joined scalar closure over current identity+lobe+support observables appears saturated on this core, and the remaining unresolved boundary likely needs a different residual-language representation rather than one more bounded recombination of the same feature family.
- A direct central-spine residual-bucket continuation then executes that exact residual-language step on the same frozen `5504` core: restrict to `bridge_center_spine_pair >= 0.500` and rerun bounded compact closure on joined identity+lobe+support features. The central-spine subset is `54` rows (`48` add1, `6` add4), and it still does **not** admit an exact compact split. The best add4-side row is `50/54` (`2/0/4`) via `identity_pocket_left_fraction >= 0.708 and support_bridge_support_edge_density >= 0.468`; the best add1-side row is `43/54` (`37/0/11`) via `support_bridge_event_present_count <= 12.500`. So the residual target sharpens again: the bridge-language compression is right, but the remaining add4 island inside the central spine is still not closed by bounded scalar thresholds on the current joined observable family.
- A direct center-spine micro-bucket decomposition then shows that this six-row add4 residual is already much more structured than it looked. Two visible predicates on the central-spine rows, `support_bridge_support_edge_density >= 0.488` and `support_bridge_support_left_fraction <= 0.390`, split the `54`-row center-spine table into four micro-buckets: one dominant mixed bucket `00` (`42` add1, `3` add4), one tiny mixed bucket `10` (`3` add1, `1` add4), one pure add4 bucket `11` (`2` rows), and one pure add1 bucket `01` (`3` rows). So the central-spine residual is no longer one amorphous six-row island; it is three small add4 islands with only one genuinely large mixed core.
- A fast visible-field closure plus nearest-neighbor pass on those micro-buckets then makes that last point explicit. The tiny `10` bucket closes exactly on visible fields via `id_deep_left_fraction >= 0.584` (`1/0/0` on add4, `3/0/0` on add1), while the dominant `00` bucket still does **not**: best visible add1 row is only `37/45`, and no visible add4 rule achieves positive recall without false positives. Its three add4 rows also sit close to ordinary add1 rows in the same visible space (nearest-neighbor distances `0.508`, `0.938`, `1.550`), so the remaining separator now looks less like one more missed scalar threshold and more like hidden support-cell / candidate-topology structure inside that single mixed core.
- A direct support-topology continuation on that dominant `00` mixed core then tests the hidden-topology hypothesis on the next richer observable family instead of on visible summaries alone. Even there, the frontier does **not** improve: best add1-side row is still only `37/45` via `bridge_event_present_count <= 12.500`, and the add4 side still has no positive-recall compact rule. The central bridge events `bridge_node_dx0_dy3` and `bridge_node_dx0_dy4` are already saturated across essentially the whole bucket, so the remaining separator is not just “one more bridge bit” or a small improvement in bridge-density summaries. The current best read is therefore tighter again: the residual `00` core is now likely hiding in finer support-cell identity or candidate-topology interactions than the present support-topology family exposes.
- A bucket-specific candidate-cell identity pass then checks that next hypothesis directly on the same frozen `00` core. This family is stronger than the visible-field or support-topology summaries, especially on the add4 side: the best add4 rule reaches `43/45` (`1/0/2`) via `deep_mirror_occ_dx4_dy3 >= 0.500 and pocket_mirror_void_right_fraction >= 0.292`, while the best add1 rule reaches `28/45` via `pocket_x_span >= 3.500`. So explicit candidate-cell identity really is closer to the missing separator than bridge-density summaries were, but it still does not close the core.
- A direct candidate-layout neighbor pass then makes the remaining structure more concrete. All three `00` add4 rows have `deep_only = []`, while their nearest add1 neighbors carry larger or shifted overlap/deep layouts. One add4 row collapses completely to `pocket == deep`, while the other two keep `deep` as a strict subset of `pocket`; their nearest add1 neighbors instead retain expanded or displaced deep/overlap cell sets. This is strong evidence that the remaining ambiguity lives in relational candidate-cell layout, not just in scalar counts.
- A final joined bucket-specific identity+support pass then checks whether the current two strongest feature families become exact only when combined on the true hard core. They do not. The joined add4 frontier falls back to `41/45`, and the joined add1 frontier stays at `37/45`, so the unresolved `00` bucket is not just a missed conjunction of the current identity and support summaries. The current best read is now: the last hard residual probably requires explicit support-cell / candidate-cell interaction structure rather than more thresholding over the present summary families.
- A direct support-candidate interaction-motif pass then executes that exact next step on the same frozen `00` hard core, adding explicit support-role/candidate-family pair motifs plus local `star` support-neighborhood motifs and nearest-neighbor motif deltas. The family still does **not** close the boundary: best add4-side rows stay at `41/45` (`0/1/3`) and best add1-side rows at `30/45` (`27/0/15`), dominated by `bridge_pair_fraction`/`pair_density` thresholds. The nearest-neighbor deltas are now explicit and small: only one add4 row carries a unique `star_bridge_p2_d2`, while the other two add4 rows differ from their nearest add1 neighbors only by one or two local star motifs. So the unresolved separator has tightened again: pairwise adjacency motifs are informative but still too coarse, and the next likely closure step is finer support-cell interaction identity (for example oriented support-pair motifs or small support-subgraph context around matched candidate cells), not more scalar threshold recombination.
- A bounded oriented support-pair/context continuation then runs that exact refinement on the same frozen `00` hard core, adding candidate-anchored oriented support-pair events plus small candidate-local support-subgraph context summaries. This again remains non-closing, but it sharpens the boundary read. The best add4-side row improves slightly to `42/45` (`1/1/2`) via `candidate_shared_edge_fraction <= 0.684`, while the best zero-FP add4 rows still sit at `41/45` and the add1 side remains weak (`20/45`, `17/0/25`). Nearest-neighbor deltas are still narrow and positional (mostly one or a few oriented bridge-bridge pair events shifting anchor coordinates). So the residual target tightens once more: oriented support-pair/subgraph context adds bounded signal but still does not isolate the three add4 exceptions, and the likely missing class is finer role-conditioned support-cell identity tied to specific candidate layouts rather than additional scalar recombination.
- A bounded role-conditioned support-cell identity pass then executes that exact next step on the same frozen `00` hard core, keying each support cell by `(role, local anchor, adjacent candidate-family offset pattern)` and rerunning compact closure with nearest-neighbor deltas. This family is informative but still non-closing and does not improve the add4 frontier: best add4-side rows remain `41/45` (`0/1/3`) and best add1-side rows remain weak at `19/45` (`16/0/26`), anchored by coarse identity-event-count and degree thresholds. The nearest-neighbor deltas stay narrow and mostly local-pattern swaps (for example `count_bridge_p2_d2` versus a few `pocket_only` offset signatures), not a clean one-clause split. So the residual target tightens again: role-conditioned support identity adds interpretable local structure but still does not isolate the three add4 exceptions; the likely missing class is finer matched-layout interaction identity (for example pair-conditioned support-cell motifs keyed jointly to candidate layout differences across nearest add1/add4 pairs), not additional scalar recombination over current identity summaries.
- A bounded nearest-opposite pair-delta pass then executes that matched-layout interaction step on the same frozen `00` hard core, building signed support-motif deltas for each row against its nearest opposite-subtype neighbor. This family again remains non-closing and still does not beat the prior add4 frontier: best add4 rows are `43/45` (`1/0/2`) via `delta_support_degree_mean >= 0.505` and `42/45` (`1/1/2`) via tight pair-distance/event-count clauses, while the strongest zero-FP row remains only `41/45` (`0/1/3`) via `abs_delta_support_degree_mean <= 0.011`; add1 rows rise to `40/45` (`37/0/5`) via `pair_selected_event_present_count >= 4.500`. The nearest-opposite map confirms the same three close add4 anchors (`0.508`, `0.938`, `1.550`) and the selected signed motifs are dominated by small `count_bridge_p2_d*` and `pocket_only` offset swaps. So the residual target tightens once more: matched nearest-opposite support deltas add structure but still do not isolate the three add4 exceptions, and the likely missing class is higher-order matched interaction context (for example two-hop support/candidate motif-cooccurrence deltas) rather than another scalar recombination over current pair-delta summaries.

What is still cheating:

- The spatial graph geometry is still hand-authored.
- The gravity-like classical limit still assumes that histories extremize spent delay `dt - sqrt(dt^2 - ds^2)` rather than deriving that accounting rule from deeper dynamics.
- The delay field is now derived from an emergent persistent pattern, but the rule family and locality preferences used to choose among candidate patterns are still hand-chosen.
- The current robustness criterion is still hand-chosen too: `center gap`, `arrival span`, selector policy, and now even the choice of combined status observable materially affect which surviving branch wins on the same fixed candidate pool.
- The current perturbation ensemble is still a first pass: it uses only two deterministic graph nudges per scenario, tracks the unperturbed frontier palette instead of doing open-ended motif rediscovery on every perturbed graph, and only evaluates the strongest retained-weight point `w = 1.0`.
- The weight-robustness checks are still local: the current pairwise comparison uses only `w = 0.95` and `w = 1.0`, and the stronger ladder check only extends that to `w = 0.90, 0.95, 1.0`, so the model is still being tested in a narrow high-end band near the spent-delay point rather than across the whole retained-weight family.
- The random perturbation ensemble is also intentionally small and seeded: it currently samples only three random variants per scenario, and even the rediscovery-limit sweep only tests a small `0..3` band of extra locally rediscovered motifs, so it is still a first robustness probe rather than a large-scale random-graph study.
- The geometry-randomization layer is still narrow too: it only jitters contiguous column profiles with small deterministic shifts, so it is a first deformation test of the graph family rather than a broad random-geometry generator.
- The procedural graph generator is still very small and structured: it only produces smooth contiguous column profiles inside the existing bounding boxes, so it is a first independent-family probe rather than a general graph-generation study.
- The robustness sweep budget is also hand-chosen: smaller graphs, reduced rule families, and shorter persistence windows trade completeness for runtime.
- Complex amplitudes are still assumed rather than derived.
- Consciousness is still outside the simulation; only record formation is present.

So this prototype does not replace physics. It is a compact testbed for checking which parts of the ontology can already be realized with a simple discrete model, and which parts are still axioms wearing different clothes.
